# 实测数据

本项目全部测量结果的唯一来源。其它文档引用本文的小节名，不复制数字。

## 滤波器与灰调级数

基底：网点区裁剪 2298×1350，阈值 127 二值化，严格 3.0 倍缩放到 766×450。PNG 为真实编码字节。

| 滤波器 | 灰调级数 | 1bit | 2bit | 4bit | 8bit | 4bit/8bit |
|---|---|---|---|---|---|---|
| BOX | 10 | 10475 | 24098 | 43281 | 45590 | 95% |
| BILINEAR | 161 | 9172 | 22453 | 56442 | 90041 | 63% |
| HAMMING | 255 | 10379 | 23628 | 53616 | 95103 | 56% |
| BICUBIC | 256 | 10316 | 23589 | 57090 | 112609 | 51% |
| LANCZOS | 256 | 11036 | 24408 | 58410 | 126974 | 46% |

均匀权重的完整窗口平均，对二值输入只能产出 `n²+1` 个取值（3×3 → 10 级，2×2 → 5 级）。
非均匀加权产出准连续谱。BILINEAR 无负瓣、不振铃，仍产出 161 级——振铃是二级效应，不是主因。

> **基底是合成图**（截图拼接 + 再编码 + 人工二值化），非原始扫描。绝对数字待真实 A 类样本复验。
> 引用这些字节数论证体积时必须带上本句。

## 缩放比与熵

BOX，源 2298×1350：

| 缩放比 | 2.00x | 2.50x | 2.87x | 3.00x | 3.50x | 4.00x |
|---|---|---|---|---|---|---|
| 灰调级数 | 5 | 15 | 15 | 10 | 38 | 46 |
| 8bit 字节 | 58088 | 57948 | 52377 | 45590 | 48584 | 38935 |

按像素数归一化，3.00x 对 2.87x（仅此一对比值）：BOX +5%、LANCZOS +9%、BICUBIC +14%。
**整数比吸附值 +5%~+14%，非数量级，且符号不处处确定**——BOX 2.50x 的 0.117 B/px 低于 3.00x 的 0.132 B/px。

## 抖动

同一基底，area 缩放后（22 灰调级）：

| 位深 | 无抖动 | Bayer 8×8 | 蓝噪声 64×64 | FS 误差扩散 |
|---|---|---|---|---|
| 1bit 字节 / RMSE | 10566 / 10.33 | 12288 / 8.71 | 14464 / 9.71 | 13463 / **6.05** |
| 2bit 字节 / RMSE | 24263 / 2.26 | 25049 / 2.77 | 25801 / 3.05 | 25074 / **1.92** |
| 4bit 字节 / RMSE | 43590 / 0.48 | 48063 / 0.57 | 49996 / 0.62 | 47510 / **0.39** |

RMSE 为 4×4 低通后的局部均值误差。逐像素 RMSE 在 1bit 上给出相反排序（无抖动 34.81 优于 FS 42.86），
与目视不符——这是判据必须低通的直接证据。抖动的体积代价 +3%~+37%，随位深升高而降低。

蓝噪声掩模自检：50% 灰占空比 0.500，低频/平均能量比 0.60。

> 本节字节数与「滤波器与灰调级数」的 BOX 行不同：那里是严格 3.0 倍，这里是 3.0026 倍。同一张基底源图，两次不同缩放。

## B 类素材普查

34 系列 / 12 GB / 30,576 张，抽样 168 张全部解码成功。

- **格式**：30,576 AVIF（8bit YUV420 有损）／278 JPG（均为封面）／0 PNG／0 RAR／469 ZIP（内含 AVIF，文件名 GBK 编码无 UTF-8 标志）
- **尺寸**：中位 1441×2048；高 p10 1600 / p90 2500 / max 2700；宽 max 5408
- **灰调级数中位 256**，34 个系列无一例外；168 张中级数 ≤32 的仅 2 张，且纯黑白占比 0.00
- 97% 近灰度，3% 彩色；疑似跨页 1.8%

总缩放比（源高 ÷ 目标高）：

| 面板 | p10 | 中位 | p90 | ≥2.0 占比 | 源比目标小 |
|---|---|---|---|---|---|
| 1264×1680 | 0.952 | 1.219 | 1.488 | **0.0%** | 19% |
| 1072×1448 | 1.105 | 1.414 | 1.727 | **0.0%** | 1% |
| 1404×1872 | 0.855 | 1.094 | 1.335 | **0.0%** | 23% |

最高的页 2700 px 对最小面板（1448）缩放比 1.865，仍 < 2。**整数预缩在此类素材上不触发。**

## B 类位深实测

30 张灰度页，Lanczos 缩放到 fit-inside，无抖动，目标面板 1264×1680：

| 位深 | PNG 中位字节 | 相对 8bit | 局部均值 RMSE |
|---|---|---|---|
| 1bit | 66876 | 8% | 18.86 |
| 2bit | 135718 | 16% | 6.48 |
| 4bit | 340976 | 40% | 1.36 |
| 8bit | 849267 | 100% | 0.00 |

另两块面板一致：4bit 40-42%、2bit 16-17%、1bit 8%。

与「滤波器与灰调级数」BOX 行的 4bit/8bit=95% 方向相反：那一行是严格整数比 box 的产物（10 个灰调级、熵极低），
本节是 256 级连续调 + 比值 1.2 的 Lanczos 输出。

## 编码器对比

15 张 B 类页，目标面板 1264×1680，源 AVIF 中位 307,950 字节。

| 候选 | 中位字节 | 相对源 | RMSE |
|---|---|---|---|
| PNG 1bit | 66876 | 22% | 18.860 |
| PNG 1bit+FS | 105141 | 34% | 10.497 |
| PNG 2bit | 140136 | 46% | 7.222 |
| PNG 2bit+FS | 179561 | 58% | 3.511 |
| AVIF q60 | 278673 | 90% | 0.895 |
| WebP q75 | 290366 | 94% | 1.218 |
| PNG 4bit | 333964 | 108% | 1.295 |
| AVIF q75 | 346153 | 112% | 0.604 |
| WebP q85 | 363588 | 118% | 0.769 |
| PNG 4bit+FS | 365826 | 119% | 0.786 |
| WebP q92 | 447654 | 145% | 0.472 |
| AVIF q90 | 486625 | 158% | 0.284 |
| PNG 8bit | 774741 | 252% | 0.000 |
| WebP lossless | 785808 | 255% | 0.000 |
| AVIF lossless | 896847 | 291% | 0.000 |

RMSE < 1.3 区间 AVIF 双轴优于 PNG（q60 比 PNG 4bit 小 17%、误差低 31%）。
2bit 及以下 AVIF/WebP 到不了。三种无损均为源的 252%~291%。

## AVIF 解码吞吐

40 张 B 类页共 12.0 MB，平均 3.13 MPix/页。文件预先读入内存，只计解码。

| 解码器 | 单线程 | rayon 32 线程 |
|---|---|---|
| dav1d（`image` + `avif-native`） | 25.5 ms/页 · 39.3 页/秒 | **140.2 页/秒** |
| rav1d（`zenavif`） | 123.6 ms/页 · 8.1 页/秒 | 47.9 页/秒 |
| 外部 `avifdec` 进程 | 186 ms/页 · 5.4 页/秒 | 未测 |

满核吞吐是决策相关的那一组：dav1d 是 rav1d 的 2.9 倍。单线程那组差 4.8 倍，
但 dav1d 可能在内部自开线程，该组不是干净的逐解码器速度比。

两者的并行扩展都只有 3.5~3.6 倍（32 线程），瓶颈不在线程数。

`avifdec` 那一行含进程启动与 PNG 编码，不可与前两行直接相减。

## AVIF 解码的可用路径

Windows MSVC 工具链上的实际约束：

| 路径 | 可用性 |
|---|---|
| `image` + `avif-native` | 可用。需 pkg-config 与 dav1d 系统库；dav1d 经 vcpkg 构建，`PKG_CONFIG_PATH` 指向其 pkgconfig 目录 |
| `zenavif`（rav1d） | 可用。构建期需 NASM，产物无运行时系统依赖 |
| `rav1d` 关闭 `asm` | 不可用。该代码路径自身编译失败 |
| `avif-decode` | 不可用。依赖 `aom-decode`（libaom 的 C 绑定） |
| `libavif-image` | 不可用。依赖 `libavif`（C） |

## 设备支持的图片格式

自带阅读器官方口径：

| 阅读器 | 格式 |
|---|---|
| Kobo | JPEG / GIF / PNG / BMP / TIFF |
| BOOX NeoReader | PNG / JPG / BMP |
| Kindle | JPEG / PNG / GIF |

三者均不含 WebP 与 AVIF。含 WebP 的 CBZ 在 Kobo 上打不开，换 JPG 即正常。

## Go 标准库 PNG 编码器

1000×1500，`image/png`：

| 输入 | 位深 | 颜色类型 | 字节 |
|---|---|---|---|
| `image.Gray`（8bit 源） | 8 | 0 | 5844 |
| `image.Gray`（仅 2 个灰度值） | 8 | 0 | 3239 |
| `image.Paletted` 2 色 | 1 | 3 | 748 |
| `image.Paletted` 4 色 | 2 | 3 | 1571 |
| `image.Paletted` 16 色 | 4 | 3 | 2770 |

对 `image.Gray` 恒写 8 位；低位深只能走调色板模式。

## 截断页的解码容忍度

`image` 0.25.10 在截断的文件上还能拿回多少。基底：64×64 合成页，各按**文件字节**截到指定比例。
「整解」指 `ImageReader::decode`；「救回」指 `into_decoder` 之后 `read_image` 写进缓冲那一段
（见 `src/decode.rs` 的 `salvage`）。缓冲按 255 预填，源页恒不取 255，救回比例因此是准的。

| 格式 | 全长 | 保留 90% | 保留 70% | 保留 50% | 保留 30% |
|---|---|---|---|---|---|
| PNG（纯黑） | 309 B | 整解失败 · 救回 93.8% | 整解失败 · 救回 62.5% | 整解失败 · 救回 29.7% | 整解失败 · 救回 0.0% |
| JPEG（平缓竖向渐变） | 555 B | 整解成功 | 整解成功 | 整解失败 · 救不回 | 整解失败 · 救不回 |
| JPEG（细纹理） | 1281 B | 整解成功 | 整解成功 | 整解成功 | 整解成功 |

另测：PNG 只留文件头 + IHDR 时 `decode` **成功**，出一张全黑图——`png` 的 `next_frame`
不把「没有 IDAT」当错。

三条结论：

- **PNG 要靠救回。**`next_frame` 遇 EOF 报错，但缓冲里已落下的行留着，救回捡的就是那一段。
  救回比例随保留比例单调下降，30% 那一档已经一行都没有。
- **JPEG 多半不必救回。**zune-jpeg 自己就宽容，整解直接就过；截得太狠时它在**构造解码器**
  那一步就要读完整个扫描段，尺寸也拿不到，那时只能算失败页。
- **能救到哪一步取决于截在哪里，不取决于比例本身。**两份 JPEG 在同一个 50% 上结论相反，
  差别只在文件长度——短文件的 50% 已经切进了头部。

样本是合成小图，不是真实片源。这组数只用来说明「有得救」与「救不了」两侧都真实存在，
不构成对任意截断文件的定量承诺。

## 覆盖范围

全部测量来自 B 类（网络发布版）。**A 类只有一个合成代理基底，真实样本尚未取得。**
凡关于 A 类的数值主张一律标注「尚未测量」。
