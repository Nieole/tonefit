//! **语义色**：屏上把「这件事怎么样」分成的**四种**（`CONTEXT.md` 的《会话》：语义色）。
//!
//! 本模块只有那一个枚举。它答的是**语义**——这一行是隔离、是跳过、是没做成——
//! 不是颜色：四种在屏上各是什么样、上不上色，都在画法那一层的 `super::shell::paint`
//! 一处定（本仓库唯一写得出颜色名的地方），本模块一个 `Color::` 都不写。
//!
//! # 它一个终端都不碰
//!
//! 因此摆在 `tui` 特性**外面**（见 `super` 的《终端库在哪一半》），与
//! [`Viewport`](super::viewport::Viewport)、[`columns`](super::columns) 同一侧。
//! 要它在这一侧的是**状态机**：屏底那一句回话由状态机说出口（[`super::look`] 的要法里
//! 带着它），而说出口的那一刻就定了它有多重（一条拒绝是出事、「再按一次」是注意）
//! （`no-false-line/05`，收停车场 Q198）。
//!
//! **自己一个模块，不并进 [`super::state`]**：它不是状态——状态机只是说得出它的那一头，
//! 画法那几块个个要它；`CONTEXT.md` 把《语义色》当《会话》一节自己的一个词，
//! 与《视口》《砍列》同级，那两个各有各的模块。另一条路与代价在停车场 Q657。
//!
//! # 它不带记号
//!
//! **没有一个「这一档配哪个记号」的方法。**《语义色》那句「颜色不是唯一载体——
//! 每一处上色的地方旁边都另有一个字或一个行首记号」是**对每一处的要求**，不是这个类型的
//! 一个属性：语义色是**多对一**的（「注意」一档在 `CONTEXT.md` 的《语义色》里列着八样，
//! 卷级的隔离、页级的差异大的页、屏底那一句问的「再按一次」都在里面），
//! 一个方法答不出那几样各配什么记号。行首记号定语义，语义不反推记号
//! （记号在 `super::shell::marks`）。
//!
//! 那句话落在屏上是**设计快照逐格相等**那一关（`super::shell` 的用例）：记号与字是快照的
//! 一部分，不上色那一屏也逐格比（停车场 Q965）。

/// **四种语义**，屏上一种一个样子（四种各是什么样见 `super::shell::paint` 那张表）。
///
/// 画法各处要的是这四个字之一，不是颜色：颜色名只在 `super::shell::paint` 里出现。
///
/// **四种有轻重**，按 [`Ord`] 从轻到重排：不要紧 < 平常 < 注意 < 出事。
/// 派生它是为了一件具体的事——**一行上摆着好几件事时取最重的那一种**。
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum Tone {
    /// **不要紧**：跳过的卷。屏上压暗。
    Muted,
    /// **平常**：正常跑完的卷、普通的页、非漫画文件那一小结、屏底那一句报的「做成了」。
    /// 屏上是终端默认色。
    Plain,
    /// **注意**：隔离、残缺、页面超宽、兜底上界、尺寸未贴合屏幕、差异大的页、过期副本，
    /// 以及屏底那一句问的「再按一次」。屏上黄。
    Caution,
    /// **出事**：坏页、卷转换失败、拒绝开始，以及屏底那一句报的「没做成」。屏上红。
    Trouble,
}

#[cfg(test)]
mod tests {
    use super::Tone;

    /// **四种语义有轻重，一行上摆着好几件事时取最重的那一种。**
    ///
    /// 这一条在 `tui` 特性外面跑：轻重是语义的事，不是颜色的事。
    #[test]
    fn the_four_tones_run_from_the_least_to_the_most_serious() {
        assert!(Tone::Muted < Tone::Plain);
        assert!(Tone::Plain < Tone::Caution);
        assert!(Tone::Caution < Tone::Trouble);
        assert_eq!(
            [Tone::Caution, Tone::Trouble, Tone::Plain]
                .into_iter()
                .max(),
            Some(Tone::Trouble)
        );
    }
}
