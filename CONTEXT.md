# tonefit — 领域上下文

tonefit 把漫画页适配到电子墨水阅读设备：在尽量不影响阅读效果的前提下，为每页选择缩放、几何裁切与输出位深。

本文是**词汇表**。issue 标题、类型名、模块名、测试名提到领域概念时用这里的词，不要漂移到本文避开的同义词。
决策依据见 `docs/adr/`，全部实测数字见 `docs/measurements.md`。

## 处理对象

| 术语 | 含义 |
|---|---|
| **页 (Page)** | 一张待处理的图片。不叫 image——后者指内存中的像素缓冲 |
| **卷 (Volume)** | 一次处理调用的作用域：一个目录或一个归档，内含按阅读顺序排列的页 |
| **源 (Source)** | 输入容器抽象。按阅读顺序吐出页，非图片文件原样透传。目录与 CBZ 在它之下同形 |
| **输出容器 (Sink)** | 输出侧容器抽象。收页与透传文件。形态随输入而定：输入是 CBZ，输出也是 CBZ |
| **成员 (Member)** | 卷里的一项：一页，或一个原样透传的文件。「成员」也指归档里的条目 |
| **A 类 / B 类** | 两类片源。A 类是原始高分辨率扫描（总缩放比 ≥ 2，可能含真 1bit 网点）；B 类是网络发布版（已降采样并有损转码，连续灰调，比值 < 2）。见 ADR 0008 |
| **处理范围 (Scope)** | 用户点名的子集。不做全库扫描、不维护全库索引，源库只读 |

## 几何

| 术语 | 含义 |
|---|---|
| **面板 (Panel)** | 物理显示面板：分辨率 + PPI + 灰阶数 + 彩色。四项，是 profile 的主键 |
| **彩色面板** | 能显示彩色的面板，Kaleido 一类。分辨率、PPI、灰阶数说的都是它**黑白那一层**。彩页在它上面保留颜色，在黑白面板上转灰 |
| **设备 (Device)** | 具体型号。只是面板的别名，多对一 |
| **Profile** | 面板 + 阈值档位 + 各开关。标定档位属于 profile，不属于面板 |
| **目标尺寸 (Target size)** | fit-inside 后每页算出的实际输出像素尺寸 |
| **总缩放比** | 源页高 ÷ 目标高 |
| **预缩 (Prescale)** | 整数倍 box 降采样。仅在总缩放比 ≥ 2 时存在 |
| **残差比 (Residual ratio)** | 预缩之后剩下的非整数段，恒 < 2 |
| **几何门 (Geometry gate)** | 判定「目标尺寸未被下游缩放」成不成立的那道门。灰阶硬上界与抖动共用它，判定同源 |

> 「面板分辨率」是设备属性，「目标尺寸」是每页算出来的，两者不是一回事。漫画页更瘦，目标尺寸的宽必然小于面板宽。

tonefit 维持一条不变量：**目标尺寸 = fit-inside(面板分辨率)，且输出不再被下游缩放。**
抖动的启用条件与灰阶硬上界都依赖它，两者经同一道几何门。
门对**整卷**只有一个结果：卷内有一页贴不住面板，整卷的抖动即整体关闭。
走彩色分支的页不参与这道门——它既不量化也不抖动，门撑的那两件事与它无关（ADR 0010）。

## 图像内容

| 术语 | 含义 |
|---|---|
| **网点 (Screentone)** | 用二值点阵模拟灰调的技法。不叫 halftone |
| **灰调 (Tone)** | 网点被降采样解析出的连续灰度。**网点是因，灰调是果** |
| **灰调级数** | 内容侧实际出现的灰度取值个数 |
| **线稿 (Line art)** | 高对比度墨线。判据对它相对不敏感——崩掉的是灰调 |
| **彩页** | 第一遍识别出带彩色内容的页。判据是色度幅度加覆盖率，两个数未标定。彩色面板上走彩色分支，黑白面板上转灰——转灰之后是离群页的主要来源 |

## 量化

| 术语 | 含义 |
|---|---|
| **位深 (Bit depth)** | 输出每像素比特数，全集 {1,2,4,8}。**编码属性** |
| **灰阶数 (Gray levels)** | 面板物理能显示的灰度级数，e-ink 恒 16。**显示属性** |
| **候选 (Candidate)** | 一个 (位深, 抖动模式) 组合 |
| **抖动 (Dither)** | 用高频误差换低频保真 |
| **标定图** | `calibrate` 出的那张灰阶阶梯图：按面板分辨率排布，并排给出各候选位深的阶梯，判读说明印在图内。它是量具，不走管线——不判定、不量化，无损写出 |
| **感知可分辨级数** | 在真机上 1:1 显示标定图、目视数出的那个数。`--gray-levels` 填的就是它 |

> 位深 4 ↔ 灰阶数 16，两者不是同一个量。灰阶数是位深的**硬上界**，在判据之前裁剪候选位深
> （e-ink → {1,2,4}，LCD → 全集）。调色板模式不是候选的一维，它在编码器接口以内。
> 不用「压缩」或「分辨率」指代位深。
>
> 感知可分辨级数与面板的物理灰阶数**不必相等**：显示固件的处理、环境光、观看距离都会改变前者。
> 灰阶数的取值来源见 ADR 0003 的《决定》——标定图是其中唯一自证得了的那一条。

## 判据

| 术语 | 含义 |
|---|---|
| **参照 (Reference)** | 缩放到目标尺寸后、未经目标位深量化的图。工作精度 8 位灰度 |
| **判据 (Metric)** | 候选与参照之间的误差数值。低通型，分块聚合，带对比度掩蔽加权 |
| **阈值 (Threshold)** | 判据的可接受上限 |
| **判定 (Verdict)** | 一页定下的候选，连同定它的理由。判据与阈值比出来的那个结果 |
| **分块 (Tile)** | 判据的聚合单位，取上分位而非均值 |
| **上包络 (Envelope)** | 卷内各页判定的 p95 聚合，定卷内主体的基准档 |
| **基准档** | 上包络定出的那一档，卷内主体共用它 |
| **驱动页 (Driver)** | 站在上包络分位秩上的那一页，基准档就是它的判定 |
| **迟滞 (Hysteresis)** | 限制档位切换频率的规则 |
| **仲裁 (Arbitration)** | 判据落在边界带时调用 SSIMULACRA2 复核 |
| **离群页** | 判据显著偏离卷内分布的页。不参与上包络，单独定档 |

> **判据是量，阈值是界**，不互换。参照不是源页——用它恰好隔离出「量化」这一个变量。

## 管线

| 术语 | 含义 |
|---|---|
| **幂等这一道** | 两遍之前的那一步：算出本卷的指纹，与上一趟写在输出里的比。对得上就整卷跳过，一遍都不做。它把源字节读一遍，因此**不是免费的**——`--no-metadata` 下整道不在 |
| **第一遍 / 第二遍** | 处理一个卷的两遍。第一遍解码、彩页识别、几何、缩放、算判据并缓存参照；第二遍只量化、编码、写出，不再碰源页 |
| **灰度路径 / 彩色分支** | 页在第一遍分出的两条路。灰度路径转灰、算判据、进缓存，第二遍照判定量化；彩色分支只缩放并编码，不进灰度缓存也不进上包络。走哪条由**面板与页**共同决定：只有彩色面板上的彩页走彩色分支 |
| **汇总** | 夹在两遍之间的那一步：要看完整卷才做得了的统计落在这里 |
| **缓存 (Cache)** | 两遍之间存放参照的地方。内存优先。寿命是一次运行；会话里参数哈希未变的相邻两趟共用一份，见《会话》的「续做」 |
| **缓存预算 (Cache budget)** | 缓存最多在内存里留多少字节。限的是峰值内存，不是卷的大小上限 |
| **溢写 (Spill)** | 越过缓存预算的那些页改存临时文件 |

> 缓存存的是**参照**，不是源页解码后的原图——两遍分工与这个选择的代价见 ADR 0005。

## I/O 与并发

| 术语 | 含义 |
|---|---|
| **介质 (Medium)** | 一条读取通道落在什么盘上。三种取值：有寻道惩罚（机械盘）、无寻道惩罚（固态盘）、未知 |
| **寻道惩罚** | 机械盘上并发随机读互相打断寻道的那笔代价。有它就串行读，没有就并发读 |
| **读取通道 (Channel)** | 路径解析到的那个卷/挂载点。介质**按它**探测，探测结果也**按它**缓存 |
| **读取层 / 计算层** | 读取层取字节，计算层解码到判据。前者的并发度由介质定，后者由核数定，两个数不相干 |
| **有界通道** | 夹在两层之间的那道口子。它按**在途字节**背压，不按条数——页的大小差着一个量级 |
| **在途字节** | 已经读出来、还没算完的源字节 |
| **读取预算** | 在途字节的上界。它是个**常数**：并发度翻倍，峰值不跟着翻 |
| **读取计划 (IoPlan)** | 一个卷这一趟怎么读：介质是什么、据此派几条读取、这个数是谁定的 |

> 介质**按路径**探测，一次运行里不同路径各判各的；探不出来的一律按未知处理，取保守并发度，
> 不假装它是本地盘的某一种。为什么不能按机器判定一次，见 ADR 0009 的《背景》。
>
> `--io-mode` 覆盖的是**策略**，不是事实：探到的介质照实进报告，变的只是这一趟派几条读取。
> 归档卷不在这套里——一个 ZipArchive 就是一个游标，读取恒串行，点名也改不了。

## 进度

| 术语 | 含义 |
|---|---|
| **步** | 进度报到的单位。一个卷的步数是它这一趟真要走的那几段之和：幂等这一道读全部成员、第一遍走每一页、第二遍写全部成员 |
| **事件 (Event)** | 库向外报到的那一条条消息。走完一步是其中一种；一卷跑完的那一条**带着这一卷的 `VolumeReport`** |
| **预扫 (Survey)** | 开工之前把点名的卷全枚举一遍，算出这一趟的**全局总步数**。只列成员、不碰像素；枚举结果交给管线复用，不扫两次 |
| **进度观察者 (Progress)** | 收事件的那一端。库只报到，印在哪、印不印、长什么样由调用方定 |

> 报到的单位是步而不是页：一个卷要走几遍随模式而变，按页报到会让进度在第一遍里一动不动、
> 到第二遍猛地冲完。幂等命中的卷提前收摊，预告的步数因此是**上界**，不是承诺。

> 事件流就是**报告的增量**：卷级事件带着 `VolumeReport`，调用方边跑边把报告攒出来。
> CLI 攒完在最后一次性渲染，TUI 攒到哪儿画到哪儿——两者拿的是同一份数据，不是两套。

> 全局总步数与单卷步数同一个性质：都是**上界**。幂等命中的卷提前收摊，预扫算进去的那几段不会走。
> 预扫在慢盘上要花一点时间，那段时间里屏上什么都没发生——它换来的是一个真的 ETA（ADR 0011）。

## 会话

TUI 是 `run` 之上的第二个薄层，与 CLI 同级：它不绕过 seam，也不多一条管线。
**无参数即会话，带参数即直接跑**——命令行那一路的行为一字不改。

| 术语 | 含义 |
|---|---|
| **会话 (Session)** | TUI 从打开到退出的这一段。一个会话里可以跑很多趟，参数在趟与趟之间改 |
| **设备层** | 会话里可改的第一层：profile（型号 + 灰阶数覆盖）。绑面板，由标定决定，改一次管很久 |
| **口味层** | 第二层：这一趟的立场——滤波器、位深、抖动、逐页、缓存预算、读取策略 |
| **范围层** | 第三层：处理范围与输出根，在会话里打进去（路径按层补全，不递归、不建索引）。每趟都不同，因此**不进预设** |
| **预设 (Preset)** | 一份命名的落盘配置，装设备层与口味层两层。CLI 只在**显式点名**时读它 |
| **试算** | 会话里按下去的那一次 dry-run：只算不写，报告照出 |
| **续做** | 试算之后紧接着按执行，于是**只跑第二遍**——参照还在缓存里。中间做过别的就整趟重跑（ADR 0012） |
| **收尾** | 按停的第一级：当前卷跑完才停。盘上只有完整的卷，下一趟幂等接着走 |
| **中止** | 按停的第二级：立刻停，并把当前卷的**半卷**删掉（ADR 0013） |
| **半卷** | 中止时留在输出里的那一份不完整的卷。它不是一份对的输出，因此不留 |

> 三层的分界线画在**生命周期**上，不画在「哪几个 flag 长得像」上：设备层错了是判定的依据错了，
> 口味层错了只是这一趟不好看，范围层错了会写到别人的目录里。预设收前两层、不收第三层，同一条线。

> 试算与执行是**同一个 `run`**，区别只在 mode：两者都报事件、都能停。
> 「先看一份报告再决定」与「看得见进度」因此不是两件事，是同一条回路的两半。
> 一趟跑起来之后设备层与口味层**只读**：卷与卷之间换参数，报告顶上那行 `profile` 就开始撒谎。

> 设备层里还挂着标定图。`calibrate` 仍是「另做一件事」——不读源、不走管线、不判定、无损写出——
> 但它出的那个数（感知可分辨级数）恰好是设备层唯一填不出来的一格，所以会话里给它一个键。
> 会话出图不改变它的地位：它还是量具，不是被处理的页。

> 中止之所以删半卷：目录卷是逐页直写的，半卷里的每一页都带着完全正确的元数据，
> 摆在文件管理器里与一本处理好的书没有分别。幂等那一道会因为「输出不齐」重做它，
> 结果不会错——但它和《失败》里说的「过期副本」是同一类陷阱，而这一个是我们自己留下的。
> 归档卷不在此列：它本来就是先写 `partial` 再改名，中止只留一个改不了名的临时文件。

## 输出

PNG 是默认，也是唯一保证四台目标设备自带阅读器都能读的格式。`--codec=avif` 是显式可选项，
面向第三方阅读器。判据层对编码器无知，编码器选择在其外层。见 ADR 0004。

| 术语 | 含义 |
|---|---|
| **记录 (Record)** | 写进输出 PNG tEXt 的自描述元数据：工具版本、profile 名、参数哈希、源哈希、判定、理由。取值一律 ASCII——tEXt 只装得下 Latin-1 |
| **指纹 (Fingerprint)** | 记录里兼作幂等依据的前四项。判定与理由是这四项推出来的结果，不进比对 |
| **源哈希** | 指纹里代表源的那一项。作用域是**卷**——理由见 ADR 0006 的《决定》末段 |
| **参数哈希** | 指纹里代表参数的那一项，收的是**会改变输出**的每一项。型号名不收（它另占指纹的一项），缓存预算、模式、输入与输出路径也不收 |
| **跳过 (Skipped)** | 幂等命中的卷级判定：上一趟的输出还齐着、指纹一项没变，这一卷一页都不重做 |

> 记录**随文件走**：它就在页里，文件被移动、改名、重新打包都不会丢，幂等因此不需要一份全库索引
> （ADR 0009 关掉的正是那个）。「不会丢」说的是记录本身——输出去处仍按源卷名算，
> 改了输出容器的名字，下一趟会在原来的名字上重写一份。
> `--no-metadata` 一关，记录与幂等一起消失——两者是同一批字段。

## 失败

| 术语 | 含义 |
|---|---|
| **失败页** | 三种页：字节读不出来的、完整尺寸解不出来的、尺寸解得出来但像素缓冲大到分配不下的 |
| **救回 (Salvage)** | 整解失败之后的第二趟：按文件头里的尺寸建好缓冲，解到哪一行算哪一行。救回来的页**不是**失败页 |
| **占位页** | 失败页在输出里留下的那一张：卷内统一尺寸的纸白页。它顶住页序与尺寸，不冒充内容，记录里的判定写着 `failed` |
| **卷内统一尺寸** | 卷内处理成了的那些页里出现次数最多的目标尺寸。占位页按它出；一页好页都没有的卷退到面板分辨率 |
| **卷级失败** | 一整卷点不开：路径不在、归档损坏、权限不够、透传文件搬不动。**预扫**发现的整趟拒绝、一页不做；预扫之后才出的记为卷级失败，其余卷照做，报告照出 |
| **隔离目录** | 含失败页的卷的去处：输出根下的 `_isolated/`。容器形态照旧——目录卷进去仍是目录，归档卷仍是归档 |
| **过期副本** | 一卷在**另一个**去处留着的上一趟输出。去处随「有没有失败页」在干净目录与隔离目录之间跳，这一趟写不到的那一份不会被覆盖，也不会被删 |

> 界线画在**能不能出一张对的页**上，而尺寸是其中的大头：尺寸解得出来的页有自己的几何，
> 照常参与几何门、判据与上包络，缺的几行留白；解不出尺寸的页连几何都没有，只能借卷内统一尺寸。
> 尺寸解得出来却分配不下缓冲的页仍算失败——「解得出尺寸」在它身上买不到任何像素。
>
> 失败是**页级**的，隔离是**卷级**的：一页失败，整卷进隔离目录，其余页照常处理。
> 透传文件不在这套里——它逐字节照搬，搬不动就是卷级的失败。
>
> 隔离过的卷不被跳过：那不是一份做完了的输出，而失败清单每一趟都要重新给得出来。

> 四种结局各占一个退出码，理由与当初把「有卷被隔离」分出来的理由是同一条：脚本要分得开。
> `0` 全部成功、`2` 做完了但有卷被隔离、`3` 有卷没做成、`1` 这一趟没做成（拒绝执行）。
> `2` 与 `3` 同时成立时取 `3`——「有卷带着坏页」和「有卷根本没做成」是两个不同的决定。

## 模式

`fast` 保吞吐下限，关闭 oxipng，仲裁仅在边界带触发。`best` 不限时，质量选项全开。

## 尚未确立

- **阈值的具体数值**——靠真实样本 + 人工 A/B 盲测标定，基准设备 Kobo Libra 2。
  P0 交付的是未标定的保守占位值，报告在 profile 那一行原样标出
- **A 类素材的一切数值**——现有实测全部来自 B 类
- **有损转码源的行为**——AVIF/JPEG 伪影会让真 1bit 源也显示 256 级灰调，一切基于源灰调级数的检测在真实片源上会失效。
  同一批伪影也给消色像素添上几级色度，**彩页识别的色度地板与覆盖率**这两个数因此落在这个洞里（ADR 0010）
- **Kaleido 彩色面板的一切数值**——彩色滤光片让彩色内容的实际分辨率降到黑白层之下，各家口径是一半。
  P0 的彩色分支只做缩放，这个折减到不了任何判定上；要利用它得先测
- **几何门不成立时该用哪个位深集合**——对齐消失，灰阶硬上界的依据随之失效（ADR 0003）。
  尚未测量，P0 仍照 {1,2,4} 裁，抖动则整体关闭
- **LCD 路径**——无灰阶硬上界，且项目所有者无 LCD 设备可标定，相关 profile 必须标注「未经标定」
- **续做的粒度**——参数哈希是一个整体，给不出「哪些参数脏了」。改一个位深就要整趟重跑第一遍，
  而位深本来不影响参照。要更细的粒度，得先给参数哈希分层（ADR 0012 的《备选方案》）
- **预设的字段演进**——预设里存着口味层的字段名。工具增删参数之后，旧预设该报错、该忽略、
  还是该迁移，尚未决定；在决定之前，预设只在同一版本内可靠
- **NAS 的最优 I/O 策略**——既非 HDD 也非 SSD。P0 把它归进「未知」并退到串行读，
  多半留了性能在桌上；实测出并发更快的用户走 `--io-mode concurrent`
