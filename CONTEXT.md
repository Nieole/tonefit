# tonefit — 领域上下文

tonefit 把漫画页适配到电子墨水阅读设备：在尽量不影响阅读效果的前提下，为每页选择缩放、几何裁切与输出位深。

本文是**词汇表**。issue 标题、类型名、模块名、测试名提到领域概念时用这里的词，不要漂移到本文避开的同义词。
决策依据见 `docs/adr/`，全部实测数字见 `docs/measurements.md`。

## 处理对象

| 术语 | 含义 |
|---|---|
| **页 (Page)** | 一张待处理的图片。不叫 image——后者指内存中的像素缓冲 |
| **源页 / 输出页** | 一张源页可以产出多张输出页（跨页拆分）。第一遍之后管线的单位是输出页：缓存、判定、报告、写出都按它数。`VolumeReport` 两个数分开给 |
| **卷 (Volume)** | 一次处理调用的作用域：一个目录或一个归档，内含按阅读顺序排列的页。**至少有一页**——一页都没有的东西不是卷 |
| **发现 (Discovery)** | 点名的一个路径展开成一批卷。归档是一个卷、内部结构照收；目录直接躺着页就同时是一个卷（只收直接那一层），并且继续往下发现。**一个目录可以既是卷又装着卷**，而一页都不被处理两遍。见 ADR 0014 |
| **分卷序列 (split sequence)** | 一组分开打的 `.rar`：`x.part1.rar` … `x.partN.rar`。UnRAR 打开**头一份**就跨卷把整套读完，因此它们合起来是**一个**卷，卷名取序列的名字（`x`）——`.partN` 那一截不进产物。**续的那几份不是卷**：不列成候选、报告里一栏都没有，也不是非卷文件——它们属于头一份那一卷（那一卷做成了，它们的字节一个不少地进产物；那一卷做不成，说得出为什么的是**头一份**那一条）。「这一份是不是另一份的续」看**内容**（归档头里那一位，`SplitPart`），不看名字；「是哪一种格式」仍只看扩展名。见 ADR 0015 决定第 1 条 |
| **点名的 / 发现的** | 一个卷是用户点了名的，还是在点名的路径底下发现出来的。只决定**点不开时的处置**：点名的整趟拒绝，发现的记下来、其余照做 |
| **源 (Source)** | 输入容器抽象。按阅读顺序吐出页，非图片文件原样透传。目录与归档在**读取与输出**上同形，在**卷边界**上不同形——归档的边界由打包者定死了，目录的边界可以再分 |
| **输出容器 (Sink)** | 输出侧容器抽象。收页与透传文件。形态随输入而定：输入是归档，输出也是归档；扩展名一律归一成 `.cbz`（ADR 0015） |
| **成员 (Member)** | 卷里的一项：一页，或一个原样透传的文件。目录卷只收**直接那一层**，子目录与躺在这一层的归档都不算——它们各自是卷。打包环境留下的边车与索引文件也不算，源在枚举时就把它们摘掉。「成员」也指归档里的条目 |
| **非卷文件** | 发现走完之后没被任何卷收下的文件：躺在目录里既不是页也不是归档的、一页都没有的归档、发现出来但点不开的归档。**不转、不拷、输出里一个字节都没有**，也不是失败 |
| **走不进去的地方 (UnreachablePlace)** | **本该走得进去、这一趟走不进去**的目录：权限没配好、盘掉了——**换一趟可能就好了**，而报告劝人做的正是这类事。本来就不看的那几种不落在这里，它们是**不看的地方**。它底下有没有卷谁都不知道，整棵子树跳过，其余卷照常跑完。报告上**自成一栏**，逐条带着路径与一句为什么。它不是非卷文件——那三类是**文件**，这是一个**地方**；也不是卷级失败——那一列里每一条都是一个卷。**它进退出码**：有这一栏就不是「全都做成了」。**点名的**那一个走不进去仍是整趟拒绝 |
| **不看的地方 (IgnoredPlace)** | **本来就不看**的目录，发现走到它跟前就绕过去：打包环境留下的那几个，以及操作系统留下的那几个（`System Volume Information`、`$RECYCLE.BIN`、`lost+found`、`.Trash-1000` 这一类，**永远**读不动）。**默默跳过**——不进退出码、不进任何一栏、报告上一个字都没有。与**走不进去的地方**恰好成一对：那一个是**看不了**，这一个是**不看**；分得开它们的是「换一趟会不会好」——报告劝人做的三件事（文件还在不在、盘还挂着没有、权限变没变）对这一类一件都做不了，说了等于噪声。判据按**名字**认，一份名单 |
| **A 类 / B 类** | 两类片源。A 类是原始高分辨率扫描（总缩放比 ≥ 2，可能含真 1bit 网点）；B 类是网络发布版（已降采样并有损转码，连续灰调，比值 < 2）。见 ADR 0008 |
| **处理范围 (Scope)** | 用户点名的子集。点名的是**在哪里找**，不是**找到什么**——发现只在点名的路径底下走。不做全库扫描、不维护全库索引，源库只读 |

## 几何

| 术语 | 含义 |
|---|---|
| **面板 (Panel)** | 物理显示面板：分辨率 + PPI + 灰阶数 + 彩色。四项，是 profile 的主键 |
| **彩色面板** | 能显示彩色的面板，Kaleido 一类。分辨率、PPI、灰阶数说的都是它**黑白那一层**。彩页在它上面保留颜色，在黑白面板上转灰 |
| **设备 (Device)** | 具体型号。只是面板的别名，多对一 |
| **Profile** | 面板 + 阈值档位 + 各开关。标定档位属于 profile，不属于面板 |
| **目标尺寸 (Target size)** | 按**适配方式**由源尺寸与面板分辨率算出的每页实际输出像素尺寸 |
| **适配方式 (Fit mode)** | 目标尺寸怎么算出来。`height`（默认）高恒等于面板高、宽随源比例、**允许超出面板宽**；`inside` 整页塞进面板，长边定夺 |
| **适配结果 (Fit)** | 一页适配出来的东西：目标尺寸，加上这个尺寸是不是**兜底上界**改出来的。适配方式是规矩，它是那条规矩在这一页上的结果 |
| **兜底上界 (Backstop)** | 目标像素数的上界，防的是**分配不下中止整趟**，不是画质。越界的页退回 fit-inside，不是失败页。不是开关，用户调不了。真实素材够不着它 |
| **跨页 (Spread)** | 一张横跨两页的源页。宽高比远超面板，`height` 下宽溢出面板 |
| **跨页拆分 (Spread splitting)** | 把跨页从**装订沟**上切成两张输出页。默认打开，次序是裁边 → 判跨页 → 拆分 → 每半再裁 → 适配 |
| **跨页候选 (Spread candidate)** | 宽高比够得上跨页的那些页，拆分两级判定的第一级。与《量化》的**候选**不是一回事——那一个是 (位深, 抖动模式) 组合，两者不许缩成同一个词 |
| **拆分阈值 (Split threshold)** | 挑跨页候选的那条线：页宽高比 ÷ 面板宽高比 的下界。可调 |
| **装订沟 (Gutter)** | 跨页正中附近贯穿全高的一段空白列。它定的是**切不切**，不只是切在哪 |
| **连续跨页 (Continuous spread)** | 找不到装订沟的跨页：一幅画横跨两页。**不切**，退回以高为准靠横向平移看 |
| **阅读方向 (Reading order)** | 拆开后两半的先后。右开（默认）右半在先，左开反之。它不改变两半各是哪一侧 |
| **总缩放比** | 源页高 ÷ 目标高 |
| **预缩 (Prescale)** | 整数倍 box 降采样。仅在总缩放比 ≥ 2 时存在 |
| **残差比 (Residual ratio)** | 预缩之后剩下的非整数段，恒 < 2 |
| **几何门 (Geometry gate)** | 判定「目标尺寸未被下游缩放」成不成立的那道门，**逐页**判。灰阶硬上界与抖动共用它，判定同源 |
| **判定范围 (Gate scope)** | 几何门问到的那些页：灰度路径上的每一页。彩色分支上的页与失败页不在里面 |
| **像素完整性** | 阅读软件有没有把输出的像素**原样**贴到面板上。它在几何门之外、也在 tonefit 的视野之外（ADR 0007），只有标定图问得出来 |

> 「面板分辨率」是设备属性，「目标尺寸」是每页算出来的，两者不是一回事。
> 漫画页比面板更瘦长、本来就受高度约束，两种适配方式因此在普通漫画页上产出**同一个尺寸**；
> 分岔只发生在比面板更扁的页上，也就是跨页——`height` 下它的宽会超出面板，靠阅读器横向平移。

**裁边、跨页拆分、以高为准三项默认全开**，各有关掉它的写法；前两项那两个开关**各有反面**（`--crop`、`--split`），套了一份关掉它的预设之后这一趟开得回来。三笔代价跟着认下：
**跨页卷的输出体积涨到几倍**、**比面板矮的页被放大到面板高**、**页与页的字号跳动变大**
（逐页各裁各的，拆开之后两半又各裁一次）。三笔的实测数分别在 measurements 的
《适配方式：fit-inside 与以高为准》《裁边》《跨页拆分》，这里不复述。

tonefit 维持一条不变量：**目标尺寸由适配方式算出，且输出不再被下游缩放。**
抖动的启用条件与灰阶硬上界都依赖它，两者经同一道几何门。
门**逐页**判，一页的门只决定这一页：**判定范围**是灰度路径上的每一页（ADR 0007）。
走彩色分支的页不在范围内——它既不量化也不抖动，门撑的那两件事与它无关（ADR 0010）；
失败页连几何都没有。部分救回页在范围内：它的尺寸是文件头里的真尺寸，答得出这个问题。
门不成立的页抖动关闭、不进上包络，位深仍不低于基准档——门只拿走抖动，不拿走档次。
一页在范围内的页都没有时门无人可关，判定为成立——那是真话，不是「不知道」。

> 门问的是**目标尺寸有没有一条边贴住面板**，两种适配方式的读数因此一样；不一样的是
> 「不再被下游缩放」要阅读器做什么：`inside` 留边，只要它填背景不重采样；
> `height` 溢出，要它平移不缩放——**后者更强**，而门够不着这个差别。

> **不变量成立不足以让抖动成立**：阅读软件可以在贴到面板之前自己再重采样一遍，
> 那一层是**像素完整性**，几何门够不着它。不成立时抖动被抹平、字节照付
> （见 measurements 的《真机像素完整性》）。tonefit 探不到它，也不据它判定——
> 标定图把这个问题交给人，一次上机当场看出来。

## 图像内容

| 术语 | 含义 |
|---|---|
| **网点 (Screentone)** | 用二值点阵模拟灰调的技法。不叫 halftone |
| **灰调 (Tone)** | 网点被降采样解析出的连续灰度。**网点是因，灰调是果** |
| **灰调级数** | 内容侧实际出现的灰度取值个数 |
| **线稿 (Line art)** | 高对比度墨线。判据对它相对不敏感——崩掉的是灰调 |
| **纸白 (Paper white)** | 一页上纸的那个灰度取值：**全页 3×3 邻域方差为零的像素里，出现次数最多的那个灰度**，并列时取更小的那个。量的是**散布全页的那一张平坦掩码**，不做连通域分析——它**不是**「最大那一块里的众数」，两者只在最大那块恰好也是最常见取值时才相同。它是**源上量出来的一个数，不恒是 255**：全语料 1202 页里 43% 的页纸白不落在输出格点上（measurements 的《全语料普查：四成三的页纸白不落在格点上》）。不取「最亮像素」（一个亮斑就带偏），不取高百分位（大片纯黑页上取到的是错的值）。**平坦像素太少的页量不出纸白**——满版无纸边的画集就是那一类，那不是失败，是这条定义说不出话的地方 |
| **彩页** | 第一遍识别出带彩色内容的页。判据是色度幅度加覆盖率，两个数未标定。彩色面板上走彩色分支，黑白面板上转灰——转灰之后是特例页的主要来源 |

## 量化

| 术语 | 含义 |
|---|---|
| **位深 (Bit depth)** | 输出每像素比特数，全集 {1,2,4,8}。**编码属性** |
| **灰阶数 (Gray levels)** | 面板物理能显示的灰度级数，e-ink 恒 16。**显示属性** |
| **候选 (Candidate)** | 一个 (位深, 抖动模式) 组合 |
| **抖动 (Dither)** | 用高频误差换低频保真 |
| **离格量 (Off-grid offset)** | 源上一个取值到最近格点的距离，`0 ≤ 离格量 ≤ 格点间距/2`（《格点间距》见《判据》）。它是**源与输出格点之间的关系**，不是源自己的属性——同一个取值换一档位深，离格量就变。**平坦区上抖动必须撒点的密度就是 `离格量 / 格点间距`**：纸白 255 离格 0，白底上一个点都不撒；纸白 253 在 2bit 上离格 2，就得把 2.4% 的像素撒到低一级的格点上。数据见 measurements 的《真机三组：4bit 上的抖动分不出，白底上的抖动分得出，分界线是纸白落不落在格点上》 |
| **标定图** | `calibrate` 出的那张图：按面板分辨率排布，**一次上机答两件事**——先是像素完整性（抖动块与同均值实心块的并置、1 像素周期光栅、四角标记），再是并排的各候选位深阶梯。判读顺序与中英两份说明都印在图内。它是量具，不走管线——不判定、不量化，无损写出 |
| **感知可分辨级数** | 在真机上 1:1 显示标定图、目视数出的那个数。`--gray-levels` 填的就是它 |

> 位深 4 ↔ 灰阶数 16，两者不是同一个量。灰阶数是位深的**硬上界**，在判据之前裁剪候选位深
> （e-ink → {1,2,4}，LCD → 全集）。调色板模式不是候选的一维，它在编码器接口以内。
> 不用「压缩」或「分辨率」指代位深。
>
> 感知可分辨级数与面板的物理灰阶数**不必相等**：显示固件的处理、环境光、观看距离都会改变前者。
> 灰阶数的取值来源见 ADR 0003 的《决定》——标定图是其中唯一自证得了的那一条。
>
> 标定图的写出是库的**第三个 seam**，不并进处理卷那个主入口：量具与被处理的页走的不是同一条路。
> 建父目录与写文件都在库内完成，命令行与会话共用同一个调用；界面文案留在界面层。

## 判据

| 术语 | 含义 |
|---|---|
| **参照 (Reference)** | 缩放到目标尺寸后、未经目标位深量化的图。工作精度 8 位灰度 |
| **判据 (Metric)** | 候选与参照之间的误差数值。**低通项＋颗粒项**，分块聚合，带对比度掩蔽加权 |
| **低通项** | 判据的一半：参照与候选各做低通之后的局部均值误差。量灰调塌陷与 banding |
| **颗粒项** | 判据的另一半：候选比参照多出来的高频起伏里超出**可见度地板**的那一部分。量抖动颗粒自身有多显眼。地板**跟着《格点间距》走**（见《判据构成》），三档各是各的数 |
| **格点间距 (quantisation step)** | 一档位深上相邻两个格点差多少灰度级：`255 / (2ⁿ − 1)`。格点是套嵌的（255 = 3×85 = 15×17），三档因此都是整数：1bit 255、2bit 85、4bit 17。判据的可见度地板按它的比例算（见《判据构成》），报告那一行也按它说 |
| **阈值 (Threshold)** | 判据的可接受上限。跟着面板走，**标定来源因此是它的一部分**，报告里与数值一同印出。搬到另一块面板上**要由实测支持**：低通核由 PPI 推出（ADR 0002），**同 PPI 的两块面板上判据量的是同一件事**，PPI 不同则不是 |
| **判定 (Verdict)** | 一页定下的候选，连同定它的理由。判据与阈值比出来的那个结果 |
| **覆盖顶死 (Pinned verdict)** | 四道裁剪（`--bit-depth`、`--dither`、面板灰阶数、几何门）合起来把候选集裁到**只剩一个**，《判定》整个被顶掉——判据说什么都不改结果。**裁的是哪一道不影响这件事**。**一个覆盖项都没点而只剩一个时不算**：那一档仍是判出来的。「开卷之前答不答得出」是另一问，答得出的那一趟第一遍就把字节编完（见《缓存》）。哪一道够得着哪一维、为什么每一趟顶死都点着 `--dither`、以及不算的那一种为什么不算，见 ADR 0005 |
| **分块 (Tile)** | 判据的聚合单位。边长 32×32 恒定，**不随页尺寸缩放**；聚合取上分位与「第 K 差的那一块」之间更严的那个，不取均值 |
| **判据聚合 (Aggregation)** | 把分块误差收成一个数的那套取法，作为一件东西：块边长、上分位、K 三个数。报告原样印出，形状见《分块》 |
| **判据构成 (Composition)** | 一块的读数由哪几项组成，作为一件东西：眼下就是颗粒项那道可见度地板，而它带的是**一个比例**——地板占《格点间距》的份额，某一档的地板由它乘那一档的间距算出（为什么是一个比例而不是三档一张表，见 ADR 0002 决定第 5 条）。与《判据聚合》不是同一层——那个说块的读数怎么收成一个数，这个说一块的读数本身从哪来。报告与判据数值一同印出 |
| **判据掩蔽 (Masking)** | 一块的读数**怎么加权**，作为一件东西：地板与拐点两个数（ADR 0002 决定第 4 条）。平坦块不打折，块内活动度越高折得越狠、最深折到**地板**；**拐点**是加权正好落在「不打折」与地板中点的那个活动度。与《判据构成》《判据聚合》平级，三者是判据形状的三件事——由什么组成、怎么加权、怎么收成一个数。**两个数都是未标定占位值**，报告与判据数值一同印出、占位这件事跟着印（缺口见《尚未确立》） |
| **上包络 (Envelope)** | 卷内**不含特例页的那些页**判定的 p95 聚合，定它们共用的基准档。彩色分支上的页与失败页不在里面，部分救回页与几何门不成立的页也不在——例外见下。**描述的是 `--envelope` 那条路**：默认路径上位深逐页各判各的、不做迟滞，这一层整个不在场（ADR 0018） |
| **基准档** | 上包络定出的那一档，卷内其余页共用它。描述的是 `--envelope` 那条路 |
| **其余页 (Envelope::body_pages)** | 摘出去的那几类（哪几类见《上包络》）之外，卷内剩下的灰度页。它们共用基准档。描述的是 `--envelope` 那条路 |
| **定档页 (Envelope::driver)** | 站在上包络分位秩上的那一页，基准档就是它的判定。描述的是 `--envelope` 那条路 |
| **迟滞 (Hysteresis)** | 限制档位切换频率的规则。描述的是 `--envelope` 那条路 |
| **上包络开关 (`--envelope`)** | 位深走哪条路的那一个开关，口味层的一项（预设键 `envelope`，反面 `--no-envelope`）。**默认关**：默认路径上位深**逐页各判各的**、不做迟滞，每一页拿到判据说它要的那一档，卷级判定是「逐页」（`VolumeVerdict::PerPage`），参照一张都不进《缓存》；开着走《上包络》那条路（ADR 0018 决定第 2、5 条）。`--per-page` 已退场，点到报错、指到这里 |
| **仲裁 (Arbitration)** | 判据落在边界带时调用 SSIMULACRA2 复核 |
| **特例页 (Envelope::outlier_pages)** | 判据显著偏离卷内分布的页。不参与上包络，单独定档。描述的是 `--envelope` 那条路 |
| **立脚点 (Anchor)** | 量一页偏离多少所站的那一档。**逐页各取一个，且不含被判的这一页**——一页要么偏离卷里的别人，要么不偏离 |

> **判据是量，阈值是界**，不互换。参照不是源页——用它恰好隔离出「量化」这一个变量。
>
> 判据是**两项之和**，因为一项不够：低通项量的正是抖动被设计来优化的那个量，
> 单靠它会结构性地偏袒抖动，在同一页之内把 1bit+FS 排在 2bit 不抖之前，而真机盲测的次序相反。
> 颗粒项补的就是「颗粒自身有多显眼」这一项（ADR 0002 决定第 5 条）。数据见 measurements 的《位深盲测》。
>
> 颗粒项那道**可见度地板**是判据的一部分，报告与判据数值一同印出：
> 它是**格点间距的一个比例**，因此一个数管三档。下界算得出来（`0.2071·s`，低于它，
> 那一档上抖动会在某个灰调上输给不抖动），上界由真机盲测夹出，与阈值同一批数据、同一块面板。
>
> 特例页判据是**两个数**：偏离量在立脚点那一档上量，立脚点站在哪个分位是其中一个，
> 偏离到阈值的几倍才算「显著」是另一个。两个数都未标定（ADR 0006）。
> 立脚点不能取上包络那一档——那一档由全卷聚合而来，被判的页自己也在里面，
> 最极端的那一页按定义永远落在自己那一档以内。
>
> 摘出去的页各有各的摘法。特例页按判据偏离摘（见上），部分救回页按**页残缺**摘——
> 它那条判据曲线是在一页大半留白的图上求出来的，而留白在任何位深上都是格点、误差恒为零。
> 两者都单独定档。门不成立的页按**几何**摘：它那套候选里没有抖动那一维，
> 而上包络取的是 (位深, 抖动模式) 这个组合——候选集不同的页排不进同一条序列。
> 它不单独定档，跟着基准档的位深走、不低于它（ADR 0007 决定第 3 条）。
> **两刀落在同一页上时门那一刀在外层**：不具代表性的曲线更没有资格把这一页压到基准档以下。
>
> **一页不剩地落在同一侧时一页都不摘**：那说明偏离的、残缺的是这一卷本身，不是其中几页，
> 而其余页不能空着。几何门那一侧照同一条走：一页门成立的灰度页都没有时，不成立的那些页就当其余页。
>
> 聚合为什么不能只取上分位：上分位是**比例**，而值得报警的损伤是**绝对面积**——
> 一块 banding 在哪张页上都一样刺眼，眼睛不关心它占全页百分之几。
> 只取比例，尾巴的宽度就随页面积膨胀，小损伤被整块丢掉（ADR 0002 决定第 3 条）。

## 管线

| 术语 | 含义 |
|---|---|
| **幂等这一道** | 两遍之前的那一步：算出本卷的指纹，与上一趟写在输出里的比。对得上就整卷跳过，一遍都不做。它把源字节读一遍，因此**不是免费的**——`--no-metadata` 下整道不在 |
| **第一遍 / 第二遍** | 处理一个卷的两遍。第一遍解码、彩页识别、几何、缩放、算判据，再把这一页交给《缓存》；第二遍只量化、编码、写出，不再碰源页。**有两条路上量化与编码提到第一遍**，那两条路上第二遍只写出：默认路径上一页判完当场就编，《覆盖顶死》那一趟当场就编 |
| **灰度路径 / 彩色分支** | 页在第一遍分出的两条路。灰度路径转灰、算判据、进缓存，第二遍照判定量化；彩色分支只缩放并编码，不进灰度缓存也不进上包络。走哪条由**面板与页**共同决定：只有彩色面板上的彩页走彩色分支 |
| **纸白对齐 (White alignment)** | 灰度路径上、**缩放之后构造参照之前**的一步：量出这一页的《纸白》，把 `[纸白, 255]` 这一段钳到 255，低于纸白的取值一个都不动。要的是白底上不再撒抖动点——255 是唯一在三档位深上都落在格点上的值（`255 = 3×85 = 15×17`，格点是套嵌的），对齐它等于对三个候选同时对齐。不做线性拉伸（那改动整条色调曲线），不做整幅平移（那把纯黑推离格点）。**钳制宽度就是《离格量》**，它同时是这一步的代价。三条守卫各放过一类页、整页原样：离格量为 0、离格量超过《纸白对齐上限》、量不出纸白。**彩页不经这一步**（ADR 0010）；**几何门不成立的页照样对齐**——它只是没有抖动那一维 |
| **纸白对齐上限 (White alignment limit)** | 这一趟最多愿意为对齐压平多宽的色调，单位是灰度级。**上限即开关**：取 0 时只有本来就在格点上的页满足条件，即一页都不改，产物与不带这个功能时逐字节相同——因此不另设布尔开关。它属于**口味层**（愿意付多少色调是取舍，不是面板的物理事实），并进参数哈希，**取值 0 也进**。取值未标定，见《尚未确立》 |
| **汇总** | 夹在两遍之间的那一步：要看完整卷才做得了的统计落在这里 |
| **缓存 (Cache)** | 两遍之间存放一页的地方。内存优先，只活在一次运行之内。续做不延长它——那件事就发生在同一次运行里（ADR 0012）。`--envelope` 那条路上装的是**参照**；默认路径与《覆盖顶死》那一趟一格从头装的就是**编好的字节**，参照一张都不进缓存 |
| **缓存预算 (Cache budget)** | 缓存最多在内存里留多少字节。限的是峰值内存，不是卷的大小上限 |
| **溢写 (Spill)** | 越过缓存预算的那些页改存临时文件 |
| **卷级计时** | 一个卷这一趟**做了多久**，按《遍》分开，外加这一卷的总数。量的是墙钟，只扣掉在**决策点**上等人的那一截——那段里库一步都没走。跳过的卷也有——幂等那一道不是免费的 |
| **阶段 (Stage)** | 管线上的一步：解码、转灰、裁边、判跨页与切开、缩放、判据、量化、编码、落盘，外加卷内的摊开、汇总与拼报告。**与《遍》不是一回事**——遍互不重叠、进报告；阶段只活在一个 feature-gated 的量具里，累加各线程墙钟之和，只印到 stderr |
| **窄计数器 (Narrow counter)** | 报告上只为钉住「某件工作确实没有发生」而在的数，三个：解码次数、缩放次数、参照进缓存次数。**不进渲染，屏上一处不露面。**记在**动作本身**上，不记在调用方的循环里——那样才躲不过去 |

> 缓存存的是**参照**，不是源页解码后的原图——两遍分工与这个选择的代价见 ADR 0005。
> 默认路径上参照一张都不进缓存，那一格装的是编好的字节。
> 那一格装参照还是装编好的字节，**由《缓存》那一条说了算**，两条路各是一种答案。
> 两条路上**峰值内存都随卷长线性涨**——攒的是参照还是编好的字节只改系数，见 ADR 0005 的《后果》。

> **窄计数器不是用量。**缓存那一行印给用户看的页数是账，跟着缓存里存的是什么走；
> 窄计数器问的始终是「这一趟工作走没走」，因此不上屏，也不随渲染的措辞变。
> 一个开关关掉了某一段工作而外部看不出分别时，它是唯一说得出「真的没做」的东西。

> 计时按**遍**分，不按**阶段**分：遍是互不重叠的墙钟，
> 而阶段在满核并行里交错跑，「解码耗时」是墙钟还是 CPU 时间说不清，聚合出来的数会骗人。
> 汇总不占一遍——它不碰 I/O，也不在步里。
>
> 要按阶段看，走那个量具（`cargo build --features profiling`，见 `src/cost.rs`）：
> 它的答法是**挑明一种口径**，而不是把一个说不清的数塞进报告让人当墙钟读。
> 那个口径是什么、怎么读，在 `src/cost.rs` 的模块文档里说一次。默认构建里它整个不在。
>
> 计时**只进报告，不进印出来的文字**：印不印由调用方定，与进度同一条规矩。

## I/O 与并发

| 术语 | 含义 |
|---|---|
| **介质 (Medium)** | 一条读取通道落在什么盘上。三种取值：有寻道惩罚（机械盘）、无寻道惩罚（固态盘）、未知 |
| **寻道惩罚** | 机械盘上并发随机读互相打断寻道的那笔代价。有它就串行读，没有就并发读 |
| **读取通道 (Channel)** | 路径解析到的那个卷/挂载点。介质**按它**探测，探测结果也**按它**缓存 |
| **读取层 / 计算层** | 读取层取字节，计算层解码到判据。**谁定的不同**——前者由介质定，后者由核数定；两个数目前相等且有理由：读得再快也没有更多的核去消化 |
| **有界通道** | 夹在两层之间的那道口子。它按**在途字节**背压，不按条数——页的大小差着一个量级 |
| **在途字节** | 已经读出来、还没算完的源字节 |
| **读取预算** | 在途字节的上界。它是个**常数**：并发度翻倍，峰值不跟着翻 |
| **一路读取 (Readers)** | 派几条读取，以及这个数是谁定的。1 即串行 |
| **读取计划 (IoPlan)** | 一个卷这一趟怎么读：介质是什么，据此**两路**各是一路读取——两遍那一路、幂等那一道 |
| **读取形态 (ArchiveReading)** | 一种归档格式怎么读，两条：**随机取**（`.cbz` / `.zip`）与**摊开**（`.rar` / `.7z`）。按格式分，不逐卷探固实与否 |
| **读取端形态 (ReadingEnd)** | 一个卷这一趟的读取端**是哪一种**：一个**目录**（一个成员一个文件），或一个**归档句柄**（一个游标）。**与容器形态不是一件事**——摊开了的归档卷容器形态仍是归档、输出仍是 `.cbz`，读取端已经是一个目录。派读取策略、探介质问的都是它 |
| **摊开 (Extraction)** | 固实归档开工前整卷解到临时目录，之后完全按目录卷走。活在那一卷上：卷一放掉就收 |

> 介质**按路径**探测，一次运行里不同路径各判各的；探不出来的一律按未知处理，取保守并发度，
> 不假装它是本地盘的某一种。为什么不能按机器判定一次，见 ADR 0009 的《背景》。
> 探的是这一卷**此刻从哪儿取字节**的那个路径：摊开了的卷因此按那个临时目录所在的
> 通道判，不按它原本来自的那个归档。
>
> `--io-mode` 覆盖的是**策略**，不是事实：探到的介质照实进报告，变的只是这一趟派几条读取。
>
> 一个卷有**两路**读取，**读取端是一个归档句柄**时两路的数不同：
>
> - **两遍那一路恒串行**——一个 ZipArchive 就是一个游标，点名也改不了。成员按顺序码在一个
>   文件里，顺着扫最快，而读取与计算的重叠已经由有界通道买下了。
> - **幂等那一道不在此列**：它给每条读取线程一个**自己的**归档句柄，各解各的成员。
>   松口只对它，是因为那一道没有可与之重叠的计算——只解压、喂哈希，整段暴露在墙钟上，
>   而两遍那一路的读取一直有解码与编码压在上面。硬约束是并行的是**解**、不是**喂**：
>   交付仍按成员次序，同一卷两种读法的**指纹逐字节相同**。
>
> **读取端是一个目录**的卷两路恒相同：目录卷（它本来就不吃归档那一笔），以及**摊开了的**
> 归档卷——它的字节一个成员一个文件地躺在临时目录里，那个游标根本不在场。
> 机械盘与未知照旧保守：几个句柄各解各的成员是随机读，那正是寻道惩罚要避开的东西。
>
> **归档卷的读取形态按格式分，两种格式两条路**（ADR 0015 决定第 3 条）：
>
> - `.cbz` / `.zip` **随机取**——每个成员各自 deflate，按下标直接定位，一次 O(1)。
>   摊开是白付一次全量写盘。
> - `.rar` / `.7z` **摊开**——固实归档取第 N 个成员要从块头解起，随机取 O(N)、全卷 O(N²)，
>   而源字节本来就要读两遍。因此开工前整卷解到临时目录，之后完全按目录卷走：
>   读取层、缓存、幂等、隔离没有一处需要认识「固实」这个词。
>
> 分岔只按**格式**，不逐卷探固实与否——逐卷探要先读一遍归档头，而那正是这条决定省掉的那一次。
> 摊开借**溢写**那套 `tempfile` 机制，落在同一个系统临时目录下，磁盘这一侧因此**没有旋钮**
> （`--cache-budget` 限的是内存）；摊了多少字节进那一卷的报告，那是那笔账唯一可见的形式。
> 摊开那一份活在卷上：跑完、按停、中止都由析构收走。

## 进度

| 术语 | 含义 |
|---|---|
| **步** | 进度报到的单位。一个卷的步数是它这一趟真要走的那几遍之和：对指纹读全部成员、读图定档走每一张**源页**、按档写出写全部**输出成员** |
| **遍 (Pass)** | 一个卷这一趟走过的一截连续工作，三个，各叫它在做的事：**对指纹**（《幂等这一道》）、**读图定档**（《第一遍》）、**按档写出**（《第二遍》）——横条上那个词、`?` 那张表、这里三处逐字相同。**步**按它划，**卷级计时**也按它分。「现在在走哪一遍」报得出来：那是这一卷此刻走进了哪一遍，**不是哪条线程此刻在哪个《阶段》**——读图定档满核并行，同一刻不同线程在不同阶段，那件事没有单一答案，屏上因此不显示它 |
| **事件 (Event)** | 库向外报到的那一条条消息。走完一步是其中一种；一卷跑完的那一条**带着这一卷的 `VolumeReport`** |
| **指令 (Instruction)** | 观察者收下事件之后回给库的那一个字：继续、收尾、中止。进度、两级停、试算→执行的决策点共用这一条回路。库那一侧只记「至今收到过最强的那一个」；观察者拿什么字来答是它自己的事，《会话》的「都这样」那个默认答案就摆在它那一侧 |
| **收场 (RunOutcome)** | 这一趟是怎么结束的：走到头、被按停停在半路（带着按下的是哪一级）、拒绝执行。收场那一条事件带着它，报告上也有一份（拒绝执行那一趟没有报告） |
| **预扫 (Survey)** | 开工之前的两步：**发现**这一趟有哪些卷，再把它们全枚举一遍算出**全局总步数**。只列成员、不碰像素，发现本身也不占步；**数完就把卷放掉**，只留下这一卷的路径与那几个数，处理那一卷时按路径再开一次——句柄与常驻内存因此不随卷数长 |
| **进度观察者 (Progress)** | 收事件、回指令的那一端。库只报到，印在哪、印不印、长什么样、要不要在决策点上等人，全由调用方定 |
| **持锁哨兵 (LockSentinel)** | 守着「不得在持锁处调观察者」那条规矩的那一格：持着**卷缓存那把锁**走到报到就当场恐慌，指名是哪一处。只在调试构建上 |

> 报到的单位是步而不是页：一个卷要走几遍随模式而变，按页报到会让进度在第一遍里一动不动、
> 到第二遍猛地冲完。预告的步数是**上界**，不是承诺，两处各出一次：幂等命中的卷提前收摊；
> 按档写出那一截按「每张源页两张输出页」预告，而一张源页切成几张要解了像素才知道（**跨页拆分**）。

> 事件流就是**报告的增量**：卷级事件带着 `VolumeReport`，调用方边跑边把报告攒出来。
> CLI 攒完在最后一次性渲染，TUI 攒到哪儿画到哪儿——两者拿的是同一份数据，不是两套。

> 观察者是**双向**的：收事件，回指令。取消因此不必另开一条信号通路，而「第二遍之前停下来问一句」
> 与「按停」走的是同一个机制——库只管在该问的地方问，等不等人由调用方定（ADR 0012 决定第 3 条）。
>
> 观察者**可能很久不返回**——会话就要在决策点上等人拿主意，那是它正常的样子。库这一侧因此
> 有一条硬规矩：**任何持锁的地方都不许调它**，持着锁等人就是把整条管线挂在一个外人手上。
> **持锁哨兵**守的是这条规矩上最要命的那一把——卷缓存那把锁，别的锁仍靠代码自己站住。
>
> 指令**只升不降**：库记的是至今为止收到过的最强的那一个。按停是个闩——手指离开键盘之后
> 下一条事件答「继续」，不该把已经按下的那一下抹掉。两级停本来就是这么用的：
> 中止是「**再按一次**」（ADR 0013 决定第 2 条），而再按一次退不回上一级。
>
> **闩不止一份，编码只有一份。**库记「观察者答过什么」，界面层各记「用户按过什么」——
> 三份各答各的问题，一份都不多余；而「三级怎么编成一个字节」是**库的公共 API**，
> 三份闩用的是同一份，不各编各的。各编各的话，编出来的数与三级的序一旦分了家，
> 「按了中止却退回收尾」在哪一份上悄悄发生都看不出来。
>
> 全局总步数与单卷步数同一个性质：都是**上界**，而且前者就是后者之和。幂等命中的卷提前收摊，
> 预扫算进去的那几遍不会走——画全局进度的那一层因此要在**一卷跑完时结清**那一卷预告剩下的步，
> 不结清的话跳过一个卷就在条上留下一段永远走不完的距离。
>
> 预扫在慢盘上要花一点时间。那一段**不是静默的空白**：预扫排在开工那条事件之前，
> 库这一侧还没有话说，交代因此由界面层自己给（命令行画一条转轮）。它换来的是一个真的
> ETA（ADR 0011）。

## 会话

TUI 是 `run` 之上的第二个薄层，与 CLI 同级：它不绕过 seam，也不多一条管线。
**无参数即会话，带参数即直接跑**——命令行那一路的行为一字不改。

| 术语 | 含义 |
|---|---|
| **会话 (Session)** | TUI 从打开到退出的这一段。一个会话里可以跑很多趟，参数在趟与趟之间改 |
| **设备层** | 会话里可改的第一层：profile（型号 + 灰阶数覆盖 + 阈值覆盖）。绑面板，由标定决定，改一次管很久 |
| **口味层** | 第二层：这一趟的立场——适配方式、裁边、拆分、拆分阈值、阅读方向、滤波器、位深、抖动、上包络、缓存预算、读取策略 |
| **范围层** | 第三层：处理范围与输出根，在会话里打进去（路径按层补全，不递归、不建索引）。每趟都不同，因此**不进预设** |
| **大小写敏感性 (Case sensitivity)** | 逐层补全按前缀筛时**认不认大小写**：这是**这一层所在的文件系统**的性质，**运行期探出来**，不按平台常量猜（Windows 不认，**macOS 默认那套也不认**，一个平台常量在 macOS 上恰好全错）。探法**只读**（源库只读），**一个进程只探一次**——那一格是逐层补全唯一记得住的东西；探不出来时按**认**走。不认的那一档上敲 `d` 补得出 `Doraemon`，而补回来的是**盘上那个写法**。**只管补全这一处**：撞名那条判据另有自己的判法 |
| **预设 (Preset)** | 一份命名的落盘配置，装设备层与口味层两层。CLI 只在**显式点名**时读它 |
| **试算** | 会话里按下去的那一次 dry-run：只算不写输出，报告照出 |
| **总览 (Overview)** | 主区最上面**钉住**的那一块，一个框：抬头（这一趟是**试算**还是**执行** · 走到第几卷 · 还剩多久；按停按到的那一级与**等答话**那一句挂在它上面，收场之后整条换成收场那句话与「用了」那个数）、全局那一行、当前卷那一行（这两行各带一条**横条**——走了多少的那一条，宽度与命令行那两条同一个出处）、**结论行**、**出事行**。**抬头答的是「此刻在写没写」**——试算答出第一个继续之后它就是执行了；**后两行答的是「这一趟交出来的是什么」**，问的因此是**这一趟至今真写出过东西没有**：`t` 起的那一趟在**第一卷真写完**之前是试算那一副，之后翻成执行那一副，一趟之内只翻一次、翻过不翻回（跟着抬头走的话，答出继续的那一帧屏上会换掉整副内容、还可能矮一行）。**结论行**：试算给判定分布，执行给完成与跳过。**出事行**：试算给判定上要注意的（特例页 · 宽溢出 · 几何门不成立），执行给盘上出的事（隔离），而**失败页 · 卷级失败 · 走不进去的地方三样两副都给**——解不出尺寸的页在第一遍就失败，卷根被删掉的卷同样记一笔，走不进去的那个目录在开工之前就撞上了。**出事行答的是「此刻」，报告末尾那几小结答的是「已定案的那几卷」**：前者数收摊了的那几卷加上当前这一卷已经报过的那几条，后者一卷不收摊就一个字不算，一卷跑到一半时两个数**故意**不一样、各自都对，收摊之后又相等——两个不同的问题、两个答案，不是一个数摆两处。**出事行一条都没有时整行不出现**，那一行让给报告区。钉住指的是它不随报告滚：报告在它下面自己那一格里滚 |
| **焦点 (Focus)** | 会话此刻**在看什么**，也就是按键归屏上哪一块管。它与**阶段**（这一趟走到哪儿了：没跑过 / 跑着 / 等答话 / 收场了）是**两维**，两者互不代替：三层只读、按停与答话那几个键归阶段那一维，`↑↓`／`⏎`／`⇥` 归焦点这一维（ADR 0017）。焦点的取值**八个**：左栏 · 编辑一行 · 取值栏 · **报告区** · **展开着一个目录** · **展开着一卷** · 预设栏 · **覆盖层**——`⇥` 在**左栏与报告区**之间切，另外六个各有自己的进出键。屏上「焦点在哪」的载体是**反白的那一行**：焦点在左栏时是配置那一行，落在报告区那三副上时恒是光标停着的那一行——默认那一副反白的是光标那一卷所在的**那一个目录**（那个光标恒是一卷，目录表自己不另记一个），展开一个目录之后是**那一卷**，展开一卷之后是**那一页**，**同一刻只有一处**。「哪些键在哪个状态下有效」仍旧**一处答完**，只是那张表按两维查——**三层只读与焦点在哪无关**，跑着与等答话时焦点落到报告区也一个改动键都不派 |
| **展开 (Expansion)** | 报告区**往下摊开一级**的那个状态，**两级**：**展开一个目录**摊出它底下那几卷（卷表），**展开一卷**摊出它的逐页那几行（逐页表）——**展得开的那几卷才摊得开**，没做成的那一卷按下去给的是一句话（见**停得住 / 展得开**）。报告区默认那一副是**目录表**——一个目录一行，一趟几百卷时一屏看得完；两级展开于是追的是「哪个目录出的事 → 哪一卷 → 哪一页把整卷的档位拉下来」。层次与**发现出来的那棵树**一致（见《发现》），分组按卷根的父目录，一处出处，命令行那一副的折叠读的是同一份。**各有一个键回得去**：`Esc` 退一级。**左栏只在展开到页那一级收起**、主区吃满宽度——卷表那几列摆得下，那一级左栏留着。三副都是表，横向摆不下时一律按**砍列**那个次序砍（各表一个次序），`←→` 一处都不横滚。逐页那一副**默认只列要紧的页**，`a` 切全部页——**等答话时 `a` 让给答话**（「剩下的卷都这样」，见**等答话**），那一刻这一副**切不过去**（列法停在按下那一下之前的那一副），屏底那一行也不摆它 |
| **报告折叠 (Report fold)** | **命令行**印出去的那份报告**摊到哪一级**，两副：**不折**（默认：目录那一行、它那几卷、每一卷的逐页那几行，三级一并摆出来）与**按目录折起**（`--brief`：只到目录那一级，一枝一行）。折的**只有正文**——抬头与末尾那几小结两副都一个字不少，没做成的那几卷、进了隔离的那几卷、发现走不进去的那几处只有末尾那几小结点得出是哪几个，连它们一起藏掉就成了一份说不出事的报告。**默认不折**：命令行那一路没有一个键可按，藏起来的那两级在屏上再没有第二个地方看得到，因此藏得由用户当场点名。它**不改产物**——不收预设、也不进参数哈希，加不加都不会让上一趟的输出过期。目录那一级的分组与聚合与**展开**读的是**同一份**（一处出处）；那一个是会话按键摊出来的状态，这一个是命令行上一次点定的开关，**两件事** |
| **要紧的页 (Notable pages)** | 展开那一副**默认列的那几页**：特例页 · 失败页 · 部分救回页 · 几何门不成立的页 · 宽溢出的页 · 兜底上界改过的页，加上**定档页**（它是这一卷的答案，非在不可）。前六种就是《语义色》在**页**这一级上分出的那几样，末一种一档都不占。判据**逐页**问，一页可以同时要紧在好几处。展开一卷的目的通常只有一个——**哪一页把整卷拉下来**——而两百页的卷里那几页不该由用户自己在四百行里找；一张要紧的页都没有的卷说一句话，不给一张空表 |
| **卷表 (Volume table)** | 报告区**展开一个目录**之后那一副：**一卷一行**的一张表（默认那一副是**目录表**，见**展开**）。列从左到右是行首记号 · 卷名 · 页数 · 基准档 · 定档页 · 耗时。跳过的卷在基准档那一列写「跳过」、定档页留空而**耗时照给**（幂等那一道不是免费的）；没做成的卷写「没做成」、那句原因成句跟在行尾；默认逐页与覆盖顶掉判定的照卷级判定说的写（「逐页」「覆盖 4bit」）；停在**决策点**上等答话的那一卷也占一行，标着「等你拿主意」。**不重排**：卷按跑完的先后添上去，出事的靠行首记号跳出来、不靠位置——重排会让刚跑完的那一卷在屏上跳位置。横向摆不下时按**砍列**那个次序砍。表**从报告区那一格的头一行起**：这一趟的前提在**覆盖层**里（`i` 一个键调得出），不占表上的行。表**下面**是当场冒出来的失败页与收场之后末尾那几小结；那几段本来就是句子，照旧当整段文字**折行**画，一句都不塞进**格** |
| **跟随 (Follow)** | 报告区那个**卷光标**（目录表与卷表共用同一个，逐页表另有自己的）的默认那一档：**跟着最新收摊的那一卷走**，一卷收摊它就落到那一卷上。光标一挪**跟随就停了**——报告区那一格的抬头说一句「跟随停了」，报告再长光标也不动；`g` 把它交回给跟随。**跟随是那个卷光标独有的**，而它记的**不是滚动量，是光标停在哪一卷上**：视口照旧由光标算出来（见**视口**），别处一处都不记着「滚到哪儿了」。光标停得住的是**表上有的每一卷**——收摊了的那几卷、**决策点上攒着的那一份**，以及**没做成的那几卷**：一行看得见就该选得中。停得住的那几卷比展得开的那几卷多出没做成的那几条（见**停得住 / 展得开**），而那一行要说的话就在它自己的行尾 |
| **停得住 / 展得开** | 报告区那几副上**一行的两件事，分开的**：**停得住**是光标落得上那一行——表上有的每一行都停得住（见**跟随**）；**展得开**是 `⏎` 按下去有没有下一级。没做成的那几卷连一份卷报告都没有：**停得住，展不开**，按下去给的是一句话，不是一副逐页表。`⏎` 展开哪一卷、`⇥` 在哪几卷之间转、展开那一副抬头上那个「第几卷」的分母，三处问的都是**展得开**那一件 |
| **砍列** | 报告区那**三张表**（目录表 · 卷表 · 逐页表）横向摆不下时按**固定次序**舍掉的那几列。**第零步与宽度无关**：**一整列都不在场时那一列连列头都不占**——那一列上一行字都没有，留着它只是让一个列头挤掉别的列（一格在不在场本身就是一句话，见**格**）；恒在的那几列空着也留着，那时那一格的空白正是答案。接着按次序砍：**三张表各有一个次序，每一个只有一处出处**（`session::columns` 的 `Column::DROPPED_IN_TURN`——画法那一层不许再写第二份，这里也不抄）。**行首记号与名字那一列恒在**，逐页表上**判定**那一列也恒在——一行上先要认得出这是哪一行、它出没出事、判成哪一档，剩下的几列都是这几件之后的事。砍无可砍仍摆不下时名字那一列**从中间省略**，两头留着：书名与第几卷都要认得出（从行尾切掉的话满屏的卷长得一模一样） |
| **让位 (Yielding)** | 屏上那几块**摆不下时谁先让**，宽高各一条，次序**只有一处出处**。**宽度不够**是**三步**：一、先让**左栏**——它先缩，缩到连一行取值都摆不下就**整个让掉**（那不是**展开**带着的「收起」，这一条没有开关）；二、按**砍列**那个次序砍；三、总览块那两行上的**横条**——先收窄，窄到「一眼看得出走了多少」都做不到就整条让掉。横条排在最后：它旁边就是走了几步 / 共几步那两个数，让掉它丢的是「一眼看得出」、不是内容，而它之前让掉的两样丢的都是字。**高度不够**：屏底照**折行**那一套按需长高，长到主区只剩总览那几行加表那一行为止；**总览块一行不砍**——它是钉住的那一块，也是唯一答得出「这一趟怎么样」的地方，三者都让完仍摆不下时**宁可少画表，不少画总览**。摆不下的**抬头**与卷名同一副省略法——**从中间省略**，不由终端库从行尾硬截：`还剩约 3m20s` 截成 `还剩约 3m` 是一个读起来完整而偏小的估计，比截出半个字更坏。**总览块正文那几行**与展开那一副钉住的那一行走的也是它，差的只是两个角占不占格：截法**只有一处出处** |
| **语义色 (Tone)** | 屏上把「这件事怎么样」分成的**四种**，一种一个样子：**平常**（终端默认色：正常跑完的卷 · 普通的页 · 非卷文件那一小结 · 屏底那一句报的「做成了」）、**注意**（黄：隔离 · 部分救回 · 宽溢出 · 兜底上界 · 几何门不成立 · 特例页 · **过期副本** · 屏底那一句问的「再按一次」）、**出事**（红：失败页 · 卷级失败 · 拒绝执行 · 屏底那一句报的「没做成」）、**不要紧**（暗：跳过的卷 · 只读时的左栏）。**报告末尾那几小结按小结分属三档**，不是整段一种色。**颜色不是唯一载体**——每一处上色的地方旁边都另有一个**字**（「隔离」「跳过」「没做成」「失败」；末尾那几小结各自以自己那个词开头）或一个**行首记号**（三张表那几行行首那一个，以及屏底那一句行首那一个），色盲与不上色的终端上一个字都不丢。只用 16 色里的基本色，**不定背景色**（背景归用户自己的终端主题，深色浅色两边都得活）。认 `NO_COLOR`：环境变量在场即四种一律退回终端默认色，**一处生效**。光标那一格反白、层抬头加粗、预设栏那条文件路径压暗**不在这四种里**，也不归 `NO_COLOR` 管——它们说的不是「这一趟怎么样」，抹掉了屏上也没有一个字补得回来。**只在会话里**：命令行印出来的那一份一个颜色都不加 |
| **字形约定** | **库出的字里**，哪个空格不许断、哪个字形宽度稳——两条都是界面层的需要，而**库得先守住**：同一句话从三张嘴里出来，最后一张在库内（ADR 0016 认下的那处例外），库因此会把字直接送进一条对齐的**列**里。两条：**不许断的那个空格**（`--fit height` 这种带空格的记号整个不断，印出去之前换回普通空格）；**库自己造的字形一个都不许是歧义宽度**（东亚宽度表上标 Ambiguous 的那些——`·` `×` `→` 一类——CJK 终端上画两格，而列宽一律按一格算）。**这两条是库的公共 API**，调用方看得见；**折法不在这里**（折到多宽、断在哪儿、禁则）——那是界面层的事，两个去处量的不是同一块地方 |
| **只列前几条 (First few)** | 一张长清单在界面上**只列前几条，剩下的报个数**（`  ……另有 2 个`）：一屏放不下的清单等于没有清单。**共用的是形状，不是句子**——**列几条**与**剩下的那一句怎么说**只有一处出处（`tonefit::FirstFew`；与**字形约定**同一条理由住在库里：预扫那条拒绝与撞名那条拒绝是库自己写下的整段字），而抬头那句话、条目怎么渲染、量词（个／卷／处／页）各处各写。用它的有六处：报告末尾那三小结（非卷文件 · 卷级失败 · 发现走不进去）、预扫那条拒绝、撞名那条拒绝，加上几何门与纸白那两句里点名头几页的那一小截——**最后那一处的上限自己给、比清单那个小**，它不是清单，是一句话里的抓手（真要逐页看，逐页那几行一页不落地列着）。两副排版：**摞成一块 (stacked)**（一条一段，剩下的另起一行收口）与**串在一句里 (strung)**（「、」连起来，剩下的接在后面），两副都住在 `tonefit::listing`——添第三副也添在那里。**截断只发生在界面这一层**——报告上那几列一条不少，要全部的调用方读那一列 |
| **折行 (Wrap / fold)** | 界面层按**显示宽度**把一行摆不下的文字折成几行：宽字符两格，断处在挨着空格或宽字符的地方（西文词与 `←→`、`⇧⇥` 这类记号不劈开），行首缩进跟着折下来的每一行走。另有**三条判据**：**禁则**（`）` `，` `。` 这类收尾记号不落在行首、`（` `「` 这类起首记号不落在行尾，半角那几个同样在表上；让得出去就让，一处断得开的都不剩时宁可破例）、**不许断的那个空格**（`--fit height` 这种带空格的记号整个不断，印出去之前换回一个普通空格——那一层标注是**库的公共 API**，见**字形约定**）、**缩进之后不先折出一个空行**（缩进后面头一截断不开时照旧跟在缩进后面，不先折出一行只有缩进的空行）。`--help`、命令行印出来的报告，以及会话的屏底、报告区、**左栏与预设栏**，**共用这一套**，只是折到的宽度不同——**屏上没有第二套折行规矩**。折出来有几行当场数得出——报告区的滚动量按它算。**展开**那一副**没有例外**：逐页那几行也是一张表，横向摆不下时按**砍列**砍、不横滚，每一行照旧折得下来，与卷表一个待遇 |
| **视口 (Viewport)** | 一个列表在一个格子里露出来的那一段：列表有多少行、格子有多高、光标停在第几行 → **从第几行画起**，外加一条滚动条的位置与长度。**滚动量是算出来的，不是记着的**——光标在哪儿视口就跟到哪儿；**唯一的例外是覆盖层**，它为什么是例外见那一条。**屏上凡是列在一个格子里的都共用这一份**，而**共用它的是哪几处、各有没有光标、各画不画滚动条，只有一处出处**（`session::viewport` 的 `Viewport` 那张表，这里不抄第二份）。滚动条走终端库自带的那个 widget，**没有可滚的东西时不画** |
| **行 (Row)** | 报告的**目录**、**卷级**与**逐页**那三段出的单位：一行对应报告上的一行，带着**它是什么行**（目录、卷、跳过、隔离、几何门、卷级判定那三种、定档页、一页的几何与判定……**全部种类以 `RowKind` 为准**）加上若干**格**。**摆法不在里面**——缩进、前缀、列头、格与格之间的分隔与单位跟着排版走，而排版**仍旧两副**：纯文本一副（命令行印的就是它）、表一副（会话的目录表、卷表与逐页表）——多出来的是表那一副底下的第三张表，**不是第三副排版**。措辞仍只有一处（ADR 0016）。**成句的那几行只有一格**，装着整句话，画它的那一头把它当整段文字折行 |
| **格 (Cell)** | 一行上的一个**值**，带着它是哪一格。哪几格在场随这一卷、这一页而变——一张彩页都没有就没有彩页那一格，跳过的卷没有基准档那一格，没裁过的页没有裁边那一格：**一格在不在场本身就是一句话**。一格的值本身是一串东西时（各候选的判据、点名的头几页），怎么串起来仍是措辞，串出来的整串是一格。每一格还带着它的**字面出处 (Provenance)**——那一格的字**是谁写的**，三档：**措辞 (Wording)**（界面层自己造的：裁边、缩放、判定、理由、尺寸、页数……字形宽度**必须稳**，见**字形约定**）、**原样 (Verbatim)**（用户数据原封带过来的：页名、卷名、去处路径、定档页名；宽度**永远稳不住**，摆不下时归**省略**管，不归那条闸门管）、**记号 (Mark)**（一个字符说完一件事，见**语义色**那句「颜色不是唯一载体」）。**哪一格属于哪一档只有一处出处**：加一列时说不出它是哪一档，那一列就摆不进表里 |
| **预设栏 (Picker)** | 会话里列出盘上那几份预设、并把当前两层存成一份的那个状态。列的是**进这一栏那一刻**盘上有的；套用一份就是把设备层与口味层**整个**换成它（它没说的那几项也跟着回到「没说」），范围层一格不动。停着的那一份**也删得掉**（按两下）。左栏此刻仍在屏上——存出去的就是它上面那两层 |
| **取值栏 (Values)** | 会话里把左栏上**那一行的取值就地摊成一列**的那个状态：摊在那一行**下面**，左栏其余各行还在场（改一个值时看得见它在整份配置里的位置）。摊开挂在 `⏎`／空格上（浏览时 `←→` 照旧就地转一格）；摊着时 `↑↓` 选，**`⏎`／空格／`→` 三个键同义**——定，或者停在一块面板上时下钻（分岔不在键上，在**格**上），`Esc`／`←` **一格不改**地回去。列的是那一行的**取值环**（型号那一行例外：它摊开的是面板，见**下钻**），**第一格恒是「没说」那一格**（印成什么照那一行自己的写法：`默认（…）`、`自动（判据说了算）`、`未挑（跑起来之前必填）`三种都有）；此刻生效的那一格前面一个记号，与光标那一格分得开。定下来走的就是环那一套本身——**两条路改的是同一格**。**第一格那条规矩只管摊开的这一层**：下钻进去那一层没有它（「没挑」在上一层的第一格上，见**下钻**）。与**预设栏**是两个状态、两个词：那一个占主区、列的是盘上那几份预设。一趟跑起来之后摊不开，与三层只读同一条 |
| **下钻 (Drill)** | **取值栏上的第二层**，只有**型号**那一行有。第一层摊的不是型号名，是**面板**——一块一行，每一行是那块面板自己的写法（分辨率 · PPI · 灰阶数 · 黑白／彩色），第一格仍是「没挑」；`⏎`／空格／`→` 停在一块面板上是**进去看它底下有哪几个型号**（面板不是型号那一行的一个取值，定不下来；三个键同义，与**取值栏**那一层同一条），进去之后 `⏎`／空格／`→` 定、`Esc`／`←` **退回面板那一层**（再按一次才出这一栏）。摊开的所以是面板：**设备只是面板的别名**，面板相同的型号输出完全一致——内置表里没有你那台设备时，你要挑的从来不是名字，是那块屏。分组的规矩与未知型号那条错误消息**同一份**。**取值栏别处一层，只有型号这一行两层**——报告区那三级是另一件事（见**展开**） |
| **覆盖层 (Overlay)** | 一个键**掀开**、盖住屏上那几块的那一张；`Esc` 关掉之后**底下那一块原样回来**（它盖住一块焦点，不替掉它）。两张：`?` 是**全部键**——按**焦点**分组，只列此刻这个阶段派得出的键，而且**此刻按下去真有第二步**（掀着这一张时按不动的那几个不列，`q` 就是一个）；`i` 是**这一趟的前提**——profile · 适配方式 · 裁边 · 跨页拆分 · 互锁 · 判据构成 · 掩蔽 · 聚合，一趟只说一次，因此不占卷表那几行。**两张是同一副形状**：一张画法，两份内容。掀着的时候**除了按停与答话，别的键一律不派**——那三个键在焦点落在哪一块上都按得动（见**焦点**），这一块与别处一样把不认的键交给阶段那一维；**交下去会撞上阶段那一维的只有两个**，两个都归这一块自己：`Esc`（关掉这一张）与 `q`（不派动作）。`Ctrl-C` 照旧退得出去。屏底那一行因此瘦身成此刻最常用的四五**件事**——摆哪几件由那一行挑，**派得出它的是哪几个键、那一句怎么说，与这一张出自同一处**（同义的键一个不漏）；**末尾恒是「全部键」那一条**。打字的那两块上调它的是 `F1`：那儿每一个字符都是一个字，`?` 与 `i` 也是字，而 `F1` 不进缓冲。装不下时滚得动，而它记的是**从第几行画起**：屏上唯一记着滚动量的一处，因为它是读物，一行上没有第二步可走，没有光标可跟 |
| **续做** | 试算与执行是**同一次运行**：每一卷的第二遍之前 `run` 都停下来等你拿主意，答继续就接着做第二遍，参照还在缓存里、第一遍不重算。**几卷都一样**——缓存逐卷建、逐卷丢，逐卷停下来问，押着的始终只有当前那一卷（ADR 0012 决定第 3 条） |
| **决策点** | 续做问那一句的地方：每一卷的「汇总之后、第二遍之前」，一卷一次。答继续就走第二遍，答收尾就停在这儿——那一卷等于走了一次试算：输出一个字节都不写，报告照出。答**都这样**则是「继续，而且剩下的卷不必再问」。那一条事件**带着这一卷到此刻为止的报告**：答这一问要看的东西要到「一卷跑完」才交出去，而那一条排在决策点之后 |
| **等答话 (Deciding)** | 会话**停在决策点上等用户拿主意**的那个状态。**试算到得了，几卷都一样，一卷一次**：主区把**那一卷**的报告画出来，`x` 接着做第二遍（第一遍不重算），`a` 剩下的卷都这样，`s` 收尾（这一卷不写，等价 dry-run）。三层此刻仍旧只读，`Ctrl-C` 仍旧退得出去——退出走中止，那一卷等于没做 |
| **都这样 (ForTheRest)** | 决策点上的第三个答法：这个字**连剩下的卷一起答了**，往下不再停。它记在**观察者那一侧**的「决策点的默认答案」上，与库那一侧记「至今收到过最强指令」的那一格分开放。**它不是闩**——闩只升不降，而它是个可以是「继续」的粘性答案；它也**不动按停按到的那一级**，答完话那一级照旧作数 |
| **收尾** | 按停的第一级：当前卷跑完才停。盘上只有完整的卷，下一趟幂等接着走 |
| **中止** | 按停的第二级：立刻停，当前卷那格 `partial` 丢弃——那一卷等于没做，最终位置上一个字节都没动过（ADR 0013） |
| **检查点** | 管线上向观察者问一次「还走不走」的那一处。停在哪一道边界上由它定：**卷边界**那个管收尾，**页边界**那个管中止（各留一个，见 ADR 0013 的《后果》） |
| **退出时那一份** | 退出会话时印到 **stdout** 的那一段——会话整个画在 stderr 上，`tonefit > 报告.txt` 因此仍然成立。**照命令行那一路的原格式**。一趟都没跑过时什么都没有。最后那一趟**做成了**就是它那一份报告；**没做成**（拒绝执行，或那条线程恐慌了）则三段接起来：**先前那一趟做成了的报告**（一趟都没做成过就没有）、**这一趟攒下来的那一份**（它说得出已经做完的哪几卷——互锁 ③ 那种拒绝要真撞上那一页才拦得住，先做完的卷已经在盘上）、以及**这一趟为什么没做成**。末一段前面空一行，与报告分得开：一段是已经算出来的东西，一段是它为什么没算完。**退出码取的是同一趟**（最后那一趟），两者对得上。先前那**几**趟里只留最近一份完整的 |

> 三层的分界线画在**生命周期**上，不画在「哪几个 flag 长得像」上：设备层错了是判定的依据错了，
> 口味层错了只是这一趟不好看，范围层错了会写到别人的目录里。预设收前两层、不收第三层，同一条线。

> 试算与执行是**同一个 `run`**，区别只在 mode：两者都报事件、都能停。
> 「先看一份报告再决定」与「看得见进度」因此不是两件事，是同一条回路的两半。
> 一趟跑起来之后**三层都只读**。设备层与口味层错在撒谎：卷与卷之间换参数，报告顶上那行
> `profile` 就跟着不作数了。范围层错在没用：`Request` 在起线程那一刻就是一份快照，
> 改了也不影响正跑的那一趟——「看着改得动、其实没生效」比按不动更坏。
> 只读要在屏上**看得出来**，不能是按了没反应。

> **展开到页与左栏收起是同一件事，不是两个开关。** 逐页那几行轻松过 100 列，而左栏那一栏
> 在这一刻是宽度里最贵的一截——分成两个开关，「展开着而左栏还在」这种没人要的组合
> 就得靠代码守着。收起**不是删掉**：一个键回到上一级，三层一格没动，光标还停在原处。
> **展开到卷那一级左栏不收起**：卷表那几列摆得下，而左栏在那一刻仍旧答着这一趟照哪几项跑的。

> **存出去的只有「说了的那几项」。** 两层的每一格都有一个「没说」的位置（屏上印成
> `默认（lanczos3）`），而存成预设时**那一项不写进那份 TOML**。「没说」与「说了一个恰好
> 等于默认值的值」因此是两件事：后者写下来，往后默认值改了它也仍是那个值；前者跟着默认走。
> 一份预设于是说得出「我只在乎这两项」，而不是把十一项全按今天的默认写死。

> **盖掉一份同名的预设、或者删掉一份，都要按两下**（与两级停同一个形状）：第一下只说一句，
> 第二下才写。那份文件是用户自己手写的东西——里面可能有注释、有本版本读不懂的预设。
> 三种写盘（存新的、覆盖、删）动的都只有**那一份自己的那几节**，文件里别的字节留在原处
> ——这一条对按节写法成立，别的写法退回整份重排（内容一份不少，注释与排版留不下来）。
> 屏上那一句因此只说无条件成立的那两半：**那一份自己的内容没了，撤不回来**；
> 文件里其余几份预设照旧留着。

> 设备层里还挂着标定图。`calibrate` 仍是「另做一件事」——不读源、不走管线、不判定、无损写出——
> 但它出的那个数（感知可分辨级数）恰好是设备层唯一填不出来的一格，所以会话里给它一个键。
> 会话出图不改变它的地位：它还是量具，不是被处理的页。

> **决策点不是第三个检查点**：检查点问的是「这一趟还走不走」，答收尾要**当前卷跑完**；
> 决策点问的是「这一卷的第二遍还做不做」，答收尾**这一卷就不写了**。同一个字，
> 停出来的现场不同，所以决策点认的是**当场答的那个字**，不是闩——不然第一遍里按下的收尾
> 会把当前卷的第二遍也吃掉，而那正是收尾说好不做的事。决策点上答的字照样进闩：
> 那一卷停在这儿之后，剩下的卷也不必开工。
>
> 决策点上等人的那段时间**不算进计时**：库在那段里一步都没走，算进来的话报出来的耗时
> 说的就成了「用户拿主意花了多久」。卷级计时与整趟耗时各自减掉它，
> **总览块**边跑边画的那两个数——全局那一行的「已用」与抬头上的「还剩多久」——同样减掉它，
> 一处答完。**等答话时抬头压根不画「还剩多久」**：那一刻横条一动不动，
> 报一个数出来说的就成了「用户还要想多久」，那一截让给「等你拿主意」那一句。

> **等答话是一个状态，不是「跑着」上的一个开关。** 两处按得动的键是两套：跑着时按得动的
> 只有停（`s`，两级），等答话时按得动的是答话那三个（`x` 接着做、`a` 剩下的卷都这样、
> `s` 收尾）。那一刻管线停在观察者里，会话这一头照旧画帧、认键——屏不因此冻住。
>
> **等答话时按的 `s` 不是按停。** 按停答的是「这一趟还走不走」，一级一级往上升；
> 这里的 `s` 答的是「这一卷的第二遍还做不做」，**按停按到的那一级一格不动**——
> 答完话它照旧作数。答出去的这个字仍旧进闩（与上面那一条一致：那一卷停在这儿之后，
> 剩下的卷也不必开工），进的是**库那一侧**记「观察者答过什么」的那一个，
> 不是会话记「用户按过什么」的那一个。
>
> **「剩下的卷都这样」进的是第三个格子**（词条见上）：观察者那一侧的「决策点的默认答案」。
> 三格各答各的问题——用户按过什么、观察者答过的最强那一个、往下每一个决策点默认答什么。
> 前两格是闩，末一格不是。
>
> 反过来，第一遍里按下的收尾也不替用户回答决策点——那一问仍旧要问。

> 收尾与中止的区别落在**那一卷算不算做完**上：收尾让当前卷写完并改名，中止把它那格
> `partial` 丢掉。两种容器形态同形——归档卷的 `partial` 是文件，目录卷的是目录，
> 改名成功才算数——所以中止从不去最终位置删东西。
>
> 中止掉的那一卷**不进报告**：它等于没做，盘上没有它这一趟的任何痕迹，报告里也不该有。
> 下一趟幂等因此不会把它误判成做完了，整卷重做。
>
> 「这一趟被按停过」因此只落在**收场**上：报告里少的那几卷是被按停拿走的，
> 不是调用方少点了几个。事件流那一侧同一个值——报过开工，就一定报得到收场。

## 输出

PNG 是默认，也是唯一保证四台目标设备自带阅读器都能读的格式。`--codec=avif` 是显式可选项，
面向第三方阅读器。判据层对编码器无知，编码器选择在其外层。见 ADR 0004。

| 术语 | 含义 |
|---|---|
| **记录 (Record)** | 写进输出 PNG tEXt 的自描述元数据：工具版本、profile 名、参数哈希、源哈希、来路、判定、理由。取值一律 ASCII——tEXt 只装得下 Latin-1 |
| **来路 (Origin)** | 记录里的一项：这一张来自哪个源成员、是那一族的第几张、那一族共几张。**不是**指纹的一部分——它不决定要不要重做，它让幂等**问得出**这个问题：一个源页产出几张由内容决定（跨页拆分），输出成员名因此在碰像素之前预告不出来 |
| **指纹 (Fingerprint)** | 记录里兼作幂等依据的前四项。判定与理由是这四项推出来的结果，不进比对 |
| **源哈希** | 指纹里代表源的那一项。作用域是**卷**——理由见 ADR 0006 的《决定》末段 |
| **参数哈希** | 指纹里代表参数的那一项，收的是**会改变输出**的每一项。型号名不收（它另占指纹的一项），缓存预算、模式、输入与输出路径也不收 |
| **跳过 (Skipped)** | 幂等命中的卷级判定：上一趟的输出还齐着、指纹一项没变，这一卷一页都不重做 |
| **输出布局** | 输出镜像源的结构，基准点是**点名路径的父目录**——点名路径自己的名字恒出现在输出根下。点名一个卷与点名一个装着卷的目录共用这一套规则。见 ADR 0014 决定第 4 条 |
| **借住的卷 (Lodger)** | 一个卷的输出去处**同时是**别的卷输出的父目录时，住在里面的那些别的卷。混装目录才有：`out\N和S` 既是封面那一卷的去处，也装着 `out\N和S\第01话.cbz`。它定的是**收尾换掉什么**——那一卷只换掉自己那几个成员，借住的一个字节不碰 |

> 记录**随文件走**：它就在页里，文件被移动、改名、重新打包都不会丢，幂等因此不需要一份全库索引
> （ADR 0009 关掉的正是那个）。「不会丢」说的是记录本身——输出去处仍按源的结构镜像算，
> 改了输出容器的名字，下一趟会在原来的名字上重写一份。
> `--no-metadata` 一关，记录与幂等一起消失——两者是同一批字段。

> 归档卷的去处还要再归一一道**扩展名**：输出一律 `.cbz`（ADR 0015）。源叫 `第10话.zip`
> 还是 `第10话.cbz`，去处都是 `第10话.cbz`——把源换个扩展名重打一遍包，幂等仍认得出
> 上一趟那份输出，一页都不重做。代价在撞车那一侧：同一目录下的这一对指着同一个去处，
> 整趟拒绝（见《失败》的「拒绝执行」）。

## 失败

| 术语 | 含义 |
|---|---|
| **失败页** | 四种页：字节读不出来的、完整尺寸解不出来的、尺寸解得出来但像素缓冲大到分配不下的、救回却一个像素都没解出来的 |
| **救回 (Salvage)** | 整解失败之后的第二趟：按文件头里的尺寸建好缓冲，解到哪个像素算哪个像素。**救回了多少**跟着这一页走，是报告里的一个数 |
| **部分救回页** | 救回到像素的页。它有自己的尺寸、判据与判定，照常写出，缺的那一段留成纸白，也不把卷送进隔离目录——但它**不参与卷级上包络**，单独定档。几何门照旧问它：那是文件头里的真尺寸 |
| **占位页** | 失败页在输出里留下的那一张：卷内统一尺寸的纸白页。它顶住页序与尺寸，不冒充内容，记录里的判定写着 `failed` |
| **卷内统一尺寸** | 卷内处理成了的那些页里出现次数最多的目标尺寸。占位页按它出；一页好页都没有的卷退到面板分辨率 |
| **卷级失败 (VolumeFailure)** | 一整卷做不成：路径不在、归档损坏、权限不够、透传文件搬不动、卷内成员名撞到同一个输出上。**预扫**发现的整趟拒绝、一页不做；预扫之后才出的记为卷级失败，其余卷照做，报告照出——它指得出是哪一卷，也说得出为什么 |
| **拒绝执行 (Refusal)** | 错在这一趟的**参数**上，换一个卷不会变好：处理范围为空、输出落在源里、输出根写不进去、两个卷撞同一个去处、预扫发现**点名的**路径点不开、覆盖项把候选集裁空。整趟当场停，交出的是那个错误——前五种发生在开工之前，一页都不做；末一种要真撞上那一页才拦得住（几何门是**页**的事实），那时先做完的卷已经在盘上。**发现出来**的归档点不开不在这一栏：它进非卷文件，其余卷照做 |
| **隔离目录** | 含失败页的卷的去处：输出根下的 `_isolated/`。容器形态照旧——目录卷进去仍是目录，归档卷仍是归档 |
| **过期副本** | 一卷在**另一个**去处留着的上一趟输出。去处随「有没有失败页」在干净目录与隔离目录之间跳，这一趟写不到的那一份不会被覆盖，也不会被删 |

> 界线画在**救不救得回像素**上。尺寸只买到几何：解得出尺寸的页有自己的目标尺寸，
> 解不出尺寸的页连几何都没有、只能借卷内统一尺寸——但尺寸本身买不到一个像素。
> 尺寸解得出来却分配不下缓冲的页、以及救回来一个像素都没有的页，因此都算失败页。
>
> 页于是有**三种状态**：完好页、部分救回页、失败页。中间那一种是自己一档，
> 不是「好页的一种」也不是「坏页的一种」：它照常出一张对的页，却没有资格替整卷说话。
>
> 失败是**页级**的，隔离是**卷级**的：一页失败，整卷进隔离目录，其余页照常处理。
> 透传文件不在这套里——它逐字节照搬，搬不动就是卷级的失败。
>
> 隔离过的卷不被跳过：那不是一份做完了的输出，而失败清单每一趟都要重新给得出来。

> **非卷文件不是失败**（见《处理对象》）：四个退出码的含义一格不动。发现出来的一个坏 zip、
> 一个字体包、跟卷摆在一起的一份 `答案.txt`——它们既没被转，也不是「没做成」，
> 只是不属于这一趟的产物。报告上它们**自成一张表**，与做成了的那些卷、没做成的那些卷
> 并列，逐条带着路径与一句为什么——不是失败，但要说得出。

> 四种结局各占一个退出码，理由与当初把「有卷被隔离」分出来的理由是同一条：脚本要分得开。
> `0` 全部成功、`2` 做完了但有卷被隔离、`3` 有卷没做成**或有地方走不进去**、`1` 这一趟没做成（拒绝执行）。
> `2` 与 `3` 同时成立时取 `3`——「有卷带着坏页」和「有卷根本没做成」是两个不同的决定。

> **拒绝执行**那几种的单子只有这一份：实现文档一律指到这个词条，不各列一遍。

## 模式

| 术语 | 含义 |
|---|---|
| **模式 (Mode)** | 一次调用**做到哪一步**，两个取值：**处理 (Process)** 照常判定并写出；**试算 (DryRun)** 只算不写——判据照求、报告照出，**一个输出都不落盘**。「不落盘」说的是**输出**：页、记录、输出容器，也就是用户留得下的那些东西。开工前那几道检查里探输出根的那一下**不在里面**——探针探完就删，探过的盘与没探过逐字节一样；反过来，试算要是答不出「你这个 `--out` 根本用不了」，那份报告就在撒谎：**拒绝执行**说的是**参数**，而两种模式的参数是同一份，那几种拒绝在试算里照样咬人（见《失败》）。会话里按下去的那一次见《试算》，两者是同一个 `run`、区别只在这一格（见《会话》） |

> **质量档 `fast`／`best` 与这一节说的《模式》不是一回事，而且尚未分岔。**
> `fast` 保吞吐下限，关闭 oxipng，仲裁仅在边界带触发；`best` 不限时，质量选项全开——
> 这描的是**目标形态**，不是今天的行为：**P0 的 Out of Scope** 把 oxipng、SSIMULACRA2 仲裁
> 与 `best` 整个摘了出去，眼下只有一档在跑，命令行上没有出口，`Mode` 上也没有这一维。
>
> 下限**按素材形态分档**，不是一个数——一页 24.3 兆像素与一页 2.9 兆像素做的不是同一件事。
> 四档连同定它们的理由在 measurements 的《吞吐目标：八十页/秒不成立，改判》。

## 互锁

| 术语 | 含义 |
|---|---|
| **互锁 (Interlock)** | 几项开关凑在一起互相削弱的那种组合。**不是错误**：每一条各有各的处置，不一定拦住用户 |
| **处置 (Voice)** | 一条互锁咬上之后**这一趟**怎么交代：报告抬头提一次、这一趟一个字都不说、或当场拒绝 |

> 全集逐条写在 `tonefit --help` 末尾的《开关互锁》里，各开关自己的帮助只留指路。
> 处置说的只是**这一趟**怎么交代，不是该不该写进说明书：
> 一趟不吭声的那一条也在《开关互锁》里。

## 尚未确立

- **拆分的三个数**——候选阈值 1.5（页宽高比 ÷ 面板宽高比 的下界）、装订沟检测窗口
  ±10% 页宽、沟宽上界 15% 页宽。三个都由实测夹出（measurements 的《跨页拆分》），
  两侧都还宽——但夹出它们的只有三卷双页片源，换一批扫描件未必成立。
  三个里只有候选阈值有命令行出口（`--split-threshold`），检测窗口与沟宽上界不可调
- **拆开之后的页间字号跳动比拆之前更大**——每半各自裁，而一张跨页的左右两半在扫描件上
  常常一边白边宽一边窄，各裁各的之后线性放大就分了家。实测卷内极差：哆啦A梦 0.390 → 0.410、
  Forget-me-not 0.066 → 0.155、改革之獸 0.015 → **0.587**（measurements 的《跨页拆分》）。
  「跳动是接受的形态」这条决定当初是在 0.112 那个量级上拍的板，而双页片源上要付的是五倍。
  要不要给它加一道夹子（例如卷内把线性放大收进一个带宽），是改「逐页各裁各的、
  不取卷级裁切框」这条明确决定。**已拍板：暂不加夹子，先扩样本**——0.587 眼下只有一卷撑着，
  另两卷是 0.410 与 0.155；换一批双页片源之后它是常态还是离群，决定了该不该为它改那条决定
- **裁边的三个数**——墨阈 200、行列墨量占比 0.5%、`crop::MIN_KEPT` 的 0.5。
  前两个原是实测那张表的施测参数，第三个是为一张几乎空白的退化页现加的守卫，
  取值只因为两侧离得远。三个都跟着素材走，换一批扫描件未必成立
- **彩色分支上裁边的代价**——它按灰度量墨（窗口不该因面板认不认得颜色而变），
  于是彩页在彩色面板上多走一遍全页 OKLab。没有彩色面板的真实语料，量不出来。
  另有一处更细的：带 alpha 的源上两条路合成次序不同，卡在墨阈上的透明像素可能一路算墨一路算纸
- **兜底上界的折算系数**——`PEAK_BYTES_PER_TARGET_PIXEL` 是照代码数出来的，不是量出来的；
  两侧上界都只管一页，而第一遍是满核并行的
- **默认适配方式上的迟滞只覆盖抖动那一维**——纯色页在门成立的候选集上升不到 4bit
  （2bit+FS 的颗粒上限压在可见度地板以下），位深那一维的端到端覆盖仍只在 `--fit inside` 上
- **阈值在 boox-poke6 以外的面板**——5.123 是两道盲测窗口里的取值：外面那道
  [4.930, 6.022) 由真机盲测在 **boox-poke6** 上夹出，里面那道 **[5.079, 5.134)** 由第十轮真机
  在 **kobo-libra-2** 的连续灰调页上夹出（两道同一条口径：下界可接受、上界不可接受）。
  **两道各在各的面板上量的、彼此不可比**，取窄的那一道是保守取法。其余面板沿用同一个数，
  **未逐面板复核**。判据跟着面板走、搬运的条件见《阈值》：同 PPI 的面板之间已有一份实测同向
  （measurements 的《位深盲测》里 Kobo Libra 2 那次复跑），PPI 不同的面板上一块的窗口
  不等于第二块也落在里面。出口是 `--threshold`。报告里印出的是数值**加标定来源**，
  读的人能自己判断它对手上那块板成不成立。
  **里面那道只有 0.055 宽**；再叠上「取点」那一层（语料上不虚抬 ＋ 连续灰调页上 `4bit+FS`
  自己也得进得了界），可挪的余地只剩 **0.021**——**比一页读数的抖动还小**。
  阈值这个旋钮基本上用满了：下一个想挪它的人，八成得先松掉某一条钉着的性质。
  **两层各是谁定的、那几个数怎么量的，都在** measurements 的
  《取值还要够高：让 `4bit+FS` 真的进得了界》——这里只留结论
- **颗粒项可见度地板的上界只有 1bit 那一档有数据**——地板是格点间距的一个比例，
  比例的**下界算得出来**（`0.2071·s`，三档同一个），上界要人眼：现行的 1bit 上界 60
  由四页真机盲测夹出，**2bit 与 4bit 上一格数据都没有**。那两档的地板眼下是「同一个比例
  乘各自的间距」推出来的（18.33 与 3.67），没有东西说得出它们该不该往上抬。
  报告在**判据构成那一行**连同来源印出；它与阈值同一批该被复核，
  搬运的条件也与阈值同一条（见《阈值》），而改这个比例要连阈值一起重标
- **掩蔽加权在大面积规则网点上可能反向**——掩蔽按块内活动度给误差打折，而活动度在网点区最高、
  折得最狠。全库扫描的双峰与三组盲判指向同一件事：抖动在细碎线条上确实被掩蔽，
  在大面积规则网点上没有（measurements 的《全库扫描：六成的页按体积选错档》）。
  《判据掩蔽》那两个数（地板与拐点）该不该跟着参照自身的高频走，未定。
  两者都是**未标定占位值**，报告在**判据掩蔽那一行**连同「未标定」一并印出，
  与《判据构成》那道地板、《判据聚合》的 K 同一个待遇。
  **取值本身不抄在这里**，它只有 `metric` 里那两个常量一处出处——抄下来，标定那一趟
  就要多改一处，而没有东西会红
- **《纸白对齐上限》那个数**——占位值 **4**，**未标定**，与《分块聚合的 K》、
  《判据掩蔽》那两个数同一档待遇。4 来自全语料分布上的一个断口（1202 页里钳制宽度 ≤4
  覆盖 99.1%，而 4 与 8 之间一页都没有，见 measurements 的《全语料普查：四成三的页纸白
  不落在格点上》）——那是**分布给的，不是人眼给的**。它该取多少要在真机上看：
  「钳掉 N 级之后最淡的网点还在不在」——**那一问仍然挂着**。
  **默认值已经取 4（开）**：不加参数的那一趟也在对齐，取 0 仍是关闭、由用户点名。
  **默认开着不等于这个数被测出来了**——它照旧是占位值
- **扫描黑边、倾斜页、不同网点线数与角度**——真实语料里一页都没有（measurements 的《覆盖范围》）。
  裁边按墨量找边，而黑边本身就是墨；几何门与装订沟检测都假定页边是干净的；
  网点的线数与角度直接进颗粒项与抖动的可见度。这三种页上那几样会怎样，没有测过
- **决定最优档的那条分界线不在 A/B 那个轴上**——ADR 0008 按**总缩放比**分类（≥2 是 A 类、
  <2 是 B 类），而九部素材实测排开之后，分界落在**内容是不是连续灰调**上：已量化源、
  网络发布版、网点扫描、线稿六部落在 73%~100%（4bit 不抖判据更优的页占比），
  连续灰调三部落在 1%~22%，中间几乎没有过渡（measurements 的《一手素材：分界线不在 A/B
  那个轴上》）。两者**正交**——`_samples/高像素` 这一个 A 类目录里既有 100% 也有 1%，
  而 B 类四部整体站在高端，与 B 类「连续灰调」那半句描述对不上。
  「已量化源」这一类更是连名字都只在 measurements 里有，词汇表没收。
  这一条不改 ADR 0008 的决定（预缩那个开关照旧按缩放比开），只标出**判定要用的那条分界
  没有一个词描述得出来**
- **有损转码源的行为**——AVIF/JPEG 伪影会让真 1bit 源也显示 256 级灰调，一切基于源灰调级数的检测在真实片源上会失效。
  同一批伪影也给消色像素添上几级色度，**彩页识别的色度地板与覆盖率**这两个数因此落在这个洞里（ADR 0010）
- **Kaleido 彩色面板的一切数值**——彩色滤光片让彩色内容的实际分辨率降到黑白层之下，各家口径是一半。
  P0 的彩色分支只做缩放，这个折减到不了任何判定上；要利用它得先测
- **几何门不成立时该用哪个位深集合**——对齐消失，灰阶硬上界的依据随之失效（ADR 0003）。
  尚未测量，P0 仍照 {1,2,4} 裁，抖动则在那些页上关闭。**洞还在，只是默认这条路上没有页掉进去**：
  以高为准让每一页的高贴住面板，门因此**每一页都成立**，掉得进去的只剩越过**兜底上界**
  退回 fit-inside 的那些页，而真实素材够不着那道线。`--fit inside` 上它照旧在，而且更尖：
  阈值标定之后门不成立的页上界降为 4bit 不抖，八卷实测里那一侧仍有页**没有一档在界内**、
  只能落到兜底（有多少页见 measurements 的《候选上界不恒在界内：门的两侧各有多少页超出》）。
  洞没有被填上，只是被一个默认值绕开了
- **LCD 路径**——无灰阶硬上界，且项目所有者无 LCD 设备可标定，相关 profile 必须标注「未经标定」
- **续做的粒度**——参数哈希是一个整体，给不出「哪些参数脏了」。改一个位深就要整趟重跑第一遍，
  而位深本来不影响参照。要更细的粒度，得先给参数哈希分层（ADR 0012 的《备选方案》）
- **预设的字段演进**——预设里存着口味层的字段名。工具增删参数之后，旧预设该报错、该忽略、
  还是该迁移，尚未决定；在决定之前，预设只在同一版本内可靠
- **分块聚合的 K**——尾巴永远不宽于几块。偏小则更严，而漏判与误判的代价不对称
  （漏掉真损伤是输出崩了，误判只是多用一档位深），因此占位值往严的一侧取。
  与阈值同一批标定，是第一批该被替掉的数（ADR 0002）。
  报告在**判据那一行**原样标出——聚合形状对所有 profile 都一样，不跟着 profile 那一行走
- **输出归档不可复现**——写出的 `.cbz` 每个成员带着写出那一刻的时钟
  （`zip` 的 `SimpleFileOptions::default()`，DOS 时间戳两秒一格），同一份源两趟之间
  整个文件的字节因此不同。幂等按自描述元数据走、不看容器时间戳，所以不挡路；
  「同内容出同字节」这条性质没有做（停车场 Q109）
- **摊开的临时目录靠析构收**——`.7z` 与 `.rar` 要整卷摊到系统临时目录，而成员要按名字读回来、
  不能用匿名文件（缓存的溢写能，因此中断不留孤儿）。跑完、中止都不留，**硬杀会留下
  一个 `tonefit-…` 目录**，而没有任何一趟会去扫它。自己去扫等于建一份跨运行状态，
  那是 ADR 0009 关掉的方向（停车场 Q120）
- **两列卷之间的先后无从复原**——`Report` 把做出了东西的卷与没做成的卷分成 `volumes` 与
  `failed_volumes` 两列，各自按先后，而**跨列的先后一处都没记**（卷报告只有时长，没有起止时刻）。
  报告与卷表因此只能先摆一列再摆另一列：第三卷没做成、第四卷做成了时，屏上它俩的先后是反的。
  会话那一侧「跟随最新那一卷」也因此不跟没做成的卷（停车场 Q151、Q200）
- **点号键与内联表那几种 TOML 写法保不住注释**——覆盖一份同名预设走的是「按节剪、按节接」，
  对本工具自己写出去的**节写法**成立。点号键、内联表、多行字符串里的 `[`、转义过的名字
  那几种同样合法的写法剪不动，当场退回**整份重排**：每一份预设的内容一份不少，
  丢的是**用户手写的注释与排版**。屏上那两句话已经收窄到无条件成立的那两半，不提注释
  （停车场 Q108）
- **`.rar` 多数不固实，而摊开是一刀切**——实测 WinRAR 不加 `-s` 造出来的包不固实，
  固实要显式打开（`.7z` 那一半照旧默认固实）。ADR 0015 决定第 3 条按格式一刀切、
  非固实的 `.rar` 也照样整卷摊开，认下的代价原按「少数情形」估。若流通中的 `.rar` 多数不固实，
  那次白付的全量写盘就是**常态**——要不要回到《备选方案》里「逐卷探固实与否」那一条，
  与这条一起判（停车场 Q125 的后半）
- **命令行上有四行不归自己排**——用法那一行由 clap 按参数拼出来，`help` 子命令、`--help`、
  `--version` 三条是 clap 内置的**英文**。四行都不过折行那一层：窄终端上由终端自己回绕，
  而一份全中文的界面里夹着三条英文说明。开 clap 的 `wrap_help` 治不了中文（停车场 Q32、Q186）
- **问不出形态的目录项无声消失**——发现往下走时，一层列得出、而其中某一项 `stat` 不出来
  （条目在两次系统调用之间被删、网络盘那一刻不通），那一项**根本不成为候选**：
  预扫看不见它，「走不进去的地方」那一栏收不到它，退出码一格不动。
  与「列不出这一层」那一种是同一类毛病，只是罕见得多（停车场 Q201）
- **两遍枚举之间源变了，两个数会分家而没人吭声**——开工那条事件报的**步数**出自预扫那一遍，
  报告里的**成员数**出自重开那一遍。源在两遍之间没动的每一趟两个数逐字相同；动了就分家，
  而眼下**静默地按上界处理**（预告的步数本来就是上界、不是承诺）。
  `p4-parking-lot/12` 那道印记盖的是「成员表出自这个句柄、字节出自那个句柄」那半个窗口，
  **这半个一格都没盖住**：两个数一个是预扫那一遍的、一个是重开那一遍的，与是不是归档卷无关，
  目录卷上同样分得了家而目录卷连印记都没有（停车场 Q93）
- **NAS 的最优 I/O 策略**——既非 HDD 也非 SSD。P0 把它归进「未知」并退到串行读，
  多半留了性能在桌上；实测出并发更快的用户走 `--io-mode concurrent`
