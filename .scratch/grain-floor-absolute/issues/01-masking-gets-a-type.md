# 01: 掩蔽那两个数有自己的类型，报告说得出

**What to build:** 用户看报告时，除了《判据构成》与《判据聚合》，还看得到**掩蔽**那两个数是多少，并且标着「未标定占位值」——照 `Aggregation` 印 K 的做法。今天它们是两个私有常数，屏上一个字都没有，而它们改了判定就变。

**取值不动，判定逐字节不变。**这一张是给 `03` 铺路：哈希收不到私有常数。

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [ ] 新增 `Masking { floor, knee }` 与它的构造函数，**与 `Composition`、`Aggregation` 平级**——ADR 0002 把前两者分成「一块的读数**由什么组成**」与「块的读数**怎么收成一个数**」，掩蔽是第三件：一块的读数**怎么加权**
- [ ] 取值仍是 **0.5 / 8.0**，仍是**未标定占位值**；本张不标定它们
- [ ] 报告印得出这两个数，连同「未标定占位值」一并说——措辞**只有一处出处**（ADR 0016），命令行的纯文本与会话的表共用同一份字
- [ ] 会话那一层跟着走既有的摆法，不新造（会话「这一趟的前提」复用报告抬头那一份）
- [ ] `CONTEXT.md` 的《尚未确立》里那两个数的记法跟着更新——它们现在有名字了
- [ ] **黄金快照逐字节不变**——取值没动，判定就不该动。这是这一张最强的验收
- [ ] 闸门三条绿（`cargo xtask gate`）
