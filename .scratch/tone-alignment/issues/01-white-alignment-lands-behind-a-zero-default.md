# 01: 纸白对齐落进管线，命令行调得动，默认关

**What to build:** 用户加上 `--white-align-limit 4` 之后，纸白离格的页在白底上**一个抖动点都不撒**——那正是真机上被判「明显影响观感」的那一类劣化（measurements 的《真机三组》B 组，四对全票）。不加这个参数时**一页都不改**：默认取 0，产物与今天逐字节相同。

这一张是整条路的 tracer bullet：量出纸白、按守卫决定改不改、改了之后参照与输出一起跟着走、参数在命令行上调得动、改了参数旧输出会过期。少任何一环这条路都跑不通。

**Blocked by:** None (can start immediately)

**Status:** resolved

- [x] **纸白对齐是一个纯函数**：`GrayImage` 进、`GrayImage` 加「这一页做了什么」出。不碰文件系统、不碰全局状态，与 `score` 同型
- [x] **纸白的定义可执行**：页面上最大那块严格平坦区（3×3 邻域方差为 0 的像素里出现次数最多的灰度）。不取最亮像素，不取高百分位（理由见 spec）
      ⚠️ **这一条的括号外与括号内不是同一件事，落地照的是括号内。**现成那份实现（`survey_flat.paper_white`）取的是**全页零方差像素的众数**——`flat` 是一张散布全页的掩码，一次连通域分析都没有；「最大那一块里的众数」与它只在最大那块恰好也是最常见取值时才同解。口径一致才比得了字节（第十轮那四页就是拿它量的），因此**行为照实现走**，而本票自己那几处措辞（`CONTEXT.md` 的《纸白》、`src/white.rs`、`--help`、夹具）一律写成真的那一句。仍写着「最大那一块」的三处（本条、`survey_flat` 的文档字符串、measurements 那一节）**一个字没改**，记 **Q520**
- [x] **对齐的手段是钳制近白**：`[纸白, 255] → 255`，低于纸白的取值一个都不动。不做线性拉伸，不做整幅平移
- [x] **三条守卫各有一条用例**：《离格量》为 0 不动 · 离格量超过上限不动 · 平坦区不足（量不出纸白）不动。三种情形都**整页逐像素不变**
      三条各把守卫按反跑过一次，红都看见了。**其中头一条的红只在标签上**：离格量为 0 时钳 `[255,255]` 本来就是恒等，拆掉那条守卫像素一个都不会变——它管的是「这一页做了什么」，不是像素
- [x] **对齐之后那一页的纸白恰好是 255**，且该页在 `2bit+FS` 上的平坦白底**一个点都不撒**——这一条直接钉住整件事的目的
- [x] **上限取 0 时任何输入逐像素不变**（这是「关闭」的语义，不是另设开关）
      **是双保险**：`limit == 0` 那条短路拆掉之后，离格那条守卫照样把每一页拦下（`离格量 > 0` 恒成立），像素还是不动。短路挡的是白花的工夫，守卫挡的是像素——这一条写进了 `align_white` 的文档
- [x] 对齐发生在**缩放之后、构造参照之前**——**参照与其后一切量化用的都是对齐过的像素**，判据两侧因此同源，量化仍是唯一被隔离出来的变量（ADR 0002 决定第 1 条）。
      **不要把这一条写成「对齐过的图就是进缓存的那一份」**：`two-pass-rework/12` 之后缓存分两副——上包络那条路攒整卷参照，逐页那条路只在滚动窗口里留几页、出了窗口当场量化编码成字节。对齐在两副之前，因此两副都吃得到；说成「进缓存的那一份」在逐页那条路上是假话
- [x] **彩页不经这一步**（ADR 0010：那条路径既不量化也不抖动）；**几何门不成立的页照样对齐**（它只是没有抖动那一维）
- [x] `--white-align-limit <级数>` 在命令行上认得，默认 **0**，`--help` 说得出它是什么、取 0 是什么意思
- [x] **参数进 `metadata::params_hash`**。ADR 0002 的《后果》记过一次同型事故——「地板不在里面……旧输出会被静默跳过」。取值 0 也照样进
- [x] `tests/pipeline.rs` 上有一条：改了这个参数之后旧输出**过期重做**而不是被跳过（写法照 `tests/idempotency.rs`）
      ⚠️ **落点不是 `tests/pipeline.rs`，是 `tests/idempotency.rs`**：那条性质的穷尽数组（`a_changed_parameter_redoes_the_volume`）本来就在那里，往 `pipeline.rs` 里另写一条就是同一条性质的第二个出处。加的那一格是**从默认的 0 改到 4**——「取值 0 也照样进哈希」不成立时唯一漏得掉的正是它
- [x] **两张新夹具**：一张纸白离格的页（现有夹具全是合成图、纸白多半正好 255），一张满版无纸边的页
- [x] `CONTEXT.md` 加三个词条：**纸白**（进《图像内容》）、**纸白对齐**与它的**上限**（进《管线》）。《离格量》已经在《量化》里了
- [x] `CONTEXT.md` 的《尚未确立》记上：**上限那个数未标定**，占位值 4，与 `K`、`MASKING_FLOOR`、`MASKING_KNEE` 同一档待遇
- [x] **黄金快照逐字节不变**——默认 0 意味着一页都不改，这是这一张最强的验收
- [x] 闸门三条绿（`cargo xtask gate`）

## 落地记录

**整条路通了，默认那一趟一页都不改。**（2026-09-10）

`white::align_white(GrayImage, WhiteAlignLimit) -> (GrayImage, WhiteAlignment)`，纯函数，
落在 `gray_page` 里缩放之后、`Reference::new` 之前——**全仓非测试代码里 `Reference::new` 只有那一处**。
`--white-align-limit <级数>` 默认 0，进 `params_hash`（取 0 也进）。
预设与会话那两层归 `03`／`04`，各留了指路注释。

### 数

| | 命令 | 结果 | 对账 |
|---|---|---|---|
| 闸门 1 | `cargo test` | `898 通过 0 失败`（lib 243 / bin 351） | 基线 885（243/349），+13 |
| 闸门 2 | `cargo test --no-default-features` | `767 通过 0 失败`（lib 243 / bin 220） | 基线 754（243/218），+13 |
| 闸门 3 | `cargo check --features profiling` | 干净 | — |

+13 那一笔两条对得上同一个账：**+2** 命令行用例（认得 `--white-align-limit`、`--help` 说得出取 0 是什么意思）、
**+7** 纸白对齐那个新 seam 上的性质、**+4** `run` 那个 seam 上的（tracer bullet · 门不成立照样对齐 · 满版页不动 · 彩页不经）。
lib 那一格没动：`metadata` 那两条是往既有的穷尽数组里各加一格，不是新用例。

`cargo doc --no-deps` **15 条，与基线相同**。中途冒到过 16——在 `align_white` 的公开文档里链了私有的 `MIN_FLAT`，
改成不链就回来了。`cargo fmt --check` 干净，`cargo clippy --all-targets` 0 条。

> **这三条最后一行来自哪一版**：闸门跑完之后又打了**三行 `///` 的订正**（`tests/alignment.rs` 一处、
> `tests/fixtures/mod.rs` 两处，都是把夹具补那一块之后过时的陈述改成实测值）。
> 那三行**不是代码、也不在库里**——集成测试不跑文档测试，文档注释改不动任何行为。
> 因此没有再跑第三趟全闸门，改按差别的大小精确认证：`cargo check --all-targets` 通过
> （每一个测试二进制都重编了）、两个带改动文档的测试二进制重跑 `7 / 88` 全绿、
> `clippy` 0 条、`doc` 15 条。**上一次不是这么处理的**：那一次动的是 `src/white.rs`
> 里的一处死枝，是**库文件里的代码**，每个二进制都链着它——那一次重跑了整趟闸门。

**黄金快照逐字节不变**：`cargo test --test golden` 两条绿，`tests/golden-snapshot.txt` 在 `git status --porcelain` 下**是空的**。
这是这一张最强的验收，也正是「默认 0 意味着一页都不改」那句话的全部证据。

### 「产物与今天逐字节相同」只在像素那一侧成立

上面那句话要带一条限定，它咬得到用户：**默认 0 那一趟写出的 PNG，tEXt 里的参数哈希变了**——
`--white-align-limit` 进了哈希文本，取值 0 也进（票面第 11 条要的正是这个，ADR 0002 的《后果》记过漏收的事故）。
于是**默认参数一个字没改的用户，升级后第一趟仍要整库重跑一次**。

**而他今天读不到原因**：报告上没有一个字说得出「哈希变了是因为多收了一个参数」，幂等那一道只答「对不上」、
不答「哪里对不上」。行为不改（那是票面要的），措辞与那一行报告记 **Q522**，落点指向 `02`。

黄金快照按 `--no-metadata` 跑，**验的正好只是像素那一侧**——记录那一侧今天没有任何用例在看。

### 两处被红逼出来的改动

- **`SHORT_HELP_INDENT` 31 → 35。**`--white-align-limit <级数>` 成了最长的开关，`-h` 那一列跟着变深，
  `the_help_folds_every_line_into_the_terminal` 当场红——**那个常量的文档预言的正是这一句**。
  顺带写下此前没人写过的一件事：**clap 排版按字符数算，而中文一个字占两格**，
  所以那个数不等于「最长开关的显示宽度」；落在屏上最深的那几行是**值名里中文最多的**
  （`<滤波器>`、`<字节数>`），不是撑开那一列的 `--white-align-limit` 自己。
- **满版无纸边那张夹具改了两次，两次都是因为「用例够不到它要验的东西」**（**Q521**，同一票内两例）：
  最亮 186 → 252（不然硬猜出来的纸白离格太远，被上限守卫拦下，逐像素那一半白测）；
  背景又补一块 68×68 的 253，内部 4356 个平坦像素——**照 measurements 里画集 Venus 实测那 4430 取的**
  （不然拆掉 `MIN_FLAT` 门限时众数落到 0、离格 255 级，**又**被上限守卫拦下）。
  两次拦下它的是同一条下游守卫，那条结论写在 Q521 里：**多重守卫串起来时，上游那道失效不会红。**

### 六条守卫逐条按反，红都自己看过

| 按反的那一处 | 红在哪一句 |
|---|---|
| 拆掉「离格量为 0 → 不动」 | 两条红在标签 `Aligned { paper_white: 255 }`。**像素没变**——钳 `[255,255]` 本来就是恒等，这条守卫管的是「这一页做了什么」，不是像素 |
| 上限守卫 `>` 改成 `<` | 四条红，含 `对齐之后白底上还撒着点 left: 2900`（2900/128000 ＝ 2.27%，与 `2/85` ＝ 2.35% 对得上） |
| 量不出纸白改成硬猜「最亮像素」 | `Aligned { paper_white: 252 }`；把标签那句摘掉再跑，逐像素那句也红 |
| 拆掉 `MIN_FLAT` 那道门限 | 两层都红：`第 0 个像素本该是 253 却是 255（共 4624 处不同）`（＝角上那块 68×68） |
| 拆掉「上限 0 即关闭」短路 | 红在标签。**像素仍不变**——上限 0 时离格守卫兜得住，「0 不改像素」是双保险 |
| 从 `gray_page` 摘掉那次对齐 | 管线三条红；`满版页不动` 照旧绿（它本来就该绿） |
| 从 `params_hash` 摘掉那一行 | 「改了纸白对齐上限，哈希没变」＋ 幂等那条一起红 |

### 与票面对不上的两处，都是有意的

- **纸白的定义照实现走，不照票面走。**票面第 2 条写「页面上最大那块严格平坦区……的众数」，
  而现成那份实现（`survey_flat.paper_white`）取的是**全页零方差像素的众数**——`flat` 是一张散布全页的掩码，
  一次连通域分析都没有。两句只在最大那块恰好也是最常见取值时才同解。
  **口径一致才比得了字节**（第十轮真机那四页就是拿它量的），因此行为照实现走，
  而本票自己那几处措辞一律写成真的那一句。仍写着「最大那一块」的三处
  （票面第 2 条、`survey_flat` 的文档字符串、measurements 那一节）**一个字没改**，记 **Q520**。
- **「改了参数旧输出过期重做」落在 `tests/idempotency.rs`，不是票面写的 `tests/pipeline.rs`。**
  那条性质的穷尽数组本来就在那里，往 `pipeline.rs` 里另写一条就是同一条性质的第二个出处。
  加的那一格是**从默认的 0 改到 4**——「取值 0 也照样进哈希」不成立时，唯一漏得掉的正是它。

### 停车场

**Q517–Q523** 七条：阶段计时没占格 · 《纸白》与《部分救回页》《占位页》里那个词不是同一个数 ·
`MIN_FLAT` 的出处与为什么不进《尚未确立》 · 三处写着「最大那一块」的假话（`no-false-line`） ·
用例够不到自己的断言（同一票内两例） · 「逐字节相同」只在像素那一侧 · 上限没有上界。

### 重渲第十轮 W 那四页比字节：四页全部对上

**这一步不在上面的复选框里**——它是 `05` 号票《补测结果》那条 ⚠️ 限定要销的东西：
第十轮真机判的那四对，材料是照本票的定义**复现**出来的，不是产品渲的。
本票落地当天用产品重渲了一遍（2026-09-10）。

**比的是对齐后的参照页那一层**，不是候选 PNG。候选是拿同一个产品二进制从参照渲出来的，
比它只会把编码器那一侧的差异混进来；真正该问的是「产品钳出来的那一页，与 Python 钳出来的那一页
是不是同一张图」。源用同一份未对齐的参照（`round10/ref_raw`），产品跑
`--white-align-limit 4 --bit-depth 8 --dither off --no-crop --no-split`——8bit 让量化成为恒等，
写出的就是对齐那一步的结果。

| 页 | 源纸白 | 离格 | 上限 0 ＝ 源？ | 对齐后纸白 | 离格 | 与复现逐像素 |
|---|---|---|---|---|---|---|
| MHZ01_088-2 | 253 | 2 | 是 | **255** | **0** | **相同（差 0 个像素）** |
| MHZ01_089-1 | 253 | 2 | 是 | **255** | **0** | **相同（差 0 个像素）** |
| MHZ01_089-2 | 253 | 2 | 是 | **255** | **0** | **相同（差 0 个像素）** |
| MHZ01_090 | 253 | 2 | 是 | **255** | **0** | **相同（差 0 个像素）** |

纸白是拿 `calibration/survey_flat.paper_white`（**复现当时用的同一份实现**）量的，
四页量出的 253／离格 2 与 `judgements/第六轮真机四组.json` 记的逐字相同。

**再往前推一层，端到端也对上**：从未对齐的源直接渲判读用的两组候选
（`2bit+FS` 与 `4bit 不抖` 各四张），八张与复现那八张**逐像素全同**，
PNG 的 **IDAT 逐字节相同**（088-2 的 2bit+FS 是 25171 字节，两边同一串），
IHDR 与 IEND 也相同。**只有 tEXt 两格不同，两格都解释得清**：

- `tonefit:params` `dc9fe48c…` → `8adb2525…`：**新参数进了哈希**，正是 Q522 记的那件事。
- `tonefit:source` 不同：复现喂的是钳过的页，产品喂的是**未对齐的源**——两边的源字节本来就不是同一份，
  而这正是要点，钳制现在由产品自己做。

**对齐后的判据读数与 `05` 记的逐位吻合**：`2bit+FS` **1.699 / 1.840 / 1.824 / 1.786**，
`4bit 不抖` **0.655 / 0.686 / 0.699 / 0.661**，四页判定都是 `2bit+FS`。

**结论：第十轮 W 那四对不必重判。**`05` 的《补测结果》里那条 ⚠️ 已销，改写成了核实过的结果。
