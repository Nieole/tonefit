# 11 — 每页结果

**What to build:** 在新界面上从卷进到页（spec《每页结果》；停车场 Q713 的结论）。

- 卷行上 `l`／`⏎` 换屏进**每页结果**：换掉卷列表、占整宽，总览照旧钉在上面；抬头是面包屑（任务 › 分区的路径 › 目录 › 卷），
  右端说此刻的列法；头一行是这一卷的灰阶分布与需留意几页；
- 列：页面 · 尺寸 · 缩放 · 灰阶 · 原因 · 画质分 · 提示；每一格的字出自报告那一处；砍列次序在 `session::columns`
  （缩放先让，再画质分，再尺寸；灰阶恒在）；
- 默认只列需留意的页，`a` 切到全部页；一页都不需留意时说一句，不给空表；
- `h`／`Esc` 回到卷列表原处；
- **展得开**的卷只有收摊了的（跳过的也算）与确认点上那一卷。没做成、被立即停止掉、还在处理、还没轮到的卷按 `l`
  不换屏，屏底说为什么（没做成的那一句就是行尾原因）；跳过的卷进得来，只说一句它跳过了。

真会话仍进旧界面。

**Blocked by:** 10 — 已结束、备注行与整卷统一灰阶

**Status:** ready-for-agent

- [ ] 「每页结果」120×36、80×24 逐格相等
- [ ] 序列：卷行 `l` → `a` → `j` → `h` 回原处；没做成的卷 `l`；跳过的卷 `l`；还在处理与还没轮到的卷 `l`；走完与期望屏相等
- [ ] 用例：停得住与展得开分开，Q713 列的那几种卷各一条
- [ ] 真会话仍进旧界面，旧用例照绿；设计快照未改
