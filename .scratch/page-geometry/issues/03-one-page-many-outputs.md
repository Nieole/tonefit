# 03 — 一页可以产出多页（预重构）

**What to build:** **用户看不到任何变化。** 管线从「一个源成员对一个输出成员」放宽成一对多，
而此刻每一页的 N 恒等于 1。

04 号票要把跨页拆成两页，那会打破现在处处成立的 1:1 假设。先把形状放宽、再落行为，
后面那张票才装得进一个会话。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] 全量测试绿，**黄金快照一个字节都不变**——这张票不改变任何外部行为，快照是它的验收
- [ ] 缓存、报告、进度步数、幂等依据、成员命名、以及「每个输出成员只对一个源成员」那道校验，
      全部认一对多的形状
- [ ] 报告把**源页数与输出页数分开**说，此刻两者相等
- [ ] N=2 的下游行为在本票内可测（不经命令行——那条路要等 04）

## 不要做的

- 不要顺手把拆分判定也写进来。这张票只放宽形状，不引入任何决定 N 是多少的逻辑。
