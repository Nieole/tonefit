# 06: 4bit 那一档只留不抖

**What to build:** 候选集里去掉 4bit+FS。它与 4bit 不抖在**两批材料、八位判读者、45 格**上无一格判出差别，而体积差 6~11 MB。去掉它有三笔收益：产物更小、判据**少算一个候选**（判据占单页处理时间 32%~73%，这是直接的吞吐收益）、报告少一档噪音。

**Blocked by:** 03

**Status:** ready-for-agent

- [ ] 几何门成立时的候选集从 6 个降到 5 个，不再含 4bit+FS
- [ ] `--dither fs` 与 `--bit-depth 4` 同时点名时的行为有明确交代（拒绝、还是落到 4bit 不抖），错误消息指得出为什么
- [ ] `src/cost.rs` 里印作「判据（6 候选）」的措辞跟着改
- [ ] 报告与逐页判据那一行不再出现 4bit+FS
- [ ] 黄金快照的差异审查后接受
- [ ] 闸门三条绿
