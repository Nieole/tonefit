# 03: 真机判读——连续灰调上哪一档对（闸 ①）

**What to build:** 一次真机判读，了结判据与判读在连续灰调上完全反向这件事。判据在那一类素材上 0/3 偏 4bit，五位模型判读者 3/3 偏 4bit，而 `docs/measurements.md`《A 类位深实测》记着「连续调彩色源上 2bit+FS 更优」——**那一行是判据算出来的，不是人标的**。两者必有一错，而 04 的四个数全建在这个方向上。体积不构成障碍：连续灰调换 4bit 只贵 1.02×。

材料是 **`AB对比（先看这个）.cbz`**：12 对页交错编排，同一页的两个版本**相邻**，翻一页就是一次 A/B 切换。跨文件切换要几秒到几十秒，而视觉工作记忆撑不住那么久——那时比的不是两张图，是一张图和一段褪色的印象。顺序随机、页上无标注，判完再对答案。12 对覆盖五类素材，闸 ① 占最后四对；其余八对是顺带收获，可能提前答掉 04 的一部分。

**Blocked by:** None (can start immediately)

**Status:** resolved

- [x] `_samples/_对比-整页/整页对比\AB对比（先看这个）.cbz` 拷进设备，**关掉阅读器的缩放与白边裁切**，不放大
- [x] 12 对逐对判，报页码即可（例如「第 8 页比第 9 页好」）；**分不出来也是答案**，不必硬选
- [x] **第 9~12 对是闸 ①**（哆啦A梦 v15，连续灰调）——这四对定 04 的方向，其余八对给 04 的四个数当旁证
- [x] 判完之后再对 `AB对比-答案（判完再看）.md`，不要提前看
- [x] 结论写回 `docs/measurements.md`，并交代《A 类位深实测》里「连续调彩色源上 2bit+FS 更优」那一行的存废
- [ ] 整卷那十份 cbz 是**补充**，不是主材料：它答的是「某一档连读几十页什么感受」，交错本答不了。只在 AB 判读留下疑问时才需要翻

**这张票不能派给 subagent**，哪怕它读上去像一次判读任务。两条实测挡着：《成品库上判不出阈值的原因是空间尺度》——局部放大块判不出整页尺度的现象，而模型看的永远是喂给它的裁切；以及《第四轮》里 `2bit+FS` 判读者之间 ρ 只有 `+0.194`（`4bit不抖` 是 `+0.713`），它落在感知边界上，加判读者只会更精确地测出「它是 50/50」。派 agent 去判会拿回一个看着有效、实际定不了阈值的结论。

## Comments

**材料已重做，第一条那句"第 5 组两份"是旧的。** 原来的编排是十份整卷 cbz（每组两份），
实测跨文件切换要几秒到几十秒，视觉工作记忆撑不住——比的不是两张图，是一张图和一段褪色的印象。

现在同目录下多了一份 `_samples/_对比-整页/整页对比\AB对比（先看这个）.cbz`：**12 对页交错编排**，
同一页的两个版本相邻，翻一页就是一次 A/B 切换；顺序随机、无标注，答案在
`AB对比-答案（判完再看）.md`。12 对里给了闸 ① 那一类（哆啦A梦，连续灰调）**四对**。

整卷那十份**保留着**，两者答的不是同一个问题：交错本答"这一页哪档好"，
整卷答"某一档连读几十页什么感受"。一本书答不了两个问题。

**这张票不能派给 subagent，哪怕它读上去像可以。** 两条实测挡着：

- `docs/measurements.md`《成品库上判不出阈值的原因是空间尺度》——局部放大块判不出整页尺度的
  现象，而模型看的永远是喂给它的裁切；
- 《2bit+FS 的争议是本质的，不是某位判读者的偏见》——第四轮里 `2bit+FS` 这一档判读者之间
  ρ 只有 `+0.194`（`4bit不抖` 是 `+0.713`），说明它就落在感知边界上。加判读者只会更精确地
  测出"它是 50/50"。

派 agent 去判会拿回一个看着有效、实际定不了阈值的结论，而 **04 那四个数全建在 03 的方向上**。

（本条由 `/run-tickets` 这一轮记入，来源是产出这批材料的那个会话；票面第一条按新编排读即可，
其余三条不变。）

---

**票面已按新编排重写**，由产出这批材料的那个会话。验收条件从「第 5 组两份」换成 12 对交错本的
判读流程，上一条里「第一条那句是旧的」那个标注因此不再需要；标题去掉了「第 5 组」——新编排里
没有这个概念，闸 ① 是第 9~12 对。「不能派 subagent」从讨论提到票面：它是执行约束，
看票的人先看验收条件，那一条不该只躺在 Comments 里。上一条原样保留，它记的是当时的处置。

## 落地记录

**判读做完了，结论已写回 `docs/measurements.md`。**（2026-09-09）

原始作答（更干净的那一页的页码）：`3 · 看不出 · 看不出 · 看不出 · 看不出 · 看不出 · 14 · 16 · 19 · 20 · 22 · 25`。
解码后：**全部 12 对 2bit+FS 赢 6 · 分不出 5 · 4bit 不抖 赢 1；闸① 那四对 2bit+FS 3 : 1。**

**闸① 的答案与立票时的命题都不同。**立票时是「判据 0/3 偏 4bit vs 模型判读 3/3 偏 4bit，
两者必有一错」；`01` 号票落地后判据改口，现行判据在那一类上 5/5 偏 4bit，与模型判读**一致**
（Q316 已记）。**真机站在 2bit+FS，3:1——两者一起被推翻。**

《A 类位深实测》里「连续调彩色源上 2bit+FS 更优」那一行的存废：**结论留着，依据换了。**
它当初是判据算出来的，而判据现在偏 4bit，原依据不成立；撑着它的改成真机这一组。
那一行已就地加了限定。

**要认的口径**：n=4、其中一对反向，方向清楚但样本薄，**不作定量依据**；
前 8 对里有 4 对是成品库素材，其中 3 对答「分不出」，与《判读的可辨性依赖材料是不是一手的》
同向，不构成独立证据。

**没走「整卷那十份」那一步**——交错本上没有留下需要靠整卷连读才答得了的疑问。

**对 `04` 的影响**：`04` 的四个数原本「全建在 03 的方向上」，而这个方向现在与判据相反。
`04` 的票面因此要重写（这一轮 grilling 另有拍板：地板上界改成算出来的、多出地板下界与
低通项地板比例、并进两次测量），**不在本票范围内**。
