# 03: 真机判读第 5 组——连续灰调上哪一档对（闸 ①）

**What to build:** 一次真机判读，了结判据与判读在连续灰调上完全反向这件事。判据在那一类素材上 0/3 偏 4bit，五位模型判读者 3/3 偏 4bit，而 `docs/measurements.md`《A 类位深实测》记着「连续调彩色源上 2bit+FS 更优」——**那一行是判据算出来的，不是人标的**。两者必有一错，而掩蔽该往哪个方向调取决于它。材料已备好，体积上也不构成障碍：连续灰调换 4bit 只贵 1.02×。

**Blocked by:** None (can start immediately)

**Status:** ready-for-human

- [ ] `D:\tonefit-对比\整页对比\` 第 5 组两份（哆啦A梦 v15，2bit+FS 与 4bit 不抖）拷进设备
- [ ] **整页 1:1 看，关掉阅读器的缩放与白边裁切**；不放大——放大会造出面板上没有的差异，缩小会抹掉面板上有的
- [ ] 给出结论：连续灰调上两档哪一个可接受，或两者都可接受
- [ ] 结论写回 `docs/measurements.md`，并交代《A 类位深实测》里「连续调彩色源上 2bit+FS 更优」那一行的存废

## Comments

**材料已重做，第一条那句"第 5 组两份"是旧的。** 原来的编排是十份整卷 cbz（每组两份），
实测跨文件切换要几秒到几十秒，视觉工作记忆撑不住——比的不是两张图，是一张图和一段褪色的印象。

现在同目录下多了一份 `D:\tonefit-对比\整页对比\AB对比（先看这个）.cbz`：**12 对页交错编排**，
同一页的两个版本相邻，翻一页就是一次 A/B 切换；顺序随机、无标注，答案在
`AB对比-答案（判完再看）.md`。12 对里给了闸 ① 那一类（哆啦A梦，连续灰调）**四对**。

整卷那十份**保留着**，两者答的不是同一个问题：交错本答"这一页哪档好"，
整卷答"某一档连读几十页什么感受"。一本书答不了两个问题。

**这张票不能派给 subagent，哪怕它读上去像可以。** 两条实测挡着：

- `docs/measurements.md`《成品库上判不出阈值的原因是空间尺度》——局部放大块判不出整页尺度的
  现象，而模型看的永远是喂给它的裁切；
- 《2bit+FS 的争议是本质的，不是某位判读者的偏见》——第四轮里 `2bit+FS` 这一档判读者之间
  ρ 只有 `+0.194`（`4bit不抖` 是 `+0.713`），说明它就落在感知边界上。加判读者只会更精确地
  测出"它是 50/50"。

派 agent 去判会拿回一个看着有效、实际定不了阈值的结论，而 **04 那四个数全建在 03 的方向上**。

（本条由 `/run-tickets` 这一轮记入，来源是产出这批材料的那个会话；票面第一条按新编排读即可，
其余三条不变。）
