# 05: 掩蔽与阈值按标定结果落地

**What to build:** 把 04 标出来的掩蔽两个常数与阈值换进判据。跑完之后，判据在**连续灰调**素材上不再把 2bit+FS 排在 4bit 前面——那是它跨档选错的全部所在（其余 14 页判据与判读本来就逐页一致）。

**Blocked by:** 04

**Status:** wontfix

- [ ] `MASKING_FLOOR`、`MASKING_KNEE`、阈值换成 04 标定的值，各自的标定来源随报告印出
- [ ] 活动度的量法**不动**（块尺度 32 px、取局部均值而非块内均值）——那一条有 ADR 0002 与《位深盲测》撑着
- [ ] 掩蔽加权仍**只由参照定、与候选无关**——它是每页每块只算一次的前提，动它要重估吞吐
- [ ] 连续灰调那 5 页上判据的偏好与判读一致
- [ ] 02 那两条相关性重跑一遍，跨档仍显著为正、同档不变差
- [ ] 黄金快照第二次逐条审查接受
- [ ] ADR 0002 里剩余的过期陈述订正
- [ ] 闸门三条绿


## Comments

**`04` 判完之后，这张票的前提没了。**（2026-09-09）Status 从 `ready-for-agent`
改成 `needs-triage`——**不是没人做，是做不了**：它等的那几个数不存在。

`04` 的真机判读结论是**六个数在现有的判据形状下定不出来**，缺的那一项是《背景亮度》
（证据见 `04` 的《落地记录》与 `judgements/第七轮真机包04.json`）。具体到本票的三个数：

- **`MASKING_FLOOR` / `MASKING_KNEE`**：`04` 只算出了可行域，**没有目视结论钉住窗口两端**
  ——L 组是平坦块、逐块活动度几乎一致，掩蔽在它上面标不出来。要钉住得另造一组
  「同一档、块内活动度分层」的材料。
- **阈值**：判据缺一维时标出来的阈值**不可迁移**——同一个阈值在近白页与中灰页上
  该松该紧不一样，而判据分不出这两者。现在标一个数，等于把缺项固化进一个常数里。

**这张票要重新评估的是**：判据形状定下来之后，它该换成什么。形状那件事是拍板的事，
`04` 只交了证据。**在形状定下来之前不要按票面开工**——照票面把三个数换掉，
换进去的会是一组在半数页上系统性偏一侧的常数。

第二条与第三条（活动度量法不动、掩蔽只由参照定）**仍然成立**，与本次结论无关。

**判为 `wontfix`。**（2026-09-11）票面是「按 04 标定结果落地」，那个结果**永久不存在**——04 的结论
是六个数在现有判据形状下定不出来。此后三轮真机（九、十、十一）与三张票把六个数各自送到了别处：

| # | 04 要标的 | 去向 |
|---|---|---|
| ⑤ 阈值 | **5.123**——第十轮真机夹出窗口 [5.079, 5.134)，`07` 取点。不是按 04 标的，是「色带那两页之下」的卷级实测选的 |
| ⑥ 纸白对齐上限 | **4**——第十轮「对齐后 B 组 4/4 分不出」核过，`tone-alignment/05` 落地 |
| ① 颗粒项地板下界 | **不需要标了**——`01` 算出闭式解（`src/metric.rs` 的 `GRAIN_RATIO`，「下界算得出来，不必标定」） |
| ② 低通项可见度地板 | **没落地**。它要挡的病例是「纸白 253 恒收 2.0 的直流偏置」（Q433）——`tone-alignment` 把 253 对齐到 255，那 2.0 没了，病例从输入端治了 |
| ③ `MASKING_FLOOR` | **仍是 0.5 占位** |
| ④ `MASKING_KNEE` | **仍是 8.0 占位** |

本票要换的是 ③④⑤。⑤ 换了；③④ 04 说用现有材料标不出来（L 组平坦块，逐块活动度几乎一致），
要另造「同一档、块内活动度分层」的材料——那是一张新票的形状，套不进本票的壳。
三轮真机都在占位值上跑、结论全同向，没有证据说占位值错了。要不要造材料标它，记在停车场 **Q623**。

验收第 2、3 条（活动度量法不动、掩蔽只由参照定）仍成立，是约束不是活。第 7 条「ADR 0002 里剩余的过期陈述」
核过：没有过期的数，「仍未标定」那几句仍然是真的。

04 的《背景亮度》那一维**没有被正面回答**——`tone-alignment` 从输入端治了症状，判据本身仍对底色盲。
这一条不归本票，在 Q623 里一并记着。
