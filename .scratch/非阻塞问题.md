# 停车场：实现途中撞见、但没有为它停下来的东西

**状态：** 长期（本轮队列全部票据共用）
**路径：** `.scratch/非阻塞问题.md` — 每一份派活说明都要交出这个路径

---

## 这个文件是什么

落地票据会撞出票面没预料到、或者票面写错了的东西。**其中几乎没有一件该让队列停下来。**
它们落在这里：**记下来、绕过去、接着走、收尾时逐条了结。**

它不是产品的长期缺口清单（那是 `CONTEXT.md` 的《尚未确立》）：

| | 《尚未确立》 | 本文件 |
|---|---|---|
| 装的是 | **产品**本身的缺口 | **执行**中冒出的问题——票面写错、规格没说、两处文档互相打架 |
| 寿命 | 比这一轮队列长 | **收尾时应当清空**——每条要么就地了结、要么升进《尚未确立》、要么另立一张票 |
| 谁读 | 下一个动这个产品的人 | 收尾那一步，以及拍板的人 |

---

## 停线——只有这三种

停下来很贵：拍板的人要把整个上下文重新装回脑子。门槛：

1. **继续做下去会写出错的东西，而撤销它要伸手到这张票之外。**
   典型：票面的指示与上游一条仍然生效的决定相抵触。
2. **动作不可逆，而且不止一个方向说得通。**
   典型：删掉或改名一个被多处引用的东西——一个 id、一个字段、一个文件。
3. **前提根本不在，这张票无从下手。**
   典型：「在 X 之上做 Y」，而 X 不存在。

其余一律：记在这里，走最站得住的那条路，接着做。**包括**票面给的数与你量出来的对不上、
验收条读着含糊、票面没想到的第三种情形、路过发现的无关缺陷，以及你确实拿不准的单项。

> ⚠️ **绕过去要留下痕迹。** 条目记的是**你走了哪条路**，不是「当时有两条路」。
> 说不出自己做了什么的条目不算条目。

> **拍过板的结论要进代码旁边。** 一条条目拍了板、而它约束着某处代码时，落地的人在那一处留一句注释、
> 指着条目号——后来读代码的人撞得到它，不必先知道停车场里有过这一条。

---

## 待处理

<!-- 新条目追加在这里；了结后连同《处置》一起挪进《已了结》 -->

> **一轮 `/settle` 的范围：`Q206–Q994`，动手那一刻在册 407 条，现已清空。Q995 起归下一轮。**
> 其中 107 条要改东西、却没有一张开着的票接得住，经 `/grill-with-docs` 拷问之后 100 条分进四个 effort（`design-parity`、`say-and-stop`、`one-source`、`green-counts`），7 条判不做；每一行指到接住它的那张票。
> 了结的判据是**有主**，不是**做完**：点得出票的条目在这里就是了结了，那张票跑不跑得成是排期的事。
> 反过来读（做完才算了结），最大的那一条就把整批扣在这里，而新的照旧往后排——
> 停车场就是这么长到三位数的。

## 已了结

只留索引——条目原文已挪进对应票据的《停车场结转》，或内容本就在下表所指的位置。

| | 条目 | 去处 |
|---|---|---|
| Q206 | 闸门 1 用的是默认 `target`，不是自己一个目录 | 记下即了：闸门 1 与日常那一路特性组合相同，共用默认 `target` 是白拿的复用，理由在 `docs/agents/gate.md`《为什么各用各的目录》 |
| Q207 | 分目录换来的是盘上三棵树，而这台机器开工时只剩 4.1 GB | 记下即了：分目录之后三棵树合计约 11.4 GB，小于共用时积下的 19 GB，闸门不添清理 |
| Q208 | `xtask` 自己不在任何一条闸门、也不在两遍 clippy 的照射范围里 | `green-counts/01`《停车场结转》 |
| Q240 | 《输出布局》与 ADR 0014 决定第 4 条写着「基准点是点名路径的父目录」，而收编之后基准点是**最外层那个**点名根的父目录 | 按推荐拍板（2026-09-22，`/settle`）：《输出布局》与 ADR 0014 决定第 4 条补「彼此嵌套时以最外层那一条的父目录为准」 |
| Q241 | 收编按**字面路径**认「同一个卷根」：大小写、`./`、软链，三种写法都折不到一起 | `one-source/03`《停车场结转》 |
| Q242 | 本轮撞见的三次假结果：一次假红、一次拼接出来的假红、一次跑了别人产物的假绿 | 随 Q244 了结：支线只写、主仓合流后串行跑闸门，几棵树不再并排跑 cargo |
| Q243 | 收编之后，几个点名路径之间的**卷序**随点名次序而变，而输出树不变 | 记下即了：头一回被发现就占座，输出树、卷数与非卷文件不随点名次序变；理由在 `discover::Found` 的文档 |
| Q244 | 几棵 worktree 在 cargo 眼里是**同一个包**：产物同名可互换，而 cargo 判它新鲜 | 已拍板（落地时）：共用 target 的并行方案作废，改成支线只写、主仓合流后串行跑闸门 |
| Q220 | 那道印记认得出「换了另一份包」，认不出另外两种换法 | `CONTEXT.md`《尚未确立》「《印记》只是必要条件」 |
| Q221 | 票面把这道核对叫「指纹」，而 `CONTEXT.md` 的《指纹》已经是幂等的依据 | `CONTEXT.md`《I/O 与并发》新添《印记 (ArchiveStamp)》 |
| Q222 | 退回串行这件事用户看不见：那句「为什么」眼下只有用例读得到 | `say-and-stop/04`《停车场结转》 |
| Q223 | 本票一条闸门都没跑成：三棵 worktree 共用 target，四趟跑出四个数 | 随 Q244 了结：支线只写、主仓合流后串行跑闸门，几棵树不再并排跑 cargo |
| Q224 | 「被换掉就退回串行」不是无条件成立：换上去那一份解不开时走的是另一支 | 记下即了：字节仍是对的，`reads` 的文档已收窄到「仍然解得开」的那一种；拆成两种错眼下没有要它的理由 |
| Q260 | 命令行按停停下来的那一趟，**报告上说不出它被按停过** | `say-and-stop/02`《停车场结转》 |
| Q261 | 预扫那一段按下去**停不下来**：闩记住了，而第一个检查点在它之后 | `say-and-stop/02`《停车场结转》 |
| Q262 | 「决策点上收尾要让」与升级那张表，跟着也各成了两份 | `say-and-stop/01`《停车场结转》 |
| Q263 | `Ctrl-C` 的 Windows 那一半本机验不到 | `session-redesign/18`《停车场结转》 |
| Q264 | 起真进程那几条用例可能对着**别的线**的二进制送信号 | 随 Q244 了结：支线只写、主仓合流后串行跑闸门，几棵树不再并排跑 cargo |
| Q300 | 「谁住在谁的去处里」按**镜像路径逐字节**认，与撞名那一道用的不是同一把尺子 | `one-source/03`《停车场结转》 |
| Q301 | 本趟成员的去处与一个**借住的卷**的去处撞名时，没有一处查得出来 | `one-source/04`《停车场结转》 |
| Q302 | `CONTEXT.md` 那句「改名成功才算数」在目录卷收窄之后不再字面成立 | 按推荐拍板（2026-09-22，`/settle`）：《会话》那段引文改成「搬到位才算数」 |
| Q303 | 这一趟没成卷的兄弟，它上一趟的产物照旧被当成陈旧产物清掉 | `CONTEXT.md`《尚未确立》「混装目录那一卷的收尾不是事务」 |
| Q304 | 收尾这一步**自己**没走完时，混装目录那一卷的最终位置上是两趟拌在一起的一份 | `CONTEXT.md`《尚未确立》「混装目录那一卷的收尾不是事务」 |
| Q305 | 借住的卷留在这一卷去处里的**过期副本**，从此不再被顺手删掉 | 记下即了：放过别人的过期副本与 `superseded` 那条规矩一致——删不删由用户定 |
| Q280 | 摊开那一段有了步，却没有自己的一段计时：进度上四段，计时上仍是三段加段外 | `say-and-stop/03`《停车场结转》 |
| Q281 | 摊开途中被中止时交出来的是一份**半摊开**的卷，类型上没有一处说得出这件事 | 记下即了：「靠调用方再问一次闩」是整条管线的做法，不是这一处的毛病 |
| Q282 | `.rar` 那条用例数的是步，不是盘上的文件：那三份夹具认不出自己那个临时目录 | 记下即了：摊开落在哪儿不开旋钮（ADR 0015），用例数步是这条边界下站得住的判据 |
| Q283 | 事件流说不出「现在在摊开」：`Stepped` 不带段，而 `Pass` 仍是三个值 | `say-and-stop/03`《停车场结转》 |
| Q284 | `cargo xtask gate` 跑满三条时中途不印任何一条的结果，掐不准也切不动并发度 | `green-counts/01`《停车场结转》 |
| Q285 | 摊开那个检查点的粒度是**一个成员**，一个 200 MB 的单页在两次询问之间仍要等很久 | 记下即了：检查点粒度是一个成员；下沉到字节块要一个块大小的旋钮，ADR 0015 不开 |
| Q286 | `.7z` 那一遍中止之后还要把剩下的块各进一次：外层逐块循环把内层那个 `false` 丢掉了 | 记下即了：`.7z` 中止之后的空转是 `sevenz-rust2` 外层循环的形状，空转认下 |
| Q306 | `score` 多收了一个位深参数，而没有东西逼它与手上那张候选图一致 | `one-source/11`《停车场结转》 |
| Q307 | 地板改了而**参数哈希没变**：这一步单独落地时，旧输出会被幂等静默跳过 | 由 `grain-floor-absolute/03` 了结（判据参数进哈希） |
| Q308 | measurements《颗粒项只在 1bit 上生效》末段说的「仓库用例只覆盖 1bit」已不成立 | 已不在：measurements 里那一句后来的订正收掉了 |
| Q309 | `CONTEXT.md`《尚未确立》里迟滞那一条的括号理由，随这道地板一起不成立了 | 按推荐拍板（2026-09-22，`/settle`）：《尚未确立》那一条整条删掉——默认路径不做迟滞（ADR 0018），括号里的算术随地板比例化不成立，剩下的只是 `--envelope` 这条出口路径上的测试覆盖 |
| Q310 | 「1bit 上逐位不变」这条兼容性底线，没有一条会红的用例**直接**钉着 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q311 | 「2bit 上读数会变」这件事**没有在一手素材上验过**，验的是合成夹具 | 由 `metric-recalibration/04` 之后的几轮一手素材真机覆盖 |
| Q312 | 票面写的比例是 0.2158，实现取的是 0.215_686_27 | 记下即了：票面是历史记录；`metric-recalibration` 那一批已收，地板比例此后另有改动 |
| Q313 | 票面第二条（跨档相关由负转正）没有成立：实测 −0.002（p=0.994） | 由后续了结：05 判 wontfix、阈值由 07 降到 5.123，跨档相关不再是验收口径；spec 那几句是开工前的预期，不是写下时就假 |
| Q314 | 票面第三条（同档不得变差）也没有成立：+0.539（p=0.017）→ +0.405（p=0.086） | 同 Q313 |
| Q315 | 自检没照 README 那条做（没开 baseline 工作树），走的是「1bit 两列是不动的对照」 | 按推荐拍板（2026-09-22，`/settle`）：judgements README《重算》的自检写成两条路，按改动碰不碰得到 1bit 选 |
| Q316 | 03 号票的前提「判据在连续灰调上 0/3 偏 4bit」已经不成立 | 由 `metric-recalibration/03` 了结 |
| Q317 | 05 号票那条验收在 05 跑之前就已经是绿的 | 05 判 wontfix，这一条验收随之无主 |
| Q318 | 《A 类位深实测》那一行是整卷统计，本票只重跑了 19 页里的 5 页 | 由 `two-pass-rework/16` 了结（按今天的默认重跑那八卷） |
| Q319 | 已量化源那一页上判据给的优劣差最小、判读给的最大 | 记下即了：04 已落地，这条线索不再挂着决定 |
| Q320 | 唯一符号不一致的那一页，也是校准锚判得最差的那一页 | 记下即了：04 已落地，这条线索不再挂着决定 |
| Q321 | measurements 里两个小节改了名 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q322 | `gate.md`「落地一张票收尾时按惯例三条都跑」对一行代码都没有的票没留口子 | 按推荐拍板（2026-09-22，`/settle`）：`docs/agents/gate.md` 补例外：`src/`、`tests/`、`xtask/`、`Cargo.toml` 一行都没动的票不必三条都跑 |
| Q323 | 仓库里从此躺着一份别人的源码：`unrar-ng` 打了补丁的一整个包 | 记下即了：vendor 整包 + `[patch.crates-io]`，撤掉的条件写在 `p4-parking-lot/upstream-bug-unrar-ng.md` |
| Q324 | 「这一份是不是另一份的续」这一问摆进了**发现**，而发现原先一个字节都不读 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q325 | 点名一份**续的**分卷（`x.part2.rar`）从此是整趟拒绝，而票面没要求这一条 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q326 | 一个**断掉的**分卷序列在报告里长什么样：那句话认的是哪个码，以及末一份凭空消失 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q327 | `CONTEXT.md` 的《卷》仍写着「一个目录或**一个归档**」，而一个分卷序列是好几个文件 | 按推荐拍板（2026-09-22，`/settle`）：《卷》补一句指路：分卷序列算一个归档 |
| Q328 | 只认 `.partN.rar` 一种分卷打法，`.7z.001` / `.z01` / 老式 `.r00` 一概不认 | 记下即了：格式集归 ADR 0015 决定第 1 条 |
| Q329 | 分卷夹具是**手搓的 RAR5 存储不压包**，真 WinRAR 打出来的分卷包一份都没验过 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q330 | 基线上带着一条 `unused import: VolumeReport` 的告警，不是本票弄出来的 | 已了结：集成分支上收掉了（`ead9574`） |
| Q331 | 票面第 2 条后半句有两种读法，本票取的是「两处印的是同一个东西」，不是「两处印的都是序列名」 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q332 | 决定第 3 条「不逐卷探固实与否」原先给的理由，在 `.rar` 上被本票推翻了 | 记下即了：文字已订正；代价那一半在 `CONTEXT.md`《尚未确立》「`.rar` 多数不固实，而摊开是一刀切」 |
| Q333 | 库出的箭头换成 `⟶`（U+27F6），不是 ASCII `>` | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q334 | 票面点名三处 `Display`，库里带歧义宽度字形的实有九处，本票九处全换 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q335 | 成句那一份（`Interlock`）的破折号 `——` 放开了 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q336 | 那条穷举的用例扫的是**源文件里的字面**，不是渲染出来的字 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q337 | 库里成句的那几条**错误消息**带着歧义宽度字形，而穷举那一关只扫 `impl Display` | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q338 | `⋅` 此刻有两处出处：`render::SEPARATOR` 与库内七处手写 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q339 | `SplitRule` 那个乘号换成 ASCII `x` 之后夹在一个数与一个中文词组之间 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q340 | 摊开的临时目录那条用例认目录靠一个成员名，而系统临时目录是**跨进程公共**的 | 随 Q244 了结：支线只写、主仓合流后串行跑闸门，几棵树不再并排跑 cargo；并排跑时各设各的 `TMPDIR` |
| Q343 | 段式迟滞取的是「短段 + 邻居都更低才压」这一种形态；模块沿用 `hysteresis` 这个名字 | 随段式迟滞退场：ADR 0018 去掉了它，`hysteresis.rs` 已删 |
| Q344 | 迟滞带方向：一页要得更低时谁都不动；而票据与 spec 在这半句上读法不同 | 随段式迟滞退场：ADR 0018 去掉了它，`hysteresis.rs` 已删 |
| Q345 | ADR 0006 决定第 6 条那句「`--per-page` 关闭上包络与迟滞」本票之后不再准确 | 由 `two-pass-rework/11` 了结（ADR 0006 已补修订） |
| Q346 | 门那两组各数各的连续，序列因此被对方的页切开 | 随段式迟滞退场：ADR 0018 去掉了它，`hysteresis.rs` 已删 |
| Q347 | 部分救回页不进段式迟滞的序列 | 随段式迟滞退场：ADR 0018 去掉了它，`hysteresis.rs` 已删 |
| Q348 | 段式迟滞压回的页没进《要紧的页》 | 随段式迟滞退场：ADR 0018 去掉了它，`hysteresis.rs` 已删 |
| Q349 | 回看是双向五页，spec 的 P-D 写的是「这一页 + 它前三页」 | 随段式迟滞退场：ADR 0018 去掉了它，`hysteresis.rs` 已删 |
| Q350 | `hysteresis` 的规则用例落在私有函数上，spec 明写「不测私有函数」 | 随段式迟滞退场：ADR 0018 去掉了它，`hysteresis.rs` 已删 |
| Q351 | 「回看有界」这个界数的是序列上的位置，不是卷内的页序 | 随段式迟滞退场：ADR 0018 去掉了它，`hysteresis.rs` 已删 |
| Q352 | 两条路的迟滞页数共用一个常数，而标定时未必该是同一个数 | 随段式迟滞退场：ADR 0018 去掉了它，`hysteresis.rs` 已删 |
| Q353 | 失败页那一段一进屏，「每一处上色的地方都配了一个字或一个行首记号」按**逐行**判就不成立 | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在 |
| Q354 | 「带失败页的卷两半都喂」没有一处守卫走得遍全仓 | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在 |
| Q363 | 探不出大小写敏感性的那一次，默认按「认」走 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q364 | 记住的那一格是**一个进程一个**，不是一个挂载点一个 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q365 | 探法是「翻一个名字的大小写去问盘」，不是「造一个探针文件」 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q366 | 撞名那条判据仍按 `cfg!(windows)` 折大小写，而补全已经改成运行期探 | `one-source/03`《停车场结转》 |
| Q373 | 名单一导出，当场逮到 bin 自己造的一个歧义宽度字形，本票原地换掉了它 | 已拍板（落地时）：走 ①，票面那一条改写成「列宽与格数一格不变」 |
| Q374 | 措辞那一档里放开了一列不报出自己那一格（耗时） | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q375 | 跨页那一格进了闸门的名单，而夹具在库外造不出它 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q376 | `session::columns` 为这条闸门敞开成 `pub(crate)` | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q377 | 基准档那一列五种说法里有四种不占一格，靠分布那一格才进得了闸门 | 记下即了：目录表哪天不再有分布那一列时 ②③ 非做不可，那时再说 |
| Q378 | 「字面出处」那一维当场补了英文名，而头一个想到的名字是被占掉的 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q383 | 「参照进缓存次数」今天与 `VolumeReport::cache.pages` 恒等，票面没提这一处 | 记下即了：走了的那条路就是推荐，不欠决定；两个数分家提前到 `two-pass-rework/06` 发生，见 Q543 |
| Q384 | 参照那个计数不是原子的，与票面验收条的字面不合 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q385 | 缩放次数按「张」记，一张彩页记一次而不是三次 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q386 | 为绕开 clippy 的参数上限，新造了 `ComputeCounters` 把两个动作器装在一起 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q387 | 给 `VolumeReport` 加一个数，要在十四处夹具字面量上各补一行 | 记下即了（`/grill-with-docs` 2026-09-22 判不做：卷报告夹具的建造器收益小于 36 处字面量的改动） |
| Q393 | 卷级扫描那一趟的「4.7 GB」与逐卷入 MB 加不起来，差着约 0.8 GB | 已答：measurements 给出了解释（那一趟数的是九卷） |
| Q394 | 同一件「卷级扫描表与重跑对调」，`p4-parking-lot/26` 与 `two-pass-rework/08` 两张票各写了一遍 | 记下即了：`two-pass-rework/08` 已由那一批自己收 |
| Q395 | 那张卷级扫描表留着，而票面说的是「两份对不上不能同时留着」；Bowing 那一格今天落哪一档仍答不出 | 由 `two-pass-rework/16` 了结（按今天的默认重跑那八卷） |
| Q396 | Linux 那一行没有清掉，是标明；`docs/measurements.md` 因此仍有一个非实测的数 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q403 | 补全候选跟着屏底一起长高；一层里有几百条时，这一格会长到主区的地板 | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在 |
| Q404 | `nfl/01` 交给 03 的那句预警没有成立：`THE_TABLE_NARROW` 一行都没掉出去 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q405 | `FOOTER_HEIGHT` 现在纯粹是地板，名字却叫 HEIGHT | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在 |
| Q413 | `docs/measurements.md` 的《介质与读取策略》没说那批「归档卷」是哪种格式，而摊开的那种从此不按顺序扫读 | `green-counts/07`《停车场结转》 |
| Q414 | `CONTEXT.md`《I/O 与并发》那段散文按容器形态分岔，本票把它改成按读取端分岔 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q415 | 摊开的临时目录若落在 tmpfs 上，介质必然探成「未知」而退到串行，可那是最快的一种介质 | `CONTEXT.md`《尚未确立》「NAS 的最优 I/O 策略」 |
| Q416 | 幂等那一道从此按临时目录的介质取数：归档在快盘、临时目录在慢盘的机器上，它会从并发 N 掉回 1 | 记下即了：走了的那条路就是推荐，不欠决定；出口是 `--io-mode concurrent` |
| Q423 | 「回看有界」那个 `2·PAGES-1` 的区间在序列头尾够不着，实际界是 `2·PAGES-2` | 随段式迟滞退场：ADR 0018 去掉了它，`hysteresis.rs` 已删 |
| Q424 | 覆盖项裁到只剩一个候选那一档，两组候选集一长一短时碰卷之前答不出来 | `one-source/02`《停车场结转》 |
| Q425 | `CacheUsage` 那三格从「存进来过多少」变成「此刻还装着多少」 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q426 | `CONTEXT.md` 的《缓存》与《第一遍 / 第二遍》按「标注、不改原义」处理 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q427 | 缓存现在收得了字节块，而彩色分支仍没走上去 | 记下即了：ADR 0010 写着那是另一次改动；缓存收字节块这一前提已成立，等有人要它 |
| Q428 | 「窗口只押几页参照」是序列上的话；乱序到达让抢跑那几页的参照也一起押着 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q429 | 「峰值内存显著下降」这条验收给不出数 | `green-counts/06`《停车场结转》 |
| Q430 | 逐页那条路上 `--dry-run` 预告的缓存用量与照做那一趟不再是同一个数 | 由 Q638 了结：试算那一格说清自己是参照的大小，不装准；「上界」那半句已被实测推翻 |
| Q431 | 滚动窗口那三百行落在 `lib.rs` 里，而同量级的东西各占一个模块 | `one-source/01`《停车场结转》 |
| Q432 | 「参照只在有界窗口内存活」这条验收，界在乱序到达下打了折而框仍是满勾 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q433 | 判据把「纸白不是 255」当损伤收了，4bit 那一列因此在这类素材上被钉死 | 已拍板（2026-09-09）：低通项也加一道可见度地板，比例并进 `metric-recalibration/04`——04 已落地 |
| Q434 | 颗粒项用 RMS，把「稀疏而响」读成了「安静」；白底页因此整齐地漏过 | `CONTEXT.md`《尚未确立》「画质分对《背景亮度》是盲的」 |
| Q435 | 停车场在两台机器并行写入下，同一轮丢过两条也撞过两次号 | 已了结：`run-tickets` 给每一份派活预留一段号，撞号与丢条的根子不在了 |
| Q436 | 「中间量当结论」在同一轮里发生了三次，而口径自检拦不住它 | 按推荐拍板（2026-09-22，`/settle`）：`docs/measurements.md` 开头立规矩：画质分的数一律取 `--dry-run` 的输出，复现脚本的自检比判定 |
| Q437 | 「判据掩蔽」那一行到底说什么，票面没给，落地时我自己定的 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q438 | `masking_weight` 改成从 `masking()` 取数，而 `Aggregation` 那三个数照旧直读常数 | 记下即了（`/grill-with-docs` 2026-09-22 判不做：下一张动聚合的票顺手带上） |
| Q439 | 抬头判据形状从两行变三行，会话「这一趟的前提」在 80×16 上只剩一行空白 | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在 |
| Q440 | 派活点名的是两处断言，而跟着抬头走的用例一共三条 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q441 | 「前提那一张装哪几件事」这份清单，抄在五处，没有东西逼它们同步 | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在；前提那一张随新界面搬进了配置视图 |
| Q447 | 近白列第五格今天就不绿：护栏立到 u ≤ 21，u = 42 那一格排在断言之外 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q448 | 这四条护栏与 `04` 要改的地板不相容：`04` 票面那句「护栏仍然全绿」做不到 | 由 `grain-floor-absolute/04` 判 wontfix 了结（地板不改，护栏照旧） |
| Q449 | 测试名取了《背景亮度》，而 `CONTEXT.md` 的词汇表里没有这个词条 | `CONTEXT.md`《量化》新添《背景亮度 (Background brightness)》 |
| Q450 | `tests/perceptual.rs` 与 `tests/metric.rs` 三个助手逐字重复，本票只收了其中一个 | 记下即了（`/grill-with-docs` 2026-09-22 判不做：下一次动那几篇测试的人顺手下沉） |
| Q451 | `docs/measurements.md` 的《背景亮度》那一节没收第八轮的订正，其中一句今天不成立 | 已订正：measurements 那一节已收进第八轮 |
| Q457 | 「判据参数一改哈希就变」那条用例落在 `src/metadata.rs`，票面点的是 `tests/` | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q458 | 票面说黄金快照里的哈希会变，而黄金那一趟按 `--no-metadata` 跑，快照里没有哈希 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q459 | ADR 0002《后果》里被别处引作先例的那一句，本票把它改成了反面 | 记下即了：同 Q482——`tone-alignment` 那一批已收，引作先例的那句结论没错 |
| Q460 | 哈希那三行的行名取自字段名，`04` 换掉 `grain_ratio` 时全库会**再**过期一次 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q461 | 票面要「加一个字段哈希自动跟上」，落地成的是「加了字段编译不过」 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q462 | 造包的配平按 `rank % 2 == 1` 隔对交替，于是「位置偏好已排除」是循环论证 | `green-counts/09`《停车场结转》 |
| Q463 | 「块内极差 ≥ 12」那个代理量分不开两侧，差 0.33 个点 | `green-counts/09`《停车场结转》 |
| Q464 | `tpr/04` 的《落地记录》点名的那条用例，05 把它改名了 | 记下即了：悬空引用以本行为索引，那条用例的今名见 `two-pass-rework/05` 的《落地记录》 |
| Q465 | `CONTEXT.md` 的「彩色分支只缩放并编码」在**试算**那一趟两样都不做 | 按推荐拍板（2026-09-22，`/settle`）：《灰度路径 / 彩色分支》补「试算那一趟两样都不做」 |
| Q466 | 三条老条目的《处置》字段被更早的合并吃掉了，而其中两条的处置今天已过期 | 本轮了结：Q224、Q433、Q434 三条各有处置 |
| Q467 | 界降到 5.123 之后，纯色 96 那个夹具两条路上演示的是同一套机制 | `green-counts/04`《停车场结转》 |
| Q468 | 窗口与仓里钉着的性质一起把取点圈到 0.021，阈值这个旋钮基本上用满了 | 已收：`CONTEXT.md`《尚未确立》「画质门槛在 boox-poke6 以外的面板」写着「可挪的余地只剩 0.021」 |
| Q469 | 画集 Venus 的 `4bit 不抖` 读数中位从 5.011 挪到 5.300，而那一档不该被地板碰到 | `green-counts/05`《停车场结转》 |
| Q470 | 界降下来把 24 页从 `2bit+FS` 抬到 `4bit 不抖`，而那正是真机说有色带的那一档 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q471 | `TONEFIT_SAMPLES` 指向本机 `_samples` 根时，`tests/smoke.rs` 当场红，而且要跑一小时四十分 | `proof-sheet/07`《停车场结转》 |
| Q472 | `Threshold` 的 `Display` 仍只说 boox-poke6，而今天钉住取值的是 kobo-libra-2 与本仓夹具 | `say-and-stop/05`《停车场结转》 |
| Q473 | 黄金快照对「逐页理由」这一维只有一行覆盖，界边判定它拦不住 | `green-counts/03`《停车场结转》 |
| Q477 | 名单与目录那一侧的谓词改口了，成员那一侧的 `is_junk` 名字没跟着改 | `one-source/12`《停车场结转》 |
| Q478 | 名单并成一份，归档**成员名**那一侧跟着扩了四个名字，而票面只说了发现那一侧 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q479 | 反向那半在本机验不到：Windows 上造不出真实的权限拒绝夹具 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q480 | 票面把「绕过发生在发现那一层」写成了要**做**的事，而它开工前就已经成立 | 记下即了：票已落地，票面是历史 |
| Q481 | `lost+found` 与 `.Trash-1000` 在大小写敏感的文件系统上也被大小写不敏感地认 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q482 | 改名的爆炸半径：五份别人票的记录里，十处指着旧名的引用从此悬空 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q487 | 《决策点》那条词条的末句，本票之后只对**读它**的观察者成立 | 按推荐拍板（2026-09-22，`/settle`）：《确认点》补「给要读它的观察者」 |
| Q488 | `Progress` 从「一个方法」变成两个，而它自己的文档写着「一个方法，不是一排」 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q489 | 会话那一侧靠「`so_far` 在不在」认决策点，而那一格从此由观察者自己的答复决定 | 由 `session-redesign` 那一批了结：`Live::pass_started` 按 `pass == Pass::Second` 认确认点 |
| Q490 | 试算那一趟（记录开着）照旧白造来路：指纹在，而读它的第二遍不在 | `one-source/09`《停车场结转》 |
| Q491 | 「命令行答的是不读」这件事，交付之后一条用例都没有 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q492 | 本票收的正是 `Q77` 判过「不值得收」的那一处，而票面与 spec 都没提它 | 已拍板：留下那个方法，`Q77` 那一行已补指路 |
| Q493 | 停车场治不了「后来有人不知道有人发现过」：真相躺在别的 effort 的《停车场结转》里，`src/` 一个字都没有 | 拍板（2026-09-22，`/settle`）：取 ②——停车场抬头立规矩，拍过板的结论在被它约束的那一处代码旁留一句、指着条目号；④ 不做 |
| Q494 | `(相对路径, 第几张, 共几张)` 这三个参数在四处同行旅行，本票把其中一处扩成了四参 | `one-source/10`《停车场结转》 |
| Q495 | 决策点那条用例钉的是「交出去的那一格是空的」，不是「库有没有白拼一份」 | 记下即了（`/grill-with-docs` 2026-09-22 判不做，随 Q387） |
| Q497 | spec 那组实测删掉之后，`09`／`10`／`11` 三张票各有一处还引着它的数 | 记下即了：09／10／11 已各自重测落地 |
| Q498 | 顶替它的那份读数也早于改判据那一批，而且它自己没记日期与代码 | 由 `two-pass-rework/16` 了结（按今天的判据与默认重跑） |
| Q499 | `16` 号票的验收条里没有「卷级扫描」，而 measurements 有两处把它指给 `16` | 由 `two-pass-rework/16` 了结（卷级扫描按今天的默认重跑了） |
| Q500 | 「众数尺寸」那一栏钉不住一卷，而它被当成过「这一趟用的是哪一份语料」的证据 | `green-counts/07`《停车场结转》 |
| Q501 | 整卷入出比那一栏四卷两种口径：三卷是容器字节，改革之獸那一格不是 | 已订正：表上改革之獸那一格已是容器字节 664.7 |
| Q502 | 一趟宽跑只留总数、不留逐卷读数，而没有一处逼它留 | 按推荐拍板（2026-09-22，`/settle`）：`docs/measurements.md` 开头立规矩：宽跑要留逐卷读数、写明落在哪儿 |
| Q503 | 把「后果」写成验收条：勾与不勾都会说谎 | 按推荐拍板（2026-09-22，`/settle`）：`docs/agents/issue-tracker.md`：复选框只放动作，开票的理由写在《不做会怎样》（Q520、Q540 那两句一般形状随它） |
| Q507 | 「屏底那一格不折行」那句假前提，票面收的那一处之外另有四处 | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在 |
| Q508 | 会话那几个模块里另有十三处 `super::<终端那一层的函数>` 同样指不到 | `green-counts/02`《停车场结转》 |
| Q509 | 画法那一层的用例模块里，六处 `super::super::…` 少数了一级 | `green-counts/02`《停车场结转》 |
| Q510 | 票面把那一类写成「十来处」，实际 40 处 | 按推荐拍板（2026-09-22，`/settle`）：`docs/agents/issue-tracker.md`：不写死会移动的个数，写数它的命令，非引不可时钉日期（Q546 并入） |
| Q511 | bin crate 的文档链接一条都没人验，一跑就是 61 条告警 | `green-counts/02`《停车场结转》 |
| Q512 | 「指得到」在私有项上做不到：票面第 2 条与第 4 条互相顶着 | `green-counts/02`《停车场结转》 |
| Q517 | 纸白对齐没有自己那一格《阶段》计时 | `green-counts/08`《停车场结转》 |
| Q518 | 《纸白》这个词，两条已有词条指的是 255，新词条指的是量出来的那个数 | 按推荐拍板（2026-09-22，`/settle`）：《残缺页》「留成纸白」改成「留成纯白（255）」 |
| Q519 | 「平坦区不足」那道门限 5000 是照抄普查脚本的，没有第二处东西说得出它该是多少 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q520 | 「纸白＝最大那块平坦区里的众数」三处同源写着，而实现取的是全页众数（`no-false-line`） | 当场订正：`CONTEXT.md`《纸白》与 measurements 已在先；`tone-alignment/spec.md` 第 3 条与 `survey_flat.py` 的文档本轮改齐，票面那一条下方本来就带 ⚠️ |
| Q521 | 绿着的用例够不到自己的断言：**多重守卫串起来时，上游那道失效不会红** | 按推荐拍板（2026-09-22，`/settle`）：`docs/agents/testing.md`《守卫按反跑一次》；变异测试另开票 |
| Q522 | 「默认 0，产物与今天逐字节相同」只在像素那一侧成立：记录变了，整库要重跑一次 | 票面那一行记下即了（票已落地）；④ 并进 Q528 |
| Q523 | 《纸白对齐上限》没有上界，`--white-align-limit 255` 会把一张暗页整幅推成纯白 | 由 `tone-alignment/02` 了结（Q527） |
| Q527 | 「这一页被钳掉多少级」照做那一趟只说得出上界，准数只在 `--dry-run`（接 Q523） | 记下即了（`/grill-with-docs` 2026-09-22 判不做：最大钳制宽度的上界就是提白上限本身，多一格没有信息量） |
| Q528 | 屏上仍说不出「你的旧输出为什么过期了」（接 Q522） | `say-and-stop/04`《停车场结转》 |
| Q529 | 卷级纸白对齐那一行在会话里一个字都看不到 | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在；提白上限在新界面的设置栏里 |
| Q530 | 票面第 3 条那个 94.6%，在它自己点的那一节里查不到 | `green-counts/07`《停车场结转》 |
| Q531 | `render::volume` 与 `render::pages` 各多了一个参数，而会话两处调用点用不上 | 记下即了：走了的那条路就是推荐，不欠决定；会话那一半随旧界面退场 |
| Q532 | 屏上一句「听起来很有道理的假话」，正着断言防不住；**反着钉**才防得住 | 按推荐拍板（2026-09-22，`/settle`）：`docs/agents/testing.md`《屏上改掉一句假话，留一条反着钉的断言》 |
| Q537 | 顶死的那一趟试算预告的缓存用量与照做那一趟不再相等 | 由 Q638 了结（同 Q430） |
| Q538 | 票面「峰值内存不再随卷长线性涨」这一格，本票范围内打不了勾 | 记下即了：票已落地；峰值那一半由 `two-pass-rework/12` 交付形状，数见 Q429 |
| Q539 | 「续做：参照还在缓存里」那句话在三条路里的两条上是假的，而它早于本票 | 按推荐拍板（2026-09-22，`/settle`）：《接着写出》改成「分析环节留在缓存里的东西还在」 |
| Q540 | 票面那三条来路不是并列的：几何门单独裁不出一个顶死来，而票面把它与面板灰阶数并排摆着 | 记下即了：走了的那条路就是推荐，不欠决定；「列表把不同级的东西拉平」那一般形状随 Q503 一起拍 |
| Q541 | 顶死那一趟报告里那一行缓存用量变小了，而票面只谈「参照进缓存次数」 | 由 Q638 了结：报告那一行说清了试算那一格与照做时缓存里装的不是一样东西 |
| Q542 | 顶死那一趟的量化与编码从第二遍那一段挪进了第一遍那一段 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q543 | Q383 预告的「两个数分家」提前到本票发生，而它的正文写着 12 号票 | 记下即了：订正 Q383 那一句的日子（两个数分家提前到 06） |
| Q544 | 黄金快照里一个「覆盖顶死」的卷都没有，那条路它从来没盖住过 | `green-counts/03`《停车场结转》 |
| Q545 | `pinned_up_front` 那个三态是嵌套 `Option`，而它的正确形状本票刚在下游造出来 | `one-source/01`《停车场结转》 |
| Q546 | 抄来的数比估来的数更危险：估的知道自己在估，抄的以为自己在引 | 并入 Q510 |
| Q547 | 改一个默认值，会让两族用例静默失效：**默认值是一个会移动的锚** | 按推荐拍板（2026-09-22，`/settle`）：`docs/agents/testing.md`《用例点名取值，不借默认值》；③ 那道机器守卫不在这里定 |
| Q548 | 票面把「报告照旧标着未标定」当成既有事实，而报告那一半从没落地过 | `say-and-stop/08`《停车场结转》 |
| Q549 | measurements 里那条「`01` 落地后要用产品重渲比字节」的限定，事情已经做完了 | 当场订正：measurements《对齐后的 B 组：4/4 分不出》那条 ⚠️ 改写成核实结果 |
| Q550 | 黄金快照里一页纸白钳得动的都没有：抬默认值这件事它**一个字节都拦不住**（同族 Q544） | `green-counts/03`《停车场结转》 |
| Q551 | 收成一处的落点：公开**编码**，还是公开**一整个闩类型** | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q552 | Q262 那两样（`answer` 与升级表 `next`）本票**判为不收** | `say-and-stop/01`《停车场结转》 |
| Q553 | `from_code` 是**全函数**，「不认得的数按最强算」从此是对外契约 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q554 | 屏底那个测试助手的「按几次」表，与闩的编码同形同值 | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在 |
| Q561 | 票面写的 dav1d 装法与本机实测对不上：不是 vcpkg，`PKG_CONFIG_PATH` 也根本没设 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q562 | 《数》跨平台不可逐格相比：同一份代码 Linux 916、Windows 915 | `green-counts/01`《停车场结转》 |
| Q563 | 「后来才被取代的原文不动」那条判法只写着**交付记录**，而本票拿它判了 ADR 与 spec | 按推荐拍板（2026-09-22，`/settle`）：`docs/agents/issue-tracker.md`：「不改写」只管《落地记录》，ADR、`docs/`、`CONTEXT.md` 写当前成立的事实 |
| Q565 | 三处 `src/` 注释指着已被订正的票面说法，而本票代码一行不动 | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在 |
| Q566 | `p4-parking-lot/spec.md` 那句「它们各自都曾是真话」，本批逐条判下来不成立 | 当场订正：`p4-parking-lot/spec.md` 那半句改成「未必都曾是真话」 |
| Q571 | 派活说明判「票面前提已不成立」，核下来仍然成立：认错的是一条测试的名字 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q572 | `--brief` 与 `--dry-run` 咬在一起，而它进不了《开关互锁》：互锁那个类型在库里 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q573 | 折叠的默认取「摊开」而不是「折起」：票面两条都留了余地 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q581 | 会话退出时 stdout 上装的是「最近一份做成了的报告」，不是每一趟各一份 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q582 | 拒绝那一趟攒下来的报告一卷都没有时也照印，印出来的是一份只有抬头的报告 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q583 | `why_nothing_is_left` 抖动那一支回的是半截话，而没有一个用户看得到它 | `one-source/09`《停车场结转》 |
| Q584 | 会话退出时印到 stdout 的**报告正文**既不折行、也不把标注换回普通空格 | `design-parity/11`《停车场结转》 |
| Q591 | 三个反面做成第二个 flag，而不是把开关改成取值式 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q592 | `--no-per-page` 是双重否定：它说的其实是「留着卷级上包络」 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q601 | 撞名那条拒绝抬头数的是卷，收口那一句数的是「处」 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q602 | 折起那一副里，进了隔离的是哪几卷仍然点不出来 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q603 | 「一张清单最多列几条」那两个数（五与三）都不是量出来的 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q604 | 「路径一行、原因一行」那副缩进仍有四份 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q605 | 「上面那些就是做出来的」在折起那一副里同样指得不准，还剩两处 | 记下即了：走了的那条路就是推荐，不欠决定；下一张动那两句抬头的票顺手做 ② |
| Q606 | ADR 0016 决定 2 逐字点名的那句措辞，如今住在库里 | 按推荐拍板（2026-09-22，`/settle`）：ADR 0016 决定第 3 条补明写的例外（与互锁同一类；点名的头几页住在 `src/listing.rs`） |
| Q611 | 五处 `src/` 注释指着本票、或指着它收掉的那两条条目，落地之后都成了指着做完的事 | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在 |
| Q612 | `Focus` 那段文档仍写着「六个取值」，而那个枚举有八个变体 | 已不在：`Focus` 随新界面重写，那段文档已换掉 |
| Q613 | 《视口》此刻指着 `Viewport` 那张表当唯一出处，而那张表把报告区三副写成「卷表」一行 | 已不在：`Viewport` 那张表已按新界面重写 |
| Q614 | `yielding.rs` 的《宽度不够》第二步写着「两张表」，而此刻是三张 | 已不在：`yielding.rs` 随新界面重写，那一句已换掉 |
| Q615 | 《砍列》与《视口》改成指路之后，没有一条用例问得出「词汇表里没有第二份」 | `one-source/08`《停车场结转》 |
| Q616 | 《模式》那一节此刻装着两件事，而「质量档」那一件没有词条名 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q617 | 「覆盖项把候选集裁空」有一半发生在开工之前，而词条把它整个算成「末一种」 | 按推荐拍板（2026-09-22，`/settle`）：《拒绝开始》把末一种分成两半写：位深那一维碰卷之前答得出，抖动那一维要撞上那一页 |
| Q618 | `Mode::DryRun` 那句仍写「一个**文件**都不落盘」，而《模式》此刻写的是「一个**输出**都不落盘」 | 当场订正：`src/request.rs` 的 `Mode::DryRun` 改成「一个输出都不落盘」并指到《模式》 |
| Q619 | `verify_bundle.py` 的 `GROUPS` 写死为第十轮那三组，本批另写了一份自检 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q620 | A 组 116 对逐对全排序是不小的判读量，要不要按色带风险面积抽样 | 由 `two-pass-rework/09` 了结 |
| Q621 | 迟滞压回的目标档：09 号票第十一轮触发「`11` 挡回来重新设计」，已在 ADR 0018 定案 | 已定案：ADR 0018，`hysteresis.rs` 已删 |
| Q622 | 色带风险面积那条「两侧零重叠」的分界，在全语料上分不开 | 当场订正：measurements《色带风险面积把五组真机页分成两侧》补一句适用面 |
| Q623 | 掩蔽两个常数仍是占位值，三轮真机没暴露它们错；要不要造材料标它 | 拍板（2026-09-22，`/settle`）：不改口——那两个数照旧印「未标定占位值」，三轮没反对不等于标定过；《背景亮度》那一维进了《尚未确立》 |
| Q624 | story 5「一趟跑得慢时知道慢在哪一步」的出口是 profiling 剖面，产品构建里没有 | `design-parity/10`《停车场结转》 |
| Q625 | 三个遍名换到了屏上与《遍》，词汇表《管线》三条与库里的注释仍叫旧名、仍说「段」 | 由后来的词汇表改齐了结：《环节》现叫查重／分析／写出，《分析环节 / 写出环节》 |
| Q626 | 决策点上 `x` 那句「接着做第二遍（第一遍不重算）」与横条上的「按档写出」是两套词，同一屏并排 | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在 |
| Q627 | `?` 表末尾三遍那一节一趟都没跑过时也在，与「只列此刻这个阶段派得出的键」那条规矩不同调 | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在 |
| Q628 | 30 列那一档上横条那一行「摆得下」读成两头在、词从中间省略；词本身在最窄那一档上看不见 | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在 |
| Q641 | 「续做」那一句要预告一个还没到的阶段上按什么：读表的那一手放进了 `state.rs` | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在 |
| Q642 | 屏上还有三处散文提着键，票面的「六处」没数进去：`Esc 关`、`GONE` 那句、`Esc 回目录表` | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在 |
| Q643 | 「还没跑过」那几句里的措辞与 `says` 那两句逐字相同，票面只让取键 | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在 |
| Q644 | 起一趟的键派不出来时「还没跑过」那几句怎么说：本票选了「不提键」 | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在 |
| Q673 | 票面要总览块「说得出隔离几卷」，而结论行那条既有决定是「隔离算进完成、由出事行说」 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q674 | 翻面之后，答收尾（等于试算）的那几卷也数进「完成」 | `design-parity/04`《停车场结转》 |
| Q633 | 旧预设里的 `per-page` 键报的是 serde 的「不认得的字段」，不像 `--per-page` 那样专门指路 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q634 | 黄金回归改成两路五组，多跑一组 14 卷（≈ +60 s），换的是「`--envelope` 逐字节相同」的证人 | `green-counts/03`《停车场结转》 |
| Q635 | 默认路径上「一组顶死、另一组没有」那一角仍攒整卷参照，等的只是理由那一个词 | `one-source/02`《停车场结转》 |
| Q637 | 报告抬头不说位深走的哪条路，全卷幂等命中那一趟因此说不出自己是 `--envelope` 还是逐页 | `say-and-stop/05`《停车场结转》 |
| Q638 | 默认路径上试算预告的缓存字节数**不是上界**：渐变页上预告 19 KB、照做 346 KB | 本条的 ④ 已落地；② 等要它准的人 |
| Q657 | 语义色搬到特性前面之后住哪儿：新开 `session/tone.rs`，没并进 `state.rs` | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q658 | 屏底那一句「不要紧」那一档配什么记号：穷举逼出来的一格，选了 `-` | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q705 | 覆盖顶死的卷在卷表上写的是分布（`2bit+FS 229`），「覆盖」这个词从那一列上退了 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q706 | story 18 的病在上一级还活着：目录那一行的基准档分布与总览块的判定行在默认路径上写的仍是「逐页 N」 | `say-and-stop/08`《停车场结转》 |
| Q707 | 档位分布那一列从「恒在」变成「最后一个让」：分布比从前的基准档宽出一倍，砍列次序重新想过 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q708 | 列头「档位分布」比「基准档」宽两格，96 列双栏那一档上卷表因此丢了耗时那一列 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q709 | 展开一卷的抬头跟着卷表换成分布之后，`--envelope` 那条路上「基准档」这个词在屏上不再出现 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q710 | `yielding.rs` 那张 40 列快照头上的文档写着卷表的列，屏上画的是目录表 | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在 |
| Q689 | 会话里那一行打出来的字怎么变成 `WhiteAlignLimit`：解析器写在 `state.rs`，没给 `WhiteAlignLimit` 加 `parse` | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q690 | `every_field` 那一格补的是 `Some(OFF)`：往返只钉 0 这一个值，退场的那条盖过 0／2／255 三个 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q691 | 左栏多一行之后三幅写死的版面用例各走了一条路：两幅抬高一行格子，一幅按原高接受多滚一行 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q692 | 左栏上那一行取 0 时只印 `0`，报告那一行说的是「0 级（没开）」：同一个事实两种写法 | 随旧界面退场：`session-redesign/15` 删掉了 `session::draw`，那几处已不在；新界面的设置栏另有写法 |
| Q665 | 页级依据「写不写」的谓词取自 `Settles`，不是 `request.envelope`：`--envelope` 加两维都点名的那一趟照写 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q667 | 页级源哈希连成员名一起喂（与卷级同一条规矩）；只喂字节也说得通 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q668 | `Stage::Hash` 一格记两遍的哈希（卷级 + 页级），剖面上「源哈希」那一行从此含第一遍那一份 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q682 | `VolumeReport::pages` 只列这一趟做了的页，留下的页不进逐页结果，只有一个数 `retained_pages` | `design-parity/10`《停车场结转》 |
| Q683 | Q635 那一角（默认路径、一维覆盖、两组门混着）没有页级依据，按页跳过在那一角不成立：改一页整卷重做 | `one-source/02`《停车场结转》 |
| Q684 | 「跑到一半崩掉之后下一趟从没做完的地方接着走」在**卷内**不成立：头一趟做的卷崩在半路仍整卷重来 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q685 | `volume_steps` 的预告没有减掉留下的页：预扫在幂等那一道之前，不知道哪几页会留下 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q697 | 依据的作用域由 `Settles` 定，不由 `--envelope` 定：默认路径上 Q635 那一角仍算、仍记卷级依据 | `one-source/02`《停车场结转》 |
| Q698 | `--envelope` 加两维都点名的那一趟改按页记依据，那一角的输出少一块 `tonefit:source`，不再与今天逐字节相同 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q699 | 透传文件在页级那条路上的那一份哈希借用了 `PageSource` 这个类型：「页级源哈希」词条本义是「这一张来自的那个源成员」 | `one-source/13`《停车场结转》 |
| Q721 | 四档吞吐目标是 7940HX 上的绝对数，第三批换了机器，抬不抬、绑不绑机器 | 记下即了：走了的那条路就是推荐，不欠决定（已写进《端到端吞吐》的引言） |
| Q722 | 吞吐三批两种计时口径：前两批是卷级计时（报告不印它），第三批是进程墙钟 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q723 | 阈值那条窗口 (5.0, 5.134) 没有在今天的几何上重扫，只跑了内置的 5.123 那一档 | 记下即了（`/grill-with-docs` 2026-09-22 判不做：挂在下一张碰画质分或阈值的票上顺手重扫） |
| Q724 | 《分阶段耗时剖面》那张表仍是 `057d171` 那一批的，本批每一趟的剖面都在手上、没有进文档 | 当场订正：measurements《分阶段耗时剖面》改成「表摄于 `057d171`」并指到第三批 |
| Q726 | 缩放那一列 24 格装不下预缩那一句（41 格），设计稿从尾部截断 | 随 Q725 了结 |
| Q727 | 型号那一项「没说」那一格：实现印 `未挑（跑起来之前必填）`，`CONTEXT.md`《详情栏》举的例子是 `未选择（…）` | 由 `session-redesign/13` 了结：实现改成「未选择」 |
| Q728 | 预设栏的末行与说明：`CONTEXT.md`《预设栏》与设计稿说「把当前设置保存为预设」，实现是「＋ 把当前的设备设置与处理选项存成一份预设」；说明那一段库里没有出处 | 由 `session-redesign/14` 了结：末行是「＋ 把当前设置保存为预设」 |
| Q729 | 画质门槛那一行印成「画质门槛　画质门槛 5.123（在 boox-poke6 上实测，其他屏幕未验证）」：项名与 `Threshold` 的 `Display` 各带一遍那四个字 | 已不成立：新界面的设计快照里没有「画质门槛 画质门槛」那种重字（`/grill-with-docs` 2026-09-22 核过） |
| Q730 | 灰阶档位那一环：设计稿三档（1bit · 2bit · 4bit），实现的环四格（多一格 8bit），词汇表的全集是 {1,2,4,8} | `design-parity/09`《停车场结转》 |
| Q731 | 「需留意的那几样」的词三处各说各的：库的 `Envelope` 说「差异大 N 页」，会话画法说「差异大的页」，设计稿总览说「与其他页差异大 N 页」 | `design-parity/09`《停车场结转》 |
| Q732 | 详情栏长说明里对选项的称呼（「按高度铺满」「逐个读」）与取值那一格印的写法（`height`、`serial`）对不上 | `design-parity/09`《停车场结转》 |
| Q734 | 预设同名覆盖按两下：设计稿没做这一步，那一串期望屏导不出 | 由 Q894 接着（设计稿补上同名覆盖那一串） |
| Q735 | 顶栏右对齐：版本号一变宽（`0.10.0`）整条顶栏挪一格，占位代不进去，全部快照要重导 | 记下即了：版本号变宽那天改设计稿的 `VERSION` 并重导 |
| Q738 | 设计稿里 `parentHint` 对分区底下的目录记的是处理路径的父目录，没人读它；导出自己按树算根 | `design-parity/01`《停车场结转》 |
| Q739 | 「推进模拟 N 秒」那一步 Rust 侧回放不了：设计稿的模拟速率不是库的；靠序列走完那一刻的场景数据摆终态 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q744 | 这一趟结束之后没轮到的那几卷仍是「等待中」：卷状态里没有「被停止拿走了」这一档 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q745 | 开工那一条带的两张表摆在 `Live` 旁边、跑完才进报告：报告上那两张在清点与跑完之间仍是空的 | `one-source/05`《停车场结转》 |
| Q746 | 拒绝开始撞在半路（互锁 ③）时开着的那一卷记成「被立即停止掉」：词汇表那一档说的是按停止那一级 | 当场订正：`src/progress.rs` 的 `Event::VolumeFailed` 那句改成「两种：立即停止，或拒绝开始撞在半路」 |
| Q747 | 开工那一条在 `Live` 上分成两半：`run_started(volumes, steps)` 与 `surveyed(清单, 两张表)` 两个方法 | `one-source/05`《停车场结转》 |
| Q748 | 卷清单是清点那一列的一份拷贝：`Survey::roster` 开工时克隆 N 条路径 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q755 | 真会话里事件上记的时刻量化到帧：开工那一刻、等人那一截的两头都是上一帧的此刻，与逐事件读表差至多一帧 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q756 | 当前卷的耗时今天没有起点：`Walking` 不记开卷那一刻，设计稿里等待确认那一卷的 5.6s 眼下算不出 | 由 `session-redesign/08`／`12` 了结：确认点上那一截照 `deliberated` 减掉，屏上冻着，快照逐格对上 |
| Q765 | 设计稿里卷的去处少了处理路径自己那一级：`~/转好的/_isolated/集英社/海贼王/第07卷`，库的镜像规则是 `…/_isolated/漫画库/集英社/海贼王/第07卷` | `design-parity/01`《停车场结转》 |
| Q766 | 无法访问的地方那句原因里的路径是 `~/` 写法，真跑出来的是绝对路径 | 由 `session-redesign/08` 了结：屏上路径走 `Home::abbreviate` |
| Q767 | `Score::from_value` 从 `#[cfg(test)] pub(crate)` 改成公开：逐页的画质分只有从这里给得进去 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q768 | 没开着的卷补的页：画质分填零、源页高取输出高、需留意的那几页按名字里的序号落位 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q769 | 设计稿的卷状态多一档 `trialed`（确认点上答了不写出的那一卷），库那一侧它收摊成完成 | `design-parity/04`《停车场结转》 |
| Q770 | 时长的写法取整不同：设计稿四舍五入，会话现有的 `overview::spell` 截断——`running` 那一景 178.5s 一边是 `2m59s`、一边是 `2m58s` | 由 `session-redesign/08` 了结：`spell` 改成四舍五入（Q809） |
| Q780 | 滚动条：设计稿在右框线上自己画 `┃` 滑块（`box` 的 `scroll`），《视口》要的是终端库自带的 widget，滑块的长度与位置两边各有一套取整 | 由 Q850 接着：实现不再走 widget |
| Q781 | 设计稿把绿用在勾选框、此刻的视图号与「＋ 添加路径」上、把蓝用在顶栏右端那一块上，《语义色》列的种类色名单里没有这两样 | 拍板（2026-09-22，`/settle`）：取 ②——《语义色》的种类色名单换成指路 `session::shell::paint` 那张表（Q795、Q825 并入） |
| Q782 | `tonefit::is_archive` 从 `pub(crate)` 改成公开：spec《缝》说库那一侧只改开工那一条事件 | 按推荐拍板（2026-09-22，`/settle`）：`session-redesign/spec.md`《缝》补「只读的查询不算开缝」 |
| Q789 | Q775 ① 说 `Viewport::new` 补上那一行余量：补进去旧界面五条用例红（覆盖层与取值栏那几屏往上滚了一行） | 由 `session-redesign/15` 了结：`Viewport::new` 已删，只剩带余量那一手 |
| Q790 | 补全候选的次序两边不同：设计稿按假盘 `FS` 的写法次序列（`漫画库 下载 Comics 转好的`、`棋魂 大友克洋 火之鸟 寄生兽`），实现按名字排——`fresh-o-Tab`、`fresh-o-Tab-Tab` 两串比不了屏 | `design-parity/01`《停车场结转》 |
| Q791 | 设计稿把补全框里每一条不是文件夹的候选都标「压缩包」（`答案.txt  压缩包`），确定时也当压缩包收；实现按扩展名认 | `design-parity/08`《停车场结转》 |
| Q792 | `Tab` 一个都对不上时设计稿 `toast` 一句「这里没有以「…」开头的项」，而输入行占着屏底，那一句根本画不出来 | `design-parity/08`《停车场结转》 |
| Q793 | 覆盖层掀着时按 `g` `t`：设计稿在覆盖层底下换了视图，表上 `gt` 只在没被盖着的块上派 | `design-parity/08`《停车场结转》 |
| Q794 | 输入行右端 `[Tab → 补全]` 按表只在还没开始那一档摆（`Complete` 那一行标 `I`），设计稿凡是不是搜索的输入行都摆它——改一项设置的值、给预设起名也摆，而那几种 `Tab` 什么都不做 | `design-parity/02`《停车场结转》 |
| Q795 | 种类色在 Q781 的名单之外又添三样：全部按键的组名与输入行的提示词（绿）、键的写法（黄）、补全框里的文件夹（蓝） | 并入 Q781 |
| Q796 | 覆盖层掀着时屏底只有 `[j/k → 滚动] [Esc → 关闭]`，末尾没有 `?`：《屏底》说「末尾恒是 `?` 全部按键」 | 按推荐拍板（2026-09-22，`/settle`）：《屏底》补覆盖层那一个例外 |
| Q797 | 设计稿「添加路径」那一景的状态按键到不了：候选摆上了、缓冲没跟着换（`~/Comics/`，按过 `Tab` 该是 `~/Comics/棋魂/`） | `design-parity/01`《停车场结转》 |
| Q798 | 设计稿补全框里 `C-n`／`C-p` 也挪候选，表上只有 `↓`／`↑`（设计稿的 `KEYMAP` 也没列 `C-n`／`C-p`） | `design-parity/08`《停车场结转》 |
| Q799 | 裁白边之后保留一圈边距（`--crop-margin`）：判为不做 | 判为不做（2026-09-17，设计评审），理由在本条原文（git） |
| Q800 | 目标体积预算（`--target-size` / `--target-bytes-per-page`）：判为不做 | 判为不做（2026-09-17，设计评审），理由的唯一出处是 `CONTEXT.md` 的《画质分》 |
| Q801 | 报告里没有产物体积这一行；而 `--envelope` 那条路上预览也报不出 | `say-and-stop/06`《停车场结转》 |
| Q802 | `tonefit sync`（投递到设备）：判为不做 | 判为不做（2026-09-17，设计评审），理由在本条原文（git） |
| Q803 | 装订沟没找到的那 17 页，可能正是《尚未确立》说「一页都没有」的扫描黑边素材 | `CONTEXT.md`《尚未确立》「扫描黑边、倾斜页、不同网点线数与角度」 |
| Q804 | `session::columns` 里会有**两个**砍列次序：旧卷表让页数、树不让 | 由 `session-redesign/15` 了结：`VolumeColumn` 随旧那一副删掉 |
| Q805 | `running-s-s` 那一串：设计稿第二次按 `s` 当场收场，实现是把闩交给线程、线程到页边界才收手 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q806 | `⏎` 在目录行上是**切换**，而按键表里 `l` 与 `⏎` 派的是同一件事 | `design-parity/05`《停车场结转》 |
| Q807 | 清点中那一副的输出目录行上，设计稿仍写着 `[i → 修改]`，而那一档 `i` 派不出去 | `design-parity/01`《停车场结转》 |
| Q808 | 没做成的卷、还在跑的卷与被立即停止掉的卷**没有卷级计时**，屏上那一列只好由会话自己量 | `say-and-stop/07`《停车场结转》 |
| Q809 | `session::draw::overview::spell` 从截断改成四舍五入 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q824 | 夹具里预设文件那条路径是**摆出来的**：设计稿把它写死了，而真文件不能摆进假家目录 | `design-parity/01`《停车场结转》 |
| Q825 | 种类色又添三样：设置栏的组名（品红）、顶栏的进度（青），外加颜色第三档「说明正文」（ANSI 7） | 并入 Q781 |
| Q826 | 会话的**时钟起点**：转轮转到第几格要一个原点，而票面与 spec 都只说了「此刻」 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q827 | Q794 那条在这张票上**不成立冲突**：值那一种输入行的起点是「还没开始」，两边都摆 `Tab` | `design-parity/02`《停车场结转》 |
| Q828 | 「掀着预设栏」与「在哪一栏」在设计稿上是两维，实现把它们并成了焦点一维 | 由 Q892 了结（按 ② 拆成两维） |
| Q829 | 「定的那一下屏底说设置已锁定」在三支里只对两支成立：自由填的那几项只读时一个字都不说 | 记下即了：走了的那条路就是推荐，不欠决定；票已落地，票面那两句是历史 |
| Q864 | 合并 08 与 13 时撞出两份转轮字形表，我在合并里收成了一处 | `one-source/06`《停车场结转》 |
| Q844 | 设计稿的环节横条走**连续时间**（`done` 带小数），实现一页一步：同一条横条差一格 | `design-parity/01`《停车场结转》 |
| Q845 | 说明卡正文：设计稿的折行**不带悬挂缩进**，而屏上《折行》那一条带 | `design-parity/01`《停车场结转》 |
| Q846 | `scene.rs` 的 `views_of` 认光标那两种读错了键名，而两棵树的闸门都绿 | `one-source/08`《停车场结转》 |
| Q847 | `ended-q` 那一串的期望屏是原型自己的 toast，真程序在结束了时 `q` 是退出 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q848 | 设计稿说明卡上 `⏎` 也关卡，spec 只写 `Esc` 关，而没有一张夹具按得到它 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q849 | `render::volume_name` 给归档卷去掉扩展名，**旧界面跟着变** | `design-parity/12`《停车场结转》 |
| Q850 | 滚动条的滑块改成照设计稿自己算，不再走终端库那个 widget——而《视口》与 `viewport.rs` 两处都写着「走那个 widget」 | 按推荐拍板（2026-09-22，`/settle`）：《视口》改成「滑块画多长、画在哪一截只有一处出处（`session::shell::canvas`）」 |
| Q851 | 结束之后 `o`／`i` 把**阶段**退回 `Stage::Fresh`，旧界面那一维跟着退 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q852 | 序列里「推进几秒」那一步：夹具只摆得出**走完那一刻**，摆不出中间态 | 由 Q898 了结：踩得到那条断言的三串已全部落地、全部绿 |
| Q853 | `ended-h-Enter-Enter` 这一串要 `⏎` 在展开着的目录行上收起它，而表上 `l` 与 `⏎` 同义 | `design-parity/05`《停车场结转》 |
| Q865 | Q852 那条断言的前提在这一票变了：推进那一步**不必是最后一步**，要紧的是**一串只推得动一次** | 由 Q898 了结 |
| Q866 | 设计稿的 `C-w` 在搜索那一行上漏了同步「此刻搜的是哪一句」，实现这一头派生因此天然一致 | `design-parity/08`《停车场结转》 |
| Q867 | 「需留意的页各几页」那一份判定从画法那一层（`tui` 后面）搬进了 `Live`（特性外面） | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q868 | 搜索那一行右端只摆两件，而 `C-w` 在它上面照样派得出（Q794 的续） | `design-parity/02`《停车场结转》 |
| Q869 | 卷列表上的 `Esc` 新派了一件事，而设计稿的按键表上没有这一行 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q870 | 隔离目录那个名字在会话这一头写死了第二份，而库那一份是私有的 | `one-source/07`《停车场结转》 |
| Q871 | 每页结果的光标存的是**没收上界**的那个数，收回来那一下摆在 `watch_the_run` 上 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q872 | 「一页都不需留意」那一句里写死着 `a → 全部页`，而切到全部页之后仍是它 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q873 | 逐页那张表的砍列次序从此有两份（Q804 那一笔的另一半） | 由 `session-redesign/15` 了结：旧那一副的砍列次序随它删掉 |
| Q874 | 屏上一块**自己那两句提到键的话**仍是手抄的，不出自按键表（Q190 的续） | `design-parity/03`《停车场结转》 |
| Q875 | 总览「问题」那一行的 `]d` 指路：进了每页结果之后那个键按不动，而那一行照旧写着它 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q876 | 逐页结果只有「屏上开着的那一卷」有整份，而**把一卷开起来**的那几串正是从没开着的那一景起步的 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q877 | 跳过的卷那一句，每页结果这一头写的是**第二份说法** | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q878 | 「一页都不需留意」那一句：设计稿写「需要留意」，词汇表的词是「需留意的页」 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q879 | 「一种要紧法 → 屏上那个词」在新旧两副界面上仍是两份 | 由 `session-redesign/15` 了结 |
| Q890 | 灰阶测试图那一句：设计稿说的是「原型不写文件」，而这一副真写得出文件 | `design-parity/01`《停车场结转》 |
| Q891 | 型号不进预设：设计稿的存与套都跳过它，而《预设》词条说预设装设备设置两组 | `design-parity/07`《停车场结转》 |
| Q892 | Q828 的前提在这一票变了：两维真拆开了，连带 `⇥` 与顶上那一条各多一句 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q893 | 预设栏上还剩两个按不动的键：末行那一件上的 `dd`，与起名那一行右端的 `Tab` | `design-parity/02`《停车场结转》 |
| Q894 | 同名覆盖那两下的那一问是会话自己编的、摆在预设栏里：设计稿根本没有覆盖这件事 | `design-parity/08`《停车场结转》 |
| Q895 | 跑着时 `dd` 照样删得掉盘上那一份预设，而 spec 说「整个视图只读」 | 按推荐拍板（2026-09-22，`/settle`）：`session-redesign/spec.md` 改窄成「三组设置只读」，与 ADR 0017 对齐；行为不动 |
| Q956 | 探针没留在 `tests/` 里，落在 `.scratch/encoder-gate/probe/` | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q957 | 这条链上「元数据读不回来」不报错：`Ok(None)` 与「本来就没有元数据」逐字相同 | `encoder-gate/04`《停车场结转》 |
| Q958 | 「页内元数据载体」这道闸要求的是哪一格，没有一处拆开写过——而 `04` 要据本票处置 ADR 0004 | `encoder-gate/04`《停车场结转》 |
| Q959 | `encode.rs` 那一处写 tEXt 的 `?` 永远不会触发，而真正的拒绝说不出是哪个字段 | 当场订正：`src/metadata.rs` 那句文档注释的「当场」——拒绝要到写文件头那一步；② 不做，今天那条用例挡着 |
| Q960 | `CONTEXT.md` 的《记录》两句都是 PNG 专属的说法，而本票量出那两句各自的来源不同 | `encoder-gate/04`《停车场结转》 |
| Q896 | 确认条第二行那四句话是这一块自己写的，与按键表撞着车（Q874 的续） | `design-parity/03`《停车场结转》 |
| Q897 | 夹具那一头替那条线程收手的**第二段**：答「不写出」之后当场收摊、收场 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q898 | Q865 的回执：两串 `-advance` 的推进都在**末一步**上，Q852 的选项②仍不需要动用 | 本轮了结，连同 Q852、Q865 |
| Q899 | `deciding-x-advance` 总进度那一行的末一位：夹具与期望屏把**同一个数**印成了两副 | `design-parity/01`《停车场结转》 |
| Q900 | 确认条上「与其他页差异大 N」那一截**没有一张快照踩得到** | `design-parity/08`《停车场结转》 |
| Q901 | 「确认点上那一卷在走哪一遍」有两副说法：库那一侧已经是写出，屏上报的是分析走完了 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q902 | 「需留意几页」那个数：设计稿自己的两块各数了一副，词汇表站在其中一边 | `design-parity/08`《停车场结转》 |
| Q903 | 掀着覆盖层答话时，设计稿不说那一句回话，这一副说 | `design-parity/08`《停车场结转》 |
| Q916 | 提出来那一段吃的是整份 `Request`，而样张手上只有「一份处理选项」 | `proof-sheet/02`《停车场结转》 |
| Q917 | 缩放那一笔的账本从参数进来，而票面写着「`counters` 样张一样都没有」 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q918 | 纸白那道 `judge` 认的是 `Mode::DryRun`，而样张既不是预览也不是照做 | `proof-sheet/02`《停车场结转》 |
| Q919 | `gray_bytes` 里盖记录与量化的先后换了，而票面写着「一行行为都不许变」 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q920 | `src/lib.rs` 的 `mod tests` 模块文档只说 N=2 那一件事，而里面早就住着别的 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q921 | `Examined` 没进 `CONTEXT.md` 的词汇表 | `proof-sheet/02`《停车场结转》 |
| Q922 | 票面说「样张要走的那几段今天全在 `gray_page` 前半截与 `gray_bytes` 里」，而前半截还有一截没提出来 | `proof-sheet/02`《停车场结转》 |
| Q923 | 盖一张《记录》要五样东西一路同行，而它们只为那一件事同行 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q936 | 票面说《落格》今天「只是一段文字」，而 `tests/pipeline.rs` 早就有一条问着它的用例 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q937 | **spec 那一句本身是错的**：它把 `truncated_page` 当「坏页」，而那张夹具是**残缺页**、有判定 | `encoder-gate/04`《停车场结转》 |
| Q938 | `8bit` 那一档上《落格》恒成立：它在闸底下，却一句话都问不出来 | `encoder-gate/04`《停车场结转》 |
| Q939 | 这道闸守得住「换掉默认编码器」，守不住「加一条默认走不到的岔路」 | `encoder-gate/04`《停车场结转》 |
| Q940 | `salvages_nothing_page` 的夹具文档说的是 `04` 号票**之前**的形态 | `encoder-gate/04`《停车场结转》 |
| Q961 | 卷列表那一格的「跳过」「没做成」写了第二份，`render::tally_column` 如今只有用例在读 | `one-source/07`《停车场结转》 |
| Q962 | 「摆进列里的字形宽度稳不稳」那一关跟着屏上的表缩小了：裁白边、彩页转灰、跨页、画质分那一串、页数、卷数、统一档位分布不再被问 | `one-source/07`《停车场结转》 |
| Q963 | 「屏上顺口提到一个键的那几句不许手抄」那条文件扫描随旧界面删掉了，新界面没有同形的一条 | `design-parity/03`《停车场结转》 |
| Q964 | 停车场 Q745 那条「报告上那两张表等跑完才进」的理由随旧界面没了，行为还在 | `one-source/05`《停车场结转》 |
| Q965 | 「上了色的地方都配着一个字或记号」那道成屏闸门随旧画法删掉了 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q966 | 词条与 ADR 里还指着旧画法：《逐格比对》点名 `session::draw::design`，ADR 0016 把 `session::draw` 那六张快照算作钉住报告字节的一半 | 按推荐拍板（2026-09-22，`/settle`）：《逐格比对》改指 `session::shell::design`；ADR 0016 第 3 条与《后果》删掉旧画法那两句 |
| Q967 | `←` `→` `⇧⇥` 三个键不再翻译：按键表上它们本来就没有主 | `design-parity/06`《停车场结转》 |
| Q968 | 灰阶测试图那句回话在屏底从行尾截断，截掉的正是文件名 | `design-parity/11`《停车场结转》 |
| Q976 | 单击一行暂停自动滚动时屏底**不说**那一句，而 `CONTEXT.md`《自动滚动》说「屏底提一句」 | 按推荐拍板（2026-09-22，`/settle`）：《自动滚动》改成「按键与滚轮挪的那一下屏底提一句，单击不提」 |
| Q977 | 双击阈值取定数 500 毫秒，不问系统的双击时长 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q978 | 双击等于 `⏎` 放到了每一种点得中的区域上（顶栏视图名除外），设计稿只在卷列表的行上认双击 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q979 | 掀着全部按键那一张、或补全框开着时，滚轮不认；设计稿 `moveBy` 在这两处照滚 | `design-parity/06`《停车场结转》 |
| Q991 | 卡顿的前后数字用替代量法量，真终端 + 触控板那一半等人来补 | 记下即了：真终端那一半已添进 `session-redesign/18` |
| Q992 | 缓存落在「那一趟攒下的东西」上，没有另做按会话状态失效的整帧缓存 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q993 | `Live::report()` 改成当场拼一份整报告交出去，不再借出里面那一份 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q994 | 一批积压里跟在滚动后面的单击，落在屏上那一帧的区域上，不是滚过之后的 | 记下即了：走了的那条路就是推荐，不欠决定 |
| Q764 | 设计稿的假盘没有导出：夹具建的那棵树是 11 个场景的场景数据提到的每一处的并集 | 按推荐拍板 ①（2026-09-15，07 号票的实现者）：并集够用——本票补全到的只有 `~/`（四项都在）与 `~/Comics/火之鸟`（在候选里露过名字、建成了空目录），没有一串序列补全进场景数据没提到的目录；候选的次序另是一条（Q790）。落在 `src/session/scene.rs` 的 `disk` 与 07 号票《停车场结转》 |
| Q774 | 序列 `fresh-q` 的期望屏是原型自己的话：屏底「退出（原型里不会真的退出）」，实现上 `q` 交出退出后不再画下一帧 | 按推荐拍板 ①（2026-09-15）：照旧——`fresh-q` 那一串的用例只断言交出退出、不比屏，那份期望屏留在盘上、清单里照旧有它。设计稿一格没动。落在 `src/session/terminal.rs` 的 `redesign::q_before_the_run_hands_out_the_exit` |
| Q775 | 设计稿的视口是记着的（`S.from` 粘着、上下各留一行余量），`CONTEXT.md`《视口》与 `session::viewport::Viewport` 是从光标算出来的、不留余量——长列表上光标往回挪时两边画出来的起点不同 | 按推荐拍板 ①（2026-09-15）：设计稿的视口改成从光标算、不记着（`viewport(cursor, total, h)`，`S.from` 整个拿掉），格子高过 8 行时留一行余量；实现那一半的余量做成 `Viewport::with_margin`（新界面的格子走它，与设计稿同一个式子），`new` 照旧给旧界面——补进 `new` 会让旧界面五条用例红，见 Q789。重导了 `ended-C-f-C-b`、`running-search-n-N` 两串。落在 `design.html` 的 `viewport`、`src/session/viewport.rs` |
| Q776 | 从中间省略的截法两边不同：设计稿 `elide` 的尾巴预算是定死的 `w-1-left`，`columns::elide` 把头上没用完的那一格还给尾巴 | 按推荐拍板 ①（2026-09-15）：设计稿 `elide` 改成实现 `columns::elide` 的截法（省略号占一格、多出的那一格给头上、头上没用完的还给尾巴）。没有一屏快照带省略，重导一格没变。落在 `design.html` 的 `elide` |
| Q777 | 窗口太小那一屏在哪一档上都写 `[q → 退出]`，而跑着与等待确认时 `q` 不退（《退出会话》） | 按推荐拍板 ①（2026-09-15）：设计稿窗口太小那一屏跑着与等待确认时写 `[C-c → 退出]`，实现按阶段从表上取（`Deed::Quit` 派不出时落到 `Deed::Interrupt`）。重导了 `running.56x14` 与 `running-shrink`。落在 `design.html` 的 `drawTooSmall`、`src/session/shell/small.rs` |
| Q778 | 等待确认时配置视图的屏底摆着 `[v → 每页结果]`，而 `v` 只在任务视图里派得出 | 按推荐拍板 ①（2026-09-15）：设计稿等待确认时配置视图的屏底不摆 `v`（`stageHints` 多问一句 `S.view === 'config'`）。重导了 `deciding-2` 一串。落在 `design.html` 的 `stageHints` |
| Q779 | 连击键的前半截按表待着，设计稿按写死的几个字待着：`d` 在光标不停在处理路径上时一边待一边不待，`]`／`[` 在还没开始时正相反 | 按推荐拍板 ①（2026-09-15）：设计稿连击键的前半截改成与表一致（`startsCombo`：`g` 哪一块都待；`d` 在 `dd` 派得出的块上待——开跑之前的卷列表、预设栏——不问光标那一行；`]`／`[` 只在清点完之后的卷列表上待）。06 认领的序列一格没变。落在 `design.html` 的 `startsCombo` |
| Q783 | 清点中那一景的屏底摆着 `[]d → 下一个问题] [/ → 搜索]`，而设计稿自己的 `KEYMAP` 与实现的按键表都把这两个键标成清点完之后才派得出 | 按推荐拍板 ①（2026-09-15）：设计稿清点中那一景的屏底不摆 `]d`、`/`（连同 `F`），只剩停止、`j/k` 与 `?`。重导了 `survey.120x36`、`survey.80x24`。落在 `design.html` 的 `footerHints` |
| Q711 | 范围层把点名路径叫成「卷」：`＋ 再打一个卷进来`、`[x] 库/卷1` | 按推荐拍板 ①（2026-09-15，06 号票的实现者）：类型名改叫处理路径（`NamedPath`、`scope.paths`、`Field::Path`／`AddPath`），新界面屏上是「处理路径 (N)」与「＋ 添加路径」；旧界面那一行的文案随那一副在 15 号票退场，旧快照一格没动。落在 `src/session/state.rs`、`src/session/shell/list.rs` 与 `.scratch/session-redesign/issues/06-skeleton-and-the-before-run-list.md`《停车场结转》 |
| Q737 | 逐格比对不比背景色：设计稿表达不了它，实现真设了背景色比对看不见 | 按推荐拍板 ①（2026-09-15，06 号票的实现者）：新画法的每一条快照与序列用例另断言整屏每一格的背景都是 `Color::Reset`（`assert_no_background`）。落在 `src/session/shell.rs` 与 `src/session/terminal.rs` 的 `redesign` 用例、06 号票《停车场结转》 |
| Q736 | 场景数据的逐页结果只给屏上开着的那一卷整份，其余各卷只给灰阶分布与要紧的那几页 | 按推荐拍板 ①（2026-09-15，05 号票的实现者）：夹具照灰阶分布补齐不要紧的页、要紧的按名字里的序号落位，设计稿的伪随机页不在 Rust 侧复刻；补的页长什么样见 Q768。落在 `src/session/scene.rs` 的 `pages_of` 与 `.scratch/session-redesign/issues/05-scene-fixture.md`《停车场结转》 |
| Q754 | `Live::new` 仍读一次系统时钟作头一个「此刻」：此后每帧由会话层给，用例给定值 | 按推荐拍板 ①（2026-09-15，05 号票的实现者）：夹具从 `live::fixture::live_for(epoch, &request, resumes)` 起（`live_at` 的兄弟，收场景数据拼出的那份 `Request`），造与头一次给合成一步。落在 `src/session/live.rs` 的 `fixture` 与 05 号票《停车场结转》 |
| Q725 | 每页结果的画质分那一列：设计稿只印判成那一档的分，库那一格是六个候选的整串 | 按推荐拍板 ①（2026-09-14）：库在逐页那一行加一格「判成那一档的画质分」（`Field::VerdictScore`，措辞仍在 `src/render.rs` 一处，命令行那一副不印它、报告字节不变），设计稿那一列照它。落在 `src/render.rs` 与 `.scratch/session-redesign/issues/02-export-design-snapshots.md`《停车场结转》 |
| Q733 | 设计稿假数据里页的目标高 1680 与型号 kobo-clara-2e 的面板高 1448 对不上 | 按推荐拍板 ①（2026-09-14）：型号改成 kobo-libra-2（1264x1680，黄金快照那一块面板），页那一列的数一个不动，顶栏、设备配置与灰阶测试图那几句跟着变。落在 `.scratch/session-redesign/design.html`（`CONFIG` 的型号、`TARGET_H`、`panelSize`）与 02 号票《停车场结转》 |
| Q718 | 设计稿里出自库与报告的字与库的原话不同（如「读不出，用空白页占位」对报告的「读不出 ⋅ 用空白页占位」，跳过的卷那一句整句不同），快照逐格比不上 | 按推荐拍板（2026-09-14）：测试夹具复刻设计稿的假数据（卷树、灰阶分布、标记、页数），凡出自库与报告的字以库原话为准；导出快照之前先把设计稿里这些字改成原话再导出，与 Q714 同一条。落在 `.scratch/session-redesign/spec.md`《Testing Decisions》，由导出快照那张票执行 |
| Q719 | 清点完就要画出整棵树，而开工那一条事件只带卷数与总步数；卷清单、非漫画文件、无法访问的地方要到收场才进报告 | 按推荐拍板（2026-09-14）：开工那一条事件追加清点的三份产出（卷清单连同各卷的卷根、步数与源页数——源页数清点数成员时本来就数出来，只是没留——、非漫画文件、无法访问的地方），库里不新算东西，会话仍只站在 `run` 之上。落在 `spec.md`《Implementation Decisions》；`CONTEXT.md`《事件》随落地那张票改 |
| Q720 | 清点中总览写「已找到几卷」，而清点途中库一条事件都不报 | 按推荐拍板（2026-09-14）：清点中只说正在清点、不报卷数，库与事件契约不动。落在 `design.html`（「清点中」场景的总览）与 `CONTEXT.md`《总览》 |
| Q715 | 设计稿的「全部按键」恒列整张 `KEYMAP`，框上却写「只列当前可用的」 | 按推荐拍板 ①（2026-09-14）：全部按键按阶段只列此刻派得出的键，一个键都不剩的组整组不出；同一个键在不同阶段做的事不同就分行各写（已结束时 `o`／`i` 是返回路径列表）。落在 `design.html` 的 `KEYMAP`（每一行带上派得出它的阶段）与 `helpColumns`；`CONTEXT.md`《覆盖层》本来就是这个说法 |
| Q716 | 设计稿里打字时掀不开「全部按键」；词汇表与现行实现是 `F1` | 按推荐拍板 ①（2026-09-14）：打字时 `F1` 掀开全部按键，关掉之后输入行与缓冲原样回来。落在 `design.html` 的 `KEYMAP`「输入」一组与 `onKey` 打字那一支；按键序列归 spec 的测试决定；`CONTEXT.md`《覆盖层》本来就是这个说法 |
| Q717 | 设计稿「全部按键」里 `q` 只写「退出」：跑着与等待确认时 `q` 不退 | 按推荐拍板 ②（2026-09-14）：还没开始与已结束时说「退出」，清点、转换与等待确认时说「不退出：先按 s 停止，或按 C-c」。落在 `design.html` 的 `KEYMAP`「全局」一组 |
| Q712 | 设计稿的卷行没有代表页那一列：`--envelope` 那一路没画 | 按推荐拍板 ①（2026-09-14）：`--envelope` 那一趟卷行多一列代表页，砍列时排在耗时之后；差异大的页也只在那一趟出现。落在 `.scratch/session-redesign/design.html`（「整卷统一灰阶」场景）与 `CONTEXT.md`《目录行 / 卷行》 |
| Q713 | 设计稿里没做成的卷进得去每页结果，屏上说「这一卷还在分析」 | 按推荐拍板 ①（2026-09-14）：展得开的只有收摊了的卷（含跳过）与确认点上那一卷，其余按下去屏底说为什么。落在 `design.html`、`CONTEXT.md`《停得住 / 展得开》《每页结果》与 ADR 0019 决定第 3 条 |
| Q714 | 「画质判定参数」的说明是新写的一套字，不出自报告抬头那一处措辞 | 按推荐拍板 ①（2026-09-14）：行内那一句取自 `render::header`，详情栏的长说明是界面层自己的解释；印的是此刻的设置。落在 `design.html`、`CONTEXT.md`《画质判定参数》与 ADR 0019 决定第 7 条 |
| Q681 | 留下的页靠「读回 → 改写卷级那一项 → 照写页写进新容器」搬过来，sink 层没有长出「部分保留」的新形态 | `two-pass-rework/15`《停车场结转》 |
| Q686 | 重做的页页级源哈希算了两遍：幂等那一道一遍（拿去比），第一遍再一遍（写进记录） | `two-pass-rework/15`《停车场结转》 |
| Q666 | 失败页的占位页不写页级源哈希；14 号票因此天然跳不过它，不必按 `failed` 特判 | `two-pass-rework/14`《停车场结转》 |
| Q669 | `PageRecord::page_source` 生产路径上没有读者，挂着 `cfg_attr(not(test), expect(dead_code))`，14 号票接上读者时要拆掉 | `two-pass-rework/14`《停车场结转》 |
| Q636 | `src/session/draw/` 两处注释仍写 `--per-page`，本票按票面没进那个目录 | `two-pass-rework/02`《停车场结转》 |
| Q670 | `tests/stop.rs` 的 `two_ctrl_c_…` 对时序敏感：默认路径第二遍成了纯写出之后，两个「相」之间只剩毫秒级空当 | 协调人就地了结：`spawn` 带 `--envelope`，理由在 `tests/stop.rs` 模块文档《为什么开着 `--envelope`》；条目原文在修它的那条提交说明里（`git log --grep Q670`） |
| Q567 | 派活说明要的 `.tmp/` 不在 `.gitignore` 里 | `two-pass-rework/09`《停车场结转》 |
| Q1 | A 类双页扫描的页间字号跳动是 0.390，为票面基线（0.112）的三倍半 | 并入 Q2 |
| Q2 | A 类双页扫描上的字号跳动比票面据以定案的那个数大 3.5 倍 | page-geometry/04 |
| Q3 | 裁边带进来三个未标定的数，`CONTEXT.md` 的《尚未确立》还没有收它们 | CONTEXT.md《尚未确立》 |
| Q4 | 彩色分支上的裁边要多走一遍全页 OKLab 换算，代价没量过 | CONTEXT.md《尚未确立》 |
| Q5 | `fixtures::gradient` 在默认路径上会被裁掉下面 21.6%，而拿它当 | page-geometry/09 |
| Q6 | 迟滞升档在默认那条路上一处覆盖都没有了 | page-geometry/08 |
| Q7 | 以高为准让目标尺寸没有了上界，极端瘦长的源页会把输出缓冲撑到分配不下 | page-geometry/07 |
| Q8 | `docs/measurements.md`《真机像素完整性》那句「输出恒等于 fit- | page-geometry/06 |
| Q9 | `tests/` 里大批夹具仍拿 `SMALLER_THAN_TARGET`（800×1 | page-geometry/09 |
| Q10 | measurements《A 类位深实测》那四个「哪一档更优」的页数是旧判据下数的 | page-geometry/06 |
| Q11 | `CONTEXT.md` 的词汇表还没有「源页 / 输出页」这一对 | CONTEXT.md《处理对象》 |
| Q12 | `p0-hardening/13` 正文那句「都不要把分阶段计时塞进 `Report`」 | p0-hardening/13 票面 |
| Q13 | `[dev-dependencies]` 里的 `encoding_rs` 与 `zip | page-geometry/09 |
| Q14 | `CONTEXT.md`《I/O 与并发》说读取层与计算层的并发度「两个数不相干」，代码 | CONTEXT.md《I/O 与并发》 |
| Q15 | `tests/isolation.rs` 的 `a_color_page_a_gray_ | page-geometry/09 |
| Q16 | `render::calibration_note` 里那句「大写英文，中文字模装不进一 | calibration-chart/01 |
| Q17 | `calibration-chart/01` 的验收里没有「中文判读说明」这一条 | calibration-chart/01 |
| Q18 | 仓库根没有 LICENSE，也没有一份第三方许可声明。** `calibration-c | LICENSE + THIRD-PARTY-NOTICES.md + licenses/OFL-1.1.txt |
| Q19 | ADR 0007《要认的代价》那条「tonefit 只产出文件，无从感知，也无法在输出时 | page-geometry/06 |
| Q20 | 幂等仍要在碰像素之前预告出全部输出成员名，而 `page-geometry/04` 的  | page-geometry/04 |
| Q21 | 「以高为准下几何门恒成立」现在有了一个例外，而那句话写在 ADR 0007 与 `pag | page-geometry/06 |
| Q22 | 兜底上界的折算系数（取 16，照代码数出来的是 9）不是量出来的；而两侧的上界都只管一页 | CONTEXT.md《尚未确立》 |
| Q23 | 本票往 `CONTEXT.md` 词汇表加了两行，而 Q3/Q11 记着「改 CONTE | CLAUDE.md《改 CONTEXT.md 的规矩》 |
| Q24 | `crop::MIN_KEPT` 那个 0.5 仍未标定，而本票往它的文档上又压了 35 | CONTEXT.md《尚未确立》（并入 Q3） |
| Q25 | 默认路径上的迟滞覆盖只走得到抖动那一维，位深那一维仍然只在 fit-inside 上有 | CONTEXT.md《尚未确立》 |
| Q26 | 进度预告在拆分开着时是上界，普通卷的进度条因此停在四分之三处 | CONTEXT.md《尚未确立》 |
| Q27 | 记录里加了来路那一项之后，一条按**字节**比两张页的用例失效了 | page-geometry/09 |
| Q28 | 装订沟的命中与误报只有计数，没有一页是目视核对过的 | CONTEXT.md《尚未确立》 |
| Q29 | 幂等的依据多了一项，而 ADR 0006 仍写着「四项」 | page-geometry/06 |
| Q30 | `src/lib.rs` 一个文件为四件事被改了 627 行，幂等那一组该独立成模块 | CONTEXT.md《尚未确立》 |
| Q31 | 本票往 `CONTEXT.md`《尚未确立》加了两条，而那一节的收口归 06 | page-geometry/06 |
| Q39 | `run` 返回 `Err` 时不发「这一趟完了」，异常收场没有自己的那一条事件 | p1-session/05 |
| Q46 | 中止掉的那一卷只有开卷那一条事件，`run` 的返回值也说不出这一趟是被中止的 | p1-session/05 |
| Q41 | 观察者花掉的时间算进了 `VolumeTiming`，而 `06` 要在决策点上等人 | p1-session/06 |
| Q63 | 退出会话时那条还在写盘的线程要有人收，而按停那两个键还没有 | p1-session/10 |
| Q69 | 跑起来之后连范围层一起冻住了，spec 只要求前两层只读 | p1-session/10 |
| Q64 | 报告区长过一格就只留最后那几行，翻不回去 | p3-session-legibility/10《停车场结转》（卷表有了自己的光标与跟随，光标一挪跟随就停、视口跟着它走）与 11（展开那一副也接上同一份视口：光标停在第几页上，滚动量不再是记着的） |
| Q71 | 按停的状态只在屏底那两行说，主区两条进度条上看不出来 | p1-session/11 |
| Q72 | 一趟跑着的时候展不开，而报告那时正在长 | p3-session-legibility/10《停车场结转》（`Mode` 拆成两维，ADR 0017；展开的索引改成认得出决策点上那一卷的取值）；逐页那一半由 11 落地（跑着与等答话时展开的都是一张逐页表，报告边长它边更新） |
| Q58 | 会话里「没说」与「说了一个恰好等于默认值的值」是两个状态，命令行上它们没有 | p1-session/12 |
| Q56 | `calibrate` 子命令读不到预设，而标定图挂在**设备层**上 | p1-session/13 |
| Q52 | 决策点那一条事件不带这一卷的报告，而会话要在那里把报告画出来 | p1-session/14 |
| Q53 | 单卷在决策点上答收尾，这一趟的收场仍是「走到头」 | p1-session/14 |
| Q77 | 卷报告每卷拼两次，而命令行那一趟只用得上第二次 | p0-hardening/13《停车场结转》（剖面量得动它，不值得收）。**已由 `two-pass-rework/07` 覆盖**，理由见该票《落地记录》与 `Q492` |
| Q35 | `CONTEXT.md`《尚未确立》那条「A 类素材的一切数值」按字面已经不成立 | page-geometry/06 → CONTEXT.md《尚未确立》 |
| Q48 | 卷内成员名撞到同一个输出上，现在走的是卷级失败，而《失败》的词条把成因列成了那四种 | p1-session/05 → CONTEXT.md《失败》 |
| Q54 | 《会话》的**设备层**词条把内容列成「型号 + 灰阶数覆盖」，而阈值也在那一层 | p1-session/07 → CONTEXT.md《会话》 |
| Q57 | 《会话》的**口味层**词条列着六项，而口味层现在有十一项 | p1-session/07 → CONTEXT.md《会话》 |
| Q61 | `tui` 关掉之后，会话那二十几条用例一条都不跑 | p1-session/08《停车场结转》（p2-loose-ends/01 把状态机搬出了特性） |
| Q68 | `tui` 关掉之后，`09` 新添的那三十几条用例同样一条都不跑 | p1-session/09《停车场结转》（同上一刀） |
| Q79 | `profiling` 特性不在 `cargo test` 的闸门里，clippy 也扫不到它 | p0-hardening/13《停车场结转》（闸门变成三条，计数那一半摘出两个纯函数） |
| Q40 | 「不得在持锁处调观察者」这条性质没有真正的闸门，靠人看加一条弱用例 | p1-session/02《停车场结转》（哨兵落地，持锁报到当场恐慌） |
| Q36 | 兜底上界那道线，仓库里三处给出三个倍数：24:1、12:1、16:1 | page-geometry/06《停车场结转》（算法收进 `MAX_TARGET_PIXELS`，24:1 是系数取 8 那份草稿的残留、已删） |
| Q50 | 卷根整个消失时走的是**页级**失败 → 隔离（退出码 2），不是卷级失败 | p2-loose-ends/05《停车场结转》（判据改成「卷根还在不在」，一页都没成仍走隔离） |
| Q51 | 输出根写不进去时每一卷各记一笔卷级失败，退出码是 `3` 而不是拒绝执行那个 `1` | p2-loose-ends/06《停车场结转》（开工前探一次，写不进去整趟拒掉；`Sink::create` 逐卷那条路留着） |
| Q47 | 幂等那一道被中止时，`volume_fingerprint` 回的是一份**喂了一半**的哈希 | p2-loose-ends/11《停车场结转》（并行不改变这条性质，判定不另立两支的返回类型） |
| Q78 | 归档卷的幂等这一道是三段里第二大的一块，全是把源字节再串行解压一遍 | p2-loose-ends/11《停车场结转》（本票收的就是它：那一段 1758→288 毫秒） |
| Q85 | 报到时手里仍攥着 rayon 工作线程与读取层的在途字节额度 | p2-loose-ends/11《停车场结转》（判定为**不改**，并行之后这条理由更强） |
| Q42 | 点名的每一个归档卷，整趟都占着一个打开的文件句柄 | p2-loose-ends/12《停车场结转》（`volume-discovery/01` 已消掉这笔代价，本票判定**不划句柄上限**） |
| Q96 | 一卷同时开着的归档句柄从 1 涨到 `1 + min(幂等那一道的并发度, 成员数)` | p2-loose-ends/12《停车场结转》（数一格没变，收进 `source::Reader`《一趟同时开着几个句柄》） |
| Q98 | 句柄开不出来时并发度悄悄降下来，而报告照旧印计划里那个数 | p2-loose-ends/12《停车场结转》（判定**不改**：它等的那个前提「有了句柄上限」不会出现） |
| Q33 | 互锁 ③ 的判定在代码里有两处形态，靠一句 `debug_assert!` 拴着 | p2-loose-ends/04《停车场结转》（走第三条路：两维各判各的，抖动那一维的判定就是互锁 ③；按页分岔判定**不做** → Q102） |
| Q32 | 中文的帮助与报告在终端上不折行，本票新加的那几行尤其长 | p2-loose-ends/07《停车场结转》（界面层出了一套按显示宽度折行的工具，帮助、报告、屏底三处共用） |
| Q65 | 折行之后有几行是「画一遍数出来」的，ratatui 那个直接答的函数挂着 unstable 的门 | p2-loose-ends/07《停车场结转》（自答：折出来的那几行就是答案，那道 unstable 的门不开） |
| Q75 | 设备层上多了一个键，屏底那一行在窄终端上就把 `q 退出` 挤出了屏 | p2-loose-ends/07《停车场结转》（屏底那一行折得开，`FOOTER_HEIGHT` 仍是三行的下限） |
| Q83 | 会话那一层胶水（`press` 与它的十条用例）仍在 `tui` 后面，卡在一处折行量算上 | p2-loose-ends/07《停车场结转》（原文那句「画一遍数出来」记错了：`opens_at` 不碰终端，那一层搬得动） |
| Q59 | 逐层补全按前缀**大小写敏感**地筛，而 Windows 的文件系统不是 | p2-loose-ends/10《停车场结转》（走「按平台分支」那一条：一个 `cfg!(windows)` 常量，不折大小写比整串 → macOS 那一档另立 Q107） |
| Q62 | 在会话里改了阈值，屏上却写着「命令行指定」 | p2-loose-ends/10《停车场结转》（那句话改成「点名指定」，不提入口；来源仍只有两种） |
| Q73 | 覆盖一份同名预设要把整份文件重排，手写的注释与排版留不下来 | p2-loose-ends/09《停车场结转》（换了写入端：按节剪、按节接，验不过退回重排；判定**不引** `toml_edit`） |
| Q74 | 会话里存得进一份预设，却删不掉一份 | p2-loose-ends/09《停车场结转》（`d` 在停着一份预设时删它，按两下，光标一挪那一问就作废） |
| Q110 | 《处理对象》的「源」「输出容器」两条词条还按「CBZ」写，输出那半句已经不全 | volume-discovery/03《停车场结转》（「源」改成读取与输出同形、卷边界不同形；「输出容器」点一句扩展名归一并指向《输出》） |
| Q140 | 型号停在内置表外的一个名字上时，按 `←` 会转不回来（死循环） | p3-session-legibility/06《停车场结转》（`back` 加第二个出口：走满一圈没经过出发点就落回环上，型号那一环上正是「没挑」） |
| Q153 | 「隔离 / 跳过 / 没做成」那几个字与四种记号分在两处，而语义色要按同一批分类上色 | p3-session-legibility/09《停车场结转》（`Mark` 多一个 `tone()`：记号与语义出自同一个取值，措辞那一层一个颜色都没长） |
| Q147 | 总览块的抬头在窄终端上由终端库硬截，截出来的时长读起来像一个完整而偏小的估计 | p3-session-legibility/13《停车场结转》（抬头摆不下时从中间省略，`yielding::elided` 一处定完，边框上那五个抬头与钉在格子里的那一行都走它） |
| Q154 | 卷表的四种记号里三种是东亚歧义宽度，而对齐现在靠宽度吃饭 | p3-session-legibility/13《停车场结转》（量下来只有一种是：`–`→`-`、`—`→`-`、`…`→`⋯`，并立了「表这一层造的字形一个都不许是歧义宽度」这条用例；出自 `render` 的两格另立 Q168） |
| Q60 | 屏上逐格读回来的文字，每个宽字符后面多一个空格 | volume-discovery/08《停车场结转》（改成按显示宽度跳格：`probe::read` 一处读完，`screen`／`painted`／`reversed_row` 三条路共用，`contains("设备层")` 从此问得出来；`tight` 留着只管空白） |
| Q127 | 票面的验收第六条说「等答话时按得动的仍只有答话那两个键」，而同一张票面第三条要加第三个键 | volume-discovery/07《停车场结转》 |
| Q129 | 拆开之后有一条用例跨着两块，把 `Prompt` 与它两个字段的可见性顶开了 | p3-session-legibility/01《停车场结转》 |
| Q130 | 按「屏上那几块」切开之后，仍有两样东西跨着两块 | p3-session-legibility/08《停车场结转》 |
| Q131 | 「拼装的规矩住在命令行那一头」落在哪个文件：选了留在 `render` 里，另立一副 | p3-session-legibility/02《停车场结转》 |
| Q132 | 票面把黄金快照当成本票唯一的验收，可 `tests/golden.rs` 根本不走 `src/render.rs` | p3-session-legibility/03《停车场结转》 |
| Q133 | 卷级失败的卷没有自己的一种「行」，而卷表要给它一行 | p3-session-legibility/08《停车场结转》 |
| Q134 | 「离群」的复合词票面没说换不换：离群判据、离群侧、离群检验、离群那一组 | p3-session-legibility/03《停车场结转》 |
| Q135 | 票面说《判据》有「三个词条」要改名，可那一节里只有两条带名字的；「主体」从来没有自己的词条 | p3-session-legibility/03《停车场结转》 |
| Q91 | 「哪几种算拒绝执行」在实现文档里抄了五份，加一种就要五处一起改 | p4-parking-lot/01《停车场结转》 |
| Q104 | 左栏与预设那一栏仍走终端库自己的折行，屏上因此有两套折行规矩 | p4-parking-lot/02《停车场结转》 |
| Q136 | 左栏折行走的是终端库那一套，而视口数的是逻辑行；窄终端上两个数对不上 | p4-parking-lot/02《停车场结转》 |
| Q138 | 票面第一条要 `→` 也摊开，而同一张票的《环那一套保留》要 `←→` 就地转一格 | p4-parking-lot/03《停车场结转》 |
| Q139 | 「摊开的第一格恒是「默认（xxx）」」与位深、抖动那两行印的「自动（判据说了算）」对不上 | p4-parking-lot/03《停车场结转》 |
| Q142 | 底下只有一个型号的那几块面板，票面没说下钻照走还是直接定 | p4-parking-lot/03《停车场结转》 |
| Q143 | 下钻进去那一层没有「没说」那一格，而《取值栏》说第一格恒是它 | p4-parking-lot/03《停车场结转》 |
| Q144 | 面板那一格上 `⏎` 派的是下钻，而票面写的是「`⏎` 定」 | p4-parking-lot/03《停车场结转》 |
| Q103 | 折行不做禁则处理：`）`、`，`、`。` 这几个收尾记号会落在行首 | p4-parking-lot/04《停车场结转》 |
| Q106 | 带空格的记号（`--fit height`）仍会断在中间那个空格上 | p4-parking-lot/04《停车场结转》 |
| Q114 | 缩进后面跟着一长串断不开的字（长路径）时，折行先折出一个空行 | p4-parking-lot/04《停车场结转》 |
| Q105 | 命令行那两处折到一个定死的 100 格，不问终端真有多宽 | p4-parking-lot/05《停车场结转》 |
| Q168 | 表上还有两格靠宽度对齐、而字形出自 `render`：尺寸那一格的 `×`、判据那一串的 `·` | p4-parking-lot/05《停车场结转》 |
| Q160 | 等答话时左栏不压暗、抬头也不改口，而那一刻三层同样只读 | p4-parking-lot/06《停车场结转》 |
| Q161 | 展开着且等答话时 `a` 归答话，「列全部页」那一下按不动 | p4-parking-lot/06《停车场结转》 |
| Q164 | 覆盖层掀着时按停与答话那三个键按不动，而 ADR 0017 决定第 4 条说它们「在焦点落在哪一块上都按得动」 | p4-parking-lot/06《停车场结转》 |
| Q165 | 「屏底末尾恒是 `? 全部键`」在打字的那两块上摆不出来 | p4-parking-lot/07《停车场结转》 |
| Q166 | 屏底那一行的键是手抄的，`?` 那张表的键是问出来的：同一个键有两处措辞 | p4-parking-lot/07《停车场结转》 |
| Q167 | `?` 那张表滤得掉「这个阶段不派」的键，滤不掉「派得出、而 press 那一层挡着」的键 | p4-parking-lot/07《停车场结转》 |
| Q180 | 屏底那两行印的是「`⏎`／`→` 定」，而取值栏那两层上三个键同义 | p4-parking-lot/07《停车场结转》 |
| Q189 | 覆盖层掀着时 `?` 那张表上照旧摆着 `q 退出会话`，而那一下按不动 | p4-parking-lot/07《停车场结转》 |
| Q146 | 试算那一副的出事行里没有失败页与卷级失败，而试算同样出得了这两种 | p4-parking-lot/08《停车场结转》 |
| Q148 | 出事行的失败页数的是收摊了的卷，比同一屏的报告区晚一整卷 | p4-parking-lot/08《停车场结转》 |
| Q149 | 决策点上答出「继续」的那一瞬间，钉住的这一块换内容、并可能矮一行 | p4-parking-lot/08《停车场结转》 |
| Q169 | 80×24 那一档主区只有 30 列，两条横条连同步数一起被切在半路 | p4-parking-lot/08《停车场结转》 |
| Q155 | 报告末尾那几小结整段一种色：六小结分属三档语义，而它是一段拼出来的文字 | p4-parking-lot/09《停车场结转》 |
| Q156 | 「过期副本」在四种语义里一档都不占 | p4-parking-lot/09《停车场结转》 |
| Q157 | 屏底那一句没有轻重之分：跑不起来的那一句与「存下了」那一句同色 | p4-parking-lot/09《停车场结转》 |
| Q159 | 卷表上的光标跳过没做成的那几卷，而它们在表上占着一行 | p4-parking-lot/10《停车场结转》 |
| Q162 | 逐页表把裁边、缩放、跨页、彩页转灰、去处五格留在了表外 | p4-parking-lot/10《停车场结转》 |
| Q117 | 发现出来的一个目录读不动时，至今一声不吭 | p4-parking-lot/11《停车场结转》 |
| Q84 | 关掉 `tui` 那一趟，整个 `session` 上放开了 `dead_code` | p4-parking-lot/24《停车场结转》 |
| Q95 | 跑过 `cargo test --no-default-features` 之后再跑 `cargo test`，`session` 那条会假红 | p4-parking-lot/24《停车场结转》 |
| Q111 | 点名两个互相嵌套的路径，同一个卷会被发现两遍、各写一份输出 | p4-parking-lot/15《停车场结转》 |
| Q99 | 成员表出自开卷那个句柄，字节出自重开的那个，两者之间归档被换掉就会静默读错 | p4-parking-lot/12《停车场结转》 |
| Q38 | 两级停是 `run` 的能力，而命令行这一路还没有按的地方 | p4-parking-lot/18《停车场结转》 |
| Q113 | 混装目录的输出去处同时是它兄弟卷的父目录，目录卷收尾时会把它们整个删掉 | p4-parking-lot/16《停车场结转》 |
| Q121 | 摊开一整卷的中途没有检查点，几百兆的 `.7z` 上按中止要等它解完 | p4-parking-lot/13《停车场结转》 |
| Q34 | A 类那两张体积表没有跟着新默认重跑，重跑的只有「哪一档更优」那一组 | two-pass-rework/16《停车场结转》（26 号票只标了施测条件，重跑在 16 号票） |
| Q37 | `fixtures::TINY`（160×224）在默认那条路上也不便宜，而拿它当「一张便宜的页」的调用还有十六处 | 记下。判据已写在 `cheap_page`／`SMALLER_THAN_TARGET`／`gradient` 三处文档里，下一张动 `tests/` 夹具的票照它判那十六处 |
| Q43 | 「逐条列出」只覆盖预扫抓到的坏路径，路径不在与扩展名不对仍是一次报一条 | **就地了结。** `volume-discovery/03` 之后两类坏路径已合流——`discover::of` 失败进 `survey::Survey::of` 的 `refused`，收齐了再报；本轮读代码复核过 |
| Q44 | 全局条的「结清」每个观察者都得自己做一遍，库不给 | 记下。19 号票只收闩的编码；全局条的结清做不做进库，那一票已判为不动 |
| Q45 | 「列前几条，剩下的说个数」这个形状在仓库里已经有三处，各写各的 | `p4-parking-lot/27`《停车场结转》 |
| Q49 | 「列前几条，剩下的说个数」现在是第四处，而 Q45 记的还是三处 | `p4-parking-lot/27`《停车场结转》（并入 Q45） |
| Q55 | 三个布尔开关的覆盖是**单向**的：预设关得掉，命令行开不回来 | `p4-parking-lot/20` |
| Q66 | 最后那一趟没做成，先前那一趟试算的报告也就印不到 stdout 了 | `p4-parking-lot/21` |
| Q67 | 口味层每一项的默认值原本只写在 `Cli` 上，会话拼不出同一份 `Request` | `p4-parking-lot/20` |
| Q70 | 闩的编码在两个 crate 里各有一份，靠两条用例分别拴着 | `p4-parking-lot/19` |
| Q76 | `Event::PassStarted` 多了一格，而三条里只有一条用得上它 | 记下。事件不加格、决策点不单开事件——19 号票写死了本批只动一处对外形状 |
| Q80 | 票面说的 `fast` 模式在实现里并不存在 | `p4-parking-lot/25` |
| Q81 | spec 那句「解码一步就会吃光单页预算」被剖面证否，但没有改它 | `p4-parking-lot/25`——**判为原文不动**（写下时按当时那个吞吐目标成立，是后来被剖面取代的；承担它的是 `p0-hardening/13` 的记录与 measurements 的《分阶段耗时剖面》） |
| Q82 | 闸门是三条命令，而仓库里没有一处**自动**跑它们 | 记下。24 号票收「一条命令跑满三条」；**谁来按下去（CI）是另一件事** |
| Q86 | 哨兵「发布构建上整个不在」那一半，三条闸门一条都够不着 | 记下。「发布构建上整个不在」那一半要先引一套基准，本轮不引 |
| Q87 | 折算系数还是 8 那会儿的两处残留：07 号票的报告样例印 67108864，已了结索引 Q22 写「系数 8」 | `p4-parking-lot/25` |
| Q88 | spec 把「卷根整个消失」的判据写成「一页都没成」，而票面要的是把这两种分开 | `p4-parking-lot/25` |
| Q89 | 《失败》的**拒绝执行**词条把成因列成五种、并写着「前四种发生在开工之前」，而现在是六种／前五种 | `p4-parking-lot/28`《停车场结转》 |
| Q90 | 那道探测**不看模式**：试算也真往盘上写一次探针，而《模式》说 dry-run 一个文件都不落盘 | `p4-parking-lot/28`《停车场结转》 |
| Q92 | 「预扫之后才出的卷级失败」那个窗口挪到了**这一卷重开之后**：一头收窄，一头加宽 | 记下。窗口两头各钉一条用例，走法写在 `volume-discovery/01` 的落地记录里 |
| Q93 | 开工那条事件报的步数出自预扫那一遍，报告里的成员数出自重开那一遍 | CONTEXT.md《尚未确立》——`p4-parking-lot/12` 已落地，其记录明写那道印记**盖的是 Q99 那半个窗口、Q93 这半个一格没盖住**，按 29 号票定的判法升进 |
| Q94 | 测试用的那份最小 `Request` 在 `src/` 里仍抄着两份 | 记下。三份变两份；剩下那一份等下一张动 `src/metadata.rs` 的票顺手收 |
| Q97 | 本票这一批实测整体比剖面那一批快一到三成，而快的有一部分与代码无关 | two-pass-rework/16《停车场结转》（归因了结；「抬不抬」那一半转成 Q721） |
| Q100 | 那笔句柄账住在 `source::Reader` 上，而它跨着四个模块 | 记下。那笔句柄账住在 `source::Reader` 上，四处引它的小节名、不复述 |
| Q101 | `docs/measurements.md` 里多了一行不是本机量出来的数 | **就地了结**（`p4-parking-lot/26`）。那一行改标成「不是实测，是平台默认值」，判定仍用得上它 |
| Q102 | 互锁 ③ 那条拒绝仍然一次把两条出路都说了，按页分岔挪得动、但那是改这句话说什么 | `p4-parking-lot/21` |
| Q107 | 认不认大小写按 `cfg!(windows)` 判，而 macOS 默认那套文件系统也不认 | `p4-parking-lot/23` |
| Q108 | 剪不动的那几种 TOML 写法仍旧丢注释：那一路退回整份重排 | CONTEXT.md《尚未确立》——点号键与内联表那几种写法保不住用户手写的注释 |
| Q109 | 输出归档带着写出那一刻的时钟，整个文件的字节因此两趟之间就不同 | CONTEXT.md《尚未确立》——输出归档不可复现 |
| Q112 | 撞名那一道挪到预扫之后，点名一个不存在的路径时输出根探针会先动一次盘 | 记下。自答过：白走一棵树 vs 拒绝之前多动一次盘，取后者，理由写在 `run` 里 |
| Q115 | 「列前几条，剩下的说个数」现在是第五处，而 Q49 记的还是四处 | `p4-parking-lot/27`《停车场结转》（并入 Q45） |
| Q116 | spec 自己对不上：《卷至少有一页》把「空目录」算进非卷文件那一栏，《非卷文件》却把那一栏定成三类**文件** | `p4-parking-lot/25` |
| Q118 | 闸门 2 的 `the_minutes_spent_deciding_are_charged_to_nobody` 五趟里红三趟，`overall()` 那两次读表之间被抢走就红 | **已了结**（`b5603d7`）——`overall()` 取一个 `now`，那个窗口按构造消失 |
| Q119 | 摊开的卷两遍那一路仍按「归档卷是一条顺序扫」派，可它的字节已经在一个目录里了 | `p4-parking-lot/14` |
| Q120 | 摊开的临时目录靠析构收，进程被硬杀就留成孤儿，而缓存的溢写不会 | CONTEXT.md《尚未确立》——摊开的临时目录靠析构收，硬杀留孤儿 |
| Q122 | `.rar` 的夹具是造好签进仓的二进制，页的生成器一改就要有 WinRAR 的人重造一遍 | 记下。三份 `.rar` 二进制夹具长住仓库，共 14 KB，对不上会当场红、不会静默过 |
| Q123 | 收下 `.rar` 之后构建 tonefit 多了一个 C++ 工具链的前提，仓库里没有一处说得出这件事 | `p4-parking-lot/25` |
| Q124 | 分卷 `.rar`（`x.part1.rar` / `x.part2.rar`）会被当成两个卷，而 UnRAR 打开头一份就跨卷读完了 | `p4-parking-lot/17` |
| Q125 | ADR 0015 说「`.rar` 与 `.7z` 两者默认都是固实归档」，`.rar` 那一半不成立 | `p4-parking-lot/25`（那两句订正）+ CONTEXT.md《尚未确立》（一刀切摊开白付的那次全量写盘） |
| Q126 | ADR 0012 决定第 1 条那句「续做只在处理范围是单卷时成立」与改过的决定第 3 条对着说 | `p4-parking-lot/25` |
| Q128 | 画法里那几处 `super::press` / `super::expand` 指的其实是 `terminal::press` / `terminal::expand` | no-false-line/08《停车场结转》 |
| Q137 | 票面要「补全候选也画得出滚动条」，可 spec 的表说那一处没有光标 | `p4-parking-lot/25` |
| Q141 | 票面的「八块面板」「二到五个型号」与 spec 的「31 个内置型号」，三个数都与内置表对不上 | `p4-parking-lot/25` |
| Q145 | 票面说「顶上从三个框九行变成一个框五行」，而屏上只有两个框六行 | `p4-parking-lot/25` |
| Q150 | 《会话》那句「会话那两条横条边跑边画的『已用』与『剩』」已经不准 | `p4-parking-lot/28`《停车场结转》 |
| Q151 | 没做成的那几卷排在收摊了的那几卷后面，不是按跑完的先后 | CONTEXT.md《尚未确立》——两列卷之间的先后无从复原 |
| Q152 | 《视口》那条列的三处里没有报告区，而卷表这一票把它接了进去 | `p4-parking-lot/28`《停车场结转》（并入 Q163） |
| Q158 | `NO_COLOR` 那一张「快照」落成了两次现画对比，不是一张录下来的常量 | 记下。现画两次的那一比是恒等式，比抄两份常量强；快照矩阵别的格不在本轮 |
| Q163 | `CONTEXT.md` 的《展开》《折行》《砍列》《视口》四条都还只认卷表那一副 | `p4-parking-lot/28`《停车场结转》 |
| Q170 | 《卷表》《下钻》《行》三条在报告区多出一级之后各有一句对不上 | `p4-parking-lot/28`《停车场结转》 |
| Q171 | 命令行那一侧的「折叠」落地成一行目录行，底下两级一行没藏 | `p4-parking-lot/22` |
| Q172 | 票面写着「闸门数一格不变、本票不增减用例」，而验收第三条要一条问得出「只有一处」的用例 | 记下。走的是 spec 那条（用例），闸门数 753→754 / 646→647；派活说明里那两个数是交付前的历史记录 |
| Q173 | 收拢之后，唯一还说「输出根写不进去是拒绝执行」的那一处，指着一份不含它的单子 | `p4-parking-lot/28`《停车场结转》——那一票落地那一刻这条自动消失 |
| Q174 | 库内 `Refusal` 上还点着单子上的一项，本票判它是引用而不是第二份抄件 | 记下。引用一个**术语**正是 `CLAUDE.md`《文档写作》第 5 条要的样子 |
| Q175 | `CONTEXT.md` 里那句「实现文档一律指到这个词条」是文档写作的规矩，不是领域术语 | **拍板：留在《失败》末尾。** 那句话要拦的是「下一个改这个词条的人不知道有五处指着它」，而那个人读的是词条；`the_refusal_list_lives_in_one_place` 也守在这里。搬走等于把守卫挪出被守的地方 |
| Q176 | 票面说「四档现存快照照旧通过、不必重录」，实际重录了两张 | `p4-parking-lot/25` |
| Q177 | 预设栏的模块文档写着「屏底那一格不折行」，而屏底那一格折行 | no-false-line/08《停车场结转》 |
| Q178 | `src/session/draw.rs` 的模块文档三处指着 `[`room`]`，而那个模块叫 `yielding` | no-false-line/08《停车场结转》 |
| Q179 | spec 的《取值栏》说它带着「下钻到第几层」，而实现记的是「下钻进了哪一块面板」 | `p4-parking-lot/25`——**判为原文不动**（那一句写下时不自相矛盾，是落地时换了更好的形状；承担它的是 `p3-session-legibility/05` 与 `06` 两份记录） |
| Q181 | 票面第五条要「黄金快照跟着重录」，而 `tests/golden.rs` 一个符号都够不着折行 | 记下。那一票自己已把第五条改写成真正钉着折行输出的三处 |
| Q182 | 「这个空格不许断」那层标注，clap 的文档注释那一头写不进去 | 记下。理由写在 `HARD_SPACE` 的文档里；`--preset 漫画` 只在长帮助里露面，90 格上没断过 |
| Q183 | 库出的那几句话里从此带着 `\u{a0}`，而库对折行一无所知 | **拍板：提成库的公共 API** → `p4-parking-lot/30`（19 号票自己写着这一条不在它上面：它与「库自己造的字形宽度要稳」是同一件事的两半；本批对外契约扩大**两处**，另一处是 19 的闩） |
| Q184 | 票面第五条要「黄金快照重录一次」，而 `tests/golden.rs` 一个字形都够不着 | 记下（同 Q132、Q181，第三次）。`tests/golden.rs` 链的是 lib、自己拼快照行，够不着界面层 |
| Q185 | ADR 0016 决定第 2 条那句示例里的分隔记号还写着 `·` | `p4-parking-lot/25`——**判为原文不动**（写下时 `SEPARATOR` 就是 `·`，是 `p4-parking-lot/05` 后来换的；那一条说的道理一个字没变，字形的出处是 `src/render.rs` 的 `SEPARATOR`） |
| Q186 | 窄终端上 clap 自己排的那几行照旧过宽，折行够不着它们 | CONTEXT.md《尚未确立》——命令行上有四行不归自己排（clap 的用法行与三条英文） |
| Q187 | 命令行印出去的还有几个歧义宽度的字形，本票只换了摆进列里的那两个 | **库那一半** → `p4-parking-lot/30`（库内九处 `Display` 全换，一条穷举的用例钉着）；**界面层那一半** → `no-false-line`（屏上没有一句假话） |
| Q188 | 「哪几格摆成列」在两处各写一份，没有东西逼它们同步 | `no-false-line`（屏上没有一句假话）——`LINED_UP` 与三个 `Column` 枚举各写一份，新添的两列漏了） |
| Q190 | 「还没跑过」那两句里的 `t`／`x` 仍是手抄的，不出自按键表 | `no-false-line`（屏上没有一句假话）——「还没跑过」那两句的键手抄，不出自按键表） |
| Q191 | 屏底那一行变长之后，说明那一行在 80 列上只剩得下前一半 | **工**（本轮先判成分岔，判错了：屏底那个模块的文档早写着让位次序，代码与它对着干，不是没人拍过板的事）→ `no-false-line`（屏上没有一句假话）。落地按 `CONTEXT.md`《让位》：先长高，长到顶才整句让位 |
| Q192 | 屏底那一行现在摆得出 `Esc 退出`，而那是最容易手滑的一个键 | **拍板：照旧，屏上如实说。** 那两个阶段上后果与 `q` 逐字相同；摘掉 `Esc` 反而给「摆出来的键都按得动」加一个例外 |
| Q193 | 20 列 6 行那一档上按键那一行折出四行，末尾那一条被屏高裁在外面 | 记下。20×6 那一档钉的是**不恐慌、不错位**，不是读得下去；`q 退出` 照旧在屏上 |
| Q194 | 80×24 上横条让完仍摆不下，正文只好从中间省略：「步数与已用整条在屏上」做不到 | **拍板：认下省略**（窄档没有横条、正文从中间省略）。改口那一半有主：`p4-parking-lot/25`——改一条交付过的验收正是它的地界 |
| Q195 | `every_kind_of_volume` 那份夹具有一卷带失败页，却一条 `PageFailed` 都没报过 | `no-false-line`（屏上没有一句假话）——`every_kind_of_volume` 只喂了报告那一半，没喂 `PageFailed`） |
| Q196 | 续做那一趟的总览块从此不给「完成 · 跳过」，也不给「隔离几卷」 | **拍板：收场之后翻成执行那一副。** 08 号票要挡的是决策点上答继续的那一帧，收场之后没有人在按键 → `no-false-line`（屏上没有一句假话） |
| Q197 | 隔离那一小结一句里数着两档：「隔离」是注意，「失败页」是出事 | 记下。给「注意」，三条理由写在 `tail_row` 的文档上 |
| Q198 | 屏底那一句挂的是三档 `NoticeKind`，而语义色是四档 `Tone`：两个类型说同一件事 | **拍板：`Tone` 搬到 `tui` 特性前面**，两个类型收成一个 → `no-false-line`（屏上没有一句假话） |
| Q199 | 逐页表跨页那一格进了表，却只有「哪一格落进哪一列」那一级的用例 | 记下。19 号票写死本批只动一处对外形状，不为一份夹具开 `Split`／`Half` |
| Q200 | 跟随跳过没做成的那几卷，而一卷都没收摊时它反而停在其中一条上 | 记下。跟随只认收摊了的那几卷；要改先得有 Q151 那个序号（已进《尚未确立》） |
| Q201 | 发现往下走时，一个**问不出形态**的目录项照旧无声消失 | CONTEXT.md《尚未确立》——问不出形态的目录项无声消失 |
| Q202 | ADR 0014 决定第 5 条只列了**归档**那一支，如今那里有三支 | `p4-parking-lot/25`——**判为原文不动**（那两支写下时都成立、今天也逐字成立，第三支是 `p4-parking-lot/11` 后来添的；**给一条决定添一支是改决定，不是订正说法**，而 25 号票写死了不改决定。第三支的出处是 `CONTEXT.md` 的《走不进去的地方》与 `Report::unreachable_places`；要不要把它写进决定第 5 条，仍归拍板的人） |
| Q203 | 永远读不动的**系统目录**不在 `JUNK_DIRECTORIES` 里，点名一个盘根从此恒是 `3` | `no-false-line`（屏上没有一句假话）——永远读不动的系统目录不在 `JUNK_DIRECTORIES`，点名盘根恒收 `3`） |
| Q204 | 末尾三小结（非卷文件 · 卷级失败 · 走不进去）是同一副骨架抄了三遍 | `p4-parking-lot/27`《停车场结转》 |
| Q205 | 段式迟滞压掉的「孤立偏离页」可能是真信号，不是噪声 | `two-pass-rework/09`——盲测排在 `metric-recalibration/01` 之后，三条账要重测 |
| Q564 | `CONTEXT.md` 的《模式》仍把 `fast`／`best` 当两档，而实现只有默认那一档 | `p4-parking-lot/28`《停车场结转》 |
| Q574 | 折起那一副里，隔离那一小结那句「原因逐条列在上面」指着一段不在场的正文 | `p4-parking-lot/27`《停车场结转》 |
| Q649 | 口味层有了纸白对齐上限，会话那一侧仍按十一项：`every_field` 那一格故意留白，`Session::request` 恒取默认 | `tone-alignment/04`《停车场结转》 |
| Q650 | `CONTEXT.md` 的《口味层》词条列了十一项，没有纸白对齐上限；《纸白对齐上限》词条自己却说它属于口味层 | `tone-alignment/04`《停车场结转》 |
