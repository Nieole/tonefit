## 待处理

- **`CONTEXT.md` 的词汇表还没有「源页 / 输出页」这一对。**
  `page-geometry/03` 落地后管线内部与 `VolumeReport` 都按这条分界线说话
  （`VolumeReport::source_pages` 与 `page_count`），而《处理对象》里只有「页 (Page)」一条。
  同一段里还有一句要跟着看：《进度》写的「第二遍写全部成员」现在只对输出那一侧成立。
  不当场处理：`page-geometry/06` 本来就要往词汇表里加装订沟、连续跨页、适配方式、阅读方向，
  这一对跟着一起收口；改 `CONTEXT.md` 的措辞是一次领域决定，不该由一张预重构票顺手加。

- **`p0-hardening/13` 正文那句「都不要把分阶段计时塞进 `Report`」现在与实现对不上。**
  11 号票落地时所有者当场改口，卷级计时按管线的三段（幂等这一道 / 第一遍 / 第二遍）分开进了
  `VolumeReport`。那不是 13 号与 11 号票面禁的「解码/缩放/判据/编码」——理由见 11 号票的
  《落地记录》——但 13 号的执行者读到那句仍会撞上。同一段里还有一句要跟着看：
  13 号第一条验收「指明前三大开销」这批字段答不了，它仍要走事件流或 feature-gated 插桩。
  不当场处理：改一张 `ready-for-agent` 票的正文与验收是改它的范围，该由所有者拍板，
  不该由它的上游票顺手改。

- **`[dev-dependencies]` 里的 `encoding_rs` 与 `zip` 是冗余的两行。**
  它们原本由一句「集成测试是独立的 crate，拿不到本包的普通依赖」作解释，而那句话不成立：
  `[dependencies]` 里的非可选项对 `tests/` 同样可见——09 号票的 `tests/concurrency.rs`
  现在就直接用着只列在 `[dependencies]` 里的 `num_cpus`。假话已在 09 号票里当场订正
  （`Cargo.toml` 的注释改成了实情），留下的是**要不要把这两行删掉**。
  两处版本与特性都一样，删掉对解析没有影响；`image` 那一处不在此列，
  dev 侧要的是 `avif` 编码特性、与运行时的 `avif-native` 不是一回事，重列有实义。
  不当场处理：动依赖表要单独跑一遍构建与全套测试，而 09 号票的范围是三处用例的断言形式。

- **`CONTEXT.md`《I/O 与并发》说读取层与计算层的并发度「两个数不相干」，代码里它们相等。**
  `IoPlan::decide` 在点名并发、以及自动探到固态盘时都取 `cores()`，
  而 `src/lib.rs` 的 `cores()` 文档给的正是一条相干的理由：「rayon 的默认线程池就是照这个数建的，
  读取层无谓比它派得更多——读得再快，也没有更多的核去消化」。
  这处分歧本来就在（09 号票没有改动任何策略），只是该票把这个等号钉进了 `tests/concurrency.rs`
  的断言里，于是那句话更显眼了。glossary 那句大概想说的是「谁定的」两条路各走各的
  （读取层看介质决定要不要并发），但落到**数**上它们是同一个。
  不当场处理：`CONTEXT.md` 是领域词汇的权威出处，改它的措辞是一次领域决定，
  该由所有者拍板，不该由一张改断言形式的票顺手改。

- **`tests/isolation.rs` 的 `a_color_page_a_gray_page_and_a_failed_page_keep_their_own_pixels`
  名不副实。** 名字承诺「三条路径上的页各自保住自己的像素」，实际断言只有三句：
  五页都写出来了、缓存里两页、以及 003 与 005 **这两张同一夹具**写出来逐字节相同。
  五页同尺寸，其余三页的像素一个都没比。08 号票那个变异（写出时每页都写第一页的字节）
  套上去它照样绿——它真正钉住的是缓存序号错格，那件事名字里一个字都没提。
  这处是 08 号票的票面据以立论的那一条（「目录卷那一侧有哨兵」），落地时发现指错了地方，
  已在该票《落地记录》里订正。不当场处理：这是 09 号票那一类「改用例名字或补断言」的活，
  而目录容器整体并不缺覆盖（同一个变异加在目录那一侧，现有用例红 8 条），
  它不挡任何人，也不该由一张补归档哨兵的票顺手改掉别处的用例名。

## 已处理

- **`render::calibration_note` 里那句「大写英文，中文字模装不进一张位图」会在
  `calibration-chart/01` 落地当天变成假话。** 那张票的验收要求判读说明含中文，
  图内一旦印上中文，终端上这句就在骗人。不当场处理：C01 是重画这张图的票，
  这句文案该跟着它一起改；加固批 12 号票只搬落盘，不碰界面文案。

  **处置**：`calibration-chart/01` 落地时一并改掉。中文字模确实装进了一张位图
  （摘自 GNU Unifont 的 190 个字形），那半句因此不只是措辞过时，前提本身也不成立了。
  改后那两行说的是：关掉缩放、适配屏幕与白边裁切；判读说明中英两份都印在图内，
  先看像素完整性再数灰阶。顺带把笼统的「缩放」拆出**白边裁切**单独点名——
  实测糊的来源正是它（measurements 的《真机像素完整性》），而它在阅读器里通常另占一个开关。

- **`calibration-chart/01` 的验收里没有「中文判读说明」这一条。**
  p0-hardening/10 把 `p0-core-pipeline/14` 的那条标准改成「含**英文**判读说明」，
  并在该票 Comments 里把中文那一份指向 `calibration-chart/01 — 标定图一并回答「阅读器有没有原样贴像素」`
  （那张票本来就要重画这张图）。但那张票的《要做到的》与《验收》两节都没收这一项，
  这个指向眼下是悬空的。不当场处理：给一张 `ready-for-agent` 的票加验收条目是改它的范围，
  该由所有者拍板，不该由一张「让措辞对上实现」的票顺手加。

  **处置**：所有者拍板加进去。`calibration-chart/01` 的验收补上「判读说明含中文」，
  并注明中文字模的代价（16×16、一句话几十个字形，现有 5×7 大写 ASCII 点阵撑不住）。
  顺带订正该票里一句过时的话——原写「先去关阅读器的缩放」，实际是白边裁切，而裁边是刚需。

- **仓库根没有 LICENSE，也没有一份第三方许可声明。** `calibration-chart/01` 起，
  `src/calibrate/glyphs.hex` 带进了摘自 GNU Unifont 的 190 个字形。来源与双许可
  （SIL OFL 1.1 / GPL2+ with font embedding exception）写在那个文件头上，
  但 OFL 要求随附许可**全文**，而本仓连自己的 LICENSE 都还没有。
  不当场处理：给仓库定许可、以及第三方声明放哪儿（根目录 `LICENSE` + `NOTICE`？`docs/`？）
  是所有者的决定，不该由一张画图的票顺手定。

  **处置**：所有者定案仓库本体用 MIT。根下新增 `LICENSE`；Unifont 的出处、双许可与「本仓按 OFL 1.1 使用」写进 `THIRD-PARTY-NOTICES.md`；OFL 1.1 全文逐字取自 SIL 官方源，落在 `licenses/OFL-1.1.txt`；`Cargo.toml` 补上 `license = "MIT"`。

- **ADR 0007《要认的代价》那条「tonefit 只产出文件，无从感知，也无法在输出时补救」
  现在有了缓解手段，ADR 里却指不到它。** 那句话本身仍成立——`calibration-chart/01`
  没有让 tonefit 探到阅读器的缩放，它把这个问题交给了人：标定图一次上机当场看出来。
  `CONTEXT.md` 的《几何》已经补了「像素完整性」一条并指向 measurements，ADR 0007 没有跟着指。
  不当场处理：ADR 是决定的记录，往里面加一句指路也是改它，该由所有者拍板。

  **处置**：并进 `page-geometry/06 — 文档与 ADR 收口`，那张票本来就要重写 ADR 0007 多处，不单开。

- **幂等仍要在碰像素之前预告出全部输出成员名，而 `page-geometry/04` 的 N 由内容决定。**
  `crate::page_targets` 现在给的是一对多的名单（`Vec<Vec<PathBuf>>`），`can_skip` 逐个比过去，
  行为与从前逐字相同——因为此刻 N 恒 1，名字只看源成员名就推得出来。跨页拆分一落，
  「这一页切成几张」要解了像素才知道，而幂等的全部意义是在解码之前答完。
  出路至少三条，各有代价：记录里加一项「这一张来自哪个源成员」（多一个 tEXt 字段）、
  按命名族去输出容器里探（`001.png` 或 `001-1.png`…，多一次枚举）、
  或退成「容器里每一张页成员都带着这份指纹」（输出里被删掉一页从此察觉不到）。
  不当场处理：这是 04 号票的形状选择，且第一条会动 `Fingerprint` 那一批字段——该由所有者拍板。

  **处置**：所有者定案走「记录里加一项『这一张来自哪个源成员』」，已写进 `page-geometry/04` 的验收，含两处代价。
