# 停车场：实现途中撞见、但没有为它停下来的东西

**状态：** 长期（本轮队列全部票据共用）
**路径：** `.scratch/非阻塞问题.md` — 每一份派活说明都要交出这个路径

---

## 这个文件是什么

落地票据会撞出票面没预料到、或者票面写错了的东西。**其中几乎没有一件该让队列停下来。**
它们落在这里：**记下来、绕过去、接着走、收尾时逐条了结。**

它不是产品的长期缺口清单（那是 `CONTEXT.md` 的《尚未确立》）：

| | 《尚未确立》 | 本文件 |
|---|---|---|
| 装的是 | **产品**本身的缺口 | **执行**中冒出的问题——票面写错、规格没说、两处文档互相打架 |
| 寿命 | 比这一轮队列长 | **收尾时应当清空**——每条要么就地了结、要么升进《尚未确立》、要么另立一张票 |
| 谁读 | 下一个动这个产品的人 | 收尾那一步，以及拍板的人 |

---

## 停线——只有这三种

停下来很贵：拍板的人要把整个上下文重新装回脑子。门槛：

1. **继续做下去会写出错的东西，而撤销它要伸手到这张票之外。**
   典型：票面的指示与上游一条仍然生效的决定相抵触。
2. **动作不可逆，而且不止一个方向说得通。**
   典型：删掉或改名一个被多处引用的东西——一个 id、一个字段、一个文件。
3. **前提根本不在，这张票无从下手。**
   典型：「在 X 之上做 Y」，而 X 不存在。

其余一律：记在这里，走最站得住的那条路，接着做。**包括**票面给的数与你量出来的对不上、
验收条读着含糊、票面没想到的第三种情形、路过发现的无关缺陷，以及你确实拿不准的单项。

> ⚠️ **绕过去要留下痕迹。** 条目记的是**你走了哪条路**，不是「当时有两条路」。
> 说不出自己做了什么的条目不算条目。

---

## 待处理

<!-- 新条目追加在这里；了结后连同《处置》一起挪进《已了结》 -->

> **P4 开工那一刻在册的是 Q34–Q171 这 87 条**（`5fdc7e2` 之前）。
> `p4-parking-lot/29` 负责把**它们**清空——那 87 条条条已经有判决、有去处。
>
> **P4 途中新记的条目（Q172 起）不在 29 号票的账上。** 它们与任何一轮队列的新条目
> 同一个性质：本轮撞出来的、没有为它停下来的东西，等下一次拍板。
> 29 号票收尾时把它们**留在《待处理》里**，并在落地记录里点一句「本轮新增 N 条，
> 各自的 `Whose call` 写着谁」——那是下一轮的起点，不是这一轮的欠账。

### Q34 — A 类那两张体积表没有跟着新默认重跑，重跑的只有「哪一档更优」那一组

- **From:** 票 `06`
- **Kind:** 票面没想到的第三种情形
- **Where:** `docs/measurements.md`《A 类位深实测》的逐档体积表与「整卷入出比」那一句
- **Why it did not block:** 票面点名要重跑的是那四个「哪一档更优」的页数，那一组用 `--dry-run`
  就量得出来。体积表要**落盘**跑五卷（4.4 GB 源、几百 MB 输出），而它们摄于页几何批之前那一套
  几何（fit-inside、不裁边、不拆分）——换成今天的默认，页尺寸与页数都变，那是重做一次测量，
  不是订正一句话。
- **What this ticket actually did:** 五卷各跑两趟 `--dry-run`（旧几何一趟、今天的默认一趟），
  **boox-poke6**，逐页比 4bit 不抖与 2bit+FS 的判据读数，两列一并写进那一节。
  旧几何那一列**逐卷复现了** `metric-ordering/01` 给的 177/230、156/185、3/413、40/129——
  原文缺的那句「在哪块面板上数的」因此也补出来了。体积表没有重跑，改为在节首标出施测条件，
  并点明重跑过的只有末尾那一组。顺带量到一件写进了那一节的事：今天的默认下画集 Venus 的
  卷级基准档从 2bit+FS 变成 **4bit 不抖**，换它的是**裁边**——隔离验过，单开裁边就够
  （`--fit inside`，拆分开关无所谓），而裁边关掉、只换适配方式仍判 2bit+FS。
- **Whose call:** 拍板的人（要不要在今天的默认下把 A 类的体积表整批重跑）
- **处置：** 待处理。

### Q37 — `fixtures::TINY`（160×224）在默认那条路上也不便宜，而拿它当「一张便宜的页」的调用还有十六处

- **From:** 票 `page-geometry/09`
- **Kind:** 路过发现
- **Where:** `tests/container.rs`（7 处）、`tests/exit_code.rs`（3 处）、
  `tests/idempotency.rs`（3 处）、`tests/pipeline.rs`（2 处）、`tests/timing.rs`（1 处）
- **Why it did not block:** 与本票了结的 `SMALLER_THAN_TARGET` 是同一类账，但**不是同一个夹具**。
  `TINY` 的文档已经写明它「只在 fit-inside 上还是小页」，而上面那十六处走的是默认那条路：
  以高为准把 160×224 放大到面板高，裁边先削掉渐变下方那 21.6%，出来是 1536×1680——
  比 `cheap_page()` 那张 800×1680 贵将近一倍，而多出来的像素一条断言都没参与。
  不当场处理：这些调用点上 `TINY` 与 `solid(TINY)`、`line_art(TINY)`、`color_page(TINY)`
  是配对出现的（同一卷里几页要同形），只换其中的 `gradient` 那一半会把卷改成混合尺寸，
  反而动了那几条用例的形状；要换就得**整个夹具**一起判——那是一次与 09 号票四条验收都无关的
  用例整理，混进来会把本票淹掉。本票因此只判了 `gradient` 与 `SMALLER_THAN_TARGET` 那两笔。
- **What this ticket actually did:** **十六处一处没动。**本票在 `TINY` 的邻居身上把规矩写了下来——
  `fixtures::cheap_page()` 是「只要一张页」的那一张，`SMALLER_THAN_TARGET` 的文档里点名
  「它不是一张便宜的页」，`gradient` 的文档里点名默认那条路会裁掉它下面 21.6%。
  下一个人按这三处文档去判 `TINY` 那十六处，判据是现成的。
- **Whose call:** 下一张动 `tests/` 夹具的票（把 `TINY` 在默认路径上的那些调用整批判一遍）
- **处置：** 待处理。

### Q38 — 两级停是 `run` 的能力，而命令行这一路还没有按的地方

- **From:** 票 `p1-session/02`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/main.rs` 的 `Bar::observe`（恒回 `Instruction::Continue`）
- **Why it did not block:** ADR 0013 决定第 3 条明写「收尾与中止是 `run` 的能力，不是 TUI 私有的：
  命令行那一路同样用得上」。本票把回路立起来之后，库这一侧已经收得下那两个字，
  缺的只是命令行那一头的**按键**——而那要接 Ctrl+C 信号处理，并决定按一次与按两次怎么分。
  本票的硬约束是「命令行现有行为一字不变」，接线当场就越了界；而它也不该由本票定形状：
  `04`（中止）落地之后中止才真的停得比收尾早，在那之前接上去，用户按第二下看不出区别。
- **What this ticket actually did:** **没有接。**`Bar::observe` 恒回继续，并在文档里点名
  这是「还没有那个键」，不是「命令行不该停」，同时点了 ADR 0013 决定第 3 条。
  Ctrl+C 仍然只有杀进程一条路——与本票落地之前逐字相同。
- **Whose call:** 拍板的人（命令行那一头要不要接 Ctrl+C 信号处理）——
  `10` 已落地会话那一侧的两级停（`s` 按一次收尾、再一次中止），命令行这一头仍然空着
- **处置：** 待处理。**`10` 没有接它**：本票的硬约束是「命令行那一路行为一字不变」，
  而接线要动 Ctrl+C 信号处理——那是命令行入口上的一处新行为，越了那条约束。
  会话那一侧的两级停已经落地，因此「按停是 `run` 的能力」现在**有了一个真的按的地方**；
  ADR 0013 决定第 3 条「命令行那一路同样用得上」仍然只兑现了一半。

### Q43 — 「逐条列出」只覆盖预扫抓到的坏路径，路径不在与扩展名不对仍是一次报一条

- **From:** 票 `p1-session/03`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/lib.rs` 的 `run`：`ensure_no_two_volumes_share_an_output` 排在
  `survey::Survey::of` **之前**；`src/source.rs` 的 `planned_output` → `identity_of`
- **Why it did not block:** 票面说「预扫阶段发现的坏路径整趟拒绝、逐条列出」，这一条做到了。
  但「路径不在」「既不是目录也不是 .cbz」两种到不了预扫——算去处也要看路径，
  撞名那道校验先一步 `bail!` 掉了第一个坏路径，用户于是一次只看见一条。
  两种坏路径因此有两套说法。改法是把撞名校验挪到预扫**之后**、按预扫开出来的卷算去处，
  那样两类坏路径合流；但那会改动一道既有校验的位置与它在别的情形下报出来的话，
  是本票之外的事。**整趟拒绝那一条不受影响**：两条路都在任何卷级事件之前返回 `Err`。
- **What this ticket actually did:** **没有动那道校验的位置。**预扫自己那一份坏路径收齐了再报
  （`src/survey.rs` 的 `refuse`，两条单元用例钉着「逐条列出」与「多了就截断」），
  集成用例 `a_path_that_cannot_be_opened_refuses_the_whole_run_before_any_volume_event`
  用的是**损坏的归档**——到得了预扫的那一类，并在用例文档里点明另两类走的是哪条路。
- **Whose call:** 拍板的人（两类坏路径要不要合流）
- **处置：** 待处理。

### Q44 — 全局条的「结清」每个观察者都得自己做一遍，库不给

- **From:** 票 `p1-session/03`
- **Kind:** 你确实拿不准的单项
- **Where:** `src/main.rs` 的 `Bar::finish_volume`；`src/progress.rs` 的 `Event::RunStarted`
- **Why it did not block:** 「预告是上界，因此实现方要结清」这条要求本身写在
  `Event::RunStarted` 的 `steps` 上（理由在 `CONTEXT.md` 的《进度》）。问题是**谁来做**：
  这件算术眼下写在 CLI 里，会话（`08`/`09`）要画同一条全局条时得再写一遍。
  库那一侧的做法是多一条事件——一卷跑完时报出「这一卷实际走了多少步」，或干脆把结清做进
  `VolumeFinished`——但那是给事件流加形状，而本票没有第二个实现方可以拿来验证这个形状对不对。
- **What this ticket actually did:** **把算术留在 CLI**，并把「实现方要自己结清」写进
  `Event::RunStarted` 的文档（那是下一个实现方一定会读到的地方），CLI 那一份由
  `a_volume_that_stops_early_settles_up_on_the_global_bar` 钉着。
- **Whose call:** `09`（会话主区）——它是第二个实现方，届时看是照抄还是提进库里
- **处置：** 待处理。

### Q45 — 「列前几条，剩下的说个数」这个形状在仓库里已经有三处，各写各的

- **From:** 票 `p1-session/03`
- **Kind:** 路过发现
- **Where:** `src/lib.rs` 的 `ensure_no_two_volumes_share_an_output`、`src/render.rs`（`SHOWN`）、
  以及本票新加的 `src/survey.rs` 的 `refuse`——三处各带一个自己的 `const SHOWN: usize = 5;`
- **Why it did not block:** 共用的只有骨架（先说总数 → 缩进逐条 → 「……另有 N 个」），
  而三处的**条目长什么样**各不相同：撞名那处是「一个去处 + 它下面几个源卷」的两层，
  报告那处是一行一页，本票这处是「路径一行、错误链一行」。抽成一个公共件要把
  「条目怎么渲染」做成闭包参数，省下的是每处四五行，换来的是三个互不相干的模块共用一个
  界面细节——而 `SHOWN` 到底该是 5 还是别的数，本来就该各说各的（一屏能放下几条，
  取决于每条占几行）。
- **What this ticket actually did:** **照既有形状写了第三处，没有抽公共件**，
  并在 `refuse` 的文档里明写它是照撞名那处办的，好让下一个动它的人一眼看见有三处。
- **Whose call:** 拍板的人（要不要给界面层一套「截断清单」的公共件；与 Q32 的折行是同一类问题）
- **处置：** 待处理。

### Q49 — 「列前几条，剩下的说个数」现在是第四处，而 Q45 记的还是三处

- **From:** 票 `p1-session/05`
- **Kind:** 路过发现
- **Where:** `src/render.rs` 的 `failed_volume_tail`（本票新加的第四处 `const SHOWN`）；
  停车场 Q45（它记的是 `03` 落地时的三处）
- **Why it did not block:** Q45 已经把这件事摆在那儿了，理由一字不变——共用的只有骨架，
  条目长什么样各不相同（本票这一处是「路径一行、原因一行」，与 `survey::refuse` 同形）。
  本票照同一条办法写了第四处，没抽公共件。
  **没有去改 Q45 的正文**：那一栏是 `03` 的记录，后来的活不该写进别人的记录里
  （`docs/agents/issue-tracker.md`：落地记录不因后来的工作被改写，改写属于后一张票自己的记录）。
  条数从三涨到四这件事因此记在这里，而不是改那条的标题。
- **What this ticket actually did:** **照同一条办法写了第四处，没抽公共件**，并在本条里
  把条数记清楚，好让拍板的人手上是四处而不是三处。
- **Whose call:** 与 Q45 同一个（拍板的人：要不要给界面层一套「截断清单」的公共件）
- **处置：** 待处理。

### Q55 — 三个布尔开关的覆盖是**单向**的：预设关得掉，命令行开不回来

- **From:** 票 `p1-session/07`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/main.rs` 的 `Cli::crop`、`Cli::per_page`、`Cli::split_rule`
  （`--no-crop`、`--no-split`、`--per-page` 三项）
- **Why it did not block:** 取值那一侧是完整的，不完整的只是**覆盖**——那条规矩连同
  它为什么不咬人，写在 `Cli` 那个 `impl` 的抬头，这里不复述。
  补法是给这三项各加一个反向 flag（`--crop`／`--split`／`--no-per-page`，clap 的
  `overrides_with`），那是**命令行表面的扩张**：本票的硬约束是「不点名 `--preset` 时
  命令行行为一字不变」，加三个新 flag 不违反它，但也不在验收里，且加了之后
  `--help` 上每一项都要多解释一句「它和它的反面」。
- **What this ticket actually did:** **没有加反向 flag。**当下的行为由
  `the_switches_that_only_say_one_thing_stay_one_way` 钉住——它变的那天红的是这一行。
- **Whose call:** 拍板的人（要不要给这三项各加一个反向 flag）
- **处置：** 待处理。

### Q66 — 最后那一趟没做成，先前那一趟试算的报告也就印不到 stdout 了

- **From:** 票 `p1-session/09`
- **Kind:** 票面读着含糊
- **Where:** `src/session/run.rs` 的 `Running::report`；`src/session/live.rs` 的 `undone`
- **Why it did not block:** 票面说「退出会话时把报告照原格式印到 stdout」，
  没说一个会话里跑了好几趟时印哪一趟的。本票取**最后那一趟**：退出码也取它，
  两者取同一趟才对得上（`tonefit > 报告.txt` 之后读 `$?`，说的得是同一件事）。
  **没做成**的那一趟没有报告可带（`run` 返回的是错误本身），于是那一次 stdout 上
  一个字节都没有——哪怕先前刚跑过一趟好好的试算。这与命令行那一路是同一条规矩，
  那边没做成时同样什么都不印。
- **What this ticket actually did:** **取最后那一趟，没做成就不印。** 攒到一半的那一份仍留在
  `Live` 上、仍画在主区（会话不因此退出，用户当场改），只是不印到 stdout。
  「没做成」收着**两种**：拒绝执行，以及那条线程恐慌了——两者都没有报告可带、
  退出码都是 `1`，分得开它们的是那句话本身（主区上印的就是它）。
  用例 `a_refused_run_leaves_the_session_open_and_prints_nothing` 钉住这一条。
- **Whose call:** 拍板的人（一个会话跑了好几趟时，stdout 上该有哪一趟）
- **处置：** 待处理。


### Q67 — 口味层每一项的默认值原本只写在 `Cli` 上，会话拼不出同一份 `Request`

- **From:** 票 `p1-session/09`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/preset.rs` 的 `impl TasteLayer`；`src/main.rs` 的 `Cli::crop`、
  `Cli::split_rule` 等那几个方法
- **Why it did not block:** 会话也要拼一份 `Request`（试算与执行只差 `mode` 一格），
  而「预设没说就落到默认值」这一步原本逐项写在 `Cli` 的那几个方法里
  （`preset.taste.crop.unwrap_or(true)`）。照抄一份到会话里，同一条命令与同一次会话
  就会在无人察觉时分家——而那正是本票「共用同一套」要防的那类事。
- **What this ticket actually did:** **把「落到默认值」那一步搬进 `TasteLayer` 自己的方法**
  （`fit()`、`crop()`、`split_rule()`……），`Cli` 改读它们，会话直接用。
  命令行那一路因此**行为一字不变**——同一段代码走过去，而不是靠比对；
  用例 `an_untouched_session_asks_for_what_a_bare_command_line_asks_for` 把两边逐项钉在一起。
  没有搬的是「命令行上显式点到的那一项赢」那条规矩与三个布尔开关的单向覆盖：
  那是命令行自己的事，搬过去会把会话不需要的东西一起带进来。
- **Whose call:** 拍板的人（`Cli` 那几个方法要不要整个收进 `preset`）
- **处置：** 待处理。

### Q70 — 闩的编码在两个 crate 里各有一份，靠两条用例分别拴着

- **From:** 票 `p1-session/10`
- **Kind:** 路过发现
- **Where:** `src/progress.rs` 的 `Standing` 与 `Instruction::code`／`from_code`；
  `src/session/run.rs` 的 `Latch` 与 `code`／`from_code`
- **Why it did not block:** 两处记的不是同一件事——库那一份记的是**观察者答过**什么
  （`Standing`，活在 `run` 的栈上），会话那一份记的是**用户按过**什么
  （`Latch`，活在 `Running` 上、一趟一份）。但两者把 `Instruction` 编成一个字节的那三行
  逐字相同，而库那一份是 `pub(crate)` 的，二进制 crate 够不着。
  要合成一份，得把「一个原子的 `Instruction`」导出成公共 API——那是一条 API 面上的决定，
  且它会把一个只有两个内部用户的形状钉进对外契约，不是本票该顺手拿的。
  它还会再长一份：ADR 0013 决定第 3 条说按停是 `run` 的能力、命令行那一路同样用得上，
  而命令行那一头接上的那天（Q38）就是第三份。
- **What this ticket actually did:** **各留一份，两处互相点名。** 会话那一份的文档写明
  它与库那一份的分别（记的是谁答的字）、为什么够不着那一份、以及**序只有一个出处**——
  `Instruction` 派生的 `Ord`，两处都只是把那个序编成一个字节。
  两处各有一条用例把「编进去的数」与「派生出来的序」拴在一起
  （库那侧的 `the_three_instructions_are_ordered_by_how_hard_they_stop`，
  会话这侧的 `the_latch_only_ever_goes_up`）：改一次变体顺序，两条一起变红。
- **Whose call:** 拍板的人（要不要把「一个原子的 `Instruction`」导出成公共 API）
- **处置：** 待处理。

### Q76 — `Event::PassStarted` 多了一格，而三条里只有一条用得上它

- **From:** 票 `p1-session/14`
- **Kind:** 你确实拿不准的单项
- **Where:** `src/progress.rs` 的 `Event::PassStarted`（`so_far: Option<&VolumeReport>`）；
  `Events::pass_started` 与 `Events::ask_before_the_second_pass`
- **Why it did not block:** 这是 Q52 的处置换来的：决策点非带一份报告不可，
  而 Q52 列的两条路里，「会话自己攒一份」到有消费方时才看得清走不通。
  眼下不咬人有三条：变体**非穷尽**，库外的 `match` 本来就要带 `..`，
  三个实现方（命令行进度条、会话、用例记账本）没有一个被逼着改；
  那一格的语义写死在文档里（只有决策点带，另两遍恒 `None`），
  `tests/resume.rs` 的 `the_decision_point_carries_the_volume_report_as_it_stands`
  连「别的两遍不带」一起钉着；而 `Pass::Second` 本来就已经是**唯一一条答复当场作数**的事件，
  这一格跟着它走，没有让另一条事件变得特殊。
  露出来的是形状上的一句话：**一个变体里有一格只对它三个取值中的一个有意义**。
  另一条路是给它单开一条事件（比如「这一卷汇总好了」，带报告；决策点仍旧是 `PassStarted`），
  那样每一格都恒有意义，代价是决策点那一刻发两条事件、而会话要把前一条记住等后一条——
  两条事件之间的那一段成了一个必须守住的不变量。
- **What this ticket actually did:** **走了「加一格」那一条，并把语义写在那一格自己身上。**
  `Pass::Second` 的文档、`Event::PassStarted` 的 `so_far` 文档、
  `Events::pass_started`（「这两遍不带 `so_far`」）三处各说一次它管什么，措辞不重复。
  没有单开事件：决策点是**一个**问话点，拆成两条事件就等于把它拆成两个。
- **Whose call:** 拍板的人（`Event` 上「一格只对一个取值有意义」这种形状要不要收）
- **处置：** 待处理。

### Q80 — 票面说的 `fast` 模式在实现里并不存在

- **From:** 票 `p0-hardening/13`
- **Kind:** 票面前提与实现对不上
- **Where:** 票 `p0-hardening/13` 的 What to build；`CONTEXT.md` 的《模式》；
  `p0-core-pipeline/spec.md` 的 Out of Scope；`src/request.rs` 的 `Mode`
- **Why it did not block:** 票面写「`fast` 模式达到 spec 定的每秒八十页」，
  而命令行上没有 `--mode`，`Mode` 只有 `Process` 与 `DryRun` 两个取值。
  `CONTEXT.md` 的《模式》说 `fast` 关闭 oxipng、仲裁仅在边界带触发，
  而 P0 的 Out of Scope 把 oxipng、SSIMULACRA2 仲裁与 `best` 模式整个摘了出去——
  **默认构建就是 `fast` 描述的那个配置**，两档因此还没有分岔。
  同一句里那个「spec 定的每秒八十页」也翻不到出处：`.scratch/` 下没有任何一份 spec
  写着这个数，它只出现在本票票面上。
- **What this ticket actually did:** **按「默认构建即 `fast`」施测，并把目标改判在
  measurements 里。**四卷全部走默认特性、默认参数的 release 构建。
  八十那个数没有 spec 可改，改判因此落在 measurements 的
  《分阶段耗时剖面》小节《吞吐目标：八十页/秒不成立，改判》，
  按素材形态分四档，各带一条实测依据；票面那一条验收指向它。
  `Mode` 与《模式》一个字没动——两档真分岔那一天要不要一个 `--mode`，是另一张票的事。

### Q81 — spec 那句「解码一步就会吃光单页预算」被剖面证否，但没有改它

- **From:** 票 `p0-hardening/13`
- **Kind:** 上游文档与实测对不上
- **Where:** `.scratch/p0-core-pipeline/spec.md` 的《输入》一节（AVIF 走 dav1d 那一段）
- **Why it did not block:** 那一句写「它是唯一能满足吞吐目标的选项……解码一步就会吃光
  单页预算」。本票的剖面把解码量出来了，它在四卷里都不是瓶颈，B 类那一卷更是最低的一档
  （数在 measurements 的《分阶段耗时剖面》）。照剖面反推，换成慢 2.9 倍的 rav1d
  也只让整趟慢个位数百分比，远谈不上「吃光预算」。
  **但决定本身不变**：dav1d 更快且已经在用，没有任何理由回头。
  错的只是当初那一句的论证强度——它是按「纯解码循环的满核页/秒」外推的，
  而整条管线在同一批页上还要付掉十倍于解码的算力，这一点当时没有数。
  不改它的理由是 `docs/agents/issue-tracker.md` 的那条：交付过的记录
  「is a historical record and is not rewritten when later work supersedes it」——
  这是被后来的测量**取代**，不是当初就写错的事实。
- **What this ticket actually did:** **一个字没改那份 spec，把新数放在 measurements。**
  剖面里解码那一行连同「13 号票立票时按纯解码满核推断解码占三分之一预算，剖面证否了这一条」
  一句写在《分阶段耗时剖面》。下一个动 AVIF 解码或者重估 P0 的人，从那一节读得到真数。
- **Whose call:** 拍板的人（那份 spec 的论证要不要补一条「后来测出来是这样」的脚注）

### Q82 — 闸门是三条命令，而仓库里没有一处**自动**跑它们

- **From:** 票 `p2-loose-ends/01`
- **Kind:** 票面没想到的第三种情形
- **Where:** `docs/agents/gate.md`；`CLAUDE.md` 的《闸门》；仓库根（没有 CI 配置，也没有 `xtask`）
- **Why it did not block:** spec 的《Testing Decisions》说闸门那一条「断言写在 CI 脚本或
  `xtask` 上，不是写在用例里」。仓库里两样都没有——没有 `.github/`、没有 `xtask`
  成员、没有 `justfile`。立一个只为跑三条 `cargo` 命令，是往仓库里加一整套构建脚手架
  （`xtask` 要加一个 workspace 成员与一份 `.cargo/config.toml` 的别名），
  而它能保证的事情与「照着 `docs/agents/gate.md` 敲三条」一样多：**没有人替你按下去**。
  真要让闸门自己跑起来，缺的不是脚手架而是 CI，那是另一件事。
- **What this ticket actually did:** **写成文档，没写成脚本。**三条命令落在
  `docs/agents/gate.md`（各盖住什么、哪种改动必须跑满三条、结果怎么读），
  `CLAUDE.md` 的《闸门》给三行命令加一个指路。**票面第一条要的是「在一处写明它们是闸门」**，
  这一条成立；「有人自动按下去」不在票面上。
- **Whose call:** 拍板的人（这个仓库要不要 CI）
- **处置：** 待处理。

### Q84 — 关掉 `tui` 那一趟，整个 `session` 上放开了 `dead_code`

- **From:** 票 `p2-loose-ends/01`
- **Kind:** 我确实拿不准的单项
- **Where:** `src/main.rs` 的 `#[cfg_attr(not(feature = "tui"), allow(dead_code))] mod session;`
- **Why it did not block:** 状态机那四个模块搬出特性之后，`tui` 关掉的那一趟里读它们的只剩
  它们自己的用例——画法与那条循环整个不编译，于是只有画法读得到的取值器
  （`Live::resumes`、`Session::title`、`Presets::names` 等，`--force-warn dead_code` 数出来
  是九处：`live.rs` 四处、`state.rs` 五处）全成了「没人读」。
  票面第五条要的是「关掉之后不留 `dead_code` 告警」，而那九处不是死代码，
  是这一趟的前提。真要一条不放松，只有把画法也搬出终端库，而画法搬不出去。
- **What this ticket actually did:** **在关掉的那一趟上整模块放开，默认那一趟一格不放松。**
  `cargo clippy --all-targets` 与 `--no-default-features` 两遍都干净，
  真死掉的东西仍由第一条闸门当场红。**放开的面比那九处宽**：`mod session` 上一句管住
  它下面全部四个模块，`complete` 与 `run` 眼下一处不占，但**将来**在它们里添的死代码
  在第二条闸门上也不报了。代价因此是将来的，不是眼下的——收窄它得逐处挂 `cfg_attr`，
  那是把一句噪声换成九句。
- **后续（`p3-session-legibility/04`）:** 视口那个纯函数（`session::viewport`）也摆在了
  特性外面，关掉 `tui` 那一趟因此多出五条**真跑得到**的用例。**放开的面一格没变**——
  `mod session` 上那一句照旧管着底下五个模块，`viewport` 自己在那一趟里除用例外没人读。
  本条的处置不变。
- **Whose call:** 拍板的人（这笔放松认不认、要不要收窄到逐处）
- **处置：** 待处理。

### Q86 — 哨兵「发布构建上整个不在」那一半，三条闸门一条都够不着

- **From:** 票 `p2-loose-ends/02`
- **Kind:** 我确实拿不准的单项
- **Where:** `docs/agents/gate.md` 的三条；`src/progress.rs` 的 `LockSentinel`
  与 `src/lib.rs` 的 `CacheGuard`（那几处 `cfg(debug_assertions)` 与
  `cfg_attr(debug_assertions, track_caller)`）
- **Why it did not block:** 三条闸门跑的都是 dev profile（`debug_assertions` 开着），
  验的全是哨兵**在**的那一半。「不在」那一半只有发布构建看得见，而那不是闸门里的命令。
  本票试过给它一条编译期断言（比 `CacheGuard` 与 `MutexGuard` 的大小），复审时发现那是**空的**：
  `LockSentinel` 是零尺寸类型，带不带那一格都是 16 字节，断言恒真。真正的开销在
  `Drop` 那两次线程局部加减与 `#[track_caller]` 多传的那个隐式参数，`size_of` 一个都量不到。
  要真验它，得让闸门多一条发布构建（`cargo check --release` 只验编译不过与不引用，
  不验开销），或者引一套基准——两样都比本票大。与 Q79（`profiling` 不在闸门里）同一族，
  与 Q82（闸门没有 CI 替你按下去）相邻但不是同一件事。
- **What this ticket actually did:** **删掉了那条空断言，没有换一条真的。**「发布构建上整个不在」
  眼下靠的是那几处 `cfg` 本身，加上收尾时手跑的一条 `cargo check --release`——
  它够得着的只有「没有一处无条件引用了被 `cfg` 掉的东西」，验不了开销。
  票据的《数》里如实记着这一条是闸门之外手跑的。
- **Whose call:** 拍板的人（闸门要不要多一条发布构建）
- **处置：** 待处理。

### Q87 — 折算系数还是 8 那会儿的两处残留：07 号票的报告样例印 67108864，已了结索引 Q22 写「系数 8」

- **From:** 票 `p2-loose-ends/03`
- **Kind:** 两处文档打架
- **Where:** `.scratch/page-geometry/issues/07-target-size-needs-a-backstop.md`《落地记录》的
  《报告》一段（「兜底上界 N 页：…越过 67108864 像素…」）；本文件《已了结》索引里 Q22 那一行
  （「兜底上界的折算系数 **8** 是照代码数出来的」）
- **Why it did not block:** 折算系数最终定成 **16**、上界是 **32 Mi**（33554432，
  `geometry::MAX_TARGET_PIXELS`；用例
  `the_backstop_is_the_decode_limit_divided_by_what_a_target_pixel_costs` 钉住这个数），
  报告印的也是 `max_target_pixels()` 这一个数。上面两处是系数取 8（512 MiB ÷ 8 = 64 Mi）
  那会儿写下的，没跟着改。两处都是**已了结票据与索引的历史记录**，读代码的人一处都碰不到；
  而改一张已了结票据的落地记录，先例是不改（`page-geometry/06` 对 Q21 的处置：
  「05 号票的票面不改——它是已了结票据的历史记录」）。
- **What this ticket actually did:** **两处一个字没改**，反而拿它们当了证据：
  `GeometryGate::Broken` 那个 24:1 正是同一份草稿的第三处残留（64 Mi ÷ 1680² = 23.8），
  查证与结论见 `p2-loose-ends/03` 的《24:1 是什么》。当前成立的那道线只有一处出处
  （`MAX_TARGET_PIXELS` 的《多瘦长的页够得着这道线》），代码、文档与用例都指着它。
- **Whose call:** 收尾的人（已了结票据里的过时数字要不要统一订正）
- **处置：** 待处理。

### Q88 — spec 把「卷根整个消失」的判据写成「一页都没成」，而票面要的是把这两种分开

- **From:** 票 `p2-loose-ends/05`
- **Kind:** 两处文档打架（打架的是 spec 与票面，不是票面与实现）
- **Where:** `.scratch/p2-loose-ends/spec.md`《退出码与失败口径》第一条
  （「**卷根整个消失** → 卷级失败（`3`）……判据是「一页都没成的卷不算做出来了」」）；
  票面 `.scratch/p2-loose-ends/issues/05-a-volume-that-vanished-did-not-get-made.md`
  的第三条验收
- **Why it did not block:** 两处给的是**两个不同的判据**。照 spec 那一句做，「卷根还在、
  里面每一页都读不出来」的卷会跟着一起被收进卷级失败，而那正是 p0 的 12 号票定死的
  页级失败，也正是 `CONTEXT.md`《失败》那句「失败是页级的，隔离是卷级的：一页失败，
  整卷进隔离目录」说的东西；票面第三条反过来明写「前者仍走隔离，后者走卷级失败——
  两种各有用例，且退出码不同」。票面更细、更晚，且与词汇表一致，因此照票面做。
- **What this ticket actually did:** 按**票面**落地：判据是**卷根还在不在**
  （`lib.rs` 的 `ensure_the_volume_root_is_still_there`），两种各有一条用例、退出码分别是
  `3` 与 `2`。**没有改 spec 那一句**——spec 是拍过板的文件，订正它是领域决定不是实现细节
  （同 `CLAUDE.md`《改 CONTEXT.md 的规矩》那条线）。
- **Whose call:** 拍板的人（要不要把 spec 那一句订正成票面的说法）
- **处置：** 待处理。

### Q89 — 《失败》的**拒绝执行**词条把成因列成五种、并写着「前四种发生在开工之前」，而现在是六种／前五种

- **From:** 票 `p2-loose-ends/06`
- **Kind:** 词汇表与实现对不上
- **Where:** `CONTEXT.md`《失败》的**拒绝执行**词条（「……处理范围为空、输出落在源里、
  两个卷撞同一个去处、预扫发现有卷点不开、覆盖项把候选集裁空。整趟当场停……
  **前四种**发生在开工之前，一页都不做」）
- **Why it did not block:** 词条的**定义**一个字都没被推翻——「错在这一趟的**参数**上，
  换一个卷不会变好」正是本票据以判定的那一句，`--out` 指到一个建不出来的路径完全落在里面。
  对不上的只有那个**枚举**与跟着它的那个计数：本票新添的「输出根写不进去」是第六种、
  开工之前的第五种。改它是往一个已经拍过板的词条里加一项并改一个数，
  按 `CLAUDE.md`《改 CONTEXT.md 的规矩》属于「改写已有词条」那一侧，
  而票面第六条写的正是「对不上就记进停车场」。
  同一个枚举在实现文档里另有四处（`src/lib.rs` 的 `run`、`src/progress.rs` 的
  `RunStarted` 与 `RunFinished`、`src/report.rs` 的 `elapsed`、`src/main.rs` 的 `survey`），
  那几处是实现文档、本票已逐一改到六种。
- **What this ticket actually did:** **`CONTEXT.md` 一个字没动**，五处实现文档全部改到与实现一致。
  Q48 是同一个词条上同一种情形的先例（成因列成四种而卷内撞名走的是卷级失败），
  它的去处是「p1-session/05 → CONTEXT.md《失败》」。
- **Whose call:** 拍板的人（要不要把「输出根写不进去」加进那个枚举，并把「前四种」改成「前五种」）
- **处置：** 待处理。

### Q90 — 那道探测**不看模式**：试算也真往盘上写一次探针，而《模式》说 dry-run 一个文件都不落盘

- **From:** 票 `p2-loose-ends/06`
- **Kind:** 票面没想到的第三种情形（Q51 早已把它挂出来：「dry-run 一个字节都不写，
  探不探得看模式」）
- **Where:** `src/lib.rs` 的 `ensure_the_output_root_takes_a_write`（`run` 在开工前无条件调它）；
  `src/request.rs` 的 `Mode::DryRun`（「只算不写：判据照求、报告照出，一个文件都不落盘」）
- **Why it did not block:** 两条各有各的道理，而本票非拍不可。**照模式分岔**的话，
  试算就答不出「你这个 `--out` 根本用不了」——而拒绝执行说的是**参数**，两种模式的参数是
  同一份，另外五种拒绝也都在试算里照样咬人（`CONTEXT.md`《会话》：试算与执行是同一个 `run`，
  区别只在 mode）。一份说「都好」而真跑起来当场被拒的试算报告是在撒谎，
  那正是「先看一份报告再决定」这条回路要买的东西。
  **不分岔**的代价是：探针那一个目录加一个文件在盘上**真实存在过**，
  进程正好在试写与删探针之间被杀就留着一个点开头的文件。
  按字面读，「一个文件都不落盘」那一刻不成立。
- **What this ticket actually did:** **不看模式，两种都探**，判准写在
  `ensure_the_output_root_takes_a_write` 的《探完盘上一个字节都不多》里：
  那句契约说的是**输出**——页、记录、输出容器，也就是用户留得下的东西；
  探针留不下任何东西，探完的盘与没探过逐字节一样。
  `a_dry_run_probes_the_output_root_and_leaves_nothing_behind` 把两半一起钉住
  （试算照样拒得动；试算跑完输出根仍不存在），
  既有的 `a_dry_run_names_the_isolation_directory_without_writing_anything` 也验着同一件事。
  被杀那一笔照实写进了函数文档，没有假装它不在。
- **Whose call:** 拍板的人（试算该不该探；要不要把《模式》那句「一个文件都不落盘」写明它说的是输出）
- **处置：** 待处理。

### Q92 — 「预扫之后才出的卷级失败」那个窗口挪到了**这一卷重开之后**：一头收窄，一头加宽

- **From:** 票 `volume-discovery/01`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/lib.rs` 的 `process_volume`（`source::open` 挪到了这里）；
  `tests/fixtures/mod.rs` 的 `RemoveOnceTheVolumeIsOpen`；
  `tests/container.rs` 的 `run_losing_the_extra`；`tests/isolation.rs` 的
  `a_volume_that_fails_after_the_survey_leaves_the_rest_of_the_run_alone`
- **Why it did not block:** 从前成员表由预扫列定，此后不再刷新——**预扫走完那一刻**起，
  少掉的成员就必然变成一个卷级失败。现在这一卷轮到时按路径重开，那之前删掉的成员
  根本不进成员表：这一卷照常做完，只是少一个成员。窗口因此从「整趟开工之后」
  缩到「这一卷重开之后」。
  两件事说明它不咬人：现实里那个窗口本来就是「轮到它时它还在不在」，重开正好答的是这一问，
  而且答得比从前**新**；`CONTEXT.md`《失败》的**卷级失败**词条说的是「预扫发现的整趟拒绝、
  预扫之后才出的记为卷级失败」——这两句一个字都没被推翻，落进那一格的现场少了一类而已。
  真要一个字不差地保住旧窗口，得让重开去核对预扫那份成员表并把对不上的当失败——
  那等于把「只数不留」买到的东西又还回去一半，且要为一种谁也没承诺过的竞态定新行为。
  **另一头同时加宽**：重开这件事**自己也会失败**——归档文件被删、损坏、权限变了，
  都成了一种从前不存在的卷级失败。最明显的一例是归档卷的卷根在预扫之后消失：
  从前它的字节从预扫握着的那个句柄里出、整卷照样做得出来，现在记一笔卷级失败、
  退出码 `3`，与目录卷同形（`ensure_the_volume_root_is_still_there` 因此两种容器都问）。
  这一头不是遗憾，是重开的题中之义：轮到它时它真的不在了。
- **What this ticket actually did:** **两头都认下，各钉一条用例。**收窄那一头：三条用它造
  卷级失败的用例改了动手的时刻——新加夹具 `RemoveOnceTheVolumeIsOpen` 记着点名那一卷开工了
  没有，在它的**第一条 `PassStarted`** 上抽走成员，那一刻这一卷刚重开完、一个字节都还没读，
  正是旧夹具在 `RunStarted` 上想造的那个现场；抽**卷根**的那两条不受影响，仍在 `RunStarted`
  上动手（`RemoveOnceTheSurveyIsDone`）。加宽那一头：新增
  `an_archive_volume_whose_file_is_gone_did_not_get_made`（`tests/isolation.rs`），
  连同 `ensure_the_volume_root_is_still_there` 文档里《两种容器形态都问》那一节。
- **Whose call:** 拍板的人（「预扫时打得开、轮到它却做不成」这句承诺以哪一遍枚举为准）
- **处置：** 待处理。

### Q93 — 开工那条事件报的步数出自预扫那一遍，报告里的成员数出自重开那一遍

- **From:** 票 `volume-discovery/01`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/survey.rs` 的 `Surveyed::steps`；`src/lib.rs` 的 `MemberCounts::of`
  与 `process_volume` 里那条区间 `debug_assert!`
- **Why it did not block:** 源在两遍枚举之间没动的那一趟——也就是正常的每一趟——两个数
  逐字相同。动了的话它们分家：步数已经加进开工那条事件报出去的全局总步数，收不回来，
  而报告说的必须是真做了的那一卷。两边取同一遍都不行：全取预扫那一遍，报告就会撒谎，
  那条「产出张数落在 [源页数, 上界] 之内」的 `debug_assert!` 还会在调试构建上当场炸
  （源多了几页就越出上界）；全取重开那一遍，开工那条事件根本还没到重开就得报出总步数。
  预告的步数本来就是**上界、不是承诺**（`CONTEXT.md`《进度》），进度条走不满或冲过头
  在幂等命中的卷上早就会发生，这里只是多一种成因。
- **What this ticket actually did:** **分开取，各取该取的那一遍**：`Surveyed` 只留步数，
  它出自预扫；`MemberCounts::of` 在 `process_volume` 里按重开的那一份重新数一遍，
  报告与那条区间断言都用它。公式只有一处（`MemberCounts::of`），两处各写一份才会真漂。
  两个数的出处都写进了各自的文档。
- **Whose call:** 拍板的人（要不要在两遍对不上时说一句，还是继续静默地按上界处理）
- **处置：** 待处理。

### Q94 — 测试用的那份最小 `Request` 在 `src/` 里仍抄着两份

- **From:** 票 `volume-discovery/01`
- **Kind:** 路过发现
- **Where:** `src/lib.rs` 的 `tests::request`（本票改成 `pub(crate)`）与
  `src/metadata.rs` 的 `tests::request`
- **Why it did not block:** 两份 15 个字段**逐字相同**，`Request` 加一格就要改两处，
  而漏改一处只会在编译期被抓到、不至于放出错的行为。本票新写的预扫用例本来要抄成第三份，
  评审的 Duplicated Code 指出来之后没有抄——改成用 `crate::tests::request()`
  加两处覆盖。剩下那一份在 `src/metadata.rs`，收拢它要动一个本票没有别的理由去动的文件
  （票面硬约束：只提交本票动过的文件）。
- **What this ticket actually did:** **没有收拢 `metadata.rs` 那一份**，只把
  `lib.rs` 那一份开到 `pub(crate)` 并让新用例用它——三份变两份，没有变一份。
- **Whose call:** 下一张动 `src/metadata.rs` 的票（顺手让它也用同一份）
- **处置：** 待处理。

### Q95 — 跑过 `cargo test --no-default-features` 之后再跑 `cargo test`，`session` 那条会假红

- **From:** 票 `volume-discovery/01`
- **Kind:** 路过发现
- **Where:** `docs/agents/gate.md`《读结果》；`tests/session.rs` 的
  `no_arguments_without_a_terminal_says_so_and_still_shows_the_usage`
- **Why it did not block:** 两种特性组合的 `tonefit.exe` 落在**同一个** target 目录里、
  互相覆盖，而 `cargo test` 判定自己那一份「新鲜」时不会重新摆放它。于是闸门第二条跑完
  之后再回头跑第一条，用例启动到的是上一趟留下的**不带 `tui`** 的那个二进制：
  无参数时它走 clap 的必填项错误、退出码 `2`，而这一条要的是会话那条岔路的 `1`。
  当场量过三件事——`cargo build` 之后那个 exe 回 `1`（会话那条），`cargo test` 之后回 `2`
  （clap 那条）；换一个干净的 `CARGO_TARGET_DIR` 从头跑第一条，17 个二进制全绿、
  一条 `FAILED` 都没有。与任何一张票的改动都无关：它只取决于上一次在这个目录里
  build 过哪一种特性组合。
- **What this ticket actually did:** **没有动闸门，也没有动那条用例。**本票的三条闸门按
  1→2→3 的次序各跑一遍，第一条的数取自它自己那一趟（以及一次干净 target 目录的复跑）。
  这一笔连同「按次序跑」写进了本票落地记录的《数》。
- **Whose call:** 拍板的人（`docs/agents/gate.md`《读结果》要不要加一句「三条按次序跑」，
  或者让闸门各用各的 target 目录）
- **处置：** 待处理。

### Q97 — 本票这一批实测整体比剖面那一批快一到三成，而快的有一部分与代码无关

- **From:** 票 `p2-loose-ends/11`
- **Kind:** 票面给的数与我量出来的对不上
- **Where:** `docs/measurements.md` 的《端到端吞吐》《三段各占多少》（本票换掉的那一批）
  与《分阶段耗时剖面》《吞吐目标：八十页/秒不成立，改判》（没换的那一批）
- **Why it did not block:** 本票在归档卷上重测吞吐，四卷一趟测出来的秒数**整体**低于文档里
  那一批：棋魂 6.55→5.99（`--io-mode serial`，与并行之前同一件事；6.55 是本票换掉的
  那一行原写的 6552 毫秒）、哆啦A梦 40.2→33.7、
  N和S 28.3→26.9、**改革之獸 10.0→8.4**。最后一个是目录卷，本票一个字都碰不到它，
  它仍快了一成半——因此这一批与那一批之间**除了本票的改动还有别的差**（机器状态、
  中间落地的别的票，两者都无从事后归因）。真要归因得把旧代码再 build 一遍逐档对齐，
  那比本票大，而本票要的 A/B **不需要它**：`--io-mode serial` 与默认在归档卷上唯一的差别
  就是幂等那一道派几条，两档在**同一批**里测出，差值站得住。
  连带的一件：《吞吐目标》那四档是按旧那一批「取实测值再留一成余量」定的，本批四卷
  全部在线以上（51 / 22 / 14 / 55 源页/秒）。
- **What this ticket actually did:** **只换掉两节，另外两节留在自己那一批里，并在四处
  写明「两批不可逐格相比」。**《端到端吞吐》与《三段各占多少》换成本批；
  《分阶段耗时剖面》与《吞吐目标》一个数没动——后者原先引着《三段各占多少》的
  「第一遍是 3.93 秒」，改成就地写死那一批的数，引用因此不会指到一批换过的秒数上。
  **四档目标一个字没抬**：那是拍板的事，不是一次重测能决定的。
- **Whose call:** 拍板的人（四档目标要不要按新的一批抬上去；要不要为「两批之间还差了什么」
  立一张归因的票）
- **处置：** 待处理。

### Q99 — 成员表出自开卷那个句柄，字节出自重开的那个，两者之间归档被换掉就会静默读错

- **From:** 票 `p2-loose-ends/11`
- **Kind:** 路过发现（评审 Spec 轴指出）
- **Where:** `src/source.rs` 的 `Member::entry` 与 `Reader::independent`；
  `src/read.rs` 的 `Concurrent::spawn`
- **Why it did not block:** `Member::entry` 是**开卷那一次**解析出的中央目录下标，
  而 `independent()` 是按路径重开、**重解一遍**中央目录。两次之间源归档被替换成另一份，
  同一个下标就指到另一个成员上——越界那一支报得出错，不越界那一支**静默读到别的字节**。
  从前全卷共用一个句柄，这条不存在。眼下不咬人：源是只读的，而「跑到一半有人换掉源文件」
  这件事本来就没有任何一处防着——`volume-discovery/01` 之后预扫与处理之间也是按路径重开，
  那一趟同样能撞上（只是它重开时连成员表一起重数，因此错的形式不同：那边是「数错」，
  这边是「读错」）。真要拦死，得让 `independent()` 拿开卷那一次的某个指纹去核
  （中央目录的偏移与条目数，或整个文件的 mtime + 大小），核不上就退回串行——
  那是给读取端加一道校验，不是本票的形状。
- **What this ticket actually did:** **认下这一条，没有加校验。**并发那一支照旧按
  `member.entry` 取成员。写进这里而不是就地加一句注释，是因为它跨着 `source` 与 `read`
  两层，而处置要拍板的人看见。
- **Whose call:** 拍板的人（源在跑的过程中被换掉，要不要有一道防线；那道防线该在哪一层）
- **处置：** 待处理。

### Q100 — 那笔句柄账住在 `source::Reader` 上，而它跨着四个模块

- **From:** 票 `p2-loose-ends/12`
- **Kind:** 你确实拿不准的单项
- **Where:** `src/source.rs` 的 `Reader`《一趟同时开着几个句柄》那一节
- **Why it did not block:** 那一节要写全三个来处，其中「写出那一侧」点着 `crate::sink` 与
  `crate::cache`，而 `source` 这一层并不知道那两个模块存在。这与 `CLAUDE.md`
  《文档写作》第 2 条「一个文件只承载它那一件事」有张力。选它的理由是式子那两格
  **都是 `Reader`**（卷自己那一个、读取线程那几个），放在别处就得跨层复述那两格；
  写出那一侧只占一句「它们排在幂等那一道之后，不同时在手上」。另一个候选是
  `CONTEXT.md`——但一笔账不是一个词，往词汇表塞它要么改已有词条（本票没被授权，
  见 `CLAUDE.md`《改 CONTEXT.md 的规矩》），要么在术语表底下另起一节散文。
- **What this ticket actually did:** **放在 `source::Reader` 上**，四处引它的小节名、不复述
  （`src/read.rs` 的模块文档与 `reads`、`a_concurrent_read_holds_one_reader_per_worker`、
  `src/survey.rs` 的模块文档）。写出那一侧只写「不同时在手上」这一句，不展开。
- **Whose call:** 拍板的人（跨层的账该不该在 `CONTEXT.md` 里有一节，还是就近挂在某个类型上）
- **处置：** 待处理。

### Q101 — `docs/measurements.md` 里多了一行不是本机量出来的数

- **From:** 票 `p2-loose-ends/12`
- **Kind:** 你确实拿不准的单项
- **Where:** `docs/measurements.md` 的《一个进程能同时开多少个文件句柄》，Linux 那一行
- **Why it did not block:** 那一节要答的是「峰值离平台上限有多远」，而本机只有 Windows。
  Linux 的默认软上限 1024 是这条判断的**最紧一档**，不写进去，那一节答不完整；
  写进去，measurements 里就有了一行取自发行版默认值、不是量出来的数。
  `CLAUDE.md` 说的是「需要实测数字时，来源只有 `docs/measurements.md`」——
  它约束的是**取数往哪里去取**，没说那个文件里不许有别的东西；这个文件里也已经有
  非实测的条目（《设备支持的图片格式》那一类出自厂商资料）。
- **What this ticket actually did:** **写进去，并在那一格里照实写「未在本机上量，
  取的是发行版默认值」。**Windows 那一行是真量的（同一个文件反复 `File::open`
  攥到十万个，3.1 秒，没撞上限）。
- **Whose call:** 拍板的人（measurements 要不要只装本机实测，非实测的数另立一处）
- **处置：** 待处理。

### Q102 — 互锁 ③ 那条拒绝仍然一次把两条出路都说了，按页分岔挪得动、但那是改这句话说什么

- **From:** 票 `p2-loose-ends/04`（Q33 了结时剩下的那一半）
- **Kind:** 你确实拿不准的单项
- **Where:** `src/lib.rs` 的 `dither_outside_the_gate_error` 与 `Candidates::new`；
  真要挪，落点是 `Compute::gray_page`
- **Why it did not block:** **不是做不到。**拒绝那句话现在在碰卷之前就备好，那时没有页可量；
  而判门的地方手上有这一页的源尺寸与 `Fit::backstopped`，
  `FitMode::Height.target(源尺寸, 面板).backstopped()` 就答得出「这一页换 `--fit height`
  解不解得了」。挡住的是别的：分岔之后**这句话本身变了**——fit-inside 上够得着出路的页
  只听见前半句（「换 --fit height，门跟着成立」），够不着的只听见后半句
  （「换过去仍是这条拒绝，走另外两条」）。而 04 号票的硬约束是**行为一格不变、
  拒绝时说什么逐字不变、黄金快照未变**，两件事直接顶上。对用户说什么是领域决定，
  不是这一处的实现细节。
- **What this ticket actually did:** **判定为不做，并把「为什么不做」写进
  `dither_outside_the_gate_error` 的文档**——挪得动、落点在哪、不做的理由是什么，
  三件事都写在那一处。眼下这句话仍**把话说全**：两条路各自的例外都写进去，
  用户照着敲不会撞第二次（`p2-loose-ends/03` 留的那两条用例照旧钉着）。
  代价是够得着出路的页也要读一遍与它无关的例外。
- **Whose call:** 拍板的人（这条拒绝要不要按页只说对这一页成立的那一半，
  连带重录黄金快照）
- **处置：** 待处理。

### Q107 — 认不认大小写按 `cfg!(windows)` 判，而 macOS 默认那套文件系统也不认

- **From:** 票 `p2-loose-ends/10`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/session/complete.rs` 的 `CASE_INSENSITIVE`
- **Why it did not block:** 票面点名的是 Windows，两条验收也只分「Windows」与「区分大小写的平台」
  两档。macOS 落在第三档：APFS 默认**不区分**大小写（能格成区分的，但默认不是）。
  本票判它走「区分」那一支——行为与落地前一字不差，因此不咬人，只是那儿敲小写仍补不出
  大写开头的卷。真要覆盖它，判据就不能再是编译期的 `cfg!`：同一台机上挂一块区分大小写的卷，
  按目标平台判就判错了。判得准的只有**运行期探**（往那一层按另一种大小写 `metadata` 一次），
  而那要在一个不记事、只 `read_dir` 一次的模块里每补一次多做一次系统调用——那是另一件事。
- **What this ticket actually did:** **按 `cfg!(windows)` 分两支**，macOS 归「区分大小写」那一支，
  理由写在 `CASE_INSENSITIVE` 的文档里。
- **Whose call:** 拍板的人（macOS 要不要一并放宽，以及要不要为此换成运行期探）
- **处置：** 待处理。

### Q108 — 剪不动的那几种 TOML 写法仍旧丢注释：那一路退回整份重排

- **From:** 票 `p2-loose-ends/09`
- **Kind:** 你确实拿不准的单项
- **Where:** `src/preset.rs` 的 `cut`、`header_of`、`rewrite`
- **Why it did not block:** 本票的验收要的是「其余预设、注释、排版全部原样留着」，
  而按节剪对**节写法**成立——那正是本模块自己写出去的形状（`write` 只有这一个形状），
  也是文档里教人手写的那一种（`no_preset_file_error` 印的那一段、模块抬头、`CONTEXT.md`）。
  剪不动的是另外几种同样合法的写法：点号键（`preset."漫画".taste.filter = …`）、内联表、
  多行字符串里冒出来的 `[`、转义过的名字、光秃秃一节 `[preset]`（点号键藏得进它正文）。
  那几种当场退回**整份重排**——每一份预设的内容一份不少，丢的是注释与排版，
  也就是本票落地之前的行为。不咬人有三条：写出去的与教出去的都是节写法；
  退回那一条**恒是对的**（`keeps_faith` 验过才作数，验不过才退），最坏是从前的样子；
  而真要把那几种写法也认下来，等于在本仓库里养一个 TOML 编辑端——
  本票判过不引 `toml_edit` 的理由，同样拦着自己手写一个。
- **What this ticket actually did:** **认得出的就剪，认不出的整份重排。**
  「认不出」的那张清单在代码里只有一处（`header_of` 的文档），上面那一段是这张票面
  自己要的复述；用例 `a_file_the_cut_cannot_fit_falls_back_to_a_rewrite` 钉住这条退路
  走得通——存与删都仍成，只是不好看。
  **屏上那两句话跟着收窄**：覆盖与删说出口的只有无条件成立的那两半（那一份自己的内容没了、
  其余几份照旧留着），注释那半句一个字都不提——一句只在多数文件上成立的话，
  比不说更坏。`CONTEXT.md` 的《会话》与本条同一口径。
- **Whose call:** 拍板的人（要不要把点号键与内联表那几种写法也认下来）
- **处置：** 待处理。

### Q109 — 输出归档带着写出那一刻的时钟，整个文件的字节因此两趟之间就不同

- **From:** 票 `volume-discovery/02`
- **Kind:** 撞上的现成缺陷
- **Where:** `src/sink.rs` 的 `ArchiveSink::write`（`SimpleFileOptions::default()`）
- **Why it did not block:** 票面第一条要的是「点名一个 `.zip`，产物与同内容 `.cbz`
  逐字节相同」。写这条用例时它**间歇性地红**——比的是两个输出归档文件的全部字节，
  而 `zip` 8 的 `SimpleFileOptions::default()` 把每个成员的修改时间填成
  `DateTime::default_for_write()`，即**当前时钟**（DOS 时间戳两秒一格）。
  两趟跨过一格就红，与读的是 `.zip` 还是 `.cbz` 毫无关系。
  成员那一层是稳的：名字与字节两趟完全相同，本票要证的那件事就在这一层。
- **What this ticket actually did:** **把「逐字节相同」测在成员这一层**——
  `a_zip_is_read_as_a_cbz_and_comes_back_out_as_a_cbz` 比的是 `read_cbz` 出来的
  成员名与成员字节，用例里写明了为什么不比整个文件。没有去动 `sink`：
  把时间戳钉成一个常量是**改产物的字节**，落在这张票之外（幂等按记录走、不看容器时间戳，
  因此不咬人；但那是一条给外部工具看的字段，钉不钉是拍板的事）。
- **Whose call:** 拍板的人（输出归档要不要做成可复现的字节——钉死成员时间戳）
- **处置：** 待处理。

### Q111 — 点名两个互相嵌套的路径，同一个卷会被发现两遍、各写一份输出

- **From:** 票 `volume-discovery/03`
- **Kind:** 本票造出来的新形状
- **Where:** `src/discover.rs` 的 `of`（一个点名路径一棵子树，各走各的）与
  `src/lib.rs` 的 `ensure_no_two_volumes_share_an_output`
- **Why it did not block:** `tonefit --out D:\out 库 库\作品` 这样点两处，
  `库\作品\第1话.cbz` 会被发现两遍：一遍来自 `库`，镜像成 `out\库\作品\第1话.cbz`；
  一遍来自 `库\作品`，镜像成 `out\作品\第1话.cbz`。**去处不同**，撞名那一道因此拦不住，
  同一卷做了两遍、写了两份。不咬人是因为两份都是对的产物、源库一个字节没动，
  代价只是白做一遍；而这不是本票造出来的**类**问题——从前点名这两个路径同样得到两个卷
  （一个 56 卷合一的怪物加一个），只是那时两边的内容不重叠。
  本票的「一页都不被处理两遍」说的是**一次发现之内**（卷与卷架不互斥那条规则），
  不是「用户点名的几棵树之间」。
- **What this ticket actually did:** **没有去重**。要拦它得在发现之后按**卷根**折一遍
  （而不是按去处），那要先答一个领域问题：两个点名路径重叠时，该拒绝（点名有歧义）
  还是该按最靠外那一个收编（去处只留一份）？两条都说得通，而选哪一条决定了
  报告里那一卷长什么名字。按本票的《硬约束》「其余一律自己判、记下来、往下走」记在这里。
- **Whose call:** 拍板的人（互相嵌套的点名是拒绝，还是折成一份）
- **处置：** 待处理。

### Q112 — 撞名那一道挪到预扫之后，点名一个不存在的路径时输出根探针会先动一次盘

- **From:** 票 `volume-discovery/03`
- **Kind:** 本票造出来的次序变化
- **Where:** `src/lib.rs` 的 `run`（开工前那几道检查的次序）
- **Why it did not block:** 撞在一起的是**发现出来的那些卷**，发现之前问不出来，
  `ensure_no_two_volumes_share_an_output` 因此从「预扫之前」挪到了「预扫之后」。
  它从前顺带承担了一件事：`source::planned_output` 会对每个点名路径调一次 `identity_of`，
  于是「点名的路径不存在」在**输出根写盘探针之前**就报出来了。如今那一步没了，
  点名一个不存在的路径时，`ensure_the_output_root_takes_a_write` 会先真建一次目录、
  写一个探针文件、再按自底向上收回去。盘上**不留东西**（那一道自己收拾），
  拒绝那句话与退出码 `1` 也一格没变，变的只是「拒绝之前动没动过盘」。
- **What this ticket actually did:** **留在这个次序上**，并在 `run` 里把理由写进注释。
  另一条路是让发现排在写盘探针之前——那样点名一个库、而 `--out` 根本用不了时，
  要白走一遍整棵树，正是 06 号票当初把探针提前的那条理由所反对的。
- **Whose call:** 自答（两害相权：白走一棵树 vs 拒绝之前多动一次盘）
- **处置：** 待处理。

### Q113 — 混装目录的输出去处同时是它兄弟卷的父目录，目录卷收尾时会把它们整个删掉

- **From:** 票 `volume-discovery/03`
- **Kind:** 本票造出来的缺陷
- **Where:** `src/sink.rs` 的 `DirectorySink::finish`（`remove_dir_all(self.path)` 那一句），
  与 `src/discover.rs` 定出来的那套去处
- **Why it did not block:** ADR 0014 决定第 1 条「一个目录可以既是卷又装着卷」落地之后，
  `N和S/`（一张封面 + 56 个 cbz）这一卷的去处 `out\N和S` **同时是**
  `out\N和S\第01话.cbz` 那些兄弟卷的父目录。目录卷收尾时把最终位置整个换掉——
  那是「不产出半成品」与「清掉陈旧产物」两条换来的，写在 `DirectorySink` 自己的文档里——
  于是父卷真被重写的那一趟会先 `remove_dir_all(out\N和S)`。
  **同一趟里不咬人**：发现是先序的，父卷恒是候选里的头一个，兄弟卷随后照写，末了的盘是对的。
  咬人的是**那一趟没走完**：中途按了收尾/中止，或某个兄弟卷做不成，上一趟好好的兄弟产物
  就跟着没了。源库一个字节没动，重跑一趟全回来，因此这不是不可逆的破坏；
  但 ADR 0013 那句「盘上因此只有完整的卷」在这个形状上弱了一格。
  顺带一笔：父卷没被幂等跳过的那一趟，兄弟卷的输出被删掉之后也一定重做——省不下来。
- **What this ticket actually did:** **没有动 `sink`。** 要修得改掉「最终位置整个换掉」
  这条决定（那是 `DirectorySink` 文档里明写的两条理由换来的），或者让收尾这一步知道
  「这个目录里还住着别的卷的输出」——后者要把发现的结果一路喂到 `sink`，
  而那一层至今只认识「一个卷写到哪儿」。两条都是拍板级的改动，不在这张票的范围里。
  本票把这笔代价写进了 ADR 0014 的《要认的代价》。
- **Whose call:** 拍板的人（目录卷收尾要不要保住「住在自己去处里的别的卷」）
- **处置：** 待处理。

### Q115 — 「列前几条，剩下的说个数」现在是第五处，而 Q49 记的还是四处

- **From:** 票 `volume-discovery/04`
- **Kind:** 路过发现
- **Where:** `src/render.rs` 的 `non_volume_tail`（本票新加的第五处 `const SHOWN`）；
  停车场 Q45（记的是三处）与 Q49（记的是四处）
- **Why it did not block:** Q45 的理由一字不变——共用的只有骨架（先说总数 → 缩进逐条 →
  「……另有 N 个」），条目长什么样各不相同。本票这一处是「路径一行、原因一行」，
  与 `failed_volume_tail`、`survey::refuse` 同形，**三者同形这件事本身**是抽公共件最有力的
  一条新证据：Q45 当初说「三处各不相同」，如今五处里有三处逐字同形了。
  但抽件仍是拍板级的决定（`SHOWN` 该不该三处共用一个数、界面层要不要一套「截断清单」），
  在这张票里动它等于顺手改掉既有两处的输出。
- **What this ticket actually did:** **照同一条办法写了第五处，没抽公共件**，
  并在 `non_volume_tail` 的文档里明写它是照 `failed_volume_tail` 那处办的。
  **没有去改 Q45 与 Q49 的正文**：那两栏是别的票的记录，后来的活不该写进别人的记录里
  （理由照 Q49 自己写的那一条，`docs/agents/issue-tracker.md`）。
  条数从四涨到五因此记在这里。
- **Whose call:** 与 Q45、Q49 同一个（拍板的人：要不要给界面层一套「截断清单」的公共件）
- **现状（`p3-session-legibility/02` 路过补记）:** 五处一处没动，条数仍是五。
  只有一处换了容身之所：`gate_line` 里那句「不成立：头几页，另有 N 页」现在是一
  [行](CONTEXT.md《会话》：行)——`RowKind::GateNote` 上一格成句的文字，
  截断仍在 `first_few_names` 里，一个字没改。抽公共件时那一处要连着「它是一行」一起看。
- **处置：** 待处理。

### Q116 — spec 自己对不上：《卷至少有一页》把「空目录」算进非卷文件那一栏，《非卷文件》却把那一栏定成三类**文件**

- **From:** 票 `volume-discovery/04`
- **Kind:** spec 内部不一致
- **Where:** `.scratch/volume-discovery/spec.md` 的《卷至少有一页》
  （「一页都没有的东西不是卷，归入非卷文件。字体包、源码包、**空目录**都落在那一栏」）
  与同一份 spec 的《非卷文件》（「三类，每条带路径与**一句为什么**：卷架上既不是页也不是
  归档的**文件**；一页都没有的**归档**；发现出来但点不开的**归档**」）；
  `CONTEXT.md`《处理对象》的「非卷文件」词条与《非卷文件》那三类一致
- **Why it did not block:** 两句话在「空目录算不算一条」上正相反。落地取了《非卷文件》
  那三类——票面的硬约束也是它，`CONTEXT.md` 的词条同样只说文件；而且反过来做会当场
  自伤：每个空目录、每个纯粹的卷架都占一行的话，一座正常的库（`作品/第N话.cbz` 两层）
  光卷架就能把清单刷满，而卷架是库的正常形状、不是一件要报的事。
  《卷至少有一页》那一段真正在说的是「输出里一个字节都没有」，那一点两种读法都成立。
- **What this ticket actually did:** **按《非卷文件》那三类落地**，并把「那张表列的是文件，
  因此空目录与纯粹的卷架不是卷、却一条也不留」这句分别写进了 ADR 0014 决定第 3 条——
  就地补一句，没有改 spec，也没有动 `CONTEXT.md` 的词条
  （`CLAUDE.md`：改写已有词条的含义要先拍板）。
- **Whose call:** 拍板的人（spec 的《卷至少有一页》那半句要不要跟着订正）
- **处置：** 待处理。

### Q117 — 发现出来的一个目录**读不动**时，至今一声不吭

- **From:** 票 `volume-discovery/04`
- **Kind:** 既有缺口（`03` 就在，本票把它的边界划清了但没有补上）
- **Where:** `src/discover.rs` 的 `push_children`（`read_dir` 出错就整棵子树跳过）、
  `src/survey.rs` 里点不开那一支的 `(Provenance::Discovered, Container::Directory)` 一格
- **Why it did not block:** 权限不足、盘拔了、路径太长——发现走不进的那个目录底下有没有卷
  谁都不知道，而这一趟从头到尾一个字都不说。本票新加的那张表**收不下它**：
  spec 的第三类写的是「发现出来但点不开的**归档**」，`CONTEXT.md` 的词条把三类都写成
  **文件**，ADR 0014 决定第 5 条也只说归档——而一个目录不是文件。
  本票因此把这一格明确成「跳过」，与 `push_children` 那句「列不动这一层就整棵子树跳过」
  同一条处置；改之前它是**碰巧**会进那张表的（`source::open` 对目录出错也走同一支），
  那才是真不一致：一个目录路径会印在一张自称只列文件的表上。
  咬人的场景是真的：NAS 上一个权限没配好的作品目录，整棵子树静默消失，
  而退出码是 `0`、报告上一行都没有。
- **What this ticket actually did:** **只划清了边界，没有补上那句话。** 补它要么给非卷文件
  加第四类（那要改 `CONTEXT.md` 的词条——拍板级），要么另开一栏「发现走不进去的地方」，
  两条都超出本票。本票在 `NonVolumeReason::Unopenable` 与 `survey` 那一格的文档里
  各指了一句到这里。
- **Whose call:** 拍板的人（读不动的目录该进非卷文件、另立一栏，还是照旧不说）
- **处置：** 待处理。

### Q118 — 闸门 2 的 `the_minutes_spent_deciding_are_charged_to_nobody` 五趟里红三趟，`overall()` 那两次读表之间被抢走就红

- **From:** 票 `volume-discovery/04`
- **Kind:** 既有缺陷（不是本票造出来的，但本票是撞得最狠的一次）
- **Where:** `src/session/live.rs` 的 `Live::overall`（`self.started.elapsed()
  .saturating_sub(self.deliberated())` 那一句）与 `the_minutes_spent_deciding_are_charged_to_nobody`
- **Why it did not block:** 那一句读了**两次表**：`started.elapsed()` 里一次（记作 T1），
  `deliberated()` → `since.elapsed()` 里又一次（T2，恒在 T1 之后）。于是
  `elapsed ≈ 300s + (决策点距开工那一小段) − (T2 − T1)`。
  前一项是 `Live::new` 到 `pass_started` 的几微秒，后一项正常是几十纳秒——
  **机器一忙，两次读表之间被调度器抢走，后一项就超过前一项**，
  `elapsed` 掉到 300s 以下一点点（实测 299.9999981s），
  而断言是 `waiting >= RAN_FOR`、**一格余量都没有**（那条用例的文档自己写着「断言不带余量」，
  它设想的抖动是 `rewind` 到断言之间的，没算上 `overall()` 自己内部这一道）。
  本机串行五趟红三趟，单跑该用例连绿；闸门 1 上它不红，只是概率问题。
  **与本票无关**已经确认到三条：`overall()` 不克隆报告，多一个字段进不了那两次读表之间；
  本票对 `live.rs` 的改动是 `Live::new` 里一行 `Vec::new()`，落在 `started` 记下之后、
  `rewind` 之前，只会让上式**前一项变大**，即让它更不容易红；
  库那一侧的计时路径一个字符没动。
- **What this ticket actually did:** **没有动它。** 修法是让 `overall()` 只读一次表
  （取一个 `now`，两处都拿它算），那是 `Live` 的计时口径问题，而计时口径归
  停车场 Q41 那一串；在这张票里改它，等于顺手改掉会话屏上那两个数的算法。
  本票据此报的闸门 2 数字取的是**没撞上它的那一趟**（580 通过 0 失败），
  撞上时 cargo 会在那个二进制上停下、后面的二进制不再跑，总数因此看着少一截。
- **Whose call:** 拍板的人（`overall()` 要不要改成一次读表；或那条断言要不要留一格余量）
- **处置：** **了结。** 排队那一侧就地收掉（`b5603d7`）：`overall()` 取一个 `now`，
  被减数与减数都拿它算，`deliberated` 因此改成收 `now`、自己一次表都不读。
  那个窗口按构造消失，不是靠给断言加余量掩掉——断言原样不动。
  闸门 2 连跑两趟 580 通过 0 失败。计时**口径**一个字没改（减的仍是等人那一截），
  因此不必等 Q41 那一串。

### Q119 — 摊开的卷两遍那一路仍按「归档卷是一条顺序扫」派，可它的字节已经在一个目录里了

- **From:** 票 `volume-discovery/05`
- **Kind:** 落地时走的那条路（不是缺陷，是一条没顺手改的口径）
- **Where:** `src/lib.rs` 的 `process_volume`（`IoPlan::decide(medium, request.io_mode,
  volume.container, cores())`）、`src/medium.rs` 的 `IoPlan::decide`
  （`container == Container::Archive` 那一支，`ChosenBy::ArchiveScan`）
- **Why it did not block:** 一个 `.7z` 卷摊开之后，`container` 仍是 `Archive`（输出也确实是
  一个 `.cbz`），于是两遍那一路照旧被按成 **1 条顺序扫**，报告上还印着
  「（归档卷是一条顺序扫）」。可那条理由在它身上不成立了——理由是
  「一个 ZipArchive 就是一个游标」，而摊开的卷读的是临时目录里一个成员一个文件，
  **与目录卷同形**，本该按介质派并发。同一处还牵着第二件事：介质是按**卷根**探的
  （那个 `.7z` 在哪块盘上），而摊开之后真正被读的是系统临时目录——两者可以不在同一块盘上。
  不阻塞是因为**慢一点不是错**：串行读一个目录照样读得对，幂等那一道本来就按介质派、
  没受影响，而 `--io-mode concurrent` 用户点得动。
- **What this ticket actually did:** **保持原样，一个字符没改。** 改它要么给
  `IoPlan::decide` 换一个入参（从容器形态换成读取形态），要么让摊开的卷把介质探到临时目录上——
  前者动的是报告里那一行的口径与 `medium.rs` 的一批用例，后者动的是 ADR 0009
  「介质按路径探测」那条决定的边界。两条都超出本票的范围（本票的决定全在 ADR 0015 里，
  而那一篇一个字没提读取计划）。
- **Whose call:** 拍板的人（摊开的卷该按目录卷派读取吗；介质该探源包还是探临时目录）
- **处置：** 待处理。

### Q120 — 摊开的临时目录靠析构收，进程被硬杀就留成孤儿，而缓存的溢写不会

- **From:** 票 `volume-discovery/05`
- **Kind:** 认下的代价（ADR 0015 那句「借溢写那套 `tempfile` 机制」读起来比实际强）
- **Where:** `src/source.rs` 的 `Extraction`（`tempfile::Builder::new().prefix("tonefit-").tempdir()`）、
  对照 `src/cache.rs` 的 `Spill::create`（`tempfile::tempfile_in`）
- **Why it did not block:** 溢写那个文件是**匿名**的——Unix 上建完就 unlink、Windows 上带
  delete-on-close，最后一个句柄一关内核就收，`cache.rs` 因此说得出「中断不留下孤儿临时文件，
  不必靠下次运行去扫」。摊开这一份**不能匿名**：成员要按名字读回来。它于是只能靠 `TempDir`
  的析构收。票面第二条要的三个时刻都成立（跑到一半在、跑完不在、中止之后也不在——
  中止走的是两级停，那一卷当场返回、卷跟着析构），但**硬杀（SIGKILL、断电、任务管理器）
  会在系统临时目录里留下一个 `tonefit-…`**，而没有任何一趟会去扫它。
  不阻塞是因为这正是系统临时目录本来的语义：那是要靠 OS 清理的那一类东西，
  而自己去扫等于建一份跨运行的状态，那是 ADR 0009 关掉的方向。
- **What this ticket actually did:** **在代码里把差别说清楚，没有加清理。** `Extraction` 的
  文档写明「靠析构收，不是靠内核收」，并点出硬杀那一种；目录名一律 `tonefit-` 打头，
  为的就是撞见它的人认得出是谁留下的。ADR 0015 那句话没改——它说的是「借那套 `tempfile`
  机制」，字面并不承诺匿名，但读的人容易顺着 `cache.rs` 那句话推过去。
- **Whose call:** 拍板的人（要不要在 ADR 0015 的《要认的代价》里补一句；要不要开工时扫一遍
  自己留下的 `tonefit-` 目录——那是跨运行状态，得先过 ADR 0009 那一关）
- **处置：** 待处理。

### Q121 — 摊开一整卷的中途没有检查点，几百兆的 `.7z` 上按中止要等它解完

- **From:** 票 `volume-discovery/05`
- **Kind:** 落地时走的那条路（新代码带进来的一个空档）
- **Where:** `src/source.rs` 的 `extract`（`for_each_entries` 那个闭包里没有停止信号）、
  `src/lib.rs` 的 `process_volume`（`source::open` 那一句排在卷边界检查点之后、页循环之前）
- **Why it did not block:** ADR 0013 的《后果》写着「`run` 要在**卷边界与页边界各留一个检查点**」，
  而摊开正好落在两者之间：卷边界那一道刚过、第一个页边界还没到。于是一卷几百兆的 `.7z`
  在摊开那一段里**按中止不会立刻停**，要等整卷解完、进到幂等那一道才停得下来，
  其间一条事件都不报（进度条停着不动）。票面第二条仍然成立——「中止之后临时目录不在」
  靠的是析构，而不是靠早停；不成立的是「立刻停」那半句。
  不阻塞是因为**补它要动 `source` 的公开签名**：`source::open` 得收一个停止信号
  （或收一个 `progress::Events`），而 `source` 至今对进度、事件、指令一无所知——
  那是一条跨模块的新依赖，本票的决定（ADR 0015）一个字没提它。
- **What this ticket actually did:** **没有补，把它标了出来。** `tests/container.rs` 的
  `a_seven_zip_leaves_no_temporary_directory_behind_even_when_the_run_is_aborted` 是
  **摊开完成之后**才答中止的（观察者要先看见那个目录才改口），它证的是「中止之后不留孤儿」，
  **不是**「摊开途中按得停」——那条用例的文档里明写着这一句，免得后来人以为这一格有人守着。
- **Whose call:** 拍板的人（`source::open` 要不要收一个停止信号；还是认下「摊开那一段停不住」
  并在 ADR 0013 的《要认的代价》里补一句）
- **处置：** 待处理。

### Q122 — `.rar` 的夹具是造好签进仓的二进制，页的生成器一改就要有 WinRAR 的人重造一遍

- **From:** 票 `volume-discovery/06`
- **Kind:** 落地时走的那条路（不是缺陷，是这个格式逼出来的一条与全仓不同的做法）
- **Where:** `tests/fixtures/rar/{solid,stored,encrypted}.rar` 三份二进制、
  `tests/fixtures/rar.rs`（`include_bytes!` 加造它们的那三条命令行）
- **Why it did not block:** 这仓库的夹具至今**全是生成出来的**——`tests/fixtures/mod.rs`
  抬头就写着「仓库里不放真实漫画素材：测试用的页全部由本模块按代码生成」，
  CBZ 按字节手搓、`.7z` 借编码端现造。`.rar` 造不出来：**Rust 这边没有写入端，
  将来也不会有**——UnRAR 许可禁止据它反推 RAR 的压缩算法，而那正是写一个 rar 打包器要做的事。
  票面第二条要真压过的固实包、第三条要真加密的包，两条都非真包不可。
  不阻塞是因为**代价是有界的**：三份加起来 14 KB，且用例一旦与夹具对不上会**当场红**、
  不会静默地过（包里那两页就是 `fixtures::cheap_page` 与 `fixtures::gradient(TINY)`
  编成的 PNG，产物比的是解出来的像素）。
- **What this ticket actually did:** **签进仓，并把重造它的办法写在夹具模块抬头。**
  三条 `Rar.exe` 命令行逐字记着（`-s -m5` / `-s- -m0` / `-hptonefit`，`-ts*-` 不存时间戳），
  谁手上有 WinRAR 谁就重造得出。只用 PNG、不用 JPEG，为的是把「`image` 换版本」
  这条会让签进仓的字节与现编的字节分道的路堵掉。
- **Whose call:** 拍板的人（这三份二进制该不该长住仓库里；要不要给它们配一条
  「有 WinRAR 就重造一遍并比对」的可选闸门）
- **处置：** 待处理。

### Q123 — 收下 `.rar` 之后构建 tonefit 多了一个 C++ 工具链的前提，仓库里没有一处说得出这件事

- **From:** 票 `volume-discovery/06`
- **Kind:** 落地带进来的一条新前提（不是缺陷，是没有落脚处）
- **Where:** `Cargo.toml` 的 `unrar-ng`（它底下 `unrar-ng-sys` 的 `build.rs` 用 `cc`
  编 UnRAR 的 C++ 源码）、`docs/agents/gate.md`（三条闸门，一句前提都没写）
- **Why it did not block:** 本机（Windows 11 + MSVC）上三条闸门全绿，编那份 C++ 只多约 40 秒，
  一次；`cargo check` 之后进增量缓存。不阻塞是因为**前提在**——票面点名的停线第 3 种
  是「前提根本不在」，而它在。可这条前提**本身是新的**：在 `06` 之前，tonefit 的依赖树里
  一个 C/C++ 编译单元都没有（`sevenz-rust2` 是纯 Rust 的，`05` 特意为此挑的它），
  clone 下来只要一个 Rust 工具链就构建得起来。如今换一台没装 MSVC / clang 的机器，
  第一条闸门就跑不起来，而报错来自 `cc`，与 tonefit 无关。
- **What this ticket actually did:** **没有写进任何文档。** ADR 0015 的《要认的代价》
  只写了许可那一条（「UnRAR 许可是一条真实的分发约束」），没写构建这一条；
  而票面明写「决定已经写死，不要回头改决定，只改状态行」，往那一篇加一条后果超出本票。
  仓库里也没有 README 或构建说明可以放它——`docs/` 下只有 `adr/`、`agents/`、
  `measurements.md` 三样。因此这条前提眼下只活在 `Cargo.toml` 那段注释里。
- **Whose call:** 拍板的人（补进 ADR 0015 的《要认的代价》，还是新开一处构建前提的说明；
  `docs/agents/gate.md` 要不要在三条命令之前先说一句「跑得起来需要什么」）
- **处置：** 待处理。

### Q124 — 分卷 `.rar`（`x.part1.rar` / `x.part2.rar`）会被当成两个卷，而 UnRAR 打开头一份就跨卷读完了

- **From:** 票 `volume-discovery/06`
- **Kind:** 落地时**没有处置**的一种输入（不是本票带进来的错，是格式集收下 `.rar` 之后才够得着的一种形状）
- **Where:** `src/source.rs` 的 `ARCHIVE_FORMATS`（`.rar` 只按扩展名认，一个 `partN.rar` 与
  一个整包分不开）、`rar_headers`（`open_for_listing` 会**自动续到下一卷**）、
  `src/discover.rs`（发现把每一个 `.rar` 各当一个卷）
- **Why it did not block:** 分卷是 rar 特有的形状——`.7z` 的分卷叫 `.7z.001`，扩展名根本不在
  格式集里，因此从来够不着；`.zip` 家族本仓也没有分卷这一路。收下 `.rar` 之后它第一次成立：
  一套 `第01卷.part1.rar` / `.part2.rar` 躺在库里，发现会认出**两个**卷，而 UnRAR 打开
  `part1` 时会自己把后续几份接着读完——于是头一卷列出的是整套的成员、出一份完整产物，
  第二卷再按 `part2` 自己那份头出第二份（成员多半带着 `SPLIT_BEFORE`），产物**重了一遍**、
  卷名也不同（`第01卷.part1.cbz` 与 `第01卷.part2.cbz`）。
  不阻塞是因为**票面与 ADR 都没提分卷**：票面四条验收一条不沾，ADR 0015 决定第 1 条写死了
  「判定只看扩展名、不去嗅内容」，而认出分卷恰恰要读归档头里的 `VOLUME` / `FIRST_VOLUME`
  标志——那是决定第 1 条要省掉的那一次。真撞上也不静默：两份产物都在盘上，看得见。
- **What this ticket actually did:** **一处没读，一个字没写。** `unrar_ng` 那一侧现成的东西
  （`ArchiveFlags::VOLUME` / `FIRST_VOLUME`、`Archive::is_multipart` / `as_first_part`）
  一个都没用上，也没有为它写用例——先定「一套分卷算一个卷还是不算卷」，代码才有得写。
- **Whose call:** 拍板的人（一套分卷是**一个卷**（那要读归档头，撞 ADR 0015 决定第 1 条），
  还是**非卷文件**（那要认得出 `partN` 这种名字），还是照今天这样各出各的产物；
  定了之后是 ADR 0015 补一条，还是另开一张票）
- **处置：** 待处理。

### Q125 — ADR 0015 说「`.rar` 与 `.7z` 两者默认都是固实归档」，`.rar` 那一半不成立

- **From:** 票 `volume-discovery/06`
- **Kind:** 文档与事实对不上（撞见的，按 `CLAUDE.md` 的规矩记下来、没有顺手改）
- **Where:** `docs/adr/0015-archive-formats-and-how-they-are-read.md` 第 15 行
  （《背景》）与第 69 行（《要认的代价》）
- **Why it did not block:** 原话是「`.rar` 与 `.7z` 则是另一件事。两者默认都是**固实归档**」，
  以及「按格式一刀切，非固实的 `.rar`（**打包时关掉了固实**）也照样摊开」。
  两句都假定固实是 `.rar` 的默认。**本票造夹具时实测不是**：WinRAR 的 `Rar.exe`
  不加 `-s` 造出来的包 `is_solid()` 为假——固实要**显式打开**（`-s`），
  不是「打包时关掉」。`.7z` 那一半照旧成立（7-Zip 默认固实）。
  不阻塞是因为**决定不靠这个前提站住**：决定第 3 条的理由是「不逐卷探」，
  而不逐卷探本身由「探要先读一遍归档头」撑着，与默认是什么无关；
  真实库里的 `.rar` 多半是别人打的，默认值也决定不了那一批到底固实不固实。
- **What this ticket actually did:** **一个字没改。** 票面明写「决定已经写死，不要回头改决定，
  只改状态行」，而这两句一句在《背景》、一句在《要认的代价》，都不是状态行。
- **Whose call:** 拍板的人（把那两句订正成「`.7z` 默认固实，`.rar` 默认不固实、固实要显式打开」；
  以及顺带重估《要认的代价》那一条——如果流通中的 `.rar` 多数不固实，
  「一刀切照样摊开」白付的那次全量写盘就不是少数情形，而是常态。
  要不要因此回到《备选方案》里「逐卷探固实与否」那一条，是同一个人的事）
- **处置：** 待处理。

### Q126 — ADR 0012 决定第 1 条那句「续做只在处理范围是单卷时成立」与改过的决定第 3 条对着说

- **From:** 票 `volume-discovery/07`
- **Kind:** 票面的指示落地之后，同一篇 ADR 里两处对不上
- **Where:** `docs/adr/0012-resume-the-second-pass-after-a-trial.md` 决定第 1 条头一句，
  以及《后果》的「**续做的好处只有单卷会话吃得到**」那一条
- **Why it did not block:** 票面明写「**只动决定第 3 条**并加一节。决定第 1 条那条内存理由
  **留在原处**」，而决定第 3 条改成「每一次试算都逐卷等答话」之后，决定第 1 条那句
  「续做只在处理范围是单卷时成立。多卷时试算与执行各跑一趟」按字面已经不成立——
  会话现在多卷也续做。**不阻塞是因为那条理由本身没错**：它否掉的是「一次押住**全部卷**
  的参照」那种续做，而逐卷决策点不是那一种，两句说的不是同一件事。
- **What this ticket actually did:** 决定第 1 条与它的理由**一个字没改**，改的只有决定第 3 条。
  新加的那一节《决定第 1 条那条内存理由不覆盖逐卷决策点》末尾点明了这句话的适用面
  （「说的是那种一次押住全部卷的续做；会话在决策点上等不等人，以决定第 3 条为准」），
  顺带把决定第 5 条那句「单卷试算走 `Retention::Keep`」也点了一句（现在是每一次试算都走）。
  **《后果》那一条改了**：它整条都是决定第 3 条的后件，而它说的正是本票推翻的那件事
  （「批量跑多卷时那条回路仍付两遍的价……批量跑 20 卷时没人盯着报告改参数」）。
  留着它就等于同一篇文档里两处对着说，而《后果》不是票面点名要留的那半。
  改后那一条说的是新的代价：批量跑要一卷一答，「剩下的卷都这样」就是为这一下加的。
  同一节里「两条路的 dry-run 不完全同形（**会话单卷那条**留参照……）」去掉了那个「单卷」——
  那条路现在是每一次试算都走。《备选方案》首条那句「**单卷限定之后**，本决定的复杂度
  已经压到「库在一个点上问一句」」同理，改成「等不等人整个落在会话那一侧之后（决定第 3 条）」
  ——那条备选的**结论一个字没动**，改的只是它凭的那个前提的说法。
- **Whose call:** 拍板的人（要不要把决定第 1 条那句话本身改写成「一次押住全部卷的续做
  只在单卷时成立」——那是领域决定，不是实现细节，按 `CLAUDE.md` 的规矩没有顺手改）
- **处置：** 待处理。

### Q128 — 画法里那几处 `super::press` / `super::expand` 指的其实是 `terminal::press` / `terminal::expand`

- **From:** 票 `p3-session-legibility/01`
- **Kind:** 路过发现的无关缺陷（仓库通例，不是本票引入的）
- **Where:** `src/session/draw/report.rs` 的 `expandable` 与 `opens_at` 两处文档；
  同一副写法在 `src/session/state.rs`、`src/session/run.rs`、`src/session/live.rs` 共十来处
- **Why it did not block:** 那几处**本来就指不到东西**——`press` 与 `expand` 住在
  `src/session/terminal.rs` 里，从 `session` 那一层看 `super::press` 就已经落空。
  而 bin crate 根本不进 `cargo doc`（与 lib 同名，cargo 跳过它），一条告警都不会多。
- **What this ticket actually did:** 按搬家的深度**补了一级**（`super::press` →
  `super::super::press`），意思与搬家前逐字相同（仍指「session 那一层的 press」）；
  **没有**改成准确的 `terminal::press`——那是票面明写不许做的「顺手能改好」的一类。
  `mod tests` 里 `expanded` 那个夹具的文档上那一处原样留着：它在搬家前就已经多差一级
  （从 `draw::tests` 看 `super` 是 `draw`），补级并不使它变准。
- **Whose call:** 无（要不要把仓库里那十来处一并改成 `terminal::press` 是一次独立的小清扫）
- **处置：** 待处理。

### Q137 — 票面要「补全候选也画得出滚动条」，可 spec 的表说那一处没有光标

- **From:** 票 `p3-session-legibility/04`
- **Kind:** 验收条读着含糊（票面与 spec 对不上）
- **Where:** 票面第二条 ⇄ spec《Implementation Decisions》第四条那张表；
  `src/session/draw/footer.rs` 的 `listed`
- **票面原话:** 「左栏、预设栏、补全候选三处都画得出滚动条；没有可滚的东西时不画」
- **现状:** spec 那张表给补全候选的「光标」一栏写的是**无**，票面第三条要它
  「列得下几条列几条并说得出还剩多少条」。没有光标就没有一个键翻得动它，
  而滚动条指的正是「按上下键还翻得到的东西」——画一条指向一个按不动的地方。
  屏底那一格又是一整块 `Paragraph`，一两行高的轨道在终端库那个 widget 上
  （两头各去一格箭头）根本画不出东西来。
- **What this ticket actually did:** 三处**共用同一份视口**（票面第五、第六条照办），
  **滚动条只画在左栏与预设栏两处**；补全候选那一处按票面第三条说「…还有 N 条」，
  那个数由同一个视口的 `hidden` 出。哪一处画、哪一处不画写进了 `Viewport` 那张表。
  票据里那一条验收勾了，但在它下面写明只落实了两处，指向本条。
- **Whose call:** 拍板的人（票面第二条与 spec 那张表相抵，改哪一边是规格的事，
  不是实现能自己定的；实现走的是 spec 那一边）。
- **处置：** 待处理。

### Q141 — 票面的「八块面板」「二到五个型号」与 spec 的「31 个内置型号」，三个数都与内置表对不上

- **From:** 票 `p3-session-legibility/06`
- **Kind:** 票面给的数与实现量出来的对不上
- **Where:** 票面第一段；`.scratch/p3-session-legibility/spec.md` 的
  《Implementation Decisions》第三条；`src/profile.rs` 的 `DEVICES` 与
  `Profile::devices_by_panel`
- **票面原话:** 「型号那一行摊开的不是 31 个名字，是**八块面板**……`→` 下钻到那块面板下的
  型号（**二到五个**）」；spec 的《取值栏》：「型号那一行摊开的是面板（**八块**……）」
- **现状:** 数出来是**34 个型号、12 块面板**，每块面板底下 **1 到 7 个**型号：
  `EINK_1264X1680_300` 7、`EINK_1072X1448_300` 7、`EINK_1404X1872_227` 5、
  `KALEIDO_1264X1680_300` 3、`KALEIDO_1404X1872_227` 2、`EINK_824X1648_300` 2、
  `EINK_1650X2200_207` 2、`EINK_1440X1920_300` 2、`KALEIDO_1072X1448_300` 1、
  `EINK_1860X2480_300` 1、`EINK_1404X1872_300` 1、`EINK_1236X1648_300` 1。
  票面那几个数多半是 `p1-session` 那一批写下的，后来内置表加过型号（Kaleido 那几个）。
- **What this ticket actually did:** **照实现走，一个数都不写死**。摊开那一列的长度、
  每一格的字面、下钻进去列几行，全从 `Profile::devices_by_panel()` 读——
  内置表里加一个型号、多一块面板，屏上当场跟着变，没有一处要跟着改。
  用例同样不抄那几个数：`the_model_row_unfolds_into_panels` 与
  `drilling_into_a_panel_lists_the_models_that_share_it` 都拿那个函数的返回值逐格对。
  **「八行放得下」这类布局假设一处都没写**：12 块面板加「没挑」那一格共 13 行，
  24 行的终端上要靠 `p3/04` 那套视口滚，快照那一张因此取的是 36 行的格子。
- **Whose call:** 拍板的人（票面与 spec 那三个数要不要改口；改口是文档的事，
  实现这一侧不认那几个数）。
- **处置：** 待处理。

### Q145 — 票面说「顶上从三个框九行变成一个框五行」，而屏上只有两个框六行

- **From:** 票 `p3-session-legibility/07`
- **Kind:** 票面给的数与实现量出来的对不上
- **Where:** 票面第一条验收；票面第二段；`src/session/draw.rs` 的 `main_pane`
- **票面原话:** 「顶上从三个框九行变成一个框五行，整块钉住，下面的报告滚它不动」；
  「要修的毛病有两处：顶上九行里有一半是重复的（摘要第一行几乎就是全局条本身）」
- **现状:** `main_pane` 今天只切**两个** 3 行的框——全局条与当前卷条，第三格是报告区
  那一整块 `Min(0)`。画法里搜不到任何叫「摘要」的东西：`p1-session/09` 立的是
  「三段各占一格」，而那三段的第三段**就是报告区**。「三个框九行」按字面因此不成立；
  票面多半把「本票要新加的那个摘要」当成已经在屏上了。
- **What this ticket actually did:** **照屏上量到的事实做**：合并前是两个框六行，
  合并后是**一个框**——出事行在场时六行（四行正文加两条边）、不在场时五行。
  没有为了凑「九行」硬拆一个框出来，也没有先画一个摘要再把它合并掉。
  票面那句「顶上九行里有一半是重复的」在实现里换了个落点仍然成立且被修掉了：
  卷数与剩余时间从此**只在抬头上说一次**（从前全局条正文里也有一份），
  收场之后「已用」也让给抬头那个「用了」——同一个数不在这一块里出现两次。
  模块文档（`src/session/draw/overview.rs` 的开头）把「合成一块之前是两个框六行」写死，
  下一个读的人不必再量一遍。
- **Whose call:** 拍板的人（票面与验收表那句「三个框九行」要不要改口成「两个框六行」）。
- **处置：** 待处理。

### Q146 — 试算那一副的出事行里没有失败页与卷级失败，而试算同样出得了这两种

- **From:** 票 `p3-session-legibility/07`
- **Kind:** 票面没想到的一种情形
- **Where:** 票面第三段；`.scratch/p3-session-legibility/spec.md` 的
  《Implementation Decisions》第五条；`src/session/draw/overview.rs` 的 `trouble_row`
- **票面原话:** 「**试算**：结论行给判定分布（`6 卷 2bit+FS   2 卷 4bit+FS`），
  出事行给要注意的（特例页几张 · 宽溢出几页 · 几何门不成立几卷）。
  **执行**：结论行给完成与跳过几卷，出事行给隔离几卷 · 失败几页 · 卷级失败几卷。」
- **现状:** 两张单子是**互斥**的，而失败页与卷级失败在试算那一趟里照样出得来：
  解不出尺寸的页在第一遍就失败（试算只是不写第二遍），卷根被删掉的卷同样记一笔卷级失败。
  照票面走，一趟试算里坏了三页、废了一卷，总览块的出事行**一个字都不说**——
  而那正是这一块存在的理由（「这一趟到底怎么样」）。报告区与报告末尾那几小结说得出，
  但那要往下翻。
- **What this ticket actually did:** **照票面走，两张单子按 mode 二选一**，
  试算那一副只数特例页 · 宽溢出 · 几何门不成立。理由是本票的验收第二条明写
  「结论行与出事行按试算／执行给不同的内容」，而把三样合成六样就等于两副合成一副——
  那是改 spec 第五条的决定，不是落地的自由度。落地记录与快照
  （`the_overview_of_a_dry_run_that_finished`）钉住的正是票面那三样。
- **Whose call:** 拍板的人（试算那一副要不要把「失败几页 · 卷级失败几卷」也带上；
  带上的话两副各四到六样，行宽在 80 列上就要砍）。
- **处置：** 待处理。

### Q148 — 出事行的失败页数的是收摊了的卷，比同一屏的报告区晚一整卷

- **From:** 票 `p3-session-legibility/07`
- **Kind:** 票面没想到的一种情形
- **Where:** `src/session/draw/overview.rs` 的 `trouble_row`；
  `src/session/live.rs` 的 `Live::failed_pages`；`src/render.rs` 的 `isolation_tail`
- **现状:** 出事行的「失败 N 页」走 `Report::failures()`，那一份只含**收摊了的卷**；
  而同一屏的报告区走 `crate::render::failing_pages(live.failed_pages())`，
  失败页**出现的当场**就画出来了。于是有一段真实存在的窗口：报告区已经写着
  「失败页（出现的当场……）解不出完整尺寸：JPEG 数据截断」，而钉在它上面、
  专门答「这一趟到底怎么样」的那一行一个字都不说；等那一卷收摊，出事行才冒出来，
  同一刻这一块从五行涨到六行，报告区整块跟着挪一行。
- **What this ticket actually did:** **仍旧读报告那一份**，不换成事件流那一份。
  理由是本票立的形状：出事行的数与措辞要与**报告末尾那几小结**对得上
  （`the_trouble_row_counts_what_the_report_tail_counts` 两头对着问），
  换成事件流那一份就有了两个出处，而两者只在「一卷收摊之后」才必然相等。
  失败页出现的当场在报告区已经看得见，丢的只是「钉住那一行也说一句」。
- **Whose call:** 拍板的人（出事行要不要读事件流那一份，代价是与报告末尾那几小结
  在一卷跑到一半时对不上）。
- **处置：** 待处理。

### Q149 — 决策点上答出「继续」的那一瞬间，钉住的这一块换内容、并可能矮一行

- **From:** 票 `p3-session-legibility/07`
- **Kind:** 票面没想到的一种情形
- **Where:** `src/session/draw/overview.rs` 的 `settled_row` 与 `trouble_row`；
  `src/session/live.rs` 的 `Live::mode`
- **票面原话:** 「**结论行与出事行的内容随这一趟是什么而变**，而「是什么」手上已经有，
  不必多一个开关」
- **现状:** `Live::mode()` 在决策点上答出第一个继续的那一刻从 `DryRun` 翻成 `Process`
  （那一卷真写了出去）。屏上因此**同一帧里**：结论行从「判定分布」换成「完成 · 跳过」，
  出事行从「特例页 · 宽溢出 · 几何门不成立」换成「隔离 · 失败页 · 卷级失败」——
  前一副几乎恒有内容、后一副此刻多半全是零，于是出事行整行消失、这一块矮一行、
  下面的报告整块上移。而那一帧正是用户刚看完判定、按下 `x` 的那一下，
  他刚才据以拿主意的那份判定分布同时也没了。
- **What this ticket actually did:** **照票面走，不多一个开关**：`Live::mode()` 是「这一趟
  落过盘没有」的唯一出处，报告抬头（`render::header` 的 dry-run 那一行）跟的也是它，
  总览块另立一个判据就等于屏上有两个说法。这一下过渡票面没交代，记在这里。
- **Whose call:** 拍板的人（续做那一趟的总览块要不要在收场之前一直按试算那一副画，
  或者两副都给）。
- **处置：** 待处理。

### Q150 — 《会话》那句「会话那两条横条边跑边画的『已用』与『剩』」已经不准

- **From:** 票 `p3-session-legibility/07`
- **Kind:** 词汇表／散文与实现对不上
- **Where:** `CONTEXT.md` 的《会话》，**决策点**词条底下那一段引文的末尾
- **原话:** 「决策点上等人的那段时间**不算进计时**……卷级计时与整趟耗时各自减掉它，
  会话那两条横条边跑边画的『已用』与『剩』也减掉它。」
- **现状:** 三处对不上：① 会话已经没有「两条横条」了，是**一块总览**（新词条就在同一张表里）；
  ② 「剩」不在正文那两行上，在**抬头**上；③ **等答话时抬头压根不画「还剩」**
  ——那一刻横条一动不动，报一个数出来说的就成了「用户还要想多久」，
  而这句话说的正是等答话那段时间。那句话要说的**实质**仍然成立：屏上那两个数都减掉了
  在决策点上等人的那一截（`Live::overall` 一处答完）。
- **What this ticket actually did:** **一个字没改**（`CLAUDE.md`《改 CONTEXT.md 的规矩》：
  撞见词汇表与实现对不上，记进停车场，不要顺手改），只在《会话》里**加了新词条**
  「总览 (Overview)」——票面第八条明确授权的那一条。
- **Whose call:** 拍板的人（那一段引文改写成「总览块边跑边画的『已用』与『还剩』」，
  并补一句「等答话时不画『还剩』」）。
- **处置：** 待处理。

### Q151 — 没做成的那几卷排在收摊了的那几卷后面，不是按跑完的先后

- **From:** 票 `p3-session-legibility/08`（卷表）
- **Kind:** 票面没想到的一种情形（数据里没有复原它所需的东西）
- **Where:** `src/session/draw/table.rs` 的 `entries`；`tonefit::Report` 的
  `volumes` 与 `failed_volumes` 两列；`src/session/live.rs` 的 `volume_finished`
  与 `volume_failed`
- **票面原话:** 「**不重排**：卷按跑完的先后添上去，出事的靠记号跳出来、不靠位置——
  重排会让刚跑完的那一卷在屏上跳位置。」
- **现状:** `Report` 把两种卷**分成两列**存：做出了东西的在 `volumes`，没做成的在
  `failed_volumes`。两列各自按先后，但**跨列的先后没有一处记着**——
  卷报告上只有时长（`VolumeTiming`），没有起止时刻。卷表因此只能先摆 `volumes`、
  再摆 `failed_volumes`：第三卷没做成、第四卷做成了时，屏上它俩的先后是反的。
- **Why it did not block:** 这一条要修就得动库那一侧的 `Report`（多一列合并的次序，
  或给卷报告加一个序号），而票面写着「库这一侧一个 seam 都不新增、也不改动」
  （spec 的《Seam》）。会话这一头自己攒一份次序也不成：那条线程回来时
  `Live::returned` 把攒的那一份**整个换成**库交出来的报告（只有库减得掉在决策点上
  等人的那几分钟），攒出来的次序到收场那一刻就没了。
- **What this ticket actually did:** 照上面的形状摆，并把这条例外写进
  `src/session/draw/table.rs` 的模块文档《不重排》——**屏上仍不按轻重排序**
  （那才是票面那条要挡的事），只是没做成的那几卷聚在末尾。行首记号照旧是找它们的路。
- **Whose call:** 拍板的人（要不要给 `Report` 一个跨两列的先后，代价是动库那一侧的形状）
- **处置：** 待处理。

### Q152 — 《视口》那条列的三处里没有报告区，而卷表这一票把它接了进去

- **From:** 票 `p3-session-legibility/08`
- **Kind:** 词汇表与实现对不上
- **Where:** `CONTEXT.md` 的《会话》，**视口 (Viewport)** 那条词条的末一句
- **原话:** 「左栏、预设栏、补全候选**共用这一份**」
- **现状:** 本票把报告区的卷表也接到了 `Viewport` 上（光标恒停在末行，算出来的起点
  就是从前那个「滚到底」，另外多了一条滚动条）。共用它的因此是**四处**，
  而词条里只列了三处。spec 的《一套视口》那张表本来就把「卷表 / 逐页表」算作四处之一，
  是词条落在了实现前面。
- **Why it did not block:** 词条那半句是**名单**，不是定义——「滚动量是算出来的，
  不是记着的」那半句一个字都不用改，卷表走的正是它。
- **What this ticket actually did:** **一个字没改**（`CLAUDE.md`《改 CONTEXT.md 的规矩》：
  改写已有词条要先拍板）。代码那一侧的名单**已经改了**——
  `src/session/viewport.rs` 的 `Viewport` 上那张表是「哪几处共用它」的唯一出处
  （词条本身也引它），本票把卷表那一行加了进去。
- **Whose call:** 拍板的人（词条那半句补上「报告区的卷表」，或改成引代码里那张表）
- **处置：** 待处理。

### Q155 — 报告末尾那几小结整段一种色：六小结分属三档语义，而它是一段拼出来的文字

- **From:** 票 `p3-session-legibility/09`
- **Kind:** 本票认下的一处折扣
- **Where:** `src/session/draw/report.rs` 的 `collapsed`（末尾那几小结那一段），
  出处在 `src/render.rs` 的 `tail`
- **现状:** `tail` 把六小结（非卷文件 · 宽溢出 · 兜底上界 · 部分救回 · 隔离 · 卷级失败）
  拼成**一段文字**交出来，而这六件事按 spec 的《语义色》分属三档：宽溢出 · 兜底上界 ·
  部分救回是「注意」，隔离也是「注意」，卷级失败是「出事」，非卷文件连失败都不是。
  一段文字只上得了一种色，本票因此给它「平常」——**末尾那几小结一个颜色都没有**。
- **顺带落空的一档:** spec 的《语义色》把**兜底上界**列在「注意」里，而屏上眼下
  **只有末尾那一小结说得出它**（逐页那一行上的 `Field::Backstop` 要等 `p3/11` 的逐页表）。
  这一段整段给了「平常」，那一档因此在本票的地界里一处都没上色。
- **那一档已经收掉（`p3-session-legibility/11`）:** 逐页表上兜底上界改过的页
  是[要紧的页](CONTEXT.md《会话》)之一，行首挂 `!`、整行走 `Tone::Caution`、
  行尾另有「兜底上界」那个词（`src/session/draw/pages.rs` 的 `says`）。
  **本条剩下的那一半原样成立**：末尾那几小结仍是一段拼出来的文字、仍整段一种色，
  这一条要拍的板还是那个——`tail` 要不要出「一小结一段」。
- **Why it did not block:** 别的五件表上或总览块上都有着色的位置（隔离、卷级失败逐卷各占一行，
  宽溢出 · 几何门不成立 · 特例页在总览块的出事行上，部分救回在那一卷那一行底下），
  少的是「同一件事第二处也上色」；只有兜底上界是真的一处都没有，而它有一张自己的票在后面。
- **What this ticket actually did:** 整段给 `Tone::Plain`，并把**为什么**写在 `collapsed`
  那一行注释上：按小结分色要先把 `tail` 拆成一小结一段，而拆它就是把措辞挪进画法这一层
  （ADR 0016：措辞只有一处出处）。**一个字都没动 `render`。**
- **Whose call:** 拍板的人（要不要让 `tail` 出「一小结一段」，代价是措辞那一层多一个出口），
  或者收报告区那几段的下一张票
- **处置：** 待处理。

### Q156 — 「过期副本」在四种语义里一档都不占

- **From:** 票 `p3-session-legibility/09`
- **Kind:** 规格没说
- **Where:** `src/session/draw/table.rs` 的 `under`（摆在一卷那一行底下的那两句）
- **现状:** spec 的《语义色》那张表逐条列出了四档各管哪几件事，**过期副本不在其中任何一档**。
  它与它并排的那一句「部分救回」是两种处境：后者明写在「注意」那一档里，
  前者说的是「盘上还躺着上一趟写下的那一份，这一趟没覆盖它，删不删由你」——
  既不是失败，也不是判定上要留神的事，可它也不像「正常跑完」。
- **Why it did not block:** 四档里「平常」正是「不必特别看它」那一档，摆进去不撒谎；
  而那一句本身成句、说得清清楚楚，不靠颜色。
- **What this ticket actually did:** 过期副本那一句给 **`Tone::Plain`**（部分救回给
  `Tone::Caution`，照 spec 那张表），并把这条选择写进 `under` 的文档：
  **不为它编第五档**——编出来的那一档在 `Tone` 上没有对应的语义。
- **Whose call:** 拍板的人（过期副本该不该进「注意」那一档）
- **处置：** 待处理。

### Q157 — 屏底那一句没有轻重之分：跑不起来的那一句与「存下了」那一句同色

- **From:** 票 `p3-session-legibility/09`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/session/state.rs` 的 `Session::notice`（`complain` 与 `says` 那一个出口）、
  `src/session/draw/footer.rs` 的 `footer`（屏底最后那几行）
- **现状:** spec 的《语义色》那张表里「用在哪」一列没有屏底那一句。而那一格装的是两种话：
  跑不起来的那几种（型号没挑、输出根没填）、标定图写不出去，与「存下了一份预设」
  「再按一次 `d` 删掉它」摆的是同一个位置、同一个样子。
  屏上说「出事」的地方因此少一处——按 `x` 却跑不起来时，那一句与刚才那句成功的话长得一样。
- **Why it did not block:** 上色要先分得出轻重，而 `notice` 眼下是一个裸 `String`——
  给它挂一档语义要动状态机那一侧的形状（`Session::complain` 与 `says` 两个出口），
  而本票的地界是画法这一层；spec 的表里也没有它。
- **What this ticket actually did:** **一格没动**，屏底那一句照旧不上色。
  本票只上了表上那几行、总览块的抬头与出事行、失败页那一段、只读时的左栏——
  四处都在 spec 那张表的「用在哪」一列里。
- **Whose call:** 拍板的人（屏底那一句要不要分轻重），或者动屏底那一格的下一张票
- **处置：** 待处理。

### Q158 — `NO_COLOR` 那一张「快照」落成了两次现画对比，不是一张录下来的常量

- **From:** 票 `p3-session-legibility/09`
- **Kind:** 与规格的一处形状差别
- **Where:** `src/session/draw/paint.rs` 的
  `the_same_screen_reads_the_same_with_or_without_colour`
- **现状:** spec 的《Testing Decisions》把「不上色（`NO_COLOR`）的一张」列在**快照矩阵**里，
  而本仓库的快照体例是把那一屏抄成一个 `const &str`（`same_screen` 逐行比）。
  本票没有抄：它**当场画两次**（`paint::forcing` 按住上色与不上色），直接比那两张。
- **Why it did not block:** 票面第五条要的是「两张的**文字**逐格相同」，而抄成两份常量
  恰恰答不出这一条——两份会被一起改掉，同时改错也照样绿。现画两次的那一比是**恒等式**，
  改了任何一处当场红；两头再各问一句「确实上了色／确实没上色」，把它比空的路也堵上。
- **What this ticket actually did:** 照上面那条做，并把理由写进那条用例的文档与票据。
  矩阵别的那几格（宽×窄、试算×执行、收起×展开、跟随、等答话、取值栏、`?` 那张表）
  不在本票的地界里。
- **Whose call:** 收快照矩阵的那张票（要不要为不上色那一副再录一份常量）
- **处置：** 待处理。

### Q159 — 卷表上的光标跳过没做成的那几卷，而它们在表上占着一行

- **From:** 票 `p3-session-legibility/10`
- **Kind:** 路过发现
- **Where:** `src/session/live.rs` 的 `Live::volumes`（光标走的那一列）、
  `src/session/draw/table.rs` 的 `Entry::at`（没做成那一行是 `None`）
- **现状:** 报告区那个光标停得住的是**说得出报告的那几卷**——收摊了的那几卷，
  加上决策点上攒着的那一份。**没做成的那几卷停不上去**：它们连一份卷报告都没有
  （`VolumeFailure` 只有一句原因），逐页那几行无从谈起，光标停上去按 `⏎` 没有第二步。
  表上它们照旧占一行（红、行首 `✗`、那句原因跟在行尾），于是 `↑↓` 一路按下去
  会**从它们上面跨过去**——屏上看得见一行，光标却停不上。
- **Why it did not block:** 两条路都说得通，而本票没有依据挑一条：
  停得上去要给它一个「按了有话说」的展开（与 `t`／`x` 在型号没挑时的待遇同一条），
  停不上去则是「屏上不摆按不动的键」的字面。两条都不影响这一票要的那几件事
  （选一卷、展开它、跟随停不停）。
- **What this ticket actually did:** **走了「停不上去」那一条**，并把理由写在
  `live::Volume` 的文档上（「没做成的那几卷不在这里」）与 `table::Entry::at` 上。
  ADR 0017 的《后果》把这一笔当作要认的代价记着。
- **Whose call:** 拍板的人（光标要不要停得上没做成的那几卷；要的话，`⏎` 在那一行上说什么）
- **处置：** 待处理。

### Q160 — 等答话时左栏不压暗、抬头也不改口，而那一刻三层同样只读

- **From:** 票 `p3-session-legibility/10`
- **Kind:** 既有实现在新说法下露出的一处不齐
- **Where:** `src/session/draw/config.rs` 的 `config`（`running` 那个判断与
  `READ_ONLY_TITLE`）
- **现状:** 「三层只读」现在有了唯一的判据——`Stage::read_only`，**跑着与等答话都算**
  （ADR 0017 决定第 3 条），按键表照它一个改动键都不派。而**画法那一侧只认「跑着」**：
  等答话时左栏各行不压暗、抬头照旧写着「配置」。屏上于是有一刻是「按不动、却不说」，
  而那正是 `CONTEXT.md` 的《会话》要防的那件事（「只读要在屏上看得出来，
  不能是按了没反应」）。**这不是本票引入的**：一维时 `Mode::Deciding` 就已经这样了。
- **Why it did not block:** 改它要动**措辞**——`READ_ONLY_TITLE` 那一句写死了「跑着」
  （「配置 · 跑着，三层都只读」），等答话时它是句假话，因此不是把判断换掉就完事，
  得先定下等答话时那一格抬头写什么。票面第四条要的是「一个改动键都不派」，
  那一条已经结构上成立（`stage_action` 一个改动键都没有）。
- **What this ticket actually did:** **画法那一侧一个字没改**（压暗仍只给跑着那一种），
  并在 `config` 里把两个判断的分别写明：反白按「焦点 + 只读」给，压暗按「跑着」给。
  按键那一半照两维走，屏上那一半留在原处。
- **Whose call:** 拍板的人（等答话时左栏压不压暗；压的话那一格抬头的措辞）
- **处置：** 待处理。

### Q161 — 展开着且等答话时 `a` 归答话，「列全部页」那一下按不动

- **From:** 票 `p3-session-legibility/11`
- **Kind:** 两个意思撞在同一个键上
- **Where:** `src/session/state.rs` 的 `expanded_action`（那一支上的 `if !matches!(stage, Stage::Deciding(_))`）、
  `src/session/draw/footer.rs` 的 `expanded_prompt`
- **现状:** 票面第二条要 `a` 切到全部页；而 `a` 在**等答话**那个阶段上早就有主——
  「剩下的卷都这样」（`p1-session/14`），且 ADR 0017 决定第 4 条写着**答话那三个键
  在焦点落在哪一块上都按得动**。展开着又停在决策点上时，同一个字母有两个意思。
- **走了哪条路:** **让的是「列全部页」**：那一支先看一眼阶段，等答话时把 `a` 交回
  `stage_action`，屏底那一行跟着不摆它（屏上不摆按不动的键）。理由是等得起——
  换一副列法晚几秒没有代价，而一条停在决策点上的线程等着一个字。
- **Why it did not block:** 两个意思都还在，只是有一刻只到得了一个；
  收起（`Esc`）之后 `a` 照旧是答话，展开着而没停在决策点上时 `a` 照旧是列全部页。
- **Whose call:** 拍板的人（要不要给「列全部页」换一个自己的键，例如 `?` 那张键位表
  排完之后腾出来的某一个），或者 `p3-session-legibility/12`——那一票要排整张键位表
- **处置：** 待处理。

### Q162 — 逐页表把裁边、缩放、跨页、彩页转灰、去处五格留在了表外

- **From:** 票 `p3-session-legibility/11`
- **Kind:** 本票认下的一处折扣
- **Where:** `src/session/columns.rs` 的 `PageColumn`（这张表有哪几列）、
  `src/session/draw/pages.rs` 的模块文档《表有意不带的那几格》
- **现状:** 展开那一副从前是把 `render::plain::pages` 那一段散文原样摊出来，逐页两行
  什么都有；改成表之后列只剩六个（记号 · 页名 · 尺寸 · 判定 · 理由 · 判据），
  **裁边、缩放、跨页哪一侧、彩页转灰、去处五格不进表**。跟着丢掉的有一句话：
  失败页那一行从前说得出**它的尺寸是卷内统一尺寸**（`Field::Scaling` 那一格，
  `p1-session/11` 的验收），表上它此刻只剩尺寸与行尾那句原因。
- **Why it did not block:** 票面第一条写死了「逐页也是一张表，与卷表同一套砍列」，
  而 120 列的屏上判定加理由加判据已经占满——多一列就要从判据那一串里砍。
  丢掉的那几格**命令行印出来的那一份一格不少**（同一批格，另一副排版，ADR 0016），
  而这一副要答的是「哪一页把整卷拉下来」。
- **What this ticket actually did:** 六列定在 `PageColumn` 上，砍列次序（判据 → 尺寸 → 理由）
  与它并排；不进表的那五格连同理由写在 `pages.rs` 的模块文档里。
- **Whose call:** 拍板的人（要不要给逐页表添一列缩放，代价是窄终端上判据更早被砍掉；
  或者给失败页那一行补一句「卷内统一尺寸」）
- **处置：** 待处理。

### Q163 — `CONTEXT.md` 的《展开》《折行》《砍列》《视口》四条都还只认卷表那一副

- **From:** 票 `p3-session-legibility/11`
- **Kind:** 词汇表与实现对不上
- **Where:** `CONTEXT.md` 的《会话》那张表里的「展开 (Expansion)」「折行 (Wrap / fold)」
  「砍列」「视口 (Viewport)」四条；实现在 `src/session/draw/pages.rs`、
  `src/session/columns.rs` 的 `PageColumn`、`src/session/viewport.rs`
- **现状:** 本票把展开那一副从「一段不折行、横着滚的散文」改成了**一张表**
  （票面第一句：与卷表同一套视口、砍列与上色）。四条各差一处：
  1. **《展开》**写着「逐页那几行**不折行、横着滚**」——横滚那一套连同 `Expansion` 上
     那个横向滚动量一起没了，`←→` 在这一块上不再派动作；这一条也还没说
     **默认只列要紧的页、`a` 切全部页**。
  2. **《折行》**末一句「**展开**那一副例外：逐页那几行不折」——表上的格由砍列收进宽度，
     而行尾那几句（失败页那一句原因、彩色分支那一句）照旧当整段文字折行，与卷表一个待遇。
  3. **《砍列》**通篇写的是「**卷表**横向摆不下时……**耗时 → 定档页 → 页数**……
     这个次序**只有一处出处**」——此刻有**两张表两个次序**（逐页那一张是
     判据 → 尺寸 → 理由），而「只有一处出处」说的是**每张表各只有一处**。
  4. **《视口》**列的三处仍是「左栏、预设栏、补全候选」——卷表那一处 `p3/10` 就该添
     （停车场 Q152 记着），逐页那一处是本票添的。

  **《展开》那一条已由 `volume-discovery/08` 收掉**（那一票明写授权改它）：那一条此刻说的是
  **两级展开**（目录 → 卷 → 页）、默认那一副是目录表、`←→` 不再横滚、默认只列要紧的页、
  `a` 切全部页，连同「左栏只在展开到页那一级收起」。**余下三条原样待处理**
  （《折行》《砍列》《视口》，连同下面那三条）。

  `p3-session-legibility/12` 又撞见三条（同一类，按本票票面记在这一条上，不新开条目）：

  5. **《焦点》**写着「焦点的取值**六个**：左栏 · 编辑一行 · 取值栏 · 报告区 · 展开着 ·
     预设栏」——**覆盖层是这一维上的第七个取值**（ADR 0017 决定第 1 条早写着它会来），
     而 `volume-discovery/08` 又添了第八个（**展开着一枝**：报告区的卷表那一级）。
     那一条还写着「按停与答话那几个键归阶段那一维」，而覆盖层上它们一个都不派
     （另见 Q164）。
  6. **《视口》**那句「**滚动量是算出来的，不是记着的**——屏上没有一处记着『滚到哪儿了』」
     此刻有**一处例外**：覆盖层是读物，一行上没有第二步可走、一个光标都停不上去，
     那一格因此记着「从第几行画起」（`Covered::from`）。
  7. **《卷表》**写着「表**上面**跟着它一起滚的是这一趟的抬头」——那几行
     `p3-session-legibility/12` 收进了覆盖层，卷表此刻从报告区那一格的头一行起。

  `p4-parking-lot/02` 又撞见一条（《折行》的另一处，同一条词条，按上面那一条的做法记在这里）：

  8. **《折行》**那句「`--help`、命令行印出来的报告、会话的**屏底与报告区**共用这一套」
     漏了两处：`p4-parking-lot/02` 把**左栏与预设那一栏**也搬到了这一套上
     （收的是 Q104／Q136），屏上从此**没有第二套折行规矩**。那一票**一个字没改这一条**
     ——它的派活说明写死了《折行》归 28 号票，撞见对不上就补在本条上。

  `p4-parking-lot/03` 又撞见一条（同一类，按上面那一条的做法记在这里）：

  9. **《下钻》**那一条写着「`⏎`／`→` 停在一块面板上是进去看……进去之后 `⏎`／`→` 定」
     ——**漏了空格**。取值栏那两层上 `⏎`／空格／`→` 三个键同义
     （`which_keys_do_what_in_which_state` 三之三钉着），而《取值栏》那一条
     已由 `p4-parking-lot/03` 改到说得出这一点。这一条**不是错的，是不全的**：
     它列的两个键都按得动，只是没把第三个列出来。那一票的派活说明写死了
     「《取值栏》之外别的词条一个字不许动」，所以补在本条上。

  `p4-parking-lot/04` 又撞见一条（《折行》的第三处，同一条词条，按上面那一条的做法记在这里）：

  10. **《折行》**那句「断处在挨着空格或宽字符的地方（西文词与 `←→`、`⇧⇥` 这类记号不劈开），
      行首缩进跟着折下来的每一行走」此刻少了三条判据：**禁则**（收尾记号不落行首、
      起首记号不落行尾，退不动时认下让它落行首）、**不许断的那个空格**（`--fit height`
      这种带空格的记号整个不断，由措辞那一层标注出来）、**缩进之后不先折出一个空行**。
      三条都落在 `src/wrap.rs`（`NEVER_STARTS_A_ROW`／`HARD_SPACE`／`fold_line`），
      收的是 Q103／Q106／Q114。那一票**一个字没改这一条**——它的派活说明同样写死了
      《折行》归 28 号票，撞见对不上就补在本条上。

- **Why it did not block:** 四条说的都是屏上那一副长什么样，而实现按票面第一句走；
  屏上、按键表、屏底那一行三处彼此一致，没有一处在撒谎。代码那一侧的文档
  （`pages.rs`、`columns.rs`、`viewport.rs` 的模块文档）逐条写着新的形状。
- **What this ticket actually did:** **一个字都没改那四条**——改已有词条的含义是领域决定，
  按 `CLAUDE.md`《改 CONTEXT.md 的规矩》要先拍板，而本票票面只授权动停车场与
  `p2-loose-ends/08`。新词「要紧的页」当场加进了《会话》（那是落地的一部分：
  `render::Notable` 的类型名取自它）。
- **Whose call:** 拍板的人（《展开》改成「一张表，砍列」还是把横滚要回来；《折行》那句
  「展开那一副例外」怎么收；《砍列》与《视口》各自把第二处添进去还是改写成「每张表一处」）
- **处置：** 待处理。**Q152 是这一条的前半截**（《视口》那一条漏着卷表），两条同进同出。

### Q164 — 覆盖层掀着时按停与答话那三个键按不动，而 ADR 0017 决定第 4 条说它们「在焦点落在哪一块上都按得动」

- **From:** 票 `p3-session-legibility/12`
- **Kind:** 决定与实现对不上
- **Where:** `src/session/state.rs` 的 `overlay_action`（它不把不认的键交给 `stage_action`）、
  `docs/adr/0017-focus-and-a-two-dimensional-mode.md` 的《决定》第 4 条
- **现状:** ADR 0017 决定第 4 条写着「**按停与答话那三个键在焦点落在哪一块上都按得动**，
  两级语义一格不变：它们问的是『这一趟还走不走』『这一卷的第二遍还做不做』，
  与眼下在看什么无关」。本票在焦点那一维上添了**覆盖层**这一个取值，而票面第五条写死了
  「**覆盖层开着时别的键一律不派**」——两句话在这一块上互相排斥：跑着的时候掀开
  `?` 那张键位表，那一刻 `s` 按不动；停在决策点上掀开它，`x`／`a`／`s` 三个也按不动。
- **走了哪条路:** **照票面**：焦点那一维上**摆得下阶段那几个键的三块**（左栏、报告区、
  展开着）都把自己不认的键交给 `stage_action`，而覆盖层不交——「进去之后别的键一律不派」
  因此是那张表的形状本身，不靠任何一处代码守着。（取值栏与预设栏也不交，
  但那两块跑起来之后根本进不去，ADR 0017 决定第 8 条；覆盖层是**跑着时进得去、
  却一个都不派**的唯一一块。）
  屏底那一行跟着只摆这一块自己的四个键（屏上不摆按不动的键）。
  **`Ctrl-C` 是唯一的例外**——它在每一个状态下都是退出（`Key::Interrupt` 的文档）。
- **Why it did not block:** 出路一个不少：`Esc` 一下就关掉，关掉之后那三个键当场又按得动
  （`the_overlay_hands_no_key_to_the_stage_while_it_is_up` 钉着这一条）。覆盖层是读物，
  停留时间以秒计，而按停要等的是当前卷跑完——晚几秒按与晚几秒生效在盘上是同一件事。
- **Whose call:** 拍板的人（要么把 ADR 0017 决定第 4 条改成「摆得下它们的那三块」
  并说清覆盖层为什么例外，
  要么让覆盖层也把不认的键交给 `stage_action`，代价是「进去之后别的键一律不派」
  这句话不再是结构上成立的）
- **处置：** 待处理。

### Q165 — 「屏底末尾恒是 `? 全部键`」在打字的那两块上摆不出来

- **From:** 票 `p3-session-legibility/12`
- **Kind:** 票面与实现对不上
- **Where:** `src/session/state.rs` 的 `minds_its_own_letters` 与 `Session::reveals`、
  `src/session/draw/footer.rs` 的 `footer`
- **现状:** 票面第一条要「屏底瘦身成此刻最常用的四五个，**末尾恒是 `? 全部键`**」。
  **编辑一行**与**打预设名**这两块上摆不出来：那儿每一个字符都进缓冲，`?` 与 `i` 也是字
  （`editing_action`、`naming_action`）。屏底那两副因此照旧只摆自己那三个键
  （`⇥ 补这一层 · ⏎ 收下 · Esc 丢掉`），而 `?` 那张表上也没有这两块——
  一张按键表说不出「a 到 z 各进缓冲」。
- **走了哪条路:** **两块一起划出去**，由一处说了算（`minds_its_own_letters`）：
  屏底摆不摆那一句问 `Session::reveals`，`?` 那张表列不列那一组由 `KeyGroup` 定，
  两处读的是同一条规矩。**「恒是」因此读作「掀得开的每一块上恒是」**。
- **Why it did not block:** 那两块本来就只有三个键，全摆在屏底上，一个都没藏起来——
  「看不见的东西等于不存在」在这两块上不成立。退出去是一个 `Esc`，出去就有 `?`。
- **Whose call:** 拍板的人（要不要给这两块留一个非字符的键掀开覆盖层，例如 `F1`——
  代价是会话此刻只认十二个键码，添一个要动 `Key` 与 `translate` 两处）
- **处置：** 待处理。

### Q166 — 屏底那一行的键是手抄的，`?` 那张表的键是问出来的：同一个键有两处措辞

- **From:** 票 `p3-session-legibility/12`
- **Kind:** 本票认下的一处折扣
- **Where:** `src/session/draw/footer.rs`（各状态那几个 `*_prompt` 函数里的字面串）、
  `src/session/draw/overlay.rs` 的 `says`
- **现状:** `?` 那张表的**键**从按键表当场问出来（`Session::key_table`），改一处按键表
  它跟着变；屏底那一行的键仍是各状态那几个函数里手写的字面串（`t 试算 · x 执行`）。
  同一个键因此有两句措辞：屏底是 `t 试算`，覆盖层是 `t 试算：只算不写，报告照出`。
  改一个键位（比如把 `t` 换成别的字母）要动两处，而只动一处不会有任何一条用例红。
- **走了哪条路:** **两处答的不是同一个问题**，这一刀写进了 `footer` 的模块文档：
  屏底答「**此刻按什么**」（最常用的四五个，随焦点与阶段变），覆盖层答
  「**这个键做什么**」（按焦点分组的全部键）。短的那一句是提示，长的那一句是解释，
  合成一句的话两头都不好用——屏底那一格摆不下解释，而一张只有提示的表读不出所以然。
- **Why it did not block:** 「哪些键在哪个状态下有效」仍旧一处答完（`Session::action`），
  两处都只是它的两副读法；而**屏上不摆按不动的键**照旧靠状态分岔守着，
  屏底手抄的那几句一处都没有绕过按键表去摆一个派不出动作的键。
- **Whose call:** 拍板的人（要不要让屏底那一行也从 `Session::key_table` 取键，
  代价是每一句提示都要有一个「短说法」与一个「长说法」两份措辞，
  而屏底那几副此刻是按**这一块此刻最该说什么**排的，不是按键排的）
- **处置：** 待处理。

### Q167 — `?` 那张表滤得掉「这个阶段不派」的键，滤不掉「派得出、而 press 那一层挡着」的键

- **From:** 票 `p3-session-legibility/12`
- **Kind:** 本票认下的一处折扣
- **Where:** `src/session/state.rs` 的 `Session::keys_of` 与 `KeyGroup::reachable`、
  `src/session/terminal.rs` 的 `press`（`Action::Expand` 与 `Action::Reveal(Overlay::Premises)`
  那两道 complain）
- **现状:** 票面第二条要「只列此刻这个阶段派得出的键」，而这张表只问得到**按键表**那一层。
  有两支的闸门在 `press` 那一侧——它们要读那一趟攒下来的报告，而状态机读不到：
  **`e` 展开**（一趟都没跑过时说「还没跑过：先按 t 试算或 x 执行」）与
  **`i` 这一趟的前提**（同一个形状）。一趟都没跑过时按 `?`，左栏那一组照旧列着 `e`、
  「任何时候」那一组照旧列着 `i`，按下去只换来一句话。
  **屏底那一行没有这个毛病**：它收得到 `live`，`browsing_keys` 用 `expandable(live)` 挡 `e`、
  `report_prompt` 与 `overlaid_prompt` 用 `live.is_some()` 挡 `i`。
- **走了哪条路:** **按阶段挡得住的那几处都挡了**：`KeyGroup::reachable` 把取值栏与预设栏
  在跑着与等答话时整组撤掉（只从左栏进得去，而那时左栏一个改动键都不派），
  把报告区与展开着在「一趟都没跑过」时整组撤掉。剩下的两支挡不住——挡它们要 `live`，
  而 `Session::key_table` 是状态机上的一个纯函数，收 `live` 就等于让状态机读那一趟攒下来的
  东西（`CONTEXT.md` 的《会话》与 ADR 0017 都把那条线画在这里）。
- **Why it did not block:** 那两支**按下去有话说**，不是按了没反应——本仓库明写着这两者
  不是一回事（`browsing_keys` 的文档：「型号还没挑时它照样摆着……那不是按不动，
  是**按了有话说**」）。而这张表本来就在列**别的块**上的键，读它的人不会指望每一行
  此刻都按得动。
- **Whose call:** 拍板的人（要不要给 `key_table` 收一个「这一趟有没有报告」的布尔——
  那是画法那一层现成就有的一个数，代价是状态机上多一个它自己答不出的参数；
  或者反过来，把 `e` 那一支在 `Stage::Fresh` 上就不派，代价是 `press` 那句
  「还没跑过」从此只有 `Stage::Running` 且一卷都没有时到得了）
- **处置：** 待处理。

### Q170 — 《卷表》《下钻》《行》三条在报告区多出一级之后各有一句对不上

- **From:** 票 `volume-discovery/08`
- **Kind:** 词汇表与实现对不上
- **Where:** `CONTEXT.md` 的《会话》那张表里的「卷表 (Volume table)」与「下钻 (Drill)」；
  实现在 `src/session/draw/directories.rs`、`src/session/draw/table.rs`、
  `src/session/state.rs` 的 `Focus`
- **现状:** 本票把报告区从两级（卷 → 页）变成三级（目录 → 卷 → 页），三条各差一句：
  1. **《卷表》**头一句写着「**报告区默认那一副**：一卷一行的一张表」——默认那一副
     此刻是**目录表**（一个目录一行），卷表是**展开一枝**之后摊出来的那一副。
     这一条其余各句（列从左到右、六种卷各写什么、不重排、砍列）**一字不差地仍旧成立**。
  2. **《下钻》**末一句「**别处一层，只有这一处两层**」——那一句说的是取值栏比别处多一层，
     而报告区此刻有**三级**。下钻自己一格没动（它仍是型号那一行的第二层）。
  3. **《行 (Row)》**头一句「报告的**卷级**与**逐页**那两段出的单位」与末段那个括号
     「表一副（会话的**卷表与逐页表**）」——此刻还有**目录**那一段（`RowKind::Directory`）
     与**目录表**。这一条其余各句仍旧成立，而「全部种类以 `RowKind` 为准」那一句
     本来就把种类的清单交给了那个枚举。
- **Why it did not block:** 票面授权改的只有《展开》那一条（「`CONTEXT.md`《会话》的
  「展开」词条改成两级」就是那次拍板），而这两条是**改写已有词条的含义**——
  按 `CLAUDE.md`《改 CONTEXT.md 的规矩》，那是领域决定，撞见了记下来、不顺手改。
  两句都不影响实现：读的人从《展开》那一条读得到三级各是什么。
- **What this ticket actually did:** **两条一个字都没动**，把这一笔记在这里。
  《展开》那一条改成了两级，并在里面点明「报告区默认那一副是目录表」——
  《卷表》头一句那个说法因此在同一张表里另有一处更新的答案。
  《行》那一条没动的第二个理由：ADR 0016 那句「措辞一处、排版**两副**」仍旧成立——
  多出来的是表那一副底下的第三张表，不是第三副排版。
- **Whose call:** 拍板的人（《卷表》头一句改成「展开一枝之后那一副」；
  《下钻》末一句改成「取值栏只有这一处两层」或者整句去掉；《行》那两处各添一个「目录」）
- **处置：** 待处理。

### Q171 — 命令行那一侧的「折叠」落地成一行目录行，底下两级一行没藏

- **From:** 票 `volume-discovery/08`
- **Kind:** 票面读着含糊
- **Where:** `src/render/plain.rs` 的 `report` 与 `directory`；`src/render.rs` 的 `grouped`
- **现状:** 票面第三条要的是「命令行那一侧有对应折叠，与 TUI 拿同一份数据」。
  本票落地成：**每一枝那几卷前面多一行目录行**（`库/作品  12 卷 · 2bit+FS 9 · 隔离 1 卷`），
  卷级与逐页那两段**一行没少**。会话那一侧折得起来（默认只给目录那一级，按 `⏎` 才往下摊），
  命令行这一侧摊的是三级全部。
- **Why it did not block:** 「两边拿的是同一份报告数据」这一半是硬的，而它成立——
  目录行的聚合与措辞只有 `render::grouped` 与 `render::directory` 一处出处，
  两边读的是同一批格（ADR 0016）。真把底下两级藏起来的话，命令行那一路**没有一个键**
  能把它们摊回来：那一份印出去就是全部，藏起来的东西在屏上再也没有第二个地方看得到。
  真要藏，得先有一个说得清的开关（`--brief` 之类），而那是票面之外的一项。
- **What this ticket actually did:** 目录行摆在它那几卷**前面**，顶格；
  卷按枝分组之后**次序跟着变了**——发现走的是先序深度优先，同一枝底下那几卷在报告上
  本来不挨着（中间夹着子目录那几卷），分组把它们收到一处。组内与枝间都仍按报告上的先后
  （「不重排」那一条一格没动）。
- **Whose call:** 拍板的人（要不要给命令行一个真折叠的开关，以及折到哪一级）
- **处置：** 待处理。

### Q169 — 80×24 那一档主区只有 30 列，两条横条连同步数一起被切在半路

- **From:** 票 `p3-session-legibility/13`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/session/draw/overview.rs` 的 `overall_row`／`walking_line` 与 `BAR_WIDTH`；
  快照 `src/session/draw/yielding.rs` 的 `AT_EIGHTY_BY_TWENTY_FOUR_RUNNING`
- **现状:** 80 列的屏上左栏 50 列、主区 30 列（`MAIN_MIN_WIDTH`），而总览块那两行各要
  六十几格：一条横条 `BAR_WIDTH` 是 30，加上方括号、`3000/5000 步` 与 `已用 5m00s`。
  屏上因此是 `│ 总体 [==================>  │`——**右边那个方括号、步数与已用一起在框外**。
  切在半路的横条比截断的抬头更难读：没有收尾的 `]`，走过的比例看着比实际大。
  抬头那一头 13 号票收了（从中间省略，见 Q147），正文这一头没有。
- **Why it did not block:** 票面第一条的验收是「总览五行在、表至少一行、屏底的退出键在屏上」，
  三条在这一档上都成立（快照钉着）；横条被切是**正文摆不下**，而总览块**一行不砍**这一条
  正是票面写死的——不许拿高度换宽度。
- **What this ticket actually did:** **横条一格没动**，把这一笔记在这里。
  改它只有两条路，两条都是一处决定：① 让横条跟着这一格的宽度缩——而
  「横条画多宽与命令行那两条同一个出处」（`BAR_WIDTH`，`crate::bar_style`）是既有的一条；
  ② 窄档上换一副短正文（只给数、不给条）——那是总览块的第三副样子，
  而它眼下只有「有没有出事行」「收没收场」两条分岔。
- **Whose call:** 拍板的人（横条在窄格子里缩，还是窄档上换一副只给数的正文）
- **处置：** 待处理。

### Q172 — 票面写着「闸门数一格不变、本票不增减用例」，而验收第三条要一条问得出「只有一处」的用例

- **From:** 票 `p4-parking-lot/01`
- **Kind:** 票面给的数与量出来的对不上
- **Where:** 派活说明《收尾》第 1 条（「闸门数应当一格不变（753 / 646）——本票不增减用例」）；
  票据验收第三条（「有一条用例或文档断言问得出『这张单子只有一处』」）；
  `.scratch/p4-parking-lot/spec.md`《Testing Decisions》（「**出处收拢那三条**各要一条
  「只有一处」的用例」）
- **Why it did not block:** 两句话只有一句能同时成立。spec 那一条写得最死——三条出处收拢
  **各要一条用例**，还点了先例（`p3/07` 那句 `assert_eq!(BAR_WIDTH, …)`）；验收第三条
  给了「用例**或**文档断言」两条路，走文档那条保得住 753 / 646 两个数，
  但「文档断言」身后没有闸门：第六份抄出来时一处都不会红，而这张票买的正是那个。
- **What this ticket actually did:** 走**用例**那条路。新添 `tests/single_source.rs` 一条集成用例
  `the_refusal_list_lives_in_one_place`（扫 `CONTEXT.md` 加 `src/` 加 `docs/`，
  问三件事：别处没有第二份、家里真住着那张单子、五处的路标还在）。
  两条跑用例的闸门因此各涨一格：闸门 1 **753 → 754**、闸门 2 **646 → 647**（闸门 3 是
  `cargo check`，没有数），
  而 lib 与 bin 两趟都一格没动（210 / 293 与 210 / 186）——涨的那一条落在 `tests/` 那一批里。
- **Whose call:** 拍板的人（「只有一处」这类断言要不要各添一条用例，还是只留文档里那句话）
- **处置：** 待处理。

### Q173 — 收拢之后，唯一还说「输出根写不进去是拒绝执行」的那一处，指着一份不含它的单子

- **From:** 票 `p4-parking-lot/01`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/lib.rs` 的 `ensure_the_output_root_takes_a_write`
  （「按 `CONTEXT.md` 的《失败》那就是**拒绝执行**」）；`CONTEXT.md`《失败》的
  **拒绝执行**词条（列着五种，不含这一种）
- **现状:** 收拢之前 `run` 与两条事件各自列**六种**，多出来的那一种是「输出根写不进去」
  （`p2-loose-ends/06` 添的），而 `CONTEXT.md` 只列**五种**——Q89 记的正是这个差。
  五份收成指路之后，那三处不再自己列，于是**整个仓库里说得出「输出根写不进去算拒绝执行」
  的只剩上面那一处**——而它偏偏是一句指路，指过去看到的五项里没有它。
  Q89 从前只是一处漂移，此刻变成了一句读得出的自相矛盾。
- **Why it did not block:** 先收拢、后改数正是 spec《Further Notes》第一条硬约束的次序
  （反过来那张单子要再抄一遍五处），而派活说明写死了「不要把五种改成六种，那是 28 号票，
  改它要先确认第六种是哪一种」。**行为一格没动**：那一种拒绝照旧拒得住
  （`src/lib.rs` 的 `ensure_the_output_root_takes_a_write` 与 `tests/isolation.rs` 的
  `an_output_root_that_cannot_be_written_is_refused_before_any_volume` 都在），
  错的只有文档。
- **What this ticket actually did:** 照票面收拢，一个数都没改，把这段窗口记在这里。
  **28 号票落地那一刻这条自动消失**——它要改的正是这一个词条。
  评审揪出的正是这一条：收拢本身把一处漂移升级成了一句当场就假的话。
- **Whose call:** 落地的人（`p4-parking-lot/28`，已排在后面）
- **处置：** 待处理。

### Q174 — 库内 `Refusal` 上还点着单子上的一项，本票判它是引用而不是第二份抄件

- **From:** 票 `p4-parking-lot/01`
- **Kind:** 你确实拿不准的单项
- **Where:** `src/lib.rs` 的 `Refusal`（「**眼下只有一处**戴它：覆盖项把候选集裁空」）
- **Why it did not block:** Q91 点名的是五处，这一处不在里面，而它说的是一句**本地的事实**
  ——哪一种拒绝戴着这个标记——不是把单子重列一遍。按 `CLAUDE.md`《文档写作》第 5 条，
  引用一个**术语**正是要的样子。代价记在明处：「覆盖项把候选集裁空」这几个字因此
  当不了 `the_refusal_list_lives_in_one_place` 的记号（那一条拿的是另外三项，
  肯定式否定式都算），单子上只抄那一项时没有闸门看着。
  同一条理由把「处理范围为空」也排除在记号之外——它另有运行时的出处。
  **剩下的缝**：只抄这两项的第二份单子不会变红。
- **What this ticket actually did:** **一个字没动**，并把「它为什么不当记号」写进那条用例的
  `REFUSAL_MARKS` 文档里——判断留在读得到的地方。
- **Whose call:** 拍板的人（只点单子上的一项算不算第二份出处）
- **处置：** 待处理。

### Q175 — `CONTEXT.md` 里那句「实现文档一律指到这个词条」是文档写作的规矩，不是领域术语

- **From:** 票 `p4-parking-lot/01`
- **Kind:** 你确实拿不准的单项
- **Where:** `CONTEXT.md`《失败》末尾那一句（「**拒绝执行**那几种的单子只有这一份：
  实现文档一律指到这个词条，不各列一遍」）
- **Why it did not block:** 派活说明写着「《失败》那一节是这张单子的家，本票**往里放**」，
  而放进去的这一句既不是术语也不是领域事实——它是给写实现文档的人的规矩，
  按 `CLAUDE.md`《文档写作》第 2 条（单一职责）该住在 `CLAUDE.md`《文档写作》或
  `docs/agents/domain.md`。不放的话，下一个改这个词条的人不知道有五处实现文档指着它。
- **What this ticket actually did:** 照评审的意见**改了位置也削了长度**：从表与展开段之间
  （那里会打断《失败》第一行的展开，第 3 条渐进披露的次序）挪到整节末尾，从五行削成一行，
  不再写测试名。**留在 `CONTEXT.md` 里没有搬走**——搬家要先定它的新家在哪一份文件里。
- **Whose call:** 拍板的人（这句话留在《失败》末尾，还是搬进 `docs/agents/domain.md`）
- **处置：** 待处理。

### Q176 — 票面说「四档现存快照照旧通过、不必重录」，实际重录了两张

- **From:** 票 `p4-parking-lot/02`
- **Kind:** 票面给的数与你量出来的对不上
- **Where:** 票面第五条验收；`src/session/draw/config.rs` 的
  `THE_UNFOLDED_MODELS_UNDER_ONE_PANEL`、`src/session/draw/yielding.rs` 的
  `TOO_NARROW_FOR_TWO_COLUMNS`
- **Why it did not block:** 票面写着「搬过来之后屏上**一个字不变**（折出来的样子与终端库
  那一套相同）」，而两套折法在**折得起来的宽度上本来就不同**，Q136 自己就点着这一条
  （「`crate::wrap` 给折下来的每一行补上行首缩进，终端库那一套不补」）。实测两处：
  **52 列**那一档上阈值那一行折下来的第二截从顶格变成缩进两格（一行变，别的逐格照旧）；
  **34 列**那一档上断处整片挪位——终端库按空格断词（`{:　<8}` 那几个全角空格算空格），
  因此从标签后面就断开、右边白白空掉十几格；`crate::wrap` **断在最后一处断得开的地方**
  （它的 `breakable` 文档写死了这一条：挑更早的那个空格会在窄终端上白让好几格），
  于是用满这一格。**变的是折在哪儿与缩进，一个字都没多也没少。**
- **走了哪条路:** **照票面的目的走，把那两张重录了**——派活说明留了这条路
  （「真要重录，先停下来想清楚为什么，并把『变的是哪一格』写进落地记录」），
  Q104 自己算的代价也正是「换来的是**两张快照重录**」。重录的两张与实际画出来的逐格核对过，
  **80 列与 60 列那两档一格没动**。
- **Whose call:** 拍板的人（「屏上一个字不变」这条验收要不要改写成「一个字都不多不少，
  折在哪儿按 `crate::wrap`」）
- **处置：** 待处理。

### Q177 — 预设栏的模块文档写着「屏底那一格不折行」，而屏底那一格折行

- **From:** 票 `p4-parking-lot/02`
- **Kind:** 路过发现
- **Where:** `src/session/draw/picker.rs` 的 `presets` 文档（「这句话摆在这一格而不是
  屏底那一句里，是因为**屏底那一格不折行**，一条长路径会被切掉，而这一格折得下来」）；
  实现在 `src/session/draw/footer.rs`（三处 `wrap::fold`）
- **Why it did not block:** 那一句要论证的**结论**照旧成立（文件位置摆在预设那一格里，
  不摆屏底），只是它给的**理由**与今天的实现对不上：屏底那一格 `p2-loose-ends/07`
  之后就折行了，而且按折出来的行数往下长（`yielding::footer_height`）。
  订正它要重写那一段论证——那一格该不该摆一条长路径是**预设栏**的事，
  不在本票的施工面（本票只把折行搬家）。
- **走了哪条路:** **一个字没改**，记在这里。本票动了那份文档的另一段
  （折行走哪一套），这一段原样留着。
- **Whose call:** 拍板的人（那句理由重写成什么：屏底折得下来，但它是**三样争一格**的地方）
- **处置：** 待处理。

### Q178 — `src/session/draw.rs` 的模块文档三处指着 `[`room`]`，而那个模块叫 `yielding`

- **From:** 票 `p4-parking-lot/02`
- **Kind:** 路过发现
- **Where:** `src/session/draw.rs` 模块文档第 10、15、34 行（`[`room`]`）；
  模块本身是 `src/session/draw/yielding.rs`
- **Why it did not block:** 三处都是**断掉的文档链接**，而 `cargo doc` 看不见它们——
  bin 与 lib 同名，`cargo doc` 因此只给 lib 出文档，bin 那一半一条告警都不报
  （15 条基线全在 `tonefit (lib doc)` 名下）。指的是哪一块读得懂（「摆不下时谁让位」
  只有 `yielding` 一处），错的只是名字：那个模块多半改过名，文档没跟着走。
- **走了哪条路:** **一个字没改**，记在这里。本票动的是 `draw.rs` 的《折行不在这里》那一节，
  与这三处不挨着；顺手改名会把一次折行搬家的 diff 混进一次无关的文档订正。
- **Whose call:** 不必拍板（改成 `[`yielding`]` 就完了，只是不该塞进本票）
- **处置：** 待处理。

### Q179 — spec 的《取值栏》说它带着「下钻到第几层」，而实现记的是「下钻进了哪一块面板」

- **From:** 票 `p4-parking-lot/03`
- **Kind:** spec 与实现对不上
- **Where:** `.scratch/p3-session-legibility/spec.md`《Implementation Decisions》第三条
  《取值栏》里「取值栏是『眼下在看什么』那一维上的一个取值」那一款；
  `src/session/state.rs` 的 `Values::panel`（`Option<Panel>`）
- **现状:** spec 写着取值栏「带着：摊的是哪一行、光标停在第几格、**下钻到第几层**」，
  而实现那一格装的是**进了哪一块面板**——`p3-session-legibility/06` 的落地记录写着为什么
  （「层号答不出『进的是哪一块』，而屏上那一行要印它；退一步也要靠它落回那一块上」），
  `p3-session-legibility/05` 的落地记录也点过同一句 spec。
- **走了哪条路:** **spec 那半句一个字没改。** 本票的地界是票面与实现**互相打架**的那五处
  （Q138／Q139／Q142／Q143／Q144），它们写下的那一刻就自相矛盾，属于
  `docs/agents/issue-tracker.md` 说的「记录里的缺陷」；而这一句是**后来才被取代**的
  ——同一份文件的下一句写着这一类「原文不动，由后来那张票的记录承担」，
  而承担它的那两条记录已经在册。改不改是判它属于哪一类，那不是本票的地界。
- **Whose call:** 拍板的人（这一句算「后来才被取代」原样留着，还是与那五处一起就地订正）
- **处置：** 待处理。

### Q180 — 屏底那两行印的是「`⏎`／`→` 定」，而取值栏那两层上三个键同义

- **From:** 票 `p4-parking-lot/03`
- **Kind:** 屏上措辞与按键表对不上
- **Where:** `src/session/draw/footer.rs` 的 `valuing_prompt`（三副措辞里的
  `⏎／→ 定` 与 `⏎／→ 看这块面板底下的型号`）；`src/session/state.rs` 的
  `which_keys_do_what_in_which_state` 三之二、三之三两段
- **现状:** 按键表上取值栏那两层是 `⏎`／空格／`→` **三个键同义**，本票刚把这一条写进
  `CONTEXT.md` 的《取值栏》；而屏底那两行只摆得出其中两个，空格按得动却不在屏上——
  「看不见的东西等于不存在」在这一格上成立。
- **走了哪条路:** **一个字没改。** 本票一行代码都不动（票面第二条验收），而屏底那一行的
  措辞归 `p4-parking-lot/07`——那一票要让屏底列出的键与按键表**出自同一处**，
  这一处正是它要收的形状：出自同一处之后，空格摆不摆由那一处答，不再手抄。
- **Whose call:** 落地的人（`p4-parking-lot/07`，已排在后面）
- **处置：** 待处理。

### Q181 — 票面第五条要「黄金快照跟着重录」，而 `tests/golden.rs` 一个符号都够不着折行

- **From:** 票 `p4-parking-lot/04`
- **Kind:** 票面写错了
- **Where:** 票面第五条验收；`src/wrap.rs`（`src/main.rs` 的 `mod wrap;`）与 `tests/golden.rs`
- **Why it did not block:** `wrap` 是 **bin crate** 的模块，而 `tests/golden.rs` 是集成测试、
  链的是 **lib crate**：它调 `tonefit::run(...)` 再自己拼快照行，一个符号都够不着折行。
  **改折行，黄金快照一个字节都不会动**——实测本票把三条判据全加上之后
  `cargo test --test golden` 全绿，一张没重录、判定的数一个没动。
  这是 **Q132** 记过的同一个形状（那一条是 `src/render.rs`）。
- **走了哪条路:** **照派活说明把第五条改写成真正钉着折行输出的那三处**：`src/wrap.rs`
  自己的 `mod tests`、会话那几张快照（`src/session/draw/` 下各模块的 `mod tests`）、
  以及三个会真去起那个二进制的集成测试（`exit_code`／`session`／`preset`——
  `--help` 与报告的折行走得到那里）。改写后的那一条勾上了，验收表下面写了「勾得有保留」。
- **Whose call:** 拍板的人（这一条验收就地改写成那三处，还是另有说法）
- **处置：** 待处理。

### Q182 — 「这个空格不许断」那层标注，clap 的文档注释那一头写不进去

- **From:** 票 `p4-parking-lot/04`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/main.rs` 的 `--preset` 那条帮助（`--preset 漫画 --filter hamming`）；
  标注的规矩在 `src/wrap.rs` 的 `HARD_SPACE`
- **Why it did not block:** 标注是一个**字符**。措辞那一层（`src/render.rs`）与库那一侧
  出的那三句话（`Interlock` 的 `Display` 两处、抖动那条拒绝两处）都写得进去——
  那几处是字符串字面量，`\u{a0}` 摆在那里看得见。**clap 的帮助有一大半是文档注释**，
  而文档注释里既插不进常量、也不该埋一个看不见的字符（埋进去下一个人复制粘贴就丢了）。
  今天不咬人：`--preset 漫画` 只在**长**帮助里露面（折到 90 格），那一档上它没断过。
  真要标，得照 `inputs_help`／`interlock_help` 那条路把整条帮助写成一个函数，
  而那条帮助有二十行，写成函数之后正文与文档注释各存一份——那是另一件事。
- **走了哪条路:** **没标它**，并把「文档注释收不下时怎么办」写进 `HARD_SPACE` 的文档
  （照 `crate::inputs_help` 那一条的办法）。真会断的那几处标齐了，
  `the_help_folds_every_line_into_the_terminal` 里新加的那一段钉着
  「`--fit height` 与 `--dither fs` 各自整个留在某一行上」。
- **Whose call:** 拍板的人（那条二十行的帮助要不要为一个空格改写成函数）
- **处置：** 待处理。

### Q183 — 库出的那几句话里从此带着 `\u{a0}`，而库对折行一无所知

- **From:** 票 `p4-parking-lot/04`
- **Kind:** 你确实拿不准的单项
- **Where:** `src/interlock.rs` 的 `Display`、`src/lib.rs` 的 `dither_outside_the_gate_error`；
  `tests/pipeline.rs` 的 `FIT_HEIGHT`／`DITHER_FS`
- **Why it did not block:** 标注要**从措辞的原文里长出来**（Q106 当年就是这么写的），
  而那三句话的原文在**库**里——ADR 0016 认下的那一处例外：同一句要从三张嘴里出来，
  最后一张在库内。于是库出的字节变了：`--fit height` 中间那一格从 U+0020 成了 U+00A0。
  **不咬人的理由**：U+00A0 是 Unicode 自己那条「这里不许断行」，不是本仓库私造的记号；
  它照旧是一个空格宽的空白，而印出去之前一律换回普通空格。
  **印出去的地方是五处，不是四处**——折行那四处（`--help`、命令行报告、
  会话屏底与报告区、左栏与预设栏）由 `wrap::fold` 顺手做了，而**拒绝执行那句话
  一格都不折**：它从 `main` 那一行 `eprintln!` 直接落到 stderr 上。
  第五处是评审当场揪出来的（本票第一版漏了它，落地记录里那句「四个出口全走 `fold`」
  当时是假的），改法是那一行自己过一遍 `wrap::printed`，
  `tests/exit_code.rs` 新加一条真进程用例钉着它。
  代价是**断言原文的用例要照标注写**（`tests/pipeline.rs` 那四条），
  以及库的调用方若不折行就会拿到一个 U+00A0。
- **走了哪条路:** **标了**，理由与写法写在 `Interlock` 的 `Display` 与 `HARD_SPACE` 上，
  库这一侧的用例改用两个常量断言（`FIT_HEIGHT`／`DITHER_FS`），不各写一遍那个转义。
  「印出去的样子」收成一处（`wrap::printed`），折行与不折行的两条路都过它。
  **没有把这个字符提成库的公共 API**：那是一次对外契约的扩大，本批只认下了一处
  （spec 的《缝》第 2 条）。
- **Whose call:** 拍板的人（这个字符要不要提成库的公共 API，让库的调用方看得见这条约定）
- **处置：** 待处理。

### Q184 — 票面第五条要「黄金快照重录一次」，而 `tests/golden.rs` 一个字形都够不着

- **From:** 票 `p4-parking-lot/05`
- **Kind:** 票面写错了的一处（第三次撞上同一个形状：Q132、Q181）
- **Where:** `.scratch/p4-parking-lot/issues/05-width-asks-the-terminal.md` 的验收第五条；
  `tests/golden.rs` 的 `page_line` 与 `cropped`
- **Why it did not block:** 黄金快照是集成测试、链的是 **lib** crate，而它**自己拼快照行**：
  尺寸那两列写的是 `format!("{}x{}", …)`——本来就是 ASCII 的 `x`，`Size` 的 `Display`
  一次都没走；串起来那个 `·` 是 `golden.rs` 与 `tests/fixtures/mod.rs` 里自己的字面量。
  换掉的那两个字形因此一个都进不了那份快照。实测 `cargo test --test golden` 全绿、
  一张没重录，`grep -c '×' tests/golden-snapshot.txt` 是 **0**。
  `Size` 的 `Display` 落在库里也没让它够着——快照那一列压根不走它。
- **走了哪条路:** 照派活说明给的三处改写那一条验收（`render` 自己的 `mod tests`、
  会话那几张 `TestBackend` 快照、以及 `tests/exit_code.rs`／`session.rs`／`preset.rs`
  三个会起真进程的），逐处核过，结果记在票据的《落地记录》。
- **Whose call:** 拍板的人（写票面的人第三次把黄金快照当成界面输出的验收）
- **处置：** 待处理。

### Q185 — ADR 0016 决定第 2 条那句示例里的分隔记号还写着 `·`

- **From:** 票 `p4-parking-lot/05`
- **Kind:** 文档与实现对不上
- **Where:** `docs/adr/0016-one-wording-two-layouts.md` 决定第 2 条
- **现状:** 那一条拿「各候选的判据 `1bit+FS 32.000 · 2bit 20.000 · …`」当例子，说明
  「一格的值本身是一串东西时，怎么串起来仍是措辞」。本票把那个记号换成了 `⋅`（U+22C5）：
  **那句话说的道理一个字都没变，例子里的字形却与实现对不上了。**
- **Why it did not block:** ADR 装的是**决定**，那一串只是决定的一张插图；照着它读代码的人
  不会因为一个记号读错。ADR 带着采纳日期，改它是另一件事——Q125 撞见 ADR 0015
  与实测对不上时走的也是这条路。
- **走了哪条路:** **没改 ADR**，把这一笔记在这里。实现那一侧的出处是 `src/render.rs`
  的 `SEPARATOR`，取哪个字形、为什么取它都写在那一个常量上。
- **Whose call:** 拍板的人（ADR 里的插图跟不跟实现走）
- **处置：** 待处理。

### Q186 — 窄终端上 clap 自己排的那几行照旧过宽，折行够不着它们

- **From:** 票 `p4-parking-lot/05`
- **Kind:** 票面没想到的第三种情形（验收第一条那半句「窄终端上不溢出」只做到一半）
- **Where:** `src/main.rs` 的 `folded_help`，以及用例里那个 `clap_lays_it_out`
- **现状:** 实测（真进程 + 一块 40 列的伪终端）过宽的只有四行，四行都是 clap 自己排的：
  `Usage: tonefit [OPTIONS] --out <目录> <路径>...`（47 格，clap 一个字都不折），
  以及它自带的 `help` 子命令、`--help`、`--version` 那三条英文说明。
  **这份命令行自己写的帮助一行都没过。**
- **Why it did not block:** 那四行不经过 `fold_help`：用法那一行由 clap 按参数拼出来，
  另外三条是 clap 内置的英文。要收它们得开 clap 的 `wrap_help`（开了也治不了中文，
  Q32 记着），或者把内置那三条各换成自己的措辞——都是另一件事。
  代价只有**回绕**：窄终端上那四行由终端自己折回来，不从行尾切掉（与 Q105 同一条理由）。
- **走了哪条路:** **认下它**，用例里把这四行**点名**排除（`clap_lays_it_out`，
  四条各写清是哪一行），其余每一行在三档宽度（40／100／200）上都钉着不过终端那么宽。
  排除得**有条件**：那四行仍不许宽过 `OFF_TERMINAL_WIDTH`——落地前那一条在 100 格上
  钉着**所有**行，不能借着「窄终端」把它整个松掉（评审的 Spec 轴指出的）。
  票据的验收第一条因此勾得有保留。
- **Whose call:** 拍板的人（clap 内置那几行要不要收进自己的措辞）
- **处置：** 待处理。

### Q187 — 命令行印出去的还有几个歧义宽度的字形，本票只换了摆进列里的那两个

- **From:** 票 `p4-parking-lot/05`
- **Kind:** 路过发现的无关缺陷（Q168 的邻居）
- **Where:** `src/crop.rs` 的 `Crop` Display（`裁边 A → B` 那个 `→`）、`src/resample.rs` 的
  `预缩 2×`、`src/metric.rs` 的 `分块 32×32`、`src/profile.rs` 的 `{} · {} PPI`、
  `src/render/plain.rs` 里格与格之间那几个 `·`、`src/session/draw/footer.rs` 屏底那一行的 `·`
- **现状:** 这几个（`→` U+2192、`×` U+00D7、`·` U+00B7）在东亚宽度表上同样标着 Ambiguous，
  CJK 终端上各画两格。
- **Why it did not block:** **到今天为止**它们一个都不落在靠宽度对齐的列上：裁边与缩放
  那两格不在逐页表的六列里（那张表是记号 · 页名 · 尺寸 · 判定 · 理由 · 判据），
  纯文本那一副与屏底那一行根本不排列。歧义宽度只在**同一列逐行比宽度**时咬人，
  这几处错一格不牵连任何人。票面点名的也只有那两个。
  **它有到期日**：`.scratch/p4-parking-lot/spec.md` 的《四、报告区》写着「逐页表把**裁边、
  缩放**、跨页、彩页转灰、去处五格收进来」——那五格一进列，`裁边 A → B` 的 `→` 与
  `预缩 2×` 就落在对齐的列上了，那张票要连着这一条一起收。
- **走了哪条路:** **没动它们**，并把判据（`wrap::width_is_stable`）的管辖面写成
  **「摆进列里的字形」**而不是「印出去的每一个字形」——边界写在那个函数与
  `src/session/columns.rs` 的模块文档上，`src/render.rs` 那条用例问的也是这几格。
- **Whose call:** 拍板的人（要不要把这条规矩推到命令行印出去的每一个字形上）
- **处置：** 待处理。

### Q188 — 「哪几格摆成列」在两处各写一份，没有东西逼它们同步

- **From:** 票 `p4-parking-lot/05`
- **Kind:** 你确实拿不准的单项（评审的 Standards 轴与 Spec 轴各点了一次）
- **Where:** `src/render.rs` 那条用例里的 `LINED_UP`（八格）；
  `src/session/columns.rs` 的 `DirectoryColumn`／`VolumeColumn`／`PageColumn`
- **现状:** 判据（`wrap::width_is_stable`）此刻管得着「摆进列里的字形」，而**哪几格摆成列**
  这件事有两处出处：列在 `columns` 那三个枚举上，措辞这一侧靠 `LINED_UP` 抄了一份。
  往哪张表添一列，`LINED_UP` 不跟着添也不会红。
- **Why it did not block:** 一处出处收不拢：Column 到 Field 的映射写在 `draw/pages.rs` 与
  `draw/directories.rs` 里，那两个模块挂在 `tui` 特性后面，而这条用例要在闸门 2
  （`--no-default-features`）上也跑得到——`render` 摆在 `tui` 外面正是为此。
  要收拢就得把那份映射搬出 `draw`，那是画法那一层的一次重排，不是本票的事。
  代价是**加一列时要在两处各加一次**，而漏掉的后果是新那一列不过这一关、屏上错开一格。
- **走了哪条路:** **认下两处**，并在 `LINED_UP` 那几行的注释里点名它对着的是哪三个 `Column`，
  让加列的人在这一处也看得见。判据本身与「画法那一层自己造的字形」那一半照旧一处出处。
  连带把评审指出的一处缝补上了：**耗时那一列**（`draw::overview::spell`）从前两层都没人管，
  本票给它补了一条用例（`the_elapsed_this_module_spells_is_the_same_width_on_any_terminal`）。
- **Whose call:** 拍板的人（Column 到 Field 那份映射要不要搬出 `draw`，好让这两处收成一处）
- **处置：** 待处理。

## 已了结

只留索引——条目原文已挪进对应票据的《停车场结转》，或内容本就在下表所指的位置。

| | 条目 | 去处 |
|---|---|---|
| Q1 | A 类双页扫描的页间字号跳动是 0.390，为票面基线（0.112）的三倍半 | 并入 Q2 |
| Q2 | A 类双页扫描上的字号跳动比票面据以定案的那个数大 3.5 倍 | page-geometry/04 |
| Q3 | 裁边带进来三个未标定的数，`CONTEXT.md` 的《尚未确立》还没有收它们 | CONTEXT.md《尚未确立》 |
| Q4 | 彩色分支上的裁边要多走一遍全页 OKLab 换算，代价没量过 | CONTEXT.md《尚未确立》 |
| Q5 | `fixtures::gradient` 在默认路径上会被裁掉下面 21.6%，而拿它当 | page-geometry/09 |
| Q6 | 迟滞升档在默认那条路上一处覆盖都没有了 | page-geometry/08 |
| Q7 | 以高为准让目标尺寸没有了上界，极端瘦长的源页会把输出缓冲撑到分配不下 | page-geometry/07 |
| Q8 | `docs/measurements.md`《真机像素完整性》那句「输出恒等于 fit- | page-geometry/06 |
| Q9 | `tests/` 里大批夹具仍拿 `SMALLER_THAN_TARGET`（800×1 | page-geometry/09 |
| Q10 | measurements《A 类位深实测》那四个「哪一档更优」的页数是旧判据下数的 | page-geometry/06 |
| Q11 | `CONTEXT.md` 的词汇表还没有「源页 / 输出页」这一对 | CONTEXT.md《处理对象》 |
| Q12 | `p0-hardening/13` 正文那句「都不要把分阶段计时塞进 `Report`」 | p0-hardening/13 票面 |
| Q13 | `[dev-dependencies]` 里的 `encoding_rs` 与 `zip | page-geometry/09 |
| Q14 | `CONTEXT.md`《I/O 与并发》说读取层与计算层的并发度「两个数不相干」，代码 | CONTEXT.md《I/O 与并发》 |
| Q15 | `tests/isolation.rs` 的 `a_color_page_a_gray_ | page-geometry/09 |
| Q16 | `render::calibration_note` 里那句「大写英文，中文字模装不进一 | calibration-chart/01 |
| Q17 | `calibration-chart/01` 的验收里没有「中文判读说明」这一条 | calibration-chart/01 |
| Q18 | 仓库根没有 LICENSE，也没有一份第三方许可声明。** `calibration-c | LICENSE + THIRD-PARTY-NOTICES.md + licenses/OFL-1.1.txt |
| Q19 | ADR 0007《要认的代价》那条「tonefit 只产出文件，无从感知，也无法在输出时 | page-geometry/06 |
| Q20 | 幂等仍要在碰像素之前预告出全部输出成员名，而 `page-geometry/04` 的  | page-geometry/04 |
| Q21 | 「以高为准下几何门恒成立」现在有了一个例外，而那句话写在 ADR 0007 与 `pag | page-geometry/06 |
| Q22 | 兜底上界的折算系数 8 是照代码数出来的，不是量出来的；而两侧的上界都只管一页 | CONTEXT.md《尚未确立》 |
| Q23 | 本票往 `CONTEXT.md` 词汇表加了两行，而 Q3/Q11 记着「改 CONTE | CLAUDE.md《改 CONTEXT.md 的规矩》 |
| Q24 | `crop::MIN_KEPT` 那个 0.5 仍未标定，而本票往它的文档上又压了 35 | CONTEXT.md《尚未确立》（并入 Q3） |
| Q25 | 默认路径上的迟滞覆盖只走得到抖动那一维，位深那一维仍然只在 fit-inside 上有 | CONTEXT.md《尚未确立》 |
| Q26 | 进度预告在拆分开着时是上界，普通卷的进度条因此停在四分之三处 | CONTEXT.md《尚未确立》 |
| Q27 | 记录里加了来路那一项之后，一条按**字节**比两张页的用例失效了 | page-geometry/09 |
| Q28 | 装订沟的命中与误报只有计数，没有一页是目视核对过的 | CONTEXT.md《尚未确立》 |
| Q29 | 幂等的依据多了一项，而 ADR 0006 仍写着「四项」 | page-geometry/06 |
| Q30 | `src/lib.rs` 一个文件为四件事被改了 627 行，幂等那一组该独立成模块 | CONTEXT.md《尚未确立》 |
| Q31 | 本票往 `CONTEXT.md`《尚未确立》加了两条，而那一节的收口归 06 | page-geometry/06 |
| Q39 | `run` 返回 `Err` 时不发「这一趟完了」，异常收场没有自己的那一条事件 | p1-session/05 |
| Q46 | 中止掉的那一卷只有开卷那一条事件，`run` 的返回值也说不出这一趟是被中止的 | p1-session/05 |
| Q41 | 观察者花掉的时间算进了 `VolumeTiming`，而 `06` 要在决策点上等人 | p1-session/06 |
| Q63 | 退出会话时那条还在写盘的线程要有人收，而按停那两个键还没有 | p1-session/10 |
| Q69 | 跑起来之后连范围层一起冻住了，spec 只要求前两层只读 | p1-session/10 |
| Q64 | 报告区长过一格就只留最后那几行，翻不回去 | p3-session-legibility/10《停车场结转》（卷表有了自己的光标与跟随，光标一挪跟随就停、视口跟着它走）与 11（展开那一副也接上同一份视口：光标停在第几页上，滚动量不再是记着的） |
| Q71 | 按停的状态只在屏底那两行说，主区两条进度条上看不出来 | p1-session/11 |
| Q72 | 一趟跑着的时候展不开，而报告那时正在长 | p3-session-legibility/10《停车场结转》（`Mode` 拆成两维，ADR 0017；展开的索引改成认得出决策点上那一卷的取值）；逐页那一半由 11 落地（跑着与等答话时展开的都是一张逐页表，报告边长它边更新） |
| Q58 | 会话里「没说」与「说了一个恰好等于默认值的值」是两个状态，命令行上它们没有 | p1-session/12 |
| Q56 | `calibrate` 子命令读不到预设，而标定图挂在**设备层**上 | p1-session/13 |
| Q52 | 决策点那一条事件不带这一卷的报告，而会话要在那里把报告画出来 | p1-session/14 |
| Q53 | 单卷在决策点上答收尾，这一趟的收场仍是「走到头」 | p1-session/14 |
| Q77 | 卷报告每卷拼两次，而命令行那一趟只用得上第二次 | p0-hardening/13《停车场结转》（剖面量得动它，不值得收） |
| Q35 | `CONTEXT.md`《尚未确立》那条「A 类素材的一切数值」按字面已经不成立 | page-geometry/06 → CONTEXT.md《尚未确立》 |
| Q48 | 卷内成员名撞到同一个输出上，现在走的是卷级失败，而《失败》的词条把成因列成了那四种 | p1-session/05 → CONTEXT.md《失败》 |
| Q54 | 《会话》的**设备层**词条把内容列成「型号 + 灰阶数覆盖」，而阈值也在那一层 | p1-session/07 → CONTEXT.md《会话》 |
| Q57 | 《会话》的**口味层**词条列着六项，而口味层现在有十一项 | p1-session/07 → CONTEXT.md《会话》 |
| Q61 | `tui` 关掉之后，会话那二十几条用例一条都不跑 | p1-session/08《停车场结转》（p2-loose-ends/01 把状态机搬出了特性） |
| Q68 | `tui` 关掉之后，`09` 新添的那三十几条用例同样一条都不跑 | p1-session/09《停车场结转》（同上一刀） |
| Q79 | `profiling` 特性不在 `cargo test` 的闸门里，clippy 也扫不到它 | p0-hardening/13《停车场结转》（闸门变成三条，计数那一半摘出两个纯函数） |
| Q40 | 「不得在持锁处调观察者」这条性质没有真正的闸门，靠人看加一条弱用例 | p1-session/02《停车场结转》（哨兵落地，持锁报到当场恐慌） |
| Q36 | 兜底上界那道线，仓库里三处给出三个倍数：24:1、12:1、16:1 | page-geometry/06《停车场结转》（算法收进 `MAX_TARGET_PIXELS`，24:1 是系数取 8 那份草稿的残留、已删） |
| Q50 | 卷根整个消失时走的是**页级**失败 → 隔离（退出码 2），不是卷级失败 | p2-loose-ends/05《停车场结转》（判据改成「卷根还在不在」，一页都没成仍走隔离） |
| Q51 | 输出根写不进去时每一卷各记一笔卷级失败，退出码是 `3` 而不是拒绝执行那个 `1` | p2-loose-ends/06《停车场结转》（开工前探一次，写不进去整趟拒掉；`Sink::create` 逐卷那条路留着） |
| Q47 | 幂等那一道被中止时，`volume_fingerprint` 回的是一份**喂了一半**的哈希 | p2-loose-ends/11《停车场结转》（并行不改变这条性质，判定不另立两支的返回类型） |
| Q78 | 归档卷的幂等这一道是三段里第二大的一块，全是把源字节再串行解压一遍 | p2-loose-ends/11《停车场结转》（本票收的就是它：那一段 1758→288 毫秒） |
| Q85 | 报到时手里仍攥着 rayon 工作线程与读取层的在途字节额度 | p2-loose-ends/11《停车场结转》（判定为**不改**，并行之后这条理由更强） |
| Q42 | 点名的每一个归档卷，整趟都占着一个打开的文件句柄 | p2-loose-ends/12《停车场结转》（`volume-discovery/01` 已消掉这笔代价，本票判定**不划句柄上限**） |
| Q96 | 一卷同时开着的归档句柄从 1 涨到 `1 + min(幂等那一道的并发度, 成员数)` | p2-loose-ends/12《停车场结转》（数一格没变，收进 `source::Reader`《一趟同时开着几个句柄》） |
| Q98 | 句柄开不出来时并发度悄悄降下来，而报告照旧印计划里那个数 | p2-loose-ends/12《停车场结转》（判定**不改**：它等的那个前提「有了句柄上限」不会出现） |
| Q33 | 互锁 ③ 的判定在代码里有两处形态，靠一句 `debug_assert!` 拴着 | p2-loose-ends/04《停车场结转》（走第三条路：两维各判各的，抖动那一维的判定就是互锁 ③；按页分岔判定**不做** → Q102） |
| Q32 | 中文的帮助与报告在终端上不折行，本票新加的那几行尤其长 | p2-loose-ends/07《停车场结转》（界面层出了一套按显示宽度折行的工具，帮助、报告、屏底三处共用） |
| Q65 | 折行之后有几行是「画一遍数出来」的，ratatui 那个直接答的函数挂着 unstable 的门 | p2-loose-ends/07《停车场结转》（自答：折出来的那几行就是答案，那道 unstable 的门不开） |
| Q75 | 设备层上多了一个键，屏底那一行在窄终端上就把 `q 退出` 挤出了屏 | p2-loose-ends/07《停车场结转》（屏底那一行折得开，`FOOTER_HEIGHT` 仍是三行的下限） |
| Q83 | 会话那一层胶水（`press` 与它的十条用例）仍在 `tui` 后面，卡在一处折行量算上 | p2-loose-ends/07《停车场结转》（原文那句「画一遍数出来」记错了：`opens_at` 不碰终端，那一层搬得动） |
| Q59 | 逐层补全按前缀**大小写敏感**地筛，而 Windows 的文件系统不是 | p2-loose-ends/10《停车场结转》（走「按平台分支」那一条：一个 `cfg!(windows)` 常量，不折大小写比整串 → macOS 那一档另立 Q107） |
| Q62 | 在会话里改了阈值，屏上却写着「命令行指定」 | p2-loose-ends/10《停车场结转》（那句话改成「点名指定」，不提入口；来源仍只有两种） |
| Q73 | 覆盖一份同名预设要把整份文件重排，手写的注释与排版留不下来 | p2-loose-ends/09《停车场结转》（换了写入端：按节剪、按节接，验不过退回重排；判定**不引** `toml_edit`） |
| Q74 | 会话里存得进一份预设，却删不掉一份 | p2-loose-ends/09《停车场结转》（`d` 在停着一份预设时删它，按两下，光标一挪那一问就作废） |
| Q110 | 《处理对象》的「源」「输出容器」两条词条还按「CBZ」写，输出那半句已经不全 | volume-discovery/03《停车场结转》（「源」改成读取与输出同形、卷边界不同形；「输出容器」点一句扩展名归一并指向《输出》） |
| Q140 | 型号停在内置表外的一个名字上时，按 `←` 会转不回来（死循环） | p3-session-legibility/06《停车场结转》（`back` 加第二个出口：走满一圈没经过出发点就落回环上，型号那一环上正是「没挑」） |
| Q153 | 「隔离 / 跳过 / 没做成」那几个字与四种记号分在两处，而语义色要按同一批分类上色 | p3-session-legibility/09《停车场结转》（`Mark` 多一个 `tone()`：记号与语义出自同一个取值，措辞那一层一个颜色都没长） |
| Q147 | 总览块的抬头在窄终端上由终端库硬截，截出来的时长读起来像一个完整而偏小的估计 | p3-session-legibility/13《停车场结转》（抬头摆不下时从中间省略，`yielding::elided` 一处定完，边框上那五个抬头与钉在格子里的那一行都走它） |
| Q154 | 卷表的四种记号里三种是东亚歧义宽度，而对齐现在靠宽度吃饭 | p3-session-legibility/13《停车场结转》（量下来只有一种是：`–`→`-`、`—`→`-`、`…`→`⋯`，并立了「表这一层造的字形一个都不许是歧义宽度」这条用例；出自 `render` 的两格另立 Q168） |
| Q60 | 屏上逐格读回来的文字，每个宽字符后面多一个空格 | volume-discovery/08《停车场结转》（改成按显示宽度跳格：`probe::read` 一处读完，`screen`／`painted`／`reversed_row` 三条路共用，`contains("设备层")` 从此问得出来；`tight` 留着只管空白） |
| Q127 | 票面的验收第六条说「等答话时按得动的仍只有答话那两个键」，而同一张票面第三条要加第三个键 | volume-discovery/07《停车场结转》 |
| Q129 | 拆开之后有一条用例跨着两块，把 `Prompt` 与它两个字段的可见性顶开了 | p3-session-legibility/01《停车场结转》 |
| Q130 | 按「屏上那几块」切开之后，仍有两样东西跨着两块 | p3-session-legibility/08《停车场结转》 |
| Q131 | 「拼装的规矩住在命令行那一头」落在哪个文件：选了留在 `render` 里，另立一副 | p3-session-legibility/02《停车场结转》 |
| Q132 | 票面把黄金快照当成本票唯一的验收，可 `tests/golden.rs` 根本不走 `src/render.rs` | p3-session-legibility/03《停车场结转》 |
| Q133 | 卷级失败的卷没有自己的一种「行」，而卷表要给它一行 | p3-session-legibility/08《停车场结转》 |
| Q134 | 「离群」的复合词票面没说换不换：离群判据、离群侧、离群检验、离群那一组 | p3-session-legibility/03《停车场结转》 |
| Q135 | 票面说《判据》有「三个词条」要改名，可那一节里只有两条带名字的；「主体」从来没有自己的词条 | p3-session-legibility/03《停车场结转》 |
| Q91 | 「哪几种算拒绝执行」在实现文档里抄了五份，加一种就要五处一起改 | p4-parking-lot/01《停车场结转》 |
| Q104 | 左栏与预设那一栏仍走终端库自己的折行，屏上因此有两套折行规矩 | p4-parking-lot/02《停车场结转》 |
| Q136 | 左栏折行走的是终端库那一套，而视口数的是逻辑行；窄终端上两个数对不上 | p4-parking-lot/02《停车场结转》 |
| Q138 | 票面第一条要 `→` 也摊开，而同一张票的《环那一套保留》要 `←→` 就地转一格 | p4-parking-lot/03《停车场结转》 |
| Q139 | 「摊开的第一格恒是「默认（xxx）」」与位深、抖动那两行印的「自动（判据说了算）」对不上 | p4-parking-lot/03《停车场结转》 |
| Q142 | 底下只有一个型号的那几块面板，票面没说下钻照走还是直接定 | p4-parking-lot/03《停车场结转》 |
| Q143 | 下钻进去那一层没有「没说」那一格，而《取值栏》说第一格恒是它 | p4-parking-lot/03《停车场结转》 |
| Q144 | 面板那一格上 `⏎` 派的是下钻，而票面写的是「`⏎` 定」 | p4-parking-lot/03《停车场结转》 |
| Q103 | 折行不做禁则处理：`）`、`，`、`。` 这几个收尾记号会落在行首 | p4-parking-lot/04《停车场结转》 |
| Q106 | 带空格的记号（`--fit height`）仍会断在中间那个空格上 | p4-parking-lot/04《停车场结转》 |
| Q114 | 缩进后面跟着一长串断不开的字（长路径）时，折行先折出一个空行 | p4-parking-lot/04《停车场结转》 |
| Q105 | 命令行那两处折到一个定死的 100 格，不问终端真有多宽 | p4-parking-lot/05《停车场结转》 |
| Q168 | 表上还有两格靠宽度对齐、而字形出自 `render`：尺寸那一格的 `×`、判据那一串的 `·` | p4-parking-lot/05《停车场结转》 |
