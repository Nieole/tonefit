# 停车场：实现途中撞见、但没有为它停下来的东西

**状态：** 长期（本轮队列全部票据共用）
**路径：** `.scratch/非阻塞问题.md` — 每一份派活说明都要交出这个路径

---

## 这个文件是什么

落地票据会撞出票面没预料到、或者票面写错了的东西。**其中几乎没有一件该让队列停下来。**
它们落在这里：**记下来、绕过去、接着走、收尾时逐条了结。**

它不是产品的长期缺口清单（那是 `CONTEXT.md` 的《尚未确立》）：

| | 《尚未确立》 | 本文件 |
|---|---|---|
| 装的是 | **产品**本身的缺口 | **执行**中冒出的问题——票面写错、规格没说、两处文档互相打架 |
| 寿命 | 比这一轮队列长 | **收尾时应当清空**——每条要么就地了结、要么升进《尚未确立》、要么另立一张票 |
| 谁读 | 下一个动这个产品的人 | 收尾那一步，以及拍板的人 |

---

## 停线——只有这三种

停下来很贵：拍板的人要把整个上下文重新装回脑子。门槛：

1. **继续做下去会写出错的东西，而撤销它要伸手到这张票之外。**
   典型：票面的指示与上游一条仍然生效的决定相抵触。
2. **动作不可逆，而且不止一个方向说得通。**
   典型：删掉或改名一个被多处引用的东西——一个 id、一个字段、一个文件。
3. **前提根本不在，这张票无从下手。**
   典型：「在 X 之上做 Y」，而 X 不存在。

其余一律：记在这里，走最站得住的那条路，接着做。**包括**票面给的数与你量出来的对不上、
验收条读着含糊、票面没想到的第三种情形、路过发现的无关缺陷，以及你确实拿不准的单项。

> ⚠️ **绕过去要留下痕迹。** 条目记的是**你走了哪条路**，不是「当时有两条路」。
> 说不出自己做了什么的条目不算条目。

---

## 待处理

<!-- 新条目追加在这里；了结后连同《处置》一起挪进《已了结》 -->

### Q32 — 中文的帮助与报告在终端上不折行，本票新加的那几行尤其长

- **From:** 票 `05`
- **Kind:** 路过发现
- **Where:** `src/main.rs` 的 `interlock_help`、`src/render.rs` 的 `interlock_lines` 与
  `overflow_tail`、以及 `Cli` 上各开关的长帮助
- **Why it did not block:** clap 按空格折行，而中文长句里一个空格都没有，整句因此原样印成一行。
  **不是本票引进的**：`--dither`、`--no-split` 那几段长帮助与报告末尾的《输出宽超过面板》
  早就是这个样子，终端自己回绕，读得下去。要真改，得在界面层引一套按**显示宽度**折行的工具
  （中文两列、ASCII 一列，且不许把 `--fit height` 这种记号折断），那是一件独立的事，
  且会一次改动几乎每一行既有输出。
- **What this ticket actually did:** **没有引折行工具。**新写的三句互锁措辞按短句拆开、
  用分句读点断开，靠终端自己回绕；`--help` 那一节每条一行、挂一个圆点，
  行首缩进两格让回绕之后仍看得出条目边界。用例因此不比渲染出来的整份帮助，
  改比 `after_long_help` 的原文——折行与否都不影响那条断言。
- **Whose call:** 拍板的人（要不要给界面层一套折行）
- **处置：** 待处理。

### Q33 — 互锁 ③ 的判定在代码里有两处形态，靠一句 `debug_assert!` 拴着

- **From:** 票 `05`
- **Kind:** 路过发现
- **Where:** `src/lib.rs` 的 `candidates`、`nothing_left_error` 与
  `dither_outside_the_gate_error`；`src/interlock.rs` 的 `Interlock::dither_outside_the_gate`
- **Why it did not block:** 两者答的不是同一个问题。`candidates` 答「这一套候选还剩几个」——
  位深与抖动两维一起裁的结果；互锁那一条答「这一趟咬上了 ③ 吗」——只看抖动那一维加几何门。
  把拒绝改成由互锁驱动，要么让 `candidates` 先问互锁再裁（多一道判定，而位深那一维仍得
  自己判空），要么让互锁去数候选集（它就得知道候选集怎么算）。两条都是把一个模块的知识
  搬进另一个，而本票要的是**处置**只写一处，那一条已经做到了。
  还有一件跟着它：拒绝那句话在**碰卷之前**就备好（`Candidates::new`），那时没有页可量，
  因此答不出「手上这一页换个适配方式解不解得了」。真要按页分岔，得把这条拒绝挪到判门的地方（`gray_page`）。
- **What this ticket actually did:** 保留 `candidates` 的裁空判定；`nothing_left_error` 收了
  `gate`，那一支加 `debug_assert!(Interlock::dither_outside_the_gate(request.dither, gate))`
  与一句注释点名它就是互锁 ③。用例跑在调试构建上，等价关系因此每一趟都验。
  错误措辞改由 `Interlock` 自己说，措辞那一侧的出处只有一个。
  按页分岔那一步没做，换成**把话说全**：fit-inside 那一侧仍指 `--fit height`，
  但把「被兜底上界退回的页换过去仍被拒」那道例外一并写进去，用例两条路一起钉着。
- **Whose call:** 拍板的人（要不要让互锁真的驱动那条拒绝）
- **处置：** 待处理。

### Q34 — A 类那两张体积表没有跟着新默认重跑，重跑的只有「哪一档更优」那一组

- **From:** 票 `06`
- **Kind:** 票面没想到的第三种情形
- **Where:** `docs/measurements.md`《A 类位深实测》的逐档体积表与「整卷入出比」那一句
- **Why it did not block:** 票面点名要重跑的是那四个「哪一档更优」的页数，那一组用 `--dry-run`
  就量得出来。体积表要**落盘**跑五卷（4.4 GB 源、几百 MB 输出），而它们摄于页几何批之前那一套
  几何（fit-inside、不裁边、不拆分）——换成今天的默认，页尺寸与页数都变，那是重做一次测量，
  不是订正一句话。
- **What this ticket actually did:** 五卷各跑两趟 `--dry-run`（旧几何一趟、今天的默认一趟），
  **boox-poke6**，逐页比 4bit 不抖与 2bit+FS 的判据读数，两列一并写进那一节。
  旧几何那一列**逐卷复现了** `metric-ordering/01` 给的 177/230、156/185、3/413、40/129——
  原文缺的那句「在哪块面板上数的」因此也补出来了。体积表没有重跑，改为在节首标出施测条件，
  并点明重跑过的只有末尾那一组。顺带量到一件写进了那一节的事：今天的默认下画集 Venus 的
  卷级基准档从 2bit+FS 变成 **4bit 不抖**，换它的是**裁边**——隔离验过，单开裁边就够
  （`--fit inside`，拆分开关无所谓），而裁边关掉、只换适配方式仍判 2bit+FS。
- **Whose call:** 拍板的人（要不要在今天的默认下把 A 类的体积表整批重跑）
- **处置：** 待处理。

### Q37 — `fixtures::TINY`（160×224）在默认那条路上也不便宜，而拿它当「一张便宜的页」的调用还有十六处

- **From:** 票 `page-geometry/09`
- **Kind:** 路过发现
- **Where:** `tests/container.rs`（7 处）、`tests/exit_code.rs`（3 处）、
  `tests/idempotency.rs`（3 处）、`tests/pipeline.rs`（2 处）、`tests/timing.rs`（1 处）
- **Why it did not block:** 与本票了结的 `SMALLER_THAN_TARGET` 是同一类账，但**不是同一个夹具**。
  `TINY` 的文档已经写明它「只在 fit-inside 上还是小页」，而上面那十六处走的是默认那条路：
  以高为准把 160×224 放大到面板高，裁边先削掉渐变下方那 21.6%，出来是 1536×1680——
  比 `cheap_page()` 那张 800×1680 贵将近一倍，而多出来的像素一条断言都没参与。
  不当场处理：这些调用点上 `TINY` 与 `solid(TINY)`、`line_art(TINY)`、`color_page(TINY)`
  是配对出现的（同一卷里几页要同形），只换其中的 `gradient` 那一半会把卷改成混合尺寸，
  反而动了那几条用例的形状；要换就得**整个夹具**一起判——那是一次与 09 号票四条验收都无关的
  用例整理，混进来会把本票淹掉。本票因此只判了 `gradient` 与 `SMALLER_THAN_TARGET` 那两笔。
- **What this ticket actually did:** **十六处一处没动。**本票在 `TINY` 的邻居身上把规矩写了下来——
  `fixtures::cheap_page()` 是「只要一张页」的那一张，`SMALLER_THAN_TARGET` 的文档里点名
  「它不是一张便宜的页」，`gradient` 的文档里点名默认那条路会裁掉它下面 21.6%。
  下一个人按这三处文档去判 `TINY` 那十六处，判据是现成的。
- **Whose call:** 下一张动 `tests/` 夹具的票（把 `TINY` 在默认路径上的那些调用整批判一遍）
- **处置：** 待处理。

### Q38 — 两级停是 `run` 的能力，而命令行这一路还没有按的地方

- **From:** 票 `p1-session/02`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/main.rs` 的 `Bar::observe`（恒回 `Instruction::Continue`）
- **Why it did not block:** ADR 0013 决定第 3 条明写「收尾与中止是 `run` 的能力，不是 TUI 私有的：
  命令行那一路同样用得上」。本票把回路立起来之后，库这一侧已经收得下那两个字，
  缺的只是命令行那一头的**按键**——而那要接 Ctrl+C 信号处理，并决定按一次与按两次怎么分。
  本票的硬约束是「命令行现有行为一字不变」，接线当场就越了界；而它也不该由本票定形状：
  `04`（中止）落地之后中止才真的停得比收尾早，在那之前接上去，用户按第二下看不出区别。
- **What this ticket actually did:** **没有接。**`Bar::observe` 恒回继续，并在文档里点名
  这是「还没有那个键」，不是「命令行不该停」，同时点了 ADR 0013 决定第 3 条。
  Ctrl+C 仍然只有杀进程一条路——与本票落地之前逐字相同。
- **Whose call:** 拍板的人（命令行那一头要不要接 Ctrl+C 信号处理）——
  `10` 已落地会话那一侧的两级停（`s` 按一次收尾、再一次中止），命令行这一头仍然空着
- **处置：** 待处理。**`10` 没有接它**：本票的硬约束是「命令行那一路行为一字不变」，
  而接线要动 Ctrl+C 信号处理——那是命令行入口上的一处新行为，越了那条约束。
  会话那一侧的两级停已经落地，因此「按停是 `run` 的能力」现在**有了一个真的按的地方**；
  ADR 0013 决定第 3 条「命令行那一路同样用得上」仍然只兑现了一半。

### Q43 — 「逐条列出」只覆盖预扫抓到的坏路径，路径不在与扩展名不对仍是一次报一条

- **From:** 票 `p1-session/03`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/lib.rs` 的 `run`：`ensure_no_two_volumes_share_an_output` 排在
  `survey::Survey::of` **之前**；`src/source.rs` 的 `planned_output` → `identity_of`
- **Why it did not block:** 票面说「预扫阶段发现的坏路径整趟拒绝、逐条列出」，这一条做到了。
  但「路径不在」「既不是目录也不是 .cbz」两种到不了预扫——算去处也要看路径，
  撞名那道校验先一步 `bail!` 掉了第一个坏路径，用户于是一次只看见一条。
  两种坏路径因此有两套说法。改法是把撞名校验挪到预扫**之后**、按预扫开出来的卷算去处，
  那样两类坏路径合流；但那会改动一道既有校验的位置与它在别的情形下报出来的话，
  是本票之外的事。**整趟拒绝那一条不受影响**：两条路都在任何卷级事件之前返回 `Err`。
- **What this ticket actually did:** **没有动那道校验的位置。**预扫自己那一份坏路径收齐了再报
  （`src/survey.rs` 的 `refuse`，两条单元用例钉着「逐条列出」与「多了就截断」），
  集成用例 `a_path_that_cannot_be_opened_refuses_the_whole_run_before_any_volume_event`
  用的是**损坏的归档**——到得了预扫的那一类，并在用例文档里点明另两类走的是哪条路。
- **Whose call:** 拍板的人（两类坏路径要不要合流）
- **处置：** 待处理。

### Q44 — 全局条的「结清」每个观察者都得自己做一遍，库不给

- **From:** 票 `p1-session/03`
- **Kind:** 你确实拿不准的单项
- **Where:** `src/main.rs` 的 `Bar::finish_volume`；`src/progress.rs` 的 `Event::RunStarted`
- **Why it did not block:** 「预告是上界，因此实现方要结清」这条要求本身写在
  `Event::RunStarted` 的 `steps` 上（理由在 `CONTEXT.md` 的《进度》）。问题是**谁来做**：
  这件算术眼下写在 CLI 里，会话（`08`/`09`）要画同一条全局条时得再写一遍。
  库那一侧的做法是多一条事件——一卷跑完时报出「这一卷实际走了多少步」，或干脆把结清做进
  `VolumeFinished`——但那是给事件流加形状，而本票没有第二个实现方可以拿来验证这个形状对不对。
- **What this ticket actually did:** **把算术留在 CLI**，并把「实现方要自己结清」写进
  `Event::RunStarted` 的文档（那是下一个实现方一定会读到的地方），CLI 那一份由
  `a_volume_that_stops_early_settles_up_on_the_global_bar` 钉着。
- **Whose call:** `09`（会话主区）——它是第二个实现方，届时看是照抄还是提进库里
- **处置：** 待处理。

### Q45 — 「列前几条，剩下的说个数」这个形状在仓库里已经有三处，各写各的

- **From:** 票 `p1-session/03`
- **Kind:** 路过发现
- **Where:** `src/lib.rs` 的 `ensure_no_two_volumes_share_an_output`、`src/render.rs`（`SHOWN`）、
  以及本票新加的 `src/survey.rs` 的 `refuse`——三处各带一个自己的 `const SHOWN: usize = 5;`
- **Why it did not block:** 共用的只有骨架（先说总数 → 缩进逐条 → 「……另有 N 个」），
  而三处的**条目长什么样**各不相同：撞名那处是「一个去处 + 它下面几个源卷」的两层，
  报告那处是一行一页，本票这处是「路径一行、错误链一行」。抽成一个公共件要把
  「条目怎么渲染」做成闭包参数，省下的是每处四五行，换来的是三个互不相干的模块共用一个
  界面细节——而 `SHOWN` 到底该是 5 还是别的数，本来就该各说各的（一屏能放下几条，
  取决于每条占几行）。
- **What this ticket actually did:** **照既有形状写了第三处，没有抽公共件**，
  并在 `refuse` 的文档里明写它是照撞名那处办的，好让下一个动它的人一眼看见有三处。
- **Whose call:** 拍板的人（要不要给界面层一套「截断清单」的公共件；与 Q32 的折行是同一类问题）
- **处置：** 待处理。

### Q49 — 「列前几条，剩下的说个数」现在是第四处，而 Q45 记的还是三处

- **From:** 票 `p1-session/05`
- **Kind:** 路过发现
- **Where:** `src/render.rs` 的 `failed_volume_tail`（本票新加的第四处 `const SHOWN`）；
  停车场 Q45（它记的是 `03` 落地时的三处）
- **Why it did not block:** Q45 已经把这件事摆在那儿了，理由一字不变——共用的只有骨架，
  条目长什么样各不相同（本票这一处是「路径一行、原因一行」，与 `survey::refuse` 同形）。
  本票照同一条办法写了第四处，没抽公共件。
  **没有去改 Q45 的正文**：那一栏是 `03` 的记录，后来的活不该写进别人的记录里
  （`docs/agents/issue-tracker.md`：落地记录不因后来的工作被改写，改写属于后一张票自己的记录）。
  条数从三涨到四这件事因此记在这里，而不是改那条的标题。
- **What this ticket actually did:** **照同一条办法写了第四处，没抽公共件**，并在本条里
  把条数记清楚，好让拍板的人手上是四处而不是三处。
- **Whose call:** 与 Q45 同一个（拍板的人：要不要给界面层一套「截断清单」的公共件）
- **处置：** 待处理。

### Q55 — 三个布尔开关的覆盖是**单向**的：预设关得掉，命令行开不回来

- **From:** 票 `p1-session/07`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/main.rs` 的 `Cli::crop`、`Cli::per_page`、`Cli::split_rule`
  （`--no-crop`、`--no-split`、`--per-page` 三项）
- **Why it did not block:** 取值那一侧是完整的，不完整的只是**覆盖**——那条规矩连同
  它为什么不咬人，写在 `Cli` 那个 `impl` 的抬头，这里不复述。
  补法是给这三项各加一个反向 flag（`--crop`／`--split`／`--no-per-page`，clap 的
  `overrides_with`），那是**命令行表面的扩张**：本票的硬约束是「不点名 `--preset` 时
  命令行行为一字不变」，加三个新 flag 不违反它，但也不在验收里，且加了之后
  `--help` 上每一项都要多解释一句「它和它的反面」。
- **What this ticket actually did:** **没有加反向 flag。**当下的行为由
  `the_switches_that_only_say_one_thing_stay_one_way` 钉住——它变的那天红的是这一行。
- **Whose call:** 拍板的人（要不要给这三项各加一个反向 flag）
- **处置：** 待处理。

### Q59 — 逐层补全按前缀**大小写敏感**地筛，而 Windows 的文件系统不是

- **From:** 票 `p1-session/08`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/session/complete.rs` 的 `level`（`name.starts_with(prefix)`）
- **Why it did not block:** 中文卷名上不咬人——本仓库面向的库里卷名多是中文与数字。
  咬得上的是 `D:/Comics/` 里打 `d:/comics/c` 这种：路径本身 Windows 认，前缀那一截却筛不出
  `Comics`。改法有两条，各有代价：按 `to_lowercase` 比会让 Linux 上两个只差大小写的目录
  互相污染；按平台分支写两套则要在一个十行的模块里引一个 `cfg`。
- **What this ticket actually did:** **照大小写敏感做，并写进了模块文档**（列出来的东西
  按用户打的写法拼回去，前缀原样留着）。没有引平台分支。
- **Whose call:** 拍板的人（要不要按平台放宽大小写）
- **处置：** 待处理。

### Q60 — 屏上逐格读回来的文字，每个宽字符后面多一个空格

- **From:** 票 `p1-session/08`
- **Kind:** 路过发现
- **Where:** `src/session/draw.rs` 的 `tests::tight`
- **Why it did not block:** 一个汉字在缓冲里占两格，第二格被 ratatui `reset` 成一个**空格**
  （不是空串），逐格 `Cell::symbol` 拼回来的文字因此是「设 备 层」。
  用 `contains("设备层")` 去问屏上有没有这几个字，一律不成立。
  `09` 要做的渲染快照多半走 ratatui 自己的 `Buffer` 比对（那一条路没有这个问题），
  但只要有人顺手写一句 `contains`，就会掉进来一次。
- **What this ticket actually did:** **本票的两条屏幕断言先把两边的空白都去掉再比**
  （`tight`），并把为什么写在那个函数的文档里。没有引任何折宽/去宽的工具——
  那是 Q32 那一条（按显示宽度折行）的地界，一件独立的事。
- **Whose call:** `09`（会话主区的渲染快照）——它是下一个要在屏幕缓冲上写断言的票
- **处置：** 待处理。

### Q62 — 在会话里改了阈值，屏上却写着「命令行指定」

- **From:** 票 `p1-session/08`
- **Kind:** 路过发现
- **Where:** `src/profile.rs` 的 `ThresholdSource::Pinned`（`Display` 里那句「命令行指定」）；
  `src/session/state.rs` 的 `threshold_shown`
- **Why it did not block:** 会话里那一行照 `Threshold` 自己的 `Display` 印，
  为的是**不另编一套说法**（spec 的 Further Notes）——而那一份里「用户点名的界」写作
  「命令行指定」。在会话里点的名不是从命令行来的，那半句因此读着不对。
  改法只有一条站得住：把库那一句换成不提入口的写法（例如「点名指定」）。
  那是**报告文字**的改动，会落到 `tests/golden.rs` 的黄金基线上——本票的硬约束是
  黄金快照不该变，而这一句与本票要做的事无关。
  眼下不咬人：命令行是唯一点得动阈值的入口，会话那一行只在用户自己刚打完那个数时露面。
- **What this ticket actually did:** **照库那一份原样印。**没有在会话里另写一句，
  也没有动库里那一句。`the_threshold_row_prints_the_source_the_report_prints` 钉住
  「两处逐字相同」——库那句改了，这一条不会红，但两处永远不会走散。
- **Whose call:** 拍板的人（那句「命令行指定」要不要换成不提入口的写法，
  连同重录一次黄金快照）
- **`12` 撞上之后：判成 `Pinned`，措辞那一半仍归拍板的人。**
  本票被点名回答「会话改出来的阈值算哪一种来源」，判**仍是 `Pinned`，不加第三个变体**：
  ① `ThresholdSource` 分的是**这个数怎么定出来的**——盲测标定的内置值，还是**用户点名**的界，
  而不是「从哪个入口点的名」。会话里打的那个数与命令行上敲的那个数是同一件事：
  都是用户在自己那台面板上量出来的（ADR 0002），判定也只看数值。
  `ThresholdSource::Pinned` 自己的文档第二句写的正是这个（「用户在自己那台设备上数出来的界
  走这一条」），代码一个字都不必改。
  ② 加一个 `Session` 变体会让同一个数**因为从哪个键进来而印出不同的话**，
  而读报告的人要判断的是「这个界对我手上这块面板成不成立」，入口对他没有用。
  ③ 本票又添了**第三个入口**：预设的设备层可以写 `threshold`，而它经
  `Profile::with_threshold` 同样成 `Pinned`——预设既不是命令行也不是会话。
  三个入口一个来源，这条判断因此更站得住，而那句「命令行指定」也更明确地错了。
  **没有改那句措辞**：本票的硬约束是「命令行那一路行为一字不变」，而报告里那一行
  正是命令行的输出。剩下要拍的只有一句话（例如换成「点名指定」），
  连同 `tests/pipeline.rs` 的 `the_threshold_says_where_it_came_from` 里那条断言。
  顺带更正原条目的一处：黄金基线 `tests/golden-snapshot.txt` 里**没有**这句话
  （那一趟不点 `--threshold`），改它不必重录黄金快照。
- **处置：** 待处理（只剩措辞一句）。

### Q65 — 折行之后有几行是「画一遍数出来」的，ratatui 那个直接答的函数挂着 unstable 的门

- **From:** 票 `p1-session/09`
- **Kind:** 路过发现
- **Where:** `src/session/draw.rs` 的 `past_the_top` 与 `tall_enough`
- **Why it did not block:** Q64 那一条要「滚到底」，而滚多少取决于**折行之后**有几行。
  直接答得出的只有 ratatui 自己的 `Paragraph::line_count`，它在 0.30.2 上挂在
  `unstable-rendered-line-info` 后面（`instability::unstable`），从本仓库调不到——
  要调就得在 `Cargo.toml` 里给一个可选依赖开一个**不稳定**特性，
  那是一条依赖面上的决定，不是本票该顺手拿的。
- **What this ticket actually did:** **让它自己画一遍，再数**：把同一份文字画进一块够高的
  临时缓冲，从底下往上找第一行不空的。折行的规矩因此仍旧只有 ratatui 那一份，
  本仓库没有第二套「中文两列、按词断行」的算法（那是 Q32 的地界）。
  临时缓冲多高由 `tall_enough` 给一个**上界**——按词断行时每行至少放得下一个词，
  行数因此不超过「每个词各占一行」；它只用来定缓冲大小，估宽了只多占些内存。
  代价是每帧多画一遍报告：一帧一次，几十卷的报告也不过几万格。
- **Whose call:** 拍板的人（要不要给 ratatui 开 `unstable-rendered-line-info`，
  或者等它转正——转正之后这两个函数一起删掉）
- **处置：** 待处理。


### Q66 — 最后那一趟没做成，先前那一趟试算的报告也就印不到 stdout 了

- **From:** 票 `p1-session/09`
- **Kind:** 票面读着含糊
- **Where:** `src/session/run.rs` 的 `Running::report`；`src/session/live.rs` 的 `undone`
- **Why it did not block:** 票面说「退出会话时把报告照原格式印到 stdout」，
  没说一个会话里跑了好几趟时印哪一趟的。本票取**最后那一趟**：退出码也取它，
  两者取同一趟才对得上（`tonefit > 报告.txt` 之后读 `$?`，说的得是同一件事）。
  **没做成**的那一趟没有报告可带（`run` 返回的是错误本身），于是那一次 stdout 上
  一个字节都没有——哪怕先前刚跑过一趟好好的试算。这与命令行那一路是同一条规矩，
  那边没做成时同样什么都不印。
- **What this ticket actually did:** **取最后那一趟，没做成就不印。** 攒到一半的那一份仍留在
  `Live` 上、仍画在主区（会话不因此退出，用户当场改），只是不印到 stdout。
  「没做成」收着**两种**：拒绝执行，以及那条线程恐慌了——两者都没有报告可带、
  退出码都是 `1`，分得开它们的是那句话本身（主区上印的就是它）。
  用例 `a_refused_run_leaves_the_session_open_and_prints_nothing` 钉住这一条。
- **Whose call:** 拍板的人（一个会话跑了好几趟时，stdout 上该有哪一趟）
- **处置：** 待处理。


### Q67 — 口味层每一项的默认值原本只写在 `Cli` 上，会话拼不出同一份 `Request`

- **From:** 票 `p1-session/09`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/preset.rs` 的 `impl TasteLayer`；`src/main.rs` 的 `Cli::crop`、
  `Cli::split_rule` 等那几个方法
- **Why it did not block:** 会话也要拼一份 `Request`（试算与执行只差 `mode` 一格），
  而「预设没说就落到默认值」这一步原本逐项写在 `Cli` 的那几个方法里
  （`preset.taste.crop.unwrap_or(true)`）。照抄一份到会话里，同一条命令与同一次会话
  就会在无人察觉时分家——而那正是本票「共用同一套」要防的那类事。
- **What this ticket actually did:** **把「落到默认值」那一步搬进 `TasteLayer` 自己的方法**
  （`fit()`、`crop()`、`split_rule()`……），`Cli` 改读它们，会话直接用。
  命令行那一路因此**行为一字不变**——同一段代码走过去，而不是靠比对；
  用例 `an_untouched_session_asks_for_what_a_bare_command_line_asks_for` 把两边逐项钉在一起。
  没有搬的是「命令行上显式点到的那一项赢」那条规矩与三个布尔开关的单向覆盖：
  那是命令行自己的事，搬过去会把会话不需要的东西一起带进来。
- **Whose call:** 拍板的人（`Cli` 那几个方法要不要整个收进 `preset`）
- **处置：** 待处理。

### Q70 — 闩的编码在两个 crate 里各有一份，靠两条用例分别拴着

- **From:** 票 `p1-session/10`
- **Kind:** 路过发现
- **Where:** `src/progress.rs` 的 `Standing` 与 `Instruction::code`／`from_code`；
  `src/session/run.rs` 的 `Latch` 与 `code`／`from_code`
- **Why it did not block:** 两处记的不是同一件事——库那一份记的是**观察者答过**什么
  （`Standing`，活在 `run` 的栈上），会话那一份记的是**用户按过**什么
  （`Latch`，活在 `Running` 上、一趟一份）。但两者把 `Instruction` 编成一个字节的那三行
  逐字相同，而库那一份是 `pub(crate)` 的，二进制 crate 够不着。
  要合成一份，得把「一个原子的 `Instruction`」导出成公共 API——那是一条 API 面上的决定，
  且它会把一个只有两个内部用户的形状钉进对外契约，不是本票该顺手拿的。
  它还会再长一份：ADR 0013 决定第 3 条说按停是 `run` 的能力、命令行那一路同样用得上，
  而命令行那一头接上的那天（Q38）就是第三份。
- **What this ticket actually did:** **各留一份，两处互相点名。** 会话那一份的文档写明
  它与库那一份的分别（记的是谁答的字）、为什么够不着那一份、以及**序只有一个出处**——
  `Instruction` 派生的 `Ord`，两处都只是把那个序编成一个字节。
  两处各有一条用例把「编进去的数」与「派生出来的序」拴在一起
  （库那侧的 `the_three_instructions_are_ordered_by_how_hard_they_stop`，
  会话这侧的 `the_latch_only_ever_goes_up`）：改一次变体顺序，两条一起变红。
- **Whose call:** 拍板的人（要不要把「一个原子的 `Instruction`」导出成公共 API）
- **处置：** 待处理。

### Q72 — 一趟跑着的时候展不开，而报告那时正在长

- **From:** 票 `p1-session/11`
- **Kind:** 确实拿不准的单项
- **Where:** `src/session/state.rs` 的 `Mode`、`running_action` 与 `expanded_action`
- **Why it did not block:** 票面没说展开在跑着的时候要不要有。真要有的话，
  `Mode` 就得拆成两维——「正跑着」与「正展开着」要同时成立——而那正是
  `p1-session/10` 在 Q69 上判过不拆的那件事。跑着时还有第二重代价：
  那一刻按得动的只有 `s`（按停），而展开态要吃掉上下左右与 `⇥`，
  按停那个键在一屏满是滚动键的提示条上会被挤没。
- **What this ticket actually did:** **展开只从浏览进得去。** 跑着时 `e` 派
  `Action::Ignored`（`running_action` 那一条 `Key::Char(_)` 收着它，
  `which_keys_do_what_in_which_state` 第六段逐个问过）。理由写进
  `CONTEXT.md` 的《会话》：报告那时还在长，而那一刻按得动的只该有停。
  代价是几十分钟的一趟里想回头看第一卷的逐页，得等它收场——而收场之后
  报告一行不少，展开看到的与跑完再看是同一份。
- **Whose call:** 拍板的人（跑着的时候要不要能展开，也就是 `Mode` 要不要拆成两维）
- **处置：** 待处理。**Q64 剩下的那一半也在这里**：跑着的时候报告区仍旧只滚到底、
  翻不回去，而翻回去落在展开那一副上——两件事同进同出，拍这一条就一起拍了。

  **`p1-session/14` 查过能不能一起收，结论是不能，理由多了一条硬的。**
  那张票加的「等答话」（`Mode::Deciding`）看上去正是「跑着但要收键」的那一刻，
  而且管线此刻**真停着**——报告不在长，本条头一条理由在那一刻不成立。
  但第三条理由挡在前面，而它不是权衡、是次序上的不可能：**展开数的是
  `Live::report().volumes`，而决策点上要看的那一卷根本不在里面**——它停在
  `Live::summarized` 上，要等答完话、那一卷收摊了才进报告（见 Q52 的处置）。
  在决策点上按展开，摊开的会是**上一卷**的逐页，而不是眼前正要拿主意的这一卷。
  真要收，得先把展开的索引从「报告上第几卷」改成一个认得出这两处的东西，
  而那一步会把 `Expansion::volumes`（进来那一刻记下的数）一并牵动。
  另外两条代价照旧：`Mode` 仍要拆成两维（Q69 判过不拆），
  而展开态要吃掉上下左右与 `⇥`，答话那两个键（`x`／`s`）在一屏满是滚动键的提示条上会被挤没。
  `14` 因此把等答话做成了一个**平的状态**（`Deciding` 与 `Running` 并列，不是 `Running` 上的开关），
  `Mode` 仍是一维的——与 `10`、`11` 拿的那一条同一个形状。

### Q73 — 覆盖一份同名预设要把整份文件重排，手写的注释与排版留不下来

- **From:** 票 `p1-session/12`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/preset.rs` 的 `insert`（重排那一条）；`Cargo.toml` 的 `toml = "1.1.4"`
- **Why it did not block:** 存**新的**一份不吃这个代价——那一条是**追加**，原文一个字节都不改
  （注释、排版、连本模块读不懂的那几份预设全部原样留着，用例
  `saving_a_new_preset_leaves_every_byte_of_the_file_where_it_was` 逐字钉住）。
  只有**覆盖同名**那一条要重排：那一份要换掉，而 `toml` 1.1 没有保留格式的编辑端
  （`Spanned` 只给得出**键**的位置，`[preset."名字".device]` 那一节的正文范围它答不出来——
  本票实测过）。真要保住，得引 `toml_edit`，而那是一条**依赖面上的决定**，
  不是本票该顺手拿的（先例：Q65 判「要不要给 ratatui 开一个不稳定特性」是同一类）。
- **What this ticket actually did:** **两条路分开走，并把代价说在按下去之前。**
  新名字追加、原文不动；同名重排，而重排之后**每一份预设的内容都还在**
  （读不懂的那几份按不透明的 `toml::Value` 原样搬过去，用例
  `overwriting_one_preset_keeps_the_others_but_not_the_comments` 一并钉住注释**没有**留下——
  哪天格式保得住了，那条断言会反过来提醒人改这一段）。
  追加完还验一遍（读得回同一份、别的几份一个没变），验不过才退回重排——
  `preset` 那张表写成内联表时接不上一节，那一种就走重排。
  屏上那句在覆盖的**第一下**就说出口：「再按一次 ⏎ 覆盖它。覆盖会把那份预设文件整个
  照标准格式重排，手写的注释与排版留不下来」。
- **Whose call:** 拍板的人（要不要引 `toml_edit` 换一个保住格式的写入端）
- **处置：** 待处理。

### Q74 — 会话里存得进一份预设，却删不掉一份

- **From:** 票 `p1-session/12`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/session/state.rs` 的 `listing_action`（预设那一栏的按键表）
- **Why it did not block:** 票面三条动作写的是**存、列出、套用**，没有删。
  眼下不咬人的原因是有出路：那份文件的位置屏上刚说过（存完那一句里带着全路径），
  用编辑器删掉那一节就是了；而覆盖同名是有的——名字打错之后重存一份对的，
  错的那份留在清单上不影响任何一趟。
  真要加，`d` 那个键是现成的（范围层那一栏上它就是「删掉这一条」），
  但删一份预设与删一条卷不是一个量级：后者是这一趟的一行，前者是盘上一份长期存着的东西，
  按错一下没有撤销。那就得再来一次「按两下」，而屏底那一行已经摆着四组键了。
- **What this ticket actually did:** **没有加删。**`d` 在预设那一栏上派 `Action::Ignored`
  （`which_keys_do_what_in_which_state` 第八段逐个问过）——屏上也就不摆它。
- **Whose call:** 拍板的人（会话里要不要能删一份预设，要的话按几下）
- **处置：** 待处理。

### Q75 — 设备层上多了一个键，屏底那一行在窄终端上就把 `q 退出` 挤出了屏

- **From:** 票 `p1-session/13`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/session/draw.rs` 的 `browsing_keys` 与 `FOOTER_HEIGHT`；
  快照 `TOO_NARROW_FOR_TWO_COLUMNS`
- **Why it did not block:** 屏底那一行**不折行**，窄了就从行尾切掉——这是 `09` 定下的样子，
  `12` 添 `p 预设` 时已经为它把「展开逐页」缩成了「展开」。本票在设备层那三行上再添
  `c 出标定图`，64 列的终端上尾巴又少露一截：快照里那一行现在停在 `· p ` 上，
  `预设 · q 退出` 整个切掉了。
  眼下不咬人有三条：只有设备层那三行受影响（一个卷都没打进来时左栏共十六行），
  别的行上那一行一字未变；
  `Esc` 与 `Ctrl-C` 都退得出去，而 `Ctrl-C` 在**每一个**状态下都是退出；
  宽终端（快照另一条用的 120 列）上一个键都不缺。
  要真收，得在界面层引一套按**显示宽度**折行的工具（与 Q32 是同一件事，中文两列、
  ASCII 一列，且不许把 `←→`、`⇧⇥` 这种记号折断），或者把 `FOOTER_HEIGHT` 加到四行——
  后者是从主区身上拿宽度里最贵的那一截，为的是一个只在窄终端上出现的问题。
- **What this ticket actually did:** **把键排在行内那个动作后面、通用那几个键前面，
  并把这件事写进那条快照用例的说明里。** 排在前面而不是尾巴上，是因为它与
  `←→ 换一个` 是同一类——**这一行上按得动什么**；排到尾巴上则等于反过来：
  设备层上永远看不见它，而那正是它唯一有效的三行。
  屏底那一格另有一处让位：说的话有几行就从上面让几行（`footer`），
  出标定图那两行因此挤掉的是浏览那一副本来就空着的第二行，不是任何一个键。
- **Whose call:** 拍板的人（屏底那一行要不要折行，或者 `FOOTER_HEIGHT` 要不要加一行）；
  与 Q32（中文不折行）是同一个决定的两半
- **处置：** 待处理。

### Q76 — `Event::PassStarted` 多了一格，而三条里只有一条用得上它

- **From:** 票 `p1-session/14`
- **Kind:** 你确实拿不准的单项
- **Where:** `src/progress.rs` 的 `Event::PassStarted`（`so_far: Option<&VolumeReport>`）；
  `Events::pass_started` 与 `Events::ask_before_the_second_pass`
- **Why it did not block:** 这是 Q52 的处置换来的：决策点非带一份报告不可，
  而 Q52 列的两条路里，「会话自己攒一份」到有消费方时才看得清走不通。
  眼下不咬人有三条：变体**非穷尽**，库外的 `match` 本来就要带 `..`，
  三个实现方（命令行进度条、会话、用例记账本）没有一个被逼着改；
  那一格的语义写死在文档里（只有决策点带，另两遍恒 `None`），
  `tests/resume.rs` 的 `the_decision_point_carries_the_volume_report_as_it_stands`
  连「别的两遍不带」一起钉着；而 `Pass::Second` 本来就已经是**唯一一条答复当场作数**的事件，
  这一格跟着它走，没有让另一条事件变得特殊。
  露出来的是形状上的一句话：**一个变体里有一格只对它三个取值中的一个有意义**。
  另一条路是给它单开一条事件（比如「这一卷汇总好了」，带报告；决策点仍旧是 `PassStarted`），
  那样每一格都恒有意义，代价是决策点那一刻发两条事件、而会话要把前一条记住等后一条——
  两条事件之间的那一段成了一个必须守住的不变量。
- **What this ticket actually did:** **走了「加一格」那一条，并把语义写在那一格自己身上。**
  `Pass::Second` 的文档、`Event::PassStarted` 的 `so_far` 文档、
  `Events::pass_started`（「这两遍不带 `so_far`」）三处各说一次它管什么，措辞不重复。
  没有单开事件：决策点是**一个**问话点，拆成两条事件就等于把它拆成两个。
- **Whose call:** 拍板的人（`Event` 上「一格只对一个取值有意义」这种形状要不要收）
- **处置：** 待处理。

### Q80 — 票面说的 `fast` 模式在实现里并不存在

- **From:** 票 `p0-hardening/13`
- **Kind:** 票面前提与实现对不上
- **Where:** 票 `p0-hardening/13` 的 What to build；`CONTEXT.md` 的《模式》；
  `p0-core-pipeline/spec.md` 的 Out of Scope；`src/request.rs` 的 `Mode`
- **Why it did not block:** 票面写「`fast` 模式达到 spec 定的每秒八十页」，
  而命令行上没有 `--mode`，`Mode` 只有 `Process` 与 `DryRun` 两个取值。
  `CONTEXT.md` 的《模式》说 `fast` 关闭 oxipng、仲裁仅在边界带触发，
  而 P0 的 Out of Scope 把 oxipng、SSIMULACRA2 仲裁与 `best` 模式整个摘了出去——
  **默认构建就是 `fast` 描述的那个配置**，两档因此还没有分岔。
  同一句里那个「spec 定的每秒八十页」也翻不到出处：`.scratch/` 下没有任何一份 spec
  写着这个数，它只出现在本票票面上。
- **What this ticket actually did:** **按「默认构建即 `fast`」施测，并把目标改判在
  measurements 里。**四卷全部走默认特性、默认参数的 release 构建。
  八十那个数没有 spec 可改，改判因此落在 measurements 的
  《分阶段耗时剖面》小节《吞吐目标：八十页/秒不成立，改判》，
  按素材形态分四档，各带一条实测依据；票面那一条验收指向它。
  `Mode` 与《模式》一个字没动——两档真分岔那一天要不要一个 `--mode`，是另一张票的事。

### Q81 — spec 那句「解码一步就会吃光单页预算」被剖面证否，但没有改它

- **From:** 票 `p0-hardening/13`
- **Kind:** 上游文档与实测对不上
- **Where:** `.scratch/p0-core-pipeline/spec.md` 的《输入》一节（AVIF 走 dav1d 那一段）
- **Why it did not block:** 那一句写「它是唯一能满足吞吐目标的选项……解码一步就会吃光
  单页预算」。本票的剖面把解码量出来了，它在四卷里都不是瓶颈，B 类那一卷更是最低的一档
  （数在 measurements 的《分阶段耗时剖面》）。照剖面反推，换成慢 2.9 倍的 rav1d
  也只让整趟慢个位数百分比，远谈不上「吃光预算」。
  **但决定本身不变**：dav1d 更快且已经在用，没有任何理由回头。
  错的只是当初那一句的论证强度——它是按「纯解码循环的满核页/秒」外推的，
  而整条管线在同一批页上还要付掉十倍于解码的算力，这一点当时没有数。
  不改它的理由是 `docs/agents/issue-tracker.md` 的那条：交付过的记录
  「is a historical record and is not rewritten when later work supersedes it」——
  这是被后来的测量**取代**，不是当初就写错的事实。
- **What this ticket actually did:** **一个字没改那份 spec，把新数放在 measurements。**
  剖面里解码那一行连同「13 号票立票时按纯解码满核推断解码占三分之一预算，剖面证否了这一条」
  一句写在《分阶段耗时剖面》。下一个动 AVIF 解码或者重估 P0 的人，从那一节读得到真数。
- **Whose call:** 拍板的人（那份 spec 的论证要不要补一条「后来测出来是这样」的脚注）

### Q82 — 闸门是三条命令，而仓库里没有一处**自动**跑它们

- **From:** 票 `p2-loose-ends/01`
- **Kind:** 票面没想到的第三种情形
- **Where:** `docs/agents/gate.md`；`CLAUDE.md` 的《闸门》；仓库根（没有 CI 配置，也没有 `xtask`）
- **Why it did not block:** spec 的《Testing Decisions》说闸门那一条「断言写在 CI 脚本或
  `xtask` 上，不是写在用例里」。仓库里两样都没有——没有 `.github/`、没有 `xtask`
  成员、没有 `justfile`。立一个只为跑三条 `cargo` 命令，是往仓库里加一整套构建脚手架
  （`xtask` 要加一个 workspace 成员与一份 `.cargo/config.toml` 的别名），
  而它能保证的事情与「照着 `docs/agents/gate.md` 敲三条」一样多：**没有人替你按下去**。
  真要让闸门自己跑起来，缺的不是脚手架而是 CI，那是另一件事。
- **What this ticket actually did:** **写成文档，没写成脚本。**三条命令落在
  `docs/agents/gate.md`（各盖住什么、哪种改动必须跑满三条、结果怎么读），
  `CLAUDE.md` 的《闸门》给三行命令加一个指路。**票面第一条要的是「在一处写明它们是闸门」**，
  这一条成立；「有人自动按下去」不在票面上。
- **Whose call:** 拍板的人（这个仓库要不要 CI）
- **处置：** 待处理。

### Q83 — 会话那一层胶水（`press` 与它的十条用例）仍在 `tui` 后面，卡在一处折行量算上

- **From:** 票 `p2-loose-ends/01`
- **Kind:** 路过发现
- **Where:** `src/session/terminal.rs` 的 `press`／`expand`；`src/session/draw.rs` 的 `opens_at`
- **Why it did not block:** 状态机够不着的那几支（起一趟、按停、展开、读写预设、出标定图）
  落在 `press` 这一层，而它整层只有一处真要终端库：`expand` 换一卷时要把视口对到那一卷的
  抬头上，那个行号是 `draw::opens_at` **让 ratatui 往一块临时缓冲上画一遍数出来的**
  （停车场 Q32／Q65 记着为什么不自己估）。搬这一层出去因此得先给那个数开一条缝——
  往 `press` 上加一个「量折行」的函数参数，或者把滚动量整个挪进状态机。
  两条都是改形状，而本票要的是搬模块不改行为；何况 `p2-loose-ends/07`（中文折行）
  要动的正是折行那一套，形状该由它定。
- **What this ticket actually did:** **只搬了不碰终端库的那四个模块**
  （`state`、`live`、`run`、`complete`，54 条用例）。胶水连同进出终端与键码翻译一起
  下沉成 `session::terminal`，仍挂 `tui`：那一层的 12 条用例（含 `press` 的 10 条）
  照旧只在默认那一趟跑。票面第二条要的是「状态机的全部用例」，这一条成立。
- **Whose call:** `p2-loose-ends/07`（折行那一套定形之后，滚动量该由谁算）
- **处置：** 待处理。

### Q84 — 关掉 `tui` 那一趟，整个 `session` 上放开了 `dead_code`

- **From:** 票 `p2-loose-ends/01`
- **Kind:** 我确实拿不准的单项
- **Where:** `src/main.rs` 的 `#[cfg_attr(not(feature = "tui"), allow(dead_code))] mod session;`
- **Why it did not block:** 状态机那四个模块搬出特性之后，`tui` 关掉的那一趟里读它们的只剩
  它们自己的用例——画法与那条循环整个不编译，于是只有画法读得到的取值器
  （`Live::resumes`、`Session::title`、`Presets::names` 等，`--force-warn dead_code` 数出来
  是九处：`live.rs` 四处、`state.rs` 五处）全成了「没人读」。
  票面第五条要的是「关掉之后不留 `dead_code` 告警」，而那九处不是死代码，
  是这一趟的前提。真要一条不放松，只有把画法也搬出终端库，而画法搬不出去。
- **What this ticket actually did:** **在关掉的那一趟上整模块放开，默认那一趟一格不放松。**
  `cargo clippy --all-targets` 与 `--no-default-features` 两遍都干净，
  真死掉的东西仍由第一条闸门当场红。**放开的面比那九处宽**：`mod session` 上一句管住
  它下面全部四个模块，`complete` 与 `run` 眼下一处不占，但**将来**在它们里添的死代码
  在第二条闸门上也不报了。代价因此是将来的，不是眼下的——收窄它得逐处挂 `cfg_attr`，
  那是把一句噪声换成九句。
- **Whose call:** 拍板的人（这笔放松认不认、要不要收窄到逐处）
- **处置：** 待处理。

### Q86 — 哨兵「发布构建上整个不在」那一半，三条闸门一条都够不着

- **From:** 票 `p2-loose-ends/02`
- **Kind:** 我确实拿不准的单项
- **Where:** `docs/agents/gate.md` 的三条；`src/progress.rs` 的 `LockSentinel`
  与 `src/lib.rs` 的 `CacheGuard`（那几处 `cfg(debug_assertions)` 与
  `cfg_attr(debug_assertions, track_caller)`）
- **Why it did not block:** 三条闸门跑的都是 dev profile（`debug_assertions` 开着），
  验的全是哨兵**在**的那一半。「不在」那一半只有发布构建看得见，而那不是闸门里的命令。
  本票试过给它一条编译期断言（比 `CacheGuard` 与 `MutexGuard` 的大小），复审时发现那是**空的**：
  `LockSentinel` 是零尺寸类型，带不带那一格都是 16 字节，断言恒真。真正的开销在
  `Drop` 那两次线程局部加减与 `#[track_caller]` 多传的那个隐式参数，`size_of` 一个都量不到。
  要真验它，得让闸门多一条发布构建（`cargo check --release` 只验编译不过与不引用，
  不验开销），或者引一套基准——两样都比本票大。与 Q79（`profiling` 不在闸门里）同一族，
  与 Q82（闸门没有 CI 替你按下去）相邻但不是同一件事。
- **What this ticket actually did:** **删掉了那条空断言，没有换一条真的。**「发布构建上整个不在」
  眼下靠的是那几处 `cfg` 本身，加上收尾时手跑的一条 `cargo check --release`——
  它够得着的只有「没有一处无条件引用了被 `cfg` 掉的东西」，验不了开销。
  票据的《数》里如实记着这一条是闸门之外手跑的。
- **Whose call:** 拍板的人（闸门要不要多一条发布构建）
- **处置：** 待处理。

### Q87 — 折算系数还是 8 那会儿的两处残留：07 号票的报告样例印 67108864，已了结索引 Q22 写「系数 8」

- **From:** 票 `p2-loose-ends/03`
- **Kind:** 两处文档打架
- **Where:** `.scratch/page-geometry/issues/07-target-size-needs-a-backstop.md`《落地记录》的
  《报告》一段（「兜底上界 N 页：…越过 67108864 像素…」）；本文件《已了结》索引里 Q22 那一行
  （「兜底上界的折算系数 **8** 是照代码数出来的」）
- **Why it did not block:** 折算系数最终定成 **16**、上界是 **32 Mi**（33554432，
  `geometry::MAX_TARGET_PIXELS`；用例
  `the_backstop_is_the_decode_limit_divided_by_what_a_target_pixel_costs` 钉住这个数），
  报告印的也是 `max_target_pixels()` 这一个数。上面两处是系数取 8（512 MiB ÷ 8 = 64 Mi）
  那会儿写下的，没跟着改。两处都是**已了结票据与索引的历史记录**，读代码的人一处都碰不到；
  而改一张已了结票据的落地记录，先例是不改（`page-geometry/06` 对 Q21 的处置：
  「05 号票的票面不改——它是已了结票据的历史记录」）。
- **What this ticket actually did:** **两处一个字没改**，反而拿它们当了证据：
  `GeometryGate::Broken` 那个 24:1 正是同一份草稿的第三处残留（64 Mi ÷ 1680² = 23.8），
  查证与结论见 `p2-loose-ends/03` 的《24:1 是什么》。当前成立的那道线只有一处出处
  （`MAX_TARGET_PIXELS` 的《多瘦长的页够得着这道线》），代码、文档与用例都指着它。
- **Whose call:** 收尾的人（已了结票据里的过时数字要不要统一订正）
- **处置：** 待处理。

### Q88 — spec 把「卷根整个消失」的判据写成「一页都没成」，而票面要的是把这两种分开

- **From:** 票 `p2-loose-ends/05`
- **Kind:** 两处文档打架（打架的是 spec 与票面，不是票面与实现）
- **Where:** `.scratch/p2-loose-ends/spec.md`《退出码与失败口径》第一条
  （「**卷根整个消失** → 卷级失败（`3`）……判据是「一页都没成的卷不算做出来了」」）；
  票面 `.scratch/p2-loose-ends/issues/05-a-volume-that-vanished-did-not-get-made.md`
  的第三条验收
- **Why it did not block:** 两处给的是**两个不同的判据**。照 spec 那一句做，「卷根还在、
  里面每一页都读不出来」的卷会跟着一起被收进卷级失败，而那正是 p0 的 12 号票定死的
  页级失败，也正是 `CONTEXT.md`《失败》那句「失败是页级的，隔离是卷级的：一页失败，
  整卷进隔离目录」说的东西；票面第三条反过来明写「前者仍走隔离，后者走卷级失败——
  两种各有用例，且退出码不同」。票面更细、更晚，且与词汇表一致，因此照票面做。
- **What this ticket actually did:** 按**票面**落地：判据是**卷根还在不在**
  （`lib.rs` 的 `ensure_the_volume_root_is_still_there`），两种各有一条用例、退出码分别是
  `3` 与 `2`。**没有改 spec 那一句**——spec 是拍过板的文件，订正它是领域决定不是实现细节
  （同 `CLAUDE.md`《改 CONTEXT.md 的规矩》那条线）。
- **Whose call:** 拍板的人（要不要把 spec 那一句订正成票面的说法）
- **处置：** 待处理。

### Q89 — 《失败》的**拒绝执行**词条把成因列成五种、并写着「前四种发生在开工之前」，而现在是六种／前五种

- **From:** 票 `p2-loose-ends/06`
- **Kind:** 词汇表与实现对不上
- **Where:** `CONTEXT.md`《失败》的**拒绝执行**词条（「……处理范围为空、输出落在源里、
  两个卷撞同一个去处、预扫发现有卷点不开、覆盖项把候选集裁空。整趟当场停……
  **前四种**发生在开工之前，一页都不做」）
- **Why it did not block:** 词条的**定义**一个字都没被推翻——「错在这一趟的**参数**上，
  换一个卷不会变好」正是本票据以判定的那一句，`--out` 指到一个建不出来的路径完全落在里面。
  对不上的只有那个**枚举**与跟着它的那个计数：本票新添的「输出根写不进去」是第六种、
  开工之前的第五种。改它是往一个已经拍过板的词条里加一项并改一个数，
  按 `CLAUDE.md`《改 CONTEXT.md 的规矩》属于「改写已有词条」那一侧，
  而票面第六条写的正是「对不上就记进停车场」。
  同一个枚举在实现文档里另有四处（`src/lib.rs` 的 `run`、`src/progress.rs` 的
  `RunStarted` 与 `RunFinished`、`src/report.rs` 的 `elapsed`、`src/main.rs` 的 `survey`），
  那几处是实现文档、本票已逐一改到六种。
- **What this ticket actually did:** **`CONTEXT.md` 一个字没动**，五处实现文档全部改到与实现一致。
  Q48 是同一个词条上同一种情形的先例（成因列成四种而卷内撞名走的是卷级失败），
  它的去处是「p1-session/05 → CONTEXT.md《失败》」。
- **Whose call:** 拍板的人（要不要把「输出根写不进去」加进那个枚举，并把「前四种」改成「前五种」）
- **处置：** 待处理。

### Q90 — 那道探测**不看模式**：试算也真往盘上写一次探针，而《模式》说 dry-run 一个文件都不落盘

- **From:** 票 `p2-loose-ends/06`
- **Kind:** 票面没想到的第三种情形（Q51 早已把它挂出来：「dry-run 一个字节都不写，
  探不探得看模式」）
- **Where:** `src/lib.rs` 的 `ensure_the_output_root_takes_a_write`（`run` 在开工前无条件调它）；
  `src/request.rs` 的 `Mode::DryRun`（「只算不写：判据照求、报告照出，一个文件都不落盘」）
- **Why it did not block:** 两条各有各的道理，而本票非拍不可。**照模式分岔**的话，
  试算就答不出「你这个 `--out` 根本用不了」——而拒绝执行说的是**参数**，两种模式的参数是
  同一份，另外五种拒绝也都在试算里照样咬人（`CONTEXT.md`《会话》：试算与执行是同一个 `run`，
  区别只在 mode）。一份说「都好」而真跑起来当场被拒的试算报告是在撒谎，
  那正是「先看一份报告再决定」这条回路要买的东西。
  **不分岔**的代价是：探针那一个目录加一个文件在盘上**真实存在过**，
  进程正好在试写与删探针之间被杀就留着一个点开头的文件。
  按字面读，「一个文件都不落盘」那一刻不成立。
- **What this ticket actually did:** **不看模式，两种都探**，判准写在
  `ensure_the_output_root_takes_a_write` 的《探完盘上一个字节都不多》里：
  那句契约说的是**输出**——页、记录、输出容器，也就是用户留得下的东西；
  探针留不下任何东西，探完的盘与没探过逐字节一样。
  `a_dry_run_probes_the_output_root_and_leaves_nothing_behind` 把两半一起钉住
  （试算照样拒得动；试算跑完输出根仍不存在），
  既有的 `a_dry_run_names_the_isolation_directory_without_writing_anything` 也验着同一件事。
  被杀那一笔照实写进了函数文档，没有假装它不在。
- **Whose call:** 拍板的人（试算该不该探；要不要把《模式》那句「一个文件都不落盘」写明它说的是输出）
- **处置：** 待处理。

### Q91 — 「哪几种算拒绝执行」在实现文档里抄了五份，加一种就要五处一起改

- **From:** 票 `p2-loose-ends/06`
- **Kind:** 路过发现
- **Where:** `src/lib.rs` 的 `run`（《两种失败分得开》）、`src/progress.rs` 的
  `Event::RunStarted` 与 `Event::RunFinished`、`src/report.rs` 的 `Report::elapsed`、
  `src/main.rs` 的 `Bar::survey`；权威那一份在 `CONTEXT.md`《失败》
- **Why it did not block:** 五处说的不是同一句话——`run` 说的是「回 `Err` 的有哪几种」，
  两条事件说的是「这几种一条事件都不发」，`elapsed` 说的是「这几道都在卷外、都要摸文件系统」，
  `main.rs` 那一处说的是「转轮盖住的不止预扫」。各自都在交代**本地**的一个事实，
  只是各自都把那张单子重抄了一遍。本票加一种，五处因此一起改（Shotgun Surgery）。
  真要收，是让那几处只留一句指路、单子只在 `CONTEXT.md`《失败》上留一份——
  那是 `CLAUDE.md`《文档写作》第 4、5 条（单一出处、稳定引用）在实现文档上的一次施工，
  会一次改动五个模块的抬头，与本票要做的事无关。
- **What this ticket actually did:** **五处照抄着改齐了**，没有收拢。改齐是硬要求：
  留一处说「四种」就会与实现打架，而实现文档不在「改词条要先拍板」那条规矩里。
- **Whose call:** 拍板的人（要不要让那五处只留指路，单子只在 `CONTEXT.md` 上留一份）
- **处置：** 待处理。

### Q92 — 「预扫之后才出的卷级失败」那个窗口挪到了**这一卷重开之后**：一头收窄，一头加宽

- **From:** 票 `volume-discovery/01`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/lib.rs` 的 `process_volume`（`source::open` 挪到了这里）；
  `tests/fixtures/mod.rs` 的 `RemoveOnceTheVolumeIsOpen`；
  `tests/container.rs` 的 `run_losing_the_extra`；`tests/isolation.rs` 的
  `a_volume_that_fails_after_the_survey_leaves_the_rest_of_the_run_alone`
- **Why it did not block:** 从前成员表由预扫列定，此后不再刷新——**预扫走完那一刻**起，
  少掉的成员就必然变成一个卷级失败。现在这一卷轮到时按路径重开，那之前删掉的成员
  根本不进成员表：这一卷照常做完，只是少一个成员。窗口因此从「整趟开工之后」
  缩到「这一卷重开之后」。
  两件事说明它不咬人：现实里那个窗口本来就是「轮到它时它还在不在」，重开正好答的是这一问，
  而且答得比从前**新**；`CONTEXT.md`《失败》的**卷级失败**词条说的是「预扫发现的整趟拒绝、
  预扫之后才出的记为卷级失败」——这两句一个字都没被推翻，落进那一格的现场少了一类而已。
  真要一个字不差地保住旧窗口，得让重开去核对预扫那份成员表并把对不上的当失败——
  那等于把「只数不留」买到的东西又还回去一半，且要为一种谁也没承诺过的竞态定新行为。
  **另一头同时加宽**：重开这件事**自己也会失败**——归档文件被删、损坏、权限变了，
  都成了一种从前不存在的卷级失败。最明显的一例是归档卷的卷根在预扫之后消失：
  从前它的字节从预扫握着的那个句柄里出、整卷照样做得出来，现在记一笔卷级失败、
  退出码 `3`，与目录卷同形（`ensure_the_volume_root_is_still_there` 因此两种容器都问）。
  这一头不是遗憾，是重开的题中之义：轮到它时它真的不在了。
- **What this ticket actually did:** **两头都认下，各钉一条用例。**收窄那一头：三条用它造
  卷级失败的用例改了动手的时刻——新加夹具 `RemoveOnceTheVolumeIsOpen` 记着点名那一卷开工了
  没有，在它的**第一条 `PassStarted`** 上抽走成员，那一刻这一卷刚重开完、一个字节都还没读，
  正是旧夹具在 `RunStarted` 上想造的那个现场；抽**卷根**的那两条不受影响，仍在 `RunStarted`
  上动手（`RemoveOnceTheSurveyIsDone`）。加宽那一头：新增
  `an_archive_volume_whose_file_is_gone_did_not_get_made`（`tests/isolation.rs`），
  连同 `ensure_the_volume_root_is_still_there` 文档里《两种容器形态都问》那一节。
- **Whose call:** 拍板的人（「预扫时打得开、轮到它却做不成」这句承诺以哪一遍枚举为准）
- **处置：** 待处理。

### Q93 — 开工那条事件报的步数出自预扫那一遍，报告里的成员数出自重开那一遍

- **From:** 票 `volume-discovery/01`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/survey.rs` 的 `Surveyed::steps`；`src/lib.rs` 的 `MemberCounts::of`
  与 `process_volume` 里那条区间 `debug_assert!`
- **Why it did not block:** 源在两遍枚举之间没动的那一趟——也就是正常的每一趟——两个数
  逐字相同。动了的话它们分家：步数已经加进开工那条事件报出去的全局总步数，收不回来，
  而报告说的必须是真做了的那一卷。两边取同一遍都不行：全取预扫那一遍，报告就会撒谎，
  那条「产出张数落在 [源页数, 上界] 之内」的 `debug_assert!` 还会在调试构建上当场炸
  （源多了几页就越出上界）；全取重开那一遍，开工那条事件根本还没到重开就得报出总步数。
  预告的步数本来就是**上界、不是承诺**（`CONTEXT.md`《进度》），进度条走不满或冲过头
  在幂等命中的卷上早就会发生，这里只是多一种成因。
- **What this ticket actually did:** **分开取，各取该取的那一遍**：`Surveyed` 只留步数，
  它出自预扫；`MemberCounts::of` 在 `process_volume` 里按重开的那一份重新数一遍，
  报告与那条区间断言都用它。公式只有一处（`MemberCounts::of`），两处各写一份才会真漂。
  两个数的出处都写进了各自的文档。
- **Whose call:** 拍板的人（要不要在两遍对不上时说一句，还是继续静默地按上界处理）
- **处置：** 待处理。

### Q94 — 测试用的那份最小 `Request` 在 `src/` 里仍抄着两份

- **From:** 票 `volume-discovery/01`
- **Kind:** 路过发现
- **Where:** `src/lib.rs` 的 `tests::request`（本票改成 `pub(crate)`）与
  `src/metadata.rs` 的 `tests::request`
- **Why it did not block:** 两份 15 个字段**逐字相同**，`Request` 加一格就要改两处，
  而漏改一处只会在编译期被抓到、不至于放出错的行为。本票新写的预扫用例本来要抄成第三份，
  评审的 Duplicated Code 指出来之后没有抄——改成用 `crate::tests::request()`
  加两处覆盖。剩下那一份在 `src/metadata.rs`，收拢它要动一个本票没有别的理由去动的文件
  （票面硬约束：只提交本票动过的文件）。
- **What this ticket actually did:** **没有收拢 `metadata.rs` 那一份**，只把
  `lib.rs` 那一份开到 `pub(crate)` 并让新用例用它——三份变两份，没有变一份。
- **Whose call:** 下一张动 `src/metadata.rs` 的票（顺手让它也用同一份）
- **处置：** 待处理。

### Q95 — 跑过 `cargo test --no-default-features` 之后再跑 `cargo test`，`session` 那条会假红

- **From:** 票 `volume-discovery/01`
- **Kind:** 路过发现
- **Where:** `docs/agents/gate.md`《读结果》；`tests/session.rs` 的
  `no_arguments_without_a_terminal_says_so_and_still_shows_the_usage`
- **Why it did not block:** 两种特性组合的 `tonefit.exe` 落在**同一个** target 目录里、
  互相覆盖，而 `cargo test` 判定自己那一份「新鲜」时不会重新摆放它。于是闸门第二条跑完
  之后再回头跑第一条，用例启动到的是上一趟留下的**不带 `tui`** 的那个二进制：
  无参数时它走 clap 的必填项错误、退出码 `2`，而这一条要的是会话那条岔路的 `1`。
  当场量过三件事——`cargo build` 之后那个 exe 回 `1`（会话那条），`cargo test` 之后回 `2`
  （clap 那条）；换一个干净的 `CARGO_TARGET_DIR` 从头跑第一条，17 个二进制全绿、
  一条 `FAILED` 都没有。与任何一张票的改动都无关：它只取决于上一次在这个目录里
  build 过哪一种特性组合。
- **What this ticket actually did:** **没有动闸门，也没有动那条用例。**本票的三条闸门按
  1→2→3 的次序各跑一遍，第一条的数取自它自己那一趟（以及一次干净 target 目录的复跑）。
  这一笔连同「按次序跑」写进了本票落地记录的《数》。
- **Whose call:** 拍板的人（`docs/agents/gate.md`《读结果》要不要加一句「三条按次序跑」，
  或者让闸门各用各的 target 目录）
- **处置：** 待处理。

### Q97 — 本票这一批实测整体比剖面那一批快一到三成，而快的有一部分与代码无关

- **From:** 票 `p2-loose-ends/11`
- **Kind:** 票面给的数与我量出来的对不上
- **Where:** `docs/measurements.md` 的《端到端吞吐》《三段各占多少》（本票换掉的那一批）
  与《分阶段耗时剖面》《吞吐目标：八十页/秒不成立，改判》（没换的那一批）
- **Why it did not block:** 本票在归档卷上重测吞吐，四卷一趟测出来的秒数**整体**低于文档里
  那一批：棋魂 6.55→5.99（`--io-mode serial`，与并行之前同一件事；6.55 是本票换掉的
  那一行原写的 6552 毫秒）、哆啦A梦 40.2→33.7、
  N和S 28.3→26.9、**改革之獸 10.0→8.4**。最后一个是目录卷，本票一个字都碰不到它，
  它仍快了一成半——因此这一批与那一批之间**除了本票的改动还有别的差**（机器状态、
  中间落地的别的票，两者都无从事后归因）。真要归因得把旧代码再 build 一遍逐档对齐，
  那比本票大，而本票要的 A/B **不需要它**：`--io-mode serial` 与默认在归档卷上唯一的差别
  就是幂等那一道派几条，两档在**同一批**里测出，差值站得住。
  连带的一件：《吞吐目标》那四档是按旧那一批「取实测值再留一成余量」定的，本批四卷
  全部在线以上（51 / 22 / 14 / 55 源页/秒）。
- **What this ticket actually did:** **只换掉两节，另外两节留在自己那一批里，并在四处
  写明「两批不可逐格相比」。**《端到端吞吐》与《三段各占多少》换成本批；
  《分阶段耗时剖面》与《吞吐目标》一个数没动——后者原先引着《三段各占多少》的
  「第一遍是 3.93 秒」，改成就地写死那一批的数，引用因此不会指到一批换过的秒数上。
  **四档目标一个字没抬**：那是拍板的事，不是一次重测能决定的。
- **Whose call:** 拍板的人（四档目标要不要按新的一批抬上去；要不要为「两批之间还差了什么」
  立一张归因的票）
- **处置：** 待处理。

### Q99 — 成员表出自开卷那个句柄，字节出自重开的那个，两者之间归档被换掉就会静默读错

- **From:** 票 `p2-loose-ends/11`
- **Kind:** 路过发现（评审 Spec 轴指出）
- **Where:** `src/source.rs` 的 `Member::entry` 与 `Reader::independent`；
  `src/read.rs` 的 `Concurrent::spawn`
- **Why it did not block:** `Member::entry` 是**开卷那一次**解析出的中央目录下标，
  而 `independent()` 是按路径重开、**重解一遍**中央目录。两次之间源归档被替换成另一份，
  同一个下标就指到另一个成员上——越界那一支报得出错，不越界那一支**静默读到别的字节**。
  从前全卷共用一个句柄，这条不存在。眼下不咬人：源是只读的，而「跑到一半有人换掉源文件」
  这件事本来就没有任何一处防着——`volume-discovery/01` 之后预扫与处理之间也是按路径重开，
  那一趟同样能撞上（只是它重开时连成员表一起重数，因此错的形式不同：那边是「数错」，
  这边是「读错」）。真要拦死，得让 `independent()` 拿开卷那一次的某个指纹去核
  （中央目录的偏移与条目数，或整个文件的 mtime + 大小），核不上就退回串行——
  那是给读取端加一道校验，不是本票的形状。
- **What this ticket actually did:** **认下这一条，没有加校验。**并发那一支照旧按
  `member.entry` 取成员。写进这里而不是就地加一句注释，是因为它跨着 `source` 与 `read`
  两层，而处置要拍板的人看见。
- **Whose call:** 拍板的人（源在跑的过程中被换掉，要不要有一道防线；那道防线该在哪一层）
- **处置：** 待处理。

### Q100 — 那笔句柄账住在 `source::Reader` 上，而它跨着四个模块

- **From:** 票 `p2-loose-ends/12`
- **Kind:** 你确实拿不准的单项
- **Where:** `src/source.rs` 的 `Reader`《一趟同时开着几个句柄》那一节
- **Why it did not block:** 那一节要写全三个来处，其中「写出那一侧」点着 `crate::sink` 与
  `crate::cache`，而 `source` 这一层并不知道那两个模块存在。这与 `CLAUDE.md`
  《文档写作》第 2 条「一个文件只承载它那一件事」有张力。选它的理由是式子那两格
  **都是 `Reader`**（卷自己那一个、读取线程那几个），放在别处就得跨层复述那两格；
  写出那一侧只占一句「它们排在幂等那一道之后，不同时在手上」。另一个候选是
  `CONTEXT.md`——但一笔账不是一个词，往词汇表塞它要么改已有词条（本票没被授权，
  见 `CLAUDE.md`《改 CONTEXT.md 的规矩》），要么在术语表底下另起一节散文。
- **What this ticket actually did:** **放在 `source::Reader` 上**，四处引它的小节名、不复述
  （`src/read.rs` 的模块文档与 `reads`、`a_concurrent_read_holds_one_reader_per_worker`、
  `src/survey.rs` 的模块文档）。写出那一侧只写「不同时在手上」这一句，不展开。
- **Whose call:** 拍板的人（跨层的账该不该在 `CONTEXT.md` 里有一节，还是就近挂在某个类型上）
- **处置：** 待处理。

### Q101 — `docs/measurements.md` 里多了一行不是本机量出来的数

- **From:** 票 `p2-loose-ends/12`
- **Kind:** 你确实拿不准的单项
- **Where:** `docs/measurements.md` 的《一个进程能同时开多少个文件句柄》，Linux 那一行
- **Why it did not block:** 那一节要答的是「峰值离平台上限有多远」，而本机只有 Windows。
  Linux 的默认软上限 1024 是这条判断的**最紧一档**，不写进去，那一节答不完整；
  写进去，measurements 里就有了一行取自发行版默认值、不是量出来的数。
  `CLAUDE.md` 说的是「需要实测数字时，来源只有 `docs/measurements.md`」——
  它约束的是**取数往哪里去取**，没说那个文件里不许有别的东西；这个文件里也已经有
  非实测的条目（《设备支持的图片格式》那一类出自厂商资料）。
- **What this ticket actually did:** **写进去，并在那一格里照实写「未在本机上量，
  取的是发行版默认值」。**Windows 那一行是真量的（同一个文件反复 `File::open`
  攥到十万个，3.1 秒，没撞上限）。
- **Whose call:** 拍板的人（measurements 要不要只装本机实测，非实测的数另立一处）
- **处置：** 待处理。

## 已了结

只留索引——条目原文已挪进对应票据的《停车场结转》，或内容本就在下表所指的位置。

| | 条目 | 去处 |
|---|---|---|
| Q1 | A 类双页扫描的页间字号跳动是 0.390，为票面基线（0.112）的三倍半 | 并入 Q2 |
| Q2 | A 类双页扫描上的字号跳动比票面据以定案的那个数大 3.5 倍 | page-geometry/04 |
| Q3 | 裁边带进来三个未标定的数，`CONTEXT.md` 的《尚未确立》还没有收它们 | CONTEXT.md《尚未确立》 |
| Q4 | 彩色分支上的裁边要多走一遍全页 OKLab 换算，代价没量过 | CONTEXT.md《尚未确立》 |
| Q5 | `fixtures::gradient` 在默认路径上会被裁掉下面 21.6%，而拿它当 | page-geometry/09 |
| Q6 | 迟滞升档在默认那条路上一处覆盖都没有了 | page-geometry/08 |
| Q7 | 以高为准让目标尺寸没有了上界，极端瘦长的源页会把输出缓冲撑到分配不下 | page-geometry/07 |
| Q8 | `docs/measurements.md`《真机像素完整性》那句「输出恒等于 fit- | page-geometry/06 |
| Q9 | `tests/` 里大批夹具仍拿 `SMALLER_THAN_TARGET`（800×1 | page-geometry/09 |
| Q10 | measurements《A 类位深实测》那四个「哪一档更优」的页数是旧判据下数的 | page-geometry/06 |
| Q11 | `CONTEXT.md` 的词汇表还没有「源页 / 输出页」这一对 | CONTEXT.md《处理对象》 |
| Q12 | `p0-hardening/13` 正文那句「都不要把分阶段计时塞进 `Report`」 | p0-hardening/13 票面 |
| Q13 | `[dev-dependencies]` 里的 `encoding_rs` 与 `zip | page-geometry/09 |
| Q14 | `CONTEXT.md`《I/O 与并发》说读取层与计算层的并发度「两个数不相干」，代码 | CONTEXT.md《I/O 与并发》 |
| Q15 | `tests/isolation.rs` 的 `a_color_page_a_gray_ | page-geometry/09 |
| Q16 | `render::calibration_note` 里那句「大写英文，中文字模装不进一 | calibration-chart/01 |
| Q17 | `calibration-chart/01` 的验收里没有「中文判读说明」这一条 | calibration-chart/01 |
| Q18 | 仓库根没有 LICENSE，也没有一份第三方许可声明。** `calibration-c | LICENSE + THIRD-PARTY-NOTICES.md + licenses/OFL-1.1.txt |
| Q19 | ADR 0007《要认的代价》那条「tonefit 只产出文件，无从感知，也无法在输出时 | page-geometry/06 |
| Q20 | 幂等仍要在碰像素之前预告出全部输出成员名，而 `page-geometry/04` 的  | page-geometry/04 |
| Q21 | 「以高为准下几何门恒成立」现在有了一个例外，而那句话写在 ADR 0007 与 `pag | page-geometry/06 |
| Q22 | 兜底上界的折算系数 8 是照代码数出来的，不是量出来的；而两侧的上界都只管一页 | CONTEXT.md《尚未确立》 |
| Q23 | 本票往 `CONTEXT.md` 词汇表加了两行，而 Q3/Q11 记着「改 CONTE | CLAUDE.md《改 CONTEXT.md 的规矩》 |
| Q24 | `crop::MIN_KEPT` 那个 0.5 仍未标定，而本票往它的文档上又压了 35 | CONTEXT.md《尚未确立》（并入 Q3） |
| Q25 | 默认路径上的迟滞覆盖只走得到抖动那一维，位深那一维仍然只在 fit-inside 上有 | CONTEXT.md《尚未确立》 |
| Q26 | 进度预告在拆分开着时是上界，普通卷的进度条因此停在四分之三处 | CONTEXT.md《尚未确立》 |
| Q27 | 记录里加了来路那一项之后，一条按**字节**比两张页的用例失效了 | page-geometry/09 |
| Q28 | 装订沟的命中与误报只有计数，没有一页是目视核对过的 | CONTEXT.md《尚未确立》 |
| Q29 | 幂等的依据多了一项，而 ADR 0006 仍写着「四项」 | page-geometry/06 |
| Q30 | `src/lib.rs` 一个文件为四件事被改了 627 行，幂等那一组该独立成模块 | CONTEXT.md《尚未确立》 |
| Q31 | 本票往 `CONTEXT.md`《尚未确立》加了两条，而那一节的收口归 06 | page-geometry/06 |
| Q39 | `run` 返回 `Err` 时不发「这一趟完了」，异常收场没有自己的那一条事件 | p1-session/05 |
| Q46 | 中止掉的那一卷只有开卷那一条事件，`run` 的返回值也说不出这一趟是被中止的 | p1-session/05 |
| Q41 | 观察者花掉的时间算进了 `VolumeTiming`，而 `06` 要在决策点上等人 | p1-session/06 |
| Q63 | 退出会话时那条还在写盘的线程要有人收，而按停那两个键还没有 | p1-session/10 |
| Q69 | 跑起来之后连范围层一起冻住了，spec 只要求前两层只读 | p1-session/10 |
| Q64 | 报告区长过一格就只留最后那几行，翻不回去 | p1-session/11 |
| Q71 | 按停的状态只在屏底那两行说，主区两条进度条上看不出来 | p1-session/11 |
| Q58 | 会话里「没说」与「说了一个恰好等于默认值的值」是两个状态，命令行上它们没有 | p1-session/12 |
| Q56 | `calibrate` 子命令读不到预设，而标定图挂在**设备层**上 | p1-session/13 |
| Q52 | 决策点那一条事件不带这一卷的报告，而会话要在那里把报告画出来 | p1-session/14 |
| Q53 | 单卷在决策点上答收尾，这一趟的收场仍是「走到头」 | p1-session/14 |
| Q77 | 卷报告每卷拼两次，而命令行那一趟只用得上第二次 | p0-hardening/13《停车场结转》（剖面量得动它，不值得收） |
| Q35 | `CONTEXT.md`《尚未确立》那条「A 类素材的一切数值」按字面已经不成立 | page-geometry/06 → CONTEXT.md《尚未确立》 |
| Q48 | 卷内成员名撞到同一个输出上，现在走的是卷级失败，而《失败》的词条把成因列成了那四种 | p1-session/05 → CONTEXT.md《失败》 |
| Q54 | 《会话》的**设备层**词条把内容列成「型号 + 灰阶数覆盖」，而阈值也在那一层 | p1-session/07 → CONTEXT.md《会话》 |
| Q57 | 《会话》的**口味层**词条列着六项，而口味层现在有十一项 | p1-session/07 → CONTEXT.md《会话》 |
| Q61 | `tui` 关掉之后，会话那二十几条用例一条都不跑 | p1-session/08《停车场结转》（p2-loose-ends/01 把状态机搬出了特性） |
| Q68 | `tui` 关掉之后，`09` 新添的那三十几条用例同样一条都不跑 | p1-session/09《停车场结转》（同上一刀） |
| Q79 | `profiling` 特性不在 `cargo test` 的闸门里，clippy 也扫不到它 | p0-hardening/13《停车场结转》（闸门变成三条，计数那一半摘出两个纯函数） |
| Q40 | 「不得在持锁处调观察者」这条性质没有真正的闸门，靠人看加一条弱用例 | p1-session/02《停车场结转》（哨兵落地，持锁报到当场恐慌） |
| Q36 | 兜底上界那道线，仓库里三处给出三个倍数：24:1、12:1、16:1 | page-geometry/06《停车场结转》（算法收进 `MAX_TARGET_PIXELS`，24:1 是系数取 8 那份草稿的残留、已删） |
| Q50 | 卷根整个消失时走的是**页级**失败 → 隔离（退出码 2），不是卷级失败 | p2-loose-ends/05《停车场结转》（判据改成「卷根还在不在」，一页都没成仍走隔离） |
| Q51 | 输出根写不进去时每一卷各记一笔卷级失败，退出码是 `3` 而不是拒绝执行那个 `1` | p2-loose-ends/06《停车场结转》（开工前探一次，写不进去整趟拒掉；`Sink::create` 逐卷那条路留着） |
| Q47 | 幂等那一道被中止时，`volume_fingerprint` 回的是一份**喂了一半**的哈希 | p2-loose-ends/11《停车场结转》（并行不改变这条性质，判定不另立两支的返回类型） |
| Q78 | 归档卷的幂等这一道是三段里第二大的一块，全是把源字节再串行解压一遍 | p2-loose-ends/11《停车场结转》（本票收的就是它：那一段 1758→288 毫秒） |
| Q85 | 报到时手里仍攥着 rayon 工作线程与读取层的在途字节额度 | p2-loose-ends/11《停车场结转》（判定为**不改**，并行之后这条理由更强） |
| Q42 | 点名的每一个归档卷，整趟都占着一个打开的文件句柄 | p2-loose-ends/12《停车场结转》（`volume-discovery/01` 已消掉这笔代价，本票判定**不划句柄上限**） |
| Q96 | 一卷同时开着的归档句柄从 1 涨到 `1 + min(幂等那一道的并发度, 成员数)` | p2-loose-ends/12《停车场结转》（数一格没变，收进 `source::Reader`《一趟同时开着几个句柄》） |
| Q98 | 句柄开不出来时并发度悄悄降下来，而报告照旧印计划里那个数 | p2-loose-ends/12《停车场结转》（判定**不改**：它等的那个前提「有了句柄上限」不会出现） |
