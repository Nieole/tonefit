# 08: 查清实测表与重跑对调那件事

**What to build:** `docs/measurements.md` 的卷级扫描表在阈值 5.5 上写着**画集 Venus = 2bit+FS、Bowing 1 = 4bit+FS**，而按同一 profile、同一阈值重跑八卷得到的结果**正好反过来**。实测数字只有一个出处，两份对不上不能同时留着。

查清是哪一份作数，把结论落进那一份唯一的出处。

`_samples` 里画集 Venus 有三份形态（摊开的目录、归档、以及高像素那一份），文档那一趟用的是哪一份没有写——这是头一个要排除的可能。

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [ ] 查清对调是语料不同、参数不同、还是代码变过
- [ ] 结论落进 `docs/measurements.md`，并把「那一趟用的是哪一份语料」补上——缺这一项是这次对不上的根因之一
- [ ] 与之相关的表格数字与结论跟着更新；作废的那一份删掉，不留两份
- [ ] **这一票不清，11 号票引用的那组实测数字全部不作数**

## Comments

重跑用的是八卷 1852 **输出页**（跨页拆分之后），而文档写的是 1187 页——源页与输出页的口径差可能也是对不上的一部分，一并查。
