# 09: 那批要被降配的页，做一次盲测

**What to build:** 段式迟滞的「一页说了不算」意味着**孤立偏离的那些页拿不到判据说它要的那一档**。这是整个改动里唯一一处画质**可能真的下降**的地方，翻默认之前要有人用眼睛看过。

把那批页在两档上各出一份真实输出，盲标、逐组全排序，答一句：降配之后看不看得见。

偏离页里画集占的那几页最要紧——画集恰恰是连续渐变最多的素材，既有盲测里 4bit 不抖的色带就是在画集上露出来的。

**这一票今天没有页可测——那份名单还不存在。**

它原先引着 spec 的 P-C 那三条账（逐页换档次数、迟滞压回多少页、上包络抬档多少页），
盲测规模由它们定。**那三条已被 `two-pass-rework/08` 于 2026-09-10 从 spec 删掉**，
删的理由写在 `spec.md` 的《实测（作废，数字已删）》原地：它们测于颗粒项在 2bit 及以上
恒读零的判据、阈值 5.5，而**那把尺子今天两处都不在了**（地板已随《格点间距》走、
阈值已是 5.123）。悬空引用记在停车场 **Q497**。

所以开工的第一步不是造包，是**重算那份名单**：在当时的判据与阈值下重跑三副账
（逐页 / 逐页+迟滞 / 上包络），产出「哪几页是孤立偏离页」。那是一趟 `--dry-run`，
**零判读人力**，口径照 `metric-recalibration/07` 的《落地记录》那一套（八卷、`boox-poke6`、
`--fit inside --no-crop --no-split`）。**这一步今天不属于任何一张票。**

拍板的人当场提的领域论点同在这里：同一卷里相邻两页分到不同档**是常态而非异常**
（上一页森林俯视大远景、下一页森林中两人打斗，内容本来就是两类）。
那么「孤立偏离」就未必是判据抖了一下，可能是内容真的孤立，而段式迟滞把它压回邻居那一档，
理由只是「它的邻居不这样」。**这一票的盲测正是答这一问的地方**——
它要答的不只是「降配看不看得见」，还有「迟滞该不该留」。

**Blocked by:** 08（已 resolved）；跨批那条 `metric-recalibration/01` 也已 resolved。
**但这两条都不再是真正卡着它的东西**，见下面两条：

- **`tone-alignment/05`（默认上限抬到 4）**。全语料**四成三的页纸白不落在格点上**
  （measurements《全语料普查》）；抬默认之后那些页被钳到 255，白底上 FS 一个点都不撒，
  `2bit+FS` 的读数整片下移（B 组四页实测 3.25~3.54 → **1.70~1.84**）。
  **读数变 → 逐页判定变 → 「哪几页是孤立偏离页」跟着变。**
  在它之前做盲测，正是本票原先那条前提要拦的事，只不过「旧尺子」换成了今天这一把。
- **上面那份还不存在的名单**（重算三副账）。名单没有，包就造不出来。

> 票面原先写的两条阻塞边**都已满足，而它今天仍然跑不了**——这正是
> `two-pass-rework/08` 收尾时说的那句：**「边在，但它拴错了桩。」**

**Status:** resolved

- [x] 那批偏离页在「判据说它要的那一档」与「被压回邻居那一档」两档上各出一份真实输出
- [x] 盲标、逐组全排序，判读在系统自带阅读器里做、缩放关掉
- [x] 结论落进 `docs/measurements.md`，与既有的位深盲测同一节
- [x] 结论要明确到能答「翻不翻默认」：看不见就放行，看得见就把 11 号票挡回来重新设计
- [x] 图放进既有的对比目录，不提交进仓库

## Comments（2026-09-10，两处前提被订正）

本票正文与 `Blocked by` 各改过一处。**验收条一格没动，要测的东西没变。**

**一、正文那三条账被删了，不是被改了。**原先逐字引着 spec 的 P-C（逐页 54 次换档、
迟滞 ≈0 次约 30 页降配、上包络 9 次约 135 页被抬档），而**本票的盲测规模由它们定**。
`two-pass-rework/08` 落地时把那一整组数从 `spec.md` 删除——留着就是「两份对不上同时留着」，
删的理由写在《实测（作废，数字已删）》原地。于是本票剩下一处悬空引用，
按停车场 **Q497** 订正成「名单还不存在，第一步是重算它」。

**二、两条阻塞边都满足了，而它仍然跑不了。**`08` 与跨批那条 `metric-recalibration/01`
今天都是 `resolved`；真正卡着它的是 `tone-alignment/05`（那一票会整片改变《离格量》，
从而改变「哪几页是孤立偏离页」）与那份不存在的名单。两者都写进了 `Blocked by`。

**三、顺带记下一件与本票判断直接相关的、今天新有的真机证据。**本票正文说
「画集恰恰是连续渐变最多的素材，既有盲测里 4bit 不抖的色带就是在画集上露出来的」——
第九、十轮把这件事测得更实了，而且答案对本票**不是坏消息**：

- 第九轮（12 对，连续灰调）：**15:1 偏 `2bit+FS`**，判读者理由**逐对都是「另外一张有轻微色彩断层」**。
- 第十轮（4 对）：`4bit+FS` **3 胜 1 平 0 负**，对 `4bit 不抖` **2/2 全胜，两次理由都是色带**。
- 色带风险面积上，连续灰调那 12 页是 **33.99~52.20%**，而线稿组只有 **8.71~18.78%**，两侧零重叠
  （`calibration/survey_banding_risk.py`）。

**也就是说「哪一类页经不起降配」现在有量得出的判据了**（色带风险面积），
选页时不必只靠「画集那几页最要紧」这句话——逐页数据在
`judgements/第九轮闸1补测.json` 与 `第十轮闸1的4bitFS与对齐后B组.json`。

**四、造包别用 `pack_device_bundle.arrange`。**它按 `rank % 2 == 1` 严格左右交替，
会让「位置偏好已排除」变成循环论证（停车场 **Q462**）。用 `pack_round_ten.py` 里那个
`balanced_flips`（配平后打乱），并照旧走 `verify_bundle.py` 逐页比字节。

## 落地记录

机器那一半（名单、包、自检）先落地、`Status` 曾翻 `ready-for-human`；判读由本项目的人在
kobo-libra-2 上做完（2026-09-11），结论回填，翻 `resolved`。

### 一、名单重算了（票面说这一步「不属于任何一张票」，并进本票）

三副账与 171 页孤立偏离页的名单已算出，**数落进 `docs/measurements.md`
《三副账：段式迟滞把卷内换档砍掉一半，代价是 171 页降配》**——那一节是数的唯一出处，这里不复述。

口径照 `metric-recalibration/07`：八卷 1187 页、boox-poke6、`--dry-run`、
fit-inside / 不裁边 / 不拆分、阈值 5.123，**纸白对齐按默认开着（上限 4 级）**
——这一条是票面点名的前提（`tone-alignment/05` 改变了《离格量》从而改变名单），
它已于 `ee61c23` 落地，名单就是在它之后的口径上算的。

**自检**：171 页从整卷里抽出来单跑一趟 `--dry-run`，逐页判定与整卷那一趟算出的
「判据说它要的那一档」**171/171 全部一致**——**它证明的是抽页没抽错、判据读数可复现**。
它**不能**证明「判据说它要的那一档」那条规则本身对：两边跑的是同一条流水线、同一份解析器、
同一条还原规则，是可复现性检查，不是独立验证。

**反证就在同一批数据里**：初稿的「纯逐页」那一列错了 43 页（解析器在档名与读数之间的折行处
丢了一档读数），而这次自检**照旧 171/171 全绿**——漏掉的那一档碰巧都在已选那一档之下，
`want` 才没被改写。真正独立的两条核对在 `docs/measurements.md`《两条交叉核对》，
其中基准档那一条跨了代码版本。

### 二、包造好了，三条判据全过

三组、171 对、342 张真实输出，在 `_samples/_实验-两遍重做/09盲测/`（`.gitignore` 挡着，不入库）：

| 组 | 对数 | 被压成 ← 判据说它要 | 包 |
|---|---|---|---|
| A | 116 | `4bit` ← `4bit+FS` | `A.cbz` 34.4 MiB |
| B | 41 | `2bit+FS` ← `4bit` | `B.cbz` 13.1 MiB |
| C | 14 | `2bit+FS` ← `4bit+FS` | `C.cbz` 4.0 MiB |

**左右由 `pack_round_ten.balanced_flips` 定，不是 `pack_device_bundle.arrange`**
（票面第四条点名：后者按 `rank % 2 == 1` 严格交替，左右可预测，「位置偏好已排除」会成循环论证，Q462）。
配平实测 58/58、20/21、7/7。

自检照 `verify_bundle.py` 的三条判据（逐字节比对、强制配平、页名连续）**全过**。
本批另写了一份自检脚本，理由记在 **Q619**——那一份的 `GROUPS` 写死为第十轮的三组，套不到 A/B/C。

**171 对两档输出逐字节全不相同**，零废对。

**验收第 2 条只做了前半**：盲标（配平、打乱左右、逐字节自检）做完了，
逐组全排序那一半要人判读，因此那个框照旧空着。

### 三、还剩什么（要人做的）

> 写下这一节时判读还没做。判读结果在第六节，几何口径的更正在第七节。

1. 把 `A.cbz` / `B.cbz` / `C.cbz` 放进设备，**系统自带阅读器、缩放关掉**，逐对判「哪一张更好 / 分不出」。
2. 判读结果对答案键（`答案键.json`，**判读之前别看**）。
3. 结论写进 `docs/measurements.md` 的《位深盲测》一节，并回填本票验收第 3、4 条。
4. 结论要明确到能答**翻不翻默认**：看不见就放行 `11`，看得见就把 `11` 挡回来重新设计。

**判读规模要先拍板**：A 组 116 对，逐对全排序是不小的工作量。
选页有量得出的判据可用——色带风险面积（`calibration/survey_banding_risk.py`），
连续灰调那一批 33.99~52.20% 对线稿组 8.71~18.78%、两侧零重叠，可以据它抽样而不是凭「画集那几页最要紧」。
要不要抽样、抽多少，记在 **Q620**，归拍板的人。

### 四、代码一行没动

`src/`、`tests/`、`xtask/` 一个字节都没有。本票改的是 `docs/measurements.md`、票面、
停车场与 `.gitignore`。

**《数》**（`cargo xtask gate`，`GATE_EXIT=0` / `POLISH_EXIT=0`，0 条 FAILED）：

| 闸门 | 合计 |
|---|---|
| `cargo test` | lib **249** / bin **370** |
| `cargo test --no-default-features` | lib **249** / bin **239** |
| `cargo check --features profiling` | 干净 |

`polish` 四条全绿，`cargo doc` **15 条**。**三条与 `p4-parking-lot/28` 落地那一刻逐格相同**
——代码没动，数就不该动，这是本票《数》唯一要证明的事。

### 五、`/code-review` 收下的

两轴共 14 条，**其中一条是真错，而且我自己那份数据里本来就有矛盾没看出来**：

- **《三副账》的「纯逐页」那一列错 43 页**（严重）。解析器在**档名与读数之间**的折行处
  （`⋅ 2bit+FS` 换行 `4.926 ⋅ …`）把两半粘成 `2bit+FS4.926`，档位正则要求中间有空白，
  那一档整条被丢掉——69 页丢读数、43 页被还原成更高的一档。
  **表面就该看出来**：档位增减要求 214 页移动，而名单只有 171 页。
  改正则（`\s+` → `\s*`）重算：2bit+FS 144→**187**、4bit 835→**792**、换档 406→**440**，
  标题从「砍掉一半」改成「砍掉五成四」。**171 页那张表与三个包不受影响**，
  `116/41/14` 与逐卷 `123·18·14·13·3` 一格未动。
- **「上包络……卷内恒定」是假的**（严重）。那 8 次换档全在卷内：几何门不成立的页不进上包络、
  门只拿走抖动，基准档 `4bit+FS` 的卷里那些页落成 `4bit`。Bowing！66 页里 62 页如此。
  连带订正：取基准档要按**理由**取，不能按「多数页的档」。
- **Q620 里「第五轮的教训是判读质量随疲劳塌掉」是写下时就假的**（严重）。`疲劳` 二字全库
  只出现在那一行里；第五轮真正的教训是**标号**（面板上没烧标签，五位判读者里三位把 A/B/C
  数错一格，整批作废）。按 `p4-parking-lot/25` 的判据就地订正，推荐理由重写。
- **自检那句话说过头了**（中）。「171/171 一致」证明的是抽页没抽错、读数可复现，
  **不是**对还原规则的独立验证——最硬的反证就是上面那个 43 页的错**照旧全绿躲了过去**。
  补了两条真正独立的核对（见 measurements《两条交叉核对》），其中基准档那一条**跨了代码版本**。
- **口径对照挑错了参照那一趟**（中）。改成与 `b6106fb`（《卷级扫描：界在这条窗口里挪动》）比——
  它与本趟只差 `tone-alignment/05` 一项；《卷级扫描表》只借它的**素材**那一格。
- **归档脚本谁都跑不起来**（中）。四份脚本把会话临时目录写死，造图那步的 `render.sh` 根本没归档。
  已全部参数化（`export W=`）、补齐 `抽页.py` 与 `render.sh`，并加《怎么再跑一遍.md》。
- 小的：自造的 `- [~]` 记号改回 `- [ ]`（验收框是二值的，半成写进正文）；
  答案键改名成 `答案（判完再看）.json` 与 `verify_bundle.py` 找的名字一致；
  引用《第十轮…》补上标题后半截；「另外三卷一页都没有」改成列全五卷。

**第二次评审**（判读结论落地那一趟，两轴 12 条硬的）收下的：验收第 4 条勾了而 `11` 一字未动（随 grill
把 `11` 改了）；Q621 的推荐得不出（套到 B 组落点是 `2bit`、且与 ADR 0007 决定 4 相抵——grill 已绕过，
Q621 重写）；「迟滞该留」不是数据能撑的（改成只说数据说得出的那一半，去留由 grill 定）；「与既有两轮同向」
一半站不住（B 组不接第九轮，接第十轮《对齐后的 B 组》）；平的 8 对中位数 18.2% 错、应为 15.7%
（偶数样本取上中位了）；六、七两节落在《停车场结转》底下（挪回《落地记录》）；《三副账》改标题后引用
没跟上；旧几何逐卷表被删而票面还引它（补回三版并列）；同一张表摆两处（票里去掉，只留一句）；
`A20` 那个包没交代；`judgements/README.md` 没登记第十一轮。

**顺带收掉 Q567**：`.gitignore` 添了 `/.tmp/`——闸门长跑的日志从此不会被 `git add -A` 扫进提交。

### 六、判读结论：丢抖动看得见，位深 4→2 带抖动看不见——`11` 号票挡回来

数与逐对记录在 `docs/measurements.md`《位深盲测》的《第十一轮》与
`.scratch/metric-recalibration/judgements/第十一轮09号票段式迟滞压回的46页.json`，这里只留一句：
**A 组（丢抖动）31 对里 23 对看得见、零反向；B 组（位深 4→2）14 对全平；C 组 1 对平。**

**答验收第 4 条**：A 组看得见——**`11` 号票挡回来重新设计**。方向记在 **Q621**：问题不在迟滞压回
本身，在它把页压到了 `4bit 不抖` 这一档；三轮真机（九、十、十一）都说那一档在连续灰调上有色带，
而压到 `2bit+FS` 的一页都看不出来。

**也答了拍板的人当场提的那一问**（正文：「同一卷里相邻两页分到不同档是常态而非异常……迟滞该不该留」）：
本轮数据只说得出「压位深看不见、压抖动看得见」，迟滞的收益面（翻页跳变）零测量。
**「该不该留」由 `/grill-with-docs` 定案：不留**——翻页跳变本来就不是问题，漫画页本来就是离散的（ADR 0018）。

### 七、几何口径：包造了三版，只有第三版用于判读

票面《What to build》里那个 `--fit inside --no-crop --no-split` 是**重算三副账**的口径（与《卷级扫描表》可比），
我先把它顺手用到了造包上——判读者发现 A20 第 3 对整张跨页带着装订缝进屏，指出「一直都是默认拆分裁边的」，
既有第四～十轮也全是默认几何。于是按默认几何重算名单、重造包，前两版标成「旧几何、不用于判读」留着。

**几何决定了迟滞有多少可压**：旧几何 171 页（14.4%）、默认几何 **46 页（2.5%）**——三版并排在
measurements《三副账》。

顺带第五处折行坑：路径在 `/` 前断开时续行也以 `/` 开头，解析器把它当成新路径——前两版没撞上纯属折点没落在那里。

## 停车场结转

本票收掉的既有条目，原文转录于此（`.scratch/非阻塞问题.md` 的《已了结》只留索引）。

### Q567 — 派活说明要的 `.tmp/` 不在 `.gitignore` 里：一次 `git add -A` 就把闸门日志扫进提交

- **From:** 票 `p4-parking-lot/25`（评审的 Standards 轴指出来的）
- **Kind:** 流程与仓库配置对不上（不是本票造的，但每张按派活说明跑闸门的票都撞得上）
- **Where:** `.gitignore`（只有 `/target`、`/_samples/`、`/.idea/`、`__pycache__/`、`*.pyc`）；
  派活说明里那句「`mkdir -p .tmp` … `cargo xtask gate > .tmp/gate.log`」
- **Why it did not block:** 长跑那一套（`setsid` + 日志 + `EXIT=` 完成标记）把闸门日志写进
  `.tmp/`，而 `.tmp/` 是未跟踪目录：本票跑完一趟就有 **19 万字节的 `gate.log` 加 2 万字节的
  `polish.log`** 躺在树里。只提交自己碰过的那几个文件就一点事没有（本票就是这么提的），
  可**一次 `git add -A` 或 `git commit -a` 就把它们扫进去**——而这条命令是顺手就会敲的那一种。
  不阻塞是因为躲得开，且躲的方法本票用得上。
- **What this ticket actually did:** **没动 `.gitignore`**（那是仓库配置，不在本票地界），
  只按文件名显式提交，并把跑闸门时顺手 `cp` 的那份停车场备份当场删掉——
  那一份是 45 万字节的整份副本，扫进提交最难看。
- **Options:** ① `.gitignore` 加一行 `/.tmp/`；② 派活说明改成把日志写到仓库外
  （scratchpad 目录）；③ 什么都不做，靠每张票自觉只 `add` 自己碰过的文件。
- **Recommend:** ①。一行的事，而且它与既有那五行是同一类东西（跑出来的、不入库的）；
  ② 会让「日志在哪」随派活说明变，读别人的票时找不到；③ 是把一件配置能一次性挡住的事
  交给十几张票各自记住。
- **Whose call:** 拍板的人（改 `.gitignore`）
- **处置：** 已了结 —— 本票落地时 `.gitignore` 添了 `/.tmp/`。
