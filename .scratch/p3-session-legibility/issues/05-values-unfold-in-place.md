# 05 — 取值栏：左栏上就地摊开取值

**What to build:** 左栏每一行的取值从「只能一格一格转」改成「摊开来看」。光标那一行按
`⏎`（或 `→`）就在它**下面**摊开一列取值，选中的那一格前面一个记号，`↑↓` 选、`⏎` 定、
`Esc` **一格不改**地回去。

要修的毛病是：环让「**这一项有哪几个取值**」在屏上根本不存在——用户只有把它轮询过
一整圈，才知道自己有哪些选择。取值只有两三个的那几行同样吃这个亏，只是按得少一点。

摊开的**第一格恒是「默认（xxx）」**：两层的每一格都有一个「没说」的位置，而那正是
存成预设时写不写进去的分别。「没说」与「说了一个恰好等于默认的值」在屏上要分得开。

**环那一套（`←→` 就地转一格）保留**：知道自己要什么的人不必先摊开再选。摊开时
`←→` 归摊开的那一层。

取值栏与**预设栏**是两个状态、两个词：这一个就地摊在左栏，那一个占主区。

型号那一行归 06 号票（它多一层下钻）。

**Blocked by:** 01, 04

**Status:** ready-for-agent

- [ ] 左栏上取值是环的那几行都摊得开
- [ ] 摊开的第一格是「默认（xxx）」，当前选中的那一格有记号，两者分得开
- [ ] `Esc` 回去时那一行**一格不改**，用例钉着
- [ ] 摊开之后左栏变长，视口接得住，光标不掉出屏
- [ ] 摊开时 `←→` 归摊开的那一层，不再就地转那一行
- [ ] 「哪些键在哪个状态下有效」仍旧一处答完，取值栏那几支逐条问过
- [ ] 跑起来之后摊不开——三层只读那条不因这张票松动
- [ ] 快照：摊开着的一张，摊开到左栏装不下的一张
- [ ] `CONTEXT.md` 的《会话》添「取值栏」词条，与「预设栏」分开
- [ ] 三条闸门全绿
