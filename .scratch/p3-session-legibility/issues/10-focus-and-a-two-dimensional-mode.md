# 10 — 焦点与两维模式

**What to build:** 会话的模式从一维拆成**两维**：

- **这一趟在哪个阶段**：没跑过 / 跑着 / 停在决策点上等答话 / 收场了
- **眼下在看什么**：左栏浏览 / 左栏摊着取值 / 编辑一行 / 报告区 / 展开着 / 预设栏 / 覆盖层

拆了之后：`⇥` 在左栏与报告区之间切焦点；**报告区有自己的光标**，`↑↓` 选一卷；
而且**跑着的时候一样能用**——几十分钟的一趟里想回头看第一卷，不必等它跑完。

**跟随**：跑着时卷表默认停在最后一行（跟着最新收摊的那一卷）；光标一动就停，
屏上说一句「跟随停了」；`g` 回到跟随。跟随是卷表**独有**的状态，别处的视口仍是
「算出来的」。

`⇥` **按状态分派，不冲突**：左栏 ⇄ 报告区切焦点；编辑一行时是逐层补全；
展开着时是换下一卷。

`p1-session/10`、`11`、`14` 先后三次判过「不拆」，依据都是「本票不需要」。
**这一张需要**，推翻的理由与代价落成 `docs/adr/0015-…`。

**Blocked by:** 08

**Status:** ready-for-agent

- [ ] `⇥` 在左栏与报告区之间切得动，屏上看得出焦点在哪
- [ ] 报告区有光标，`↑↓` 选一卷；跑着、等答话、收场了三个阶段都用得动
- [ ] 跟随：默认跟着最新的卷；光标一动就停并在屏上说一句；`g` 回到跟随
- [ ] **三层只读由阶段那一维说了算**，与焦点在哪无关：跑着与等答话时左栏一个改动键都不派
- [ ] 答话那两个键与按停在焦点落在报告区时照样按得动，两级语义不变
- [ ] 「哪些键在哪个状态下有效」仍旧**一处答完**，两维之后逐条问过
- [ ] `docs/adr/0015-…` 写下来：为什么这次要拆，以及先前三次判「不拆」各是什么依据
- [ ] 快照：焦点在左栏、焦点在报告区、跟随停了，各一张
- [ ] `CONTEXT.md` 的《会话》添「焦点」「跟随」两个词条
- [ ] 三条闸门全绿
