# 03 — 换词：驱动页、主体、离群页

**What to build:** 报告里三个要查文档才懂的词换成人话，`CONTEXT.md`、报告文字、
代码文档一起换：

| 今天 | 换成 | 英文标识符 |
|---|---|---|
| 驱动页 | **定档页** | `driver`（不动） |
| 主体 / 主体页 | **其余页** | `body_pages`（不动） |
| 离群页 | **特例页** | `outlier_pages`（不动） |
| 基准档 | **不换** | — |

换的是**中文说法**。英文标识符**不动**：它们对这三个概念仍是准确的英文，而改名要动
二十来个文件、二百六十余处，风险与收益不成比例。词条里把英文名带上，两边对得起来。

**命令行报告与会话同时换**——同一件事不许有两个名字。

《判据》里「上包络 = 卷内**主体**页判定的 p95 聚合」**不直译**，改写成
「**不含特例页的那些页**判定的 p95 聚合」：直译成「其余页」会读成
「相对特例页剩下的那些」，定义绕回自己。

**Blocked by:** None — can start immediately。与 02 都大改同一个模块，两张票别同时开工。

**Status:** ready-for-agent

- [ ] `CONTEXT.md` 的《判据》三个词条改名，英文标识符名跟着列出来
- [ ] 上包络那一条按上面那句改写，不直译
- [ ] `docs/adr/` 里提到这三个词的正文一起改
- [ ] 报告文字（命令行与会话）三个词全换，一处不漏
- [ ] 黄金快照重录一次，**逐条确认变的只有那三个词**、判定的数一个都没动，理由写进落地记录
- [ ] 停车场里提到这三个词的条目跟着改，免得往后两套说法并存
- [ ] 三条闸门全绿
