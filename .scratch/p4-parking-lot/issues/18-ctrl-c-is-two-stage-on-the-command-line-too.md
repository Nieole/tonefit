# 18 — 命令行也接上两级停

**What to build:** 两级停是 `run` 的能力，会话里接上了，**命令行那一路至今没有按的地方**——
`ADR 0013` 决定第 3 条只兑现了一半。批量跑几十卷时按 `Ctrl-C` 今天是硬杀，
当前卷靠 partial 丢弃兜底。

接上信号处理：**一次收尾**（当前卷跑完才停，盘上多一个完整的卷）、**两次中止**
（立刻停，当前卷那格丢弃）。**两级的语义与会话那两级一格不差**——
那是同一条闩、同一个检查点，不是命令行另立的一套。

**这张排在 19 号之前**：接不接决定闩的编码将来是两份还是三份。

收停车场的 **Q38**。

**Blocked by:** None — can start immediately

**Status:** resolved

- [ ] 命令行上按一次 `Ctrl-C` 是收尾：当前卷跑完才停，盘上只有完整的卷
- [ ] 按两次是中止：立刻停，当前卷那格丢弃，最终位置上一个字节都没动过
- [x] 两级的语义与会话那两级逐条相同，退出码照既有那四个走
- [ ] 按下之后屏上说得出按到了哪一级
- [ ] 还在写盘的那一截有人收，不留半成品
- [x] `ADR 0013` 决定第 3 条从此两边都兑现，票面与 ADR 措辞对得上
- [ ] 三条闸门全绿

> ## 勾得有保留
>
> **七条里只勾了两条**，另外五条**不是没做**——实现与用例都在，缺的是**一次可信的运行**。
>
> **为什么这一头跑不出可信的结果**：本票在一棵 worktree 里落地，而三棵 worktree
> 在 cargo 眼里是同一个包、单元哈希相同，产物混在同一个 `target` 目录里（根因见停车场
> **Q244**，是并行方案本身的缺陷，不是这一票的）。**这对本票格外要命**：它是这一批里
> 唯一**起真进程送 `SIGINT`** 的一张，而起的那个二进制可能是别的线编的——
> 那样看到的「盘上留下什么、退出码是几」一个字都不能信，**绿的红的都不可信**。
> 落地途中当场撞上两次（`tests/stop.rs` 两条全红，子进程被 `SIGINT` 打死，
> 而那一刻本线一次编译都没起，见 **Q264**）。协调那一头因此判：**支线只写，主仓只跑**。
>
> **没勾的五条各差什么**：
>
> | 没勾的 | 实现在哪 | 证它的用例 | 还差什么 |
> |---|---|---|---|
> | 按一次是收尾 | `install_the_stop_key` + `Latch::press` + `Bar::observe` 回 `answer(…)` | `tests/stop.rs::one_ctrl_c_lets_the_volume_in_flight_finish_and_stops_there` | 一次可信的运行 |
> | 按两次是中止 | 同上（第二下升到中止，页边界那个检查点接住） | `tests/stop.rs::two_ctrl_c_throws_the_volume_in_flight_away_and_the_final_place_is_untouched` | 同上 |
> | 屏上说得出哪一级 | `install_the_stop_key` 里那句 `frame.println(pressed_note(pressed))` | `each_level_says_which_one_it_is_on_screen`（只断言**那句话**，画出来那一截本仓没有用例断言得了） | 一次可信的运行；而「画到屏上」终究要有人看一眼真终端 |
> | 不留半成品 | **不在本票**：`src/sink.rs` 两个 `Drop`，早就落地、早就有用例（`tests/events.rs` 的 `aborting_at_a_page_boundary_throws_the_partial_container_away`）。本票只是让命令行够得着它 | `tests/stop.rs` 第二条顺带断言输出根里没有 `卷02.partial` | 一次可信的运行 |
> | 三条闸门全绿 | —— | —— | **一遍都没跑**（协调那一头判定停跑一切 `cargo`）。手上唯一跑过的是 `cargo fmt` 与 `cargo clippy --all-targets`，那一遍屏上写着 `Checking tonefit v0.1.0 (/tmp/tonefit-C)`、**零告警**——它说明这份代码编得过、clippy 干净，**不说明用例是绿的** |
>
> **勾了的两条为什么勾得住**：两条都是**读代码就核得出**的，不靠跑。
> 「两级的语义与会话那两级逐条相同」——同一张升级表（`next` 对 `Session::raise_stop`）、
> 同一条让的规矩（`answer`）、同一对检查点（库那一侧一格没动）；「退出码照既有那四个走」
> ——`exit_code` 那个 `match` 一格没改。ADR 那一条是文档改动，票面与它措辞对得上，
> 也是读出来的。评审那两条轴子都逐条核过这两点（见《落地记录》第七节）。
>
> 五处**判断**（不是保留，但要写明读法）：
>
> - **第 4 条「屏上说得出」只在对面是终端时说得出。**那一句走 stderr，
>   而 indicatif 认得出对面不是终端、那时一个字节都不写（`Bar` 的既有性质，报告走 stdout
>   才不被混进文件）。用例断言的因此是**那句话本身**（`pressed_note`：两级各说各的，
>   收尾那一句还得说出下一下是什么），不是「它真被画到屏上了」——画出来那一截
>   与进度条其余部分同一个待遇，本仓没有任何一条用例断言过它。
>   重定向出去的那份**报告**眼下也说不出这一趟被按停过（`render::outcome` 挂在 `tui` 后面，
>   而 `render.rs` 不在本票可动的文件里），记在新的 **Q260**。
> - **引了一个包：`ctrlc`。**树里现成的 `signal-hook` 挂在 crossterm、也就是 `tui` 特性后面，
>   而闸门第二条把 `tui` 关掉——命令行照旧要按得动，那一份因此用不上。
>   Linux 与 Windows 上多编两个包（它自己 + `nix` 0.31），`Cargo.lock` 多六格
>   （另外四格是 apple 那一侧的）。**为什么不自己写**：这件事有一半在 Windows 上，
>   而本机验不到那一半（**Q263**）。理由全文在 `Cargo.toml` 那一条的注释上。
> - **闩的编码成了三份，票面预告过**——那一笔记在**既有的 Q70** 上（它当年就预告了
>   「命令行那一头接上的那天就是第三份」，而 19 号票票面收的正是 Q70），本票给它补了一条《后续》。
>   票面没预告的是另一半：`answer`（决策点上收尾要让）与升级那张表跟着也各成了两份，
>   记在新的 **Q262**。两样都非有不可——会话那一份整个在 `tui` 特性后面，命令行够不着；
>   少了 `answer`，第一遍里按下的收尾会把当前卷的第二遍吃掉。
> - **预扫那一段按下去说得出、停不下来**：屏上那一句由信号那条线程当场说，闩也记住了
>   （第一卷因此一个字节都不写），但进程要等预扫整个走完才停得下来。
>   键装在 `run` **之前**（那一下不会丢），预扫自己没有加检查点——加它要动库。记在新的 **Q261**。
> - **落地的次序不是红→绿。**实现先写完，用例后补——派活说明里那句「先调用 `tdd` 技能」
>   是实现落地之后才到手上的（`/implement` 那个技能对子 agent 展不开，正文里那句
>   「Use /tdd where possible」从来没到过这一头）。补上之后按那几条**回头审过一遍**，
>   改了两处，见《落地记录》第五节。
>
> **`CONTEXT.md` 一个字没改**：本票一个新概念都没引进来——闩、收尾、中止、检查点、
> 决策点五个词条早就在册，命令行这一头用的就是它们。
> **退出码仍是四个**，`exit_code` 那个 `match` 一格没动。

## 落地记录

### 一、按的地方：`Ctrl-C` 记进一条闩

`src/main.rs` 多了一格 `static PRESSED: Latch`，与一句装键：

```rust
fn install_the_stop_key(frame: MultiProgress) {
    let _ = ctrlc::set_handler(move || {
        if let Some(pressed) = PRESSED.press() {
            let _ = frame.println(pressed_note(pressed));
        }
    });
}
```

升级那张表拎成了 `next`（继续 → 收尾 → 中止 → 中止），与会话那一侧
`Session::raise_stop` 里那个 `match` 逐条相同，库那一侧的 `fetch_max` 说的是同一件事。
`Latch::press` 走 `fetch_update` 而不是 `fetch_add`（加下去按满 256 次就绕回「继续」，
而闩不许退回去），并**交回升到的那一级**——第三下交回 `None`，两级停就是两级，
命令行这一头没有第三种停法。那个 `Option` 同时就是屏上那一句的开关：一级只说一次，
不必另记一格「说过了没有」（按下 `Ctrl-C` 的那条线程只有一条）。

装在 `execute()` 里、`tonefit::run` **之前**：

- 会话那一岔在函数头上就让开了（`without_arguments`），那一路一个字都动不到——
  会话进 raw mode 之后 `Ctrl-C` 是一个**按键**、意思是退出会话，与这里的信号不是一回事；
- **预扫在 `run` 里面**，装晚一步那一段就按不动（那一下会丢，而不只是晚一点停，见 Q261）。

装不上**照旧硬杀**，这一趟不因此拒绝执行：那时的行为与本票落地之前逐字相同，
而「一个键没装上」不在 `CONTEXT.md` 的《失败》那张单子上——为它新开一种拒绝，
换来的是一趟本来跑得完的活儿跑不成。

### 二、停在哪儿：一格新代码都没有

`Bar::observe` 从「恒回继续」改成回**闩记着的那一级**，中间过一遍 `answer`：

```rust
answer(at_the_decision_point, PRESSED.pressed())
```

停在哪一道边界上**库那一对检查点早就写好了**（卷边界管收尾、页边界管中止，
`progress::Events` 的 `standing` 与 `aborting`），本票一格都没动它们——
命令行这一头因此不是「另立一套停法」，是「多了一个按的地方」。
`tests/events.rs`、`tests/resume.rs`、`tests/container.rs` 那一批（自己造观察者答
`Finish`／`Abort`）验的正是这一对检查点，本票一条都没改。

`answer` 那一条规矩（**决策点上的收尾要让、中止不让**）是本票必须复制的第二样：
决策点问的是「这一卷的第二遍还做不做」，拿闩去答，第一遍里按下的收尾会顺手把当前卷的
第二遍吃掉——那一卷等于走了一次试算，盘上少的**正是收尾说好要留下的那一卷**。
让掉的那一下不会丢：答复照样进库那一侧的闩（`fetch_max`），当前卷跑完之后卷边界那个
检查点照样停。

### 三、屏上那一句

**由信号那条线程当场说，不由 `Bar::observe` 说。**后者要等**下一条事件**才被调到，
而预扫那一段一条事件都没有（`Bar::survey` 的文档写着：那时这一层收不到任何事件，
转轮是自己转的）——几十个归档卷列归档头正是最长的那一段，在那里按下去而屏上一声不吭，
用户必然当它没收到、再按一下，而再按一下就是中止。`ctrlc` 的回调跑在它自己那条
**普通线程**上，这一句因此当场就说得出口。（这一条是评审提出来的，见第七节。）

话走 `MultiProgress::println` 而不是 `eprintln!`：横条归它画，绕过它插一行就把光标弄乱了。
`MultiProgress` 是个把手，克隆的与 `Bar` 手上的是同一份，因此在 `Bar` 交给 `Request`
之前把它取下来即可。印在进度条**上面**、不写进条上那行字：收尾要等当前卷跑完，
那可能是几分钟，而横条上那行字会被下一卷换掉；中止那一句更是只有留下来才看得见——
那一趟紧接着就收摊，横条整片抹掉。

收尾那一句**把下一下是什么也说了**：

```
按停（收尾）：当前卷跑完就停，剩下的卷不开工。再按一次是中止——当前卷丢掉，立刻停。
按停（中止）：当前卷丢掉，立刻停。那一卷等于没做，最终位置上一个字节都没动过。
```

不说的话，用户会当它没收到、再按一下，而再按一下当前卷就丢了。
措辞留在 `main.rs` 而不是 `render`：那一份是**报告**的措辞，这两句是**进度显示**的，
与进度条上那几句（「点名 N 个路径……」「整趟 N 卷」）一个待遇——它们也都在这一层。

### 四、引了一个包：`ctrlc`

**为什么非引不可，全文写在 `Cargo.toml` 里那一条的注释上，这里不复述**（`CLAUDE.md`
《文档写作》第 4 条：单一出处）。三句话的结论：树里现成的 `signal-hook` 挂在 `tui` 后面而
闸门第二条把 `tui` 关掉；自己写要写 unix 与 Windows 两套，而后者本机编都编不到（Q263）；
它的回调跑在一条普通线程上，那个回调因此不必守异步信号安全那套规矩，
而它那个信号量**会计数**，两下挨着按不会并成一下。

代价也在那条注释里：Linux 与 Windows 上多编两个包，`Cargo.lock` 多六格。
另外 `libc` 进了 `[dev-dependencies]`（用例要往子进程送 `SIGINT`），不是新的一棵树。
许可是 MIT OR Apache-2.0，`THIRD-PARTY-NOTICES.md` 不必加——那一份只收
**随程序分发而许可与本体不同**的那几样。

### 五、用例：两个缝，四条加两条

`tdd` 那几条是实现落地之后才到手上的，因此**红→绿那一段没有走**。补用例时按那几条
回头审了一遍，两处照着改：

- **原本打算在 `main.rs` 里 `raise(SIGINT)` 给自己一下**，断言那格 `static` 升到了哪一级。
  撤掉了：它与进程那一层那两条**重合**（后者把同一条链子整个走了一遍），
  而它断言的是一个全局静态的内部状态——正是「绕后门查状态」那一类。
  `libc` 因此只留在 dev 依赖里给 `tests/stop.rs` 用。
- **原本会写成「送完信号睡一会儿再看盘上有什么」**。改成**盘上的记号**：
  `出/卷01` 出现说明第一卷改名到位了、第二卷正在走第一遍；`出/卷02.partial` 出现说明
  第二卷的第二遍开始了。两个记号都是「一出现就恒在」的，**转到它成立为止**即可。

两个缝，各自的用例：

| 缝 | 在哪 | 几条 | 问的是 |
|---|---|---|---|
| 进程这一层 | `tests/stop.rs` | 2 | 按下 `Ctrl-C` 之后盘上留下什么、退出码是几 |
| 观察者那条回路上的闩 | `src/main.rs` 的 `mod tests` | 4 | 按一次／两次各到哪一级，以及回给库的是哪个字 |

`tests/stop.rs` 那两条各按在自己的记号上：

- **按一次**（按在第二卷的**第一遍**里，那正是收尾唯一会被弄错的地方）：
  盘上是 `["卷01", "卷02"]`，两卷各四页一页不少，退出码 `0`。
  **少一个说明决策点那一让没让，多一个说明根本没停**——这一条因此同时钉住了 `answer`。
- **按两次**（第二下按在 `卷02.partial` 出现之后，那格临时容器**真的建出来过**）：
  盘上只剩 `["卷01"]`，`卷02` 与 `卷02.partial` 都没有，退出码 `0`。
  两下之间隔着第二卷的一整遍，因此**不会并成一下**（`SIGINT` 不排队，挨着送的第二下
  可能撞在第一下还没递到的时候被丢掉）。

`main.rs` 那四条：按一次交回收尾／按两次交回中止／第三下交回 `None`；手写的编码与
`Instruction` 派生的 `Ord` 对得上、升级那张表只升不降（与会话、库那两份同一条）；
决策点上收尾让、中止不让、别处照闩答；两级各说各的那一句。
库外造不出一条 `Event` 来（它两级非穷尽），`Bar::observe` 本身因此问不动——
与会话那一侧 `Watch::observe` 一个处境，整条链子接得上没有由 `tests/stop.rs` 在真进程上答。

**这两条在这一头跑不出可信的结果。**三棵 worktree 在 cargo 眼里是同一个包、产物混在
同一个 `target` 目录里（Q244），起真进程那几条用例因此可能对着**别的线**那一份二进制
送信号——本票落地途中当场撞上两次（Q264）。它们**写好了、也手工复现过**
（把二进制拷到一个私有路径上按了两遍，见 Q264），但**没有一遍算数的运行**：
验收表里那几条因此没有勾，《数》写着「待主仓闸门」。

`tests/stop.rs` 第一条上还留着一道**微秒级的时序缝**：第一卷改名到位与卷边界那个
检查点之间要是被信号撞上，这一趟会停在 `["卷01"]` 上而红。它比第二卷那一整遍
（秒级）小四五个数量级，而且**撞上是红、不是假绿**——用例说谎不了，只会白响一次。
写在那条用例的文档里。

### 六、ADR 0013 决定第 3 条

原文「命令行那一路同样用得上」改成**两边都按得动**，并写明会话是 `s`、命令行是 `Ctrl-C`，
两边**同一条闩、同一对检查点**、第三下与第二下一个待遇。
《后果》多一条要认的代价：**命令行上 `Ctrl-C` 从此不是硬杀**——按下去要等一个检查点，
真要立刻弄死进程仍有 `SIGQUIT` 与 `kill`，而那时盘上留下的只是一个改不了名的临时容器。
状态行加一笔「决定第 3 条修订（2026-09-07）」。

### 七、评审提的几条

**两条轴子各报了几条，改了五处**（都在 `src/main.rs` 与两份 markdown 里）：

- **屏上那一句原本挂在 `Bar::observe` 上，而预扫那一段一条事件都没有**——
  按下去屏上一声不吭，而 `Ctrl-C` 又不再是硬杀，用户必然再按一下，而再按一下当前卷就丢了。
  **这一条是真的**：`Bar::survey` 自己的文档就写着「预扫期间这一层收不到任何事件」。
  改法见第三节：那一句移到信号那条线程上当场说，`Bar` 那格 `said: AtomicU8`
  与 `say_pressed` 一并删掉——评审另外提的两条（那一格是**同一个文件里第四份**
  「AtomicU8 + `code()`」的手工编码；`said` 这个名字读不出它装什么，还与
  `session::run::Asked::said` 同名异义）跟着一起没了。Q261 里那句「那一段里屏上那一句照说」
  当时是**假的**，一并改正。
- **`install_the_stop_key() -> Result<()>` 是个没有读者的 `Result`**（唯一的调用处
  `let _ =` 丢掉）。改成不交返回值：那个错误就地丢掉，为什么丢写在函数文档上。
- **Q262 与既有的 Q70 讲同一件事，互不点名**，而 19 号票票面收的是 Q70——照原样，
  19 号票收 Q70、漏 Q262。改法：闩那一半**还给 Q70**（给它补一条《后续》，
  记下第三份长出来了、两个 `Latch` 同名不同 API），Q262 收窄成它之外多出来的那一半
  （`answer`、`at_the_decision_point`、升级那张表），两条互相点名。
- **引 `ctrlc` 的理由写了三遍**（`Cargo.toml` 的注释、本票第四节、Q263）。
  全文只留在 `Cargo.toml` 那一条注释上，另外两处收成结论加一句指路。
- **《已了结》索引里 Q38 那一行缺《停车场结转》**，同表最近几行都带。补上。

评审另外**核过、确认没问题**的几点：`CONTEXT.md` 一字未动（闩、收尾、中止、检查点、
决策点五个词早就在册）；`exit_code` 那个 `match` 一格未改；`pressed_note` 的措辞留在
`main.rs` 与既有那几句进度显示同层；改动文件全在派活说明列的白名单内；
`Cargo.lock` 只多六格，与记录相符；没有 scope creep。

**没照改的一条（判断题）**：评审提「`src/main.rs` 已经两千多行，本次再加信号处理与闩，
一个 `mod stop` 会让 19 号票便宜得多」。没做——派活说明把可动的文件列死了，
新开一个模块文件不在其中；而这正是 19 号票要收的东西，落点已经记在 Q262 的《Whose call》上。

**改完之后又复评了一遍**，它核实了两件事、另报了三处，三处都照改：

- 复评核实的：`ctrlc` 3.5.2 只起一条名叫 `ctrl-c` 的普通线程、在循环里**串行**调回调，
  而 `set_handler` 被一个 `INIT` 卡成一个进程只装得上一次——「按的地方只有一处」
  这条不变量因此站得住，`Latch::press` 交回的那一级不会说谎；
  `bar.frame.clone()` 与 `Bar` 手上的确实是同一份（`MultiProgress` 里是
  `Arc<RwLock<MultiState>>`），从那条线程上调 `println` 安全。
- **`pressed_note` 里还留着一句指着已删的 `Bar::say_pressed` 的悬空引用**——改掉了；
  顺手把「只有一处按」那条不变量的**来处**写进 `Latch::press` 的文档（原本只说了结论）。
- **本票第二节的代码块与第五节末尾两处自相矛盾**（前者还印着删掉的 `say_pressed`，
  后者说闸门是加锁跑过的，而《数》写着一遍没跑）——都改了。
- **`tests/stop.rs` 第一条留着一道微秒级的时序缝**（第一卷改名到位与卷边界检查点之间
  被信号撞上就红）——写进那条用例的文档：缝比第二卷那一整遍小四五个数量级，
  而且撞上是红不是假绿。
- 复评还指出 ADR 新写的「两边同一条闩」与 Q70 后续记的「闩已成三份」读着打架。
  ADR 那一句改成**「两边落到同一条闩」**并点明那是 `run` 自己记的那一格——
  两边按下的字都从观察者那条回路流进它，而三份记的是「按下之前各自攒着的那一级」。

### 数

**待主仓闸门。**这一头一遍都没跑——三棵 worktree 的产物混在同一个 `target` 目录里
（Q244），在这里跑出来的数说明不了任何事，而本票是唯一起真进程送信号的一张（Q264）。
协调那一头判：**支线只写，主仓只跑**；三条闸门与「真跑一遍 `tests/stop.rs` 那两条」
由主仓合流之后做，数记在那时。

手上唯一有过的一遍是 `cargo fmt` 与 `cargo clippy --offline --all-targets`
（末一行 `Finished \`dev\` profile … in 5.40s`，屏上写着 `Checking tonefit v0.1.0 (/tmp/tonefit-C)`，
**零告警**）。它说得出的只有一件事：**这份代码编得过、clippy 干净**——
用例绿不绿它一个字都没说。

## 停车场结转

**了结一条**（原文连同处置挪到这里，`## 待处理` 里已删掉，`## 已了结` 索引表加了一行），
**新记五条**（留在《待处理》里）：

| 新记的 | 一句话 |
|---|---|
| **Q260** | 命令行按停停下来的那一趟，**报告上**说不出它被按停过——屏上那一句走 stderr，而 `render::outcome` 挂在 `tui` 后面，`render.rs` 不在本票可动的文件里 |
| **Q261** | 预扫那一段按下去不当场停：闩记住了、第一卷一个字节都不写，但要等预扫走完——键装在 `run` 之前，那一下不会丢，预扫自己没有检查点 |
| **Q262** | 闩的编码成了三份（票面预告过），而「决策点上收尾要让」那条规矩成了两份（票面没预告）——名字与会话那一份逐字相同，留给 19 号票 |
| **Q263** | `Ctrl-C` 的 Windows 那一半本机验不到（只装了 linux 一个 target）——引 `ctrlc` 而不是自己写，换的正是那一半有人替我们编过 |
| **Q264** | 三条线共用一个 `target`，起真进程那几条用例会跑到别的线的二进制上——本票落地途中当场撞上两次，症状与 Q95 一模一样 |

### Q38 — 两级停是 `run` 的能力，而命令行这一路还没有按的地方

- **From:** 票 `p1-session/02`
- **Kind:** 票面没想到的第三种情形
- **Where:** `src/main.rs` 的 `Bar::observe`（恒回 `Instruction::Continue`）
- **Why it did not block:** ADR 0013 决定第 3 条明写「收尾与中止是 `run` 的能力，不是 TUI 私有的：
  命令行那一路同样用得上」。本票把回路立起来之后，库这一侧已经收得下那两个字，
  缺的只是命令行那一头的**按键**——而那要接 Ctrl+C 信号处理，并决定按一次与按两次怎么分。
  本票的硬约束是「命令行现有行为一字不变」，接线当场就越了界；而它也不该由本票定形状：
  `04`（中止）落地之后中止才真的停得比收尾早，在那之前接上去，用户按第二下看不出区别。
- **What this ticket actually did:** **没有接。**`Bar::observe` 恒回继续，并在文档里点名
  这是「还没有那个键」，不是「命令行不该停」，同时点了 ADR 0013 决定第 3 条。
  Ctrl+C 仍然只有杀进程一条路——与本票落地之前逐字相同。
- **Whose call:** 拍板的人（命令行那一头要不要接 Ctrl+C 信号处理）——
  `10` 已落地会话那一侧的两级停（`s` 按一次收尾、再一次中止），命令行这一头仍然空着
- **处置：** **了结。命令行这一头接上了。**`Ctrl-C` 按一次是收尾、按两次是中止，
  按下的那一级记进 `src/main.rs` 的 `PRESSED` 那条闩，`Bar::observe` 从「恒回继续」
  改成回那一级——它文档里那句「还没有那个键」因此删掉了。
  这一条当年列出的两个前提都已经成立：`04` 早就落地，中止**真的**停得比收尾早
  （页边界 vs 卷边界，用户按第二下看得出区别，`tests/stop.rs` 那两条断言的正是这个差别）；
  而「命令行现有行为一字不变」那条硬约束是 `p1-session/02` 的，不是本票的——
  本票的活儿就是改它。按的形状由票面定死：**一次收尾、两次中止**，
  与会话那两级同一条闩、同一对检查点，不新造第三种停法。
  ADR 0013 决定第 3 条就地修订，写明**两边都按得动**。
