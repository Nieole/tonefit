# 06 — 覆盖层交键给阶段，等答话时左栏压暗

**What to build:** 两件屏上说得清、按得动的事：

- **覆盖层掀着时按停与答话那三个键仍旧按得动。** 今天掀开 `?` 那张键位表，
  跑着的那一刻 `s` 按不动、停在决策点上时 `x`／`a`／`s` 三个也按不动——
  而 `ADR 0017` 决定第 4 条写着它们「在焦点落在哪一块上都按得动」。
  **照 ADR 走**：覆盖层与别的五块一样，把自己不认的键交给阶段那一维。
  「进去之后别的键一律不派」收窄成「**除了按停与答话**，别的键一律不派」，
  票面与 ADR 两处同时改口。
- **等答话时左栏压暗、抬头改口。** 那一刻三层同样只读，而屏上看不出来——
  左栏不压暗、抬头那一格写死「跑着」（等答话时那是假话）。
  **先定那句话写什么**，再压暗。

`a` 那个键在等答话时归答话（「剩下的卷都这样」）、展开着时是「列全部页」，
两者撞在同一个字母上——**照实现定下来**：等答话时让给答话，屏底那一行跟着不摆它。
这一条只改措辞。

收停车场的 **Q160**、**Q161**、**Q164**。

**Blocked by:** 03 — 取值栏那两层照实现定下来

**Status:** ready-for-agent

- [ ] 覆盖层掀着时按停按得动，两级语义一格不变；等答话时答话那三个也按得动
- [ ] `Esc` 关掉覆盖层仍旧原样回到盖住的那一块
- [ ] 等答话时左栏压暗、抬头说得出这一刻是等答话，措辞只有一处出处
- [ ] `ADR 0017` 决定第 4 条与相关票面同时改口，两处不再互相排斥
- [ ] 「哪些键在哪个状态下有效」仍旧一处答完，覆盖层那一支逐条问过
- [ ] 快照：跑着时掀开 `?`、等答话时的左栏，各一张
- [ ] 三条闸门全绿
