# 23 — 补全按运行期探大小写

**What to build:** 逐层补全认不认大小写今天按 `cfg!(windows)` 判，
而 **macOS 默认那套文件系统也不认大小写**——macOS 上敲小写补不出大写开头的卷名，
补全在那台机器上是用不了的。

改成**运行期探**：在那个只读一次目录的地方多探一次，把结果记住。

收停车场的 **Q107**。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] macOS 上小写敲得出大写开头的卷名
- [ ] Linux 上大小写仍旧敏感，行为不变
- [ ] 那一次探测不让每敲一个字符都多一次系统调用
- [ ] 判据只有一处出处，不再按平台常量猜
- [ ] 三条闸门全绿
