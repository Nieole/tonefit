# 01 — 「哪几种算拒绝执行」收成一处

**What to build:** 加一种新的拒绝执行时，只改一处就够。那张单子今天在实现文档里抄了五份
（`run`、开工与收场那两条事件、耗时那一格、命令行的预扫），加一种就要五处一起改，
而下一张动它的票（28 号）正好是「五种改六种」。

收拢之后：单子只在 `CONTEXT.md` 的《失败》留一份，五处实现文档只留**指路**——
`CLAUDE.md`《文档写作》第 4 条（单一出处）与第 5 条（稳定引用）的一次施工。

**这张排在 28 号之前**：反过来那张单子要再抄一遍五处。

收停车场的 **Q91**。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] 那张单子在仓库里只有一份，其余四处引用它的小节名而不复述
- [ ] 引用用小节标题，五处任何一处的文件改动都不使引用失效
- [ ] 有一条用例或文档断言问得出「这张单子只有一处」
- [ ] 命令行印出去的字一个字节不变
- [ ] 三条闸门全绿
