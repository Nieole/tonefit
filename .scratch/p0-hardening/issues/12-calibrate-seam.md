# 12 — `calibrate` 的 seam 归属

**What to build:** 标定图这条路要么走主入口，要么把「两个 seam」这个说法改掉——现在是两者都不成立。

spec 写死两个对外 seam、所有模式走同一个入口，模式里含标定。实现里模式只有处理与试算两种，标定另开了一个公开函数当第三个 seam，落盘逻辑（建父目录、写文件）留在 CLI 层——那是 CLI 唯一一处自己碰文件系统产出产物的地方。后果是标定这条路的落盘行为只有起子进程才测得到。

**这块正是 TUI 改造要动的地方**（TUI 需要一切走库的 seam），口径应当在那边一并定。

**Blocked by:** 与 TUI 改造的设计口径 —— 等那边定下库与界面层的边界。

**Status:** needs-info

- [ ] 口径定下：标定走主入口，或明确承认第三个 seam
- [ ] 无论哪种，落盘逻辑从界面层移进库内
- [ ] spec 与 CONTEXT 同步到定下的口径
- [ ] 标定这条路在库这一侧可测
